# Development Log - 2026-08-09

## Goal and boundaries

Correct proposal-review replay measurement without adding a product App Server
client, changing Core or Adapter behavior, retaining raw events, or sending a
new external replay.

Acceptance required exact structured recognition of the proposal tool call,
native form, safe result fields, and turn completion; unrelated mentions of the
tool name had to remain ignored. Work would stop if it required an ADR change,
product-scope expansion, or another external authorization.

After product review of the valid `not_called` result, a second bounded goal
clarified the proposal-review trigger without changing the Adapter protocol,
task schema, native form, decision mapping, release identity, or installed
runtime. Acceptance required only a task matching and explicitly authorizing
the exact requested repository change to suppress proposal review; unrelated,
measurement-only, and differently scoped tasks could not count, and read-only
work had to remain outside the trigger.

The product owner then separately authorized the next bounded step: build and
hash the exact reviewed source, replace only the existing isolated AgentGov
pipx runtime, and complete installed-runtime discovery and trigger-metadata
preflight. This authorization excludes source or project-config changes,
dependency downloads, new environments, Codex startup, external replay,
downstream implementation, Git, publication, release, and deployment.

After reviewing that completed installation preflight, the product owner
separately authorized exactly one fresh live replay. The authorization permits
one ordinary repository-change request in one ephemeral read-only Codex App
Server turn. It does not authorize answering any native proposal form, creating
or implementing the proposed task, a second turn, source/config/install change,
Git, publication, release, deployment, or consumer mutation.

After reviewing the completed single replay, the product owner directed the
next step: diagnose the remaining invocation-to-elicitation gap. This admits
only local read-only analysis of official App Server documentation, protocol
surfaces, Adapter source, deterministic fixtures, and retained normalized
evidence. It excludes another turn or replay, form response, task admission,
implementation, source/config/install change, Git, publication, release,
deployment, and consumer mutation.

After reviewing that diagnosis, the product owner directed the recommended
next step: strengthen the test-only replay evidence contract. This admits only
completion-presence/status fields, a strict privacy allow-list for structured
AgentGov errors, deterministic fixtures, and synchronized evidence
documentation. It excludes historical-event reconstruction, another turn or
replay, form interaction, Adapter/protocol/config/install change, product
behavior, Git, publication, release, deployment, and consumer mutation.

After reviewing the available periodic-review directions, the product owner
selected the combined foreground-session and scheduled-CI reminder. The
admitted slice uses a default three-task/seven-day cadence, keeps semantic
requirement, architecture, and functionality drift advisory, and allows only
explicit create-only review or snooze records. It excludes a hidden daemon,
automatic requirement/ADR changes, external notification writes, published
release identity changes, Git transitions, publication, release, and
deployment.

The product owner completed the first real human-confirmed baseline review,
then explicitly authorized the next bounded usability slice: connect a due
drift reminder to a capability-gated native host form. The current Agent may
provide only a normalized advisory candidate and bounded evidence; the human
must choose whether to record that exact candidate, snooze, or create no
record. The slice excludes agent-supplied human decisions, hidden services,
installed-runtime/config changes, external replay, Git, publication, release,
deployment, and consumer workflow activation.

## Completed

- Added the shared drift-review policy, status, and immutable record contracts.
  Missing baselines are visibly due; three distinct verified tasks or seven
  elapsed days make a confirmed baseline due again; a human-confirmed snooze
  defers the reminder by the configured interval.
- Added `agentgov review drift` terminal, JSON, and non-failing GitHub output,
  plus explicit dry-run-first review/snooze record creation.
- Foreground cycles now add a non-blocking advisory due finding. The coding
  agent receives a drift card only when no higher-priority task, scope, or
  completion card applies.
- Development Monitor 1.5 displays the shared due state and claim limits. A
  future-version managed workflow adds the same scheduled warning and job
  summary while published 0.2.1 and v0.3.0rc1 output remains unchanged.
- Independent advisory review found two material presentation/authority risks:
  a due reminder could displace a task-readmission or blocking card, and record
  application was not bound tightly enough to the exact preview. The final
  implementation makes the drift card subordinate to every consequential
  governance state and requires a real interactive terminal plus exact
  `RECORD` confirmation before creating one reviewed record.
- Drift-reminder focused regression passed 26 tests. The full baseline passed
  772 tests with 2 Windows symlink-permission skips. All 59 schemas parsed;
  `git diff --check` passed. The task check passed while admitted; the final
  paused state deliberately requires new human authorization before more work.
- Native AgentGov self-review tools were not exposed in this parent session.
  A distinct manual advisory pass found no requirement or architecture drift:
  the slice fills the reminder and Monitor direction already accepted by
  ADR-0013. Functionality expanded only within the approved bounded contract;
  no daemon, external notification writer, telemetry, semantic auto-verdict,
  Git, release, deployment, or consumer-workflow authority was introduced.
- Added development Adapter `1.4.0` and the capability-gated
  `agentgov_drift_review_record` tool. Form-capable clients now see seven tools;
  clients without negotiated form elicitation still see only the five
  read-only base tools. The installed Adapter remains `1.3.0` and unchanged.
- Bound the foreground reminder card to that native tool. The current Agent can
  supply only one advisory candidate, exactly one normalized observation for
  requirement, architecture, and functionality, and readable
  repository-relative evidence refs. It cannot supply or infer the form choice.
- The human form offers only record the exact displayed candidate, snooze the
  explicitly displayed configured interval, or create no record. The Adapter
  revalidates the same due-state digest before a create-only write; a repeat or
  stale response cannot create a second baseline.
- Successful review/snooze returns the shared `not_due` status and refreshes
  the AgentGov-owned local Monitor. If Monitor refresh fails after the immutable
  record succeeds, the result remains `recorded`/`snoozed` with a separate
  bounded refresh failure, preventing a misleading duplicate retry.
- Native-form focused regression passed 107 tests. The complete source suite
  passed 776 tests with 2 Windows symlink-permission skips. All 59 schemas
  parsed, repository governance reported 26 passes, 2 warnings, 4 advisories,
  and no failures, and `git diff --check` passed.
- Native AgentGov self-review tools remained unavailable in this parent
  session. A distinct manual advisory review found and corrected one material
  usability ambiguity: the form originally said only “configured interval” and
  now displays the exact snooze-day value. The review found no requirement or
  architecture drift and no new semantic, task, scope, Git, release,
  deployment, external-write, installation, configuration, or replay authority.
  The pre-existing `.codex/config.toml` hash remained
  `4CCA2D57EDEADDFE52D3E6C4DD4D774192BBDBCAB4E84E07DF73E14A861C0348`.

- Repaired standalone Codex authentication and replaced the stale installed
  runtime with the current `0.3.0rc1` source wheel. Installed MCP discovery
  reports Adapter `1.3.0` and all six tools.
- Added project MCP configuration for the installed AgentGov executable.
- Isolated the Windows App Server bridge failure to non-UTF-8 standard input
  when the repository path contains non-ASCII characters.
- Used the separately authorized replay to complete one ephemeral read-only
  turn. No native form appeared and no task or other repository content was
  created.
- Reclassified the replay as `INVALID_MEASUREMENT`: its temporary text search
  could match tool inventory or instructions and therefore did not prove a
  proposal-tool call.
- Added a test-only structured event normalizer and reducer that recognize only
  exact AgentGov proposal-call, elicitation, result, and terminal shapes while
  discarding raw arguments, messages, tool content, error detail, metadata, and
  usage.
- After explicit product-owner authorization, ran exactly one additional
  UTF-8-safe ephemeral read-only App Server replay. It initialized, created a
  thread, and completed a real turn with normalized state `not_called`, zero
  exact proposal-tool calls, and zero forms.
- Confirmed that this is valid negative end-to-end journey evidence rather than
  Adapter pass/fail evidence. Because inventory is intentionally excluded, the
  evidence cannot assign the cause to discovery/configuration or Agent
  invocation. The turn created no task or other repository content, and no
  second replay was attempted.
- Removed the temporary one-shot bridge, unused decision channel, generated
  schema bundle, and Python bytecode after extracting only the normalized
  outcome.
- Ran a no-turn App Server configuration/status diagnostic that confirmed the
  project MCP layer loaded and exposed all six AgentGov tools, including
  `agentgov_task_proposal_review`. This diagnostic started no model turn,
  presented no form, and wrote no task.
- Clarified repository instructions, MCP server guidance, and proposal-tool
  metadata so only a human-admitted task matching the exact requested change
  counts as authorization. Unrelated, measurement-only, and differently scoped
  tasks do not count; read-only work does not trigger proposal review.
- Recorded the same trigger boundary in ADR-0015, the proposal-admission and MCP
  guides, README, status, development plan, and replay evidence. The correction
  changes guidance and metadata only; it does not alter protocol, schema,
  authority, release identity, or the installed runtime.
- Built the exact reviewed source offline with the already-present Python
  3.12.13/setuptools 83.0.0/wheel 0.47.0 toolchain. The local-only
  `0.3.0rc1`-identified wheel was 423,355 bytes with SHA-256
  `F109E8A951605AE947374EE28BB76B569A344BC3DD20A752E1686AF8C317FDFE`.
- Replaced only the existing Python 3.11.9 AgentGov pipx runtime using
  `--force-reinstall --no-deps --no-index`. No MCP process was running, so no
  process was stopped. No dependency was downloaded and no environment was
  created.
- Confirmed the installed `governance_mcp.py` hash exactly matches source,
  distribution/import identity is `0.3.0rc1`, Adapter identity is `1.3.0`, and
  protocol identity is `2026-07-28`. The pre-existing pipx management metadata
  still records its original `0.1.0` source and is not used as content identity.
- Confirmed installed STDIO discovery exposes six tools with form capability,
  including one non-read-only proposal tool, and five tools without it. Both
  installed guidance surfaces contain the clarified trigger boundaries.
- Preserved `.codex/config.toml` byte-for-byte with SHA-256
  `4CCA2D57EDEADDFE52D3E6C4DD4D774192BBDBCAB4E84E07DF73E14A861C0348`.
  No Codex turn or external replay was started.
- Completed a no-model replay preflight that initialized App Server, created an
  ephemeral read-only thread, loaded the project configuration, found the ready
  AgentGov server, and confirmed all six tools without changing repository or
  task-set state.
- Consumed the separately authorized single turn with an ordinary bounded
  repository-change request and no governance/tool coaching. The exact proposal
  tool started once and the turn completed, but no AgentGov form request was
  presented. No decision was supplied, no task or implementation was created,
  no repository state changed, and no second replay occurred.
- Classified the bounded result as invocation evidence only: the matching
  trigger reached the current-host tool, while native elicitation/presentation
  remains unproven. No raw replay material was retained.

## Validation

- `python -m unittest tests.test_replay_event_normalizer -v`: 8 passed.
- `python -m unittest discover -s tests -p test_governance_mcp.py -v`: 26 passed.
- Final focused normalizer, task-contract, and documentation validation:
  53 passed.
- `python -m unittest discover -s tests -v`: 754 passed, 2 skipped.
- Trigger-clarification focused regression: 61 passed.
- Trigger-clarification full regression: 755 passed, 2 skipped.
- Trigger-clarification task check: 6 passed, 1 advisory, no failures while
  admitted for implementation.
- Final paused task check: 5 passed, 1 warning, 1 advisory, no failures.
- Trigger-clarification scope check: 18 passed, 1 advisory, with the sole
  failure being the preserved pre-existing excluded `.codex/config.toml`.
- Post-clarification repository check: 26 passed, 2 warnings, 4 advisories, no
  failures; `git diff --check` passed.
- Clarified-trigger installed-package MCP regression: 26 passed.
- Clarified-trigger installation source regression: 61 focused tests passed;
  the full suite passed 755 with 2 Windows symlink-permission skips.
- Installation task check while admitted: 6 passed, 1 advisory, no failures;
  scope check: 20 passed, 1 advisory, no failures.
- Final paused installation-task check: 5 passed, 1 expected paused warning,
  1 advisory, no failures. The final scope command now fails closed solely
  because a paused task cannot authorize further work; the admitted-state
  20-pass scope result above is the completion evidence.
- Post-installation repository check: 26 passed, 2 warnings, 4 advisories, no
  failures; `git diff --check` passed.
- Removed the exact temporary wheel and its matching pip cache copy after
  installed validation; the recorded hash remains reproducible from source.
- Task-contract check: 5 pass, 1 warning for the intentionally paused decision,
  1 advisory; no failures.
- Repository check: 26 pass, 2 warnings, 4 advisories; no failures.
- Pre-replay scope check while the exact single replay was admitted: 10 pass,
  1 advisory, no failures. After the replay consumed that authorization, the
  task returned to paused and any later scope check must fail closed.
- `git diff --check`: passed.
- Exactly one additional replay was sent after the measurement correction
  under separate authorization; no second replay occurred. No commit, push,
  publication, release, or deployment occurred.
- Matching-trigger replay preflight: initialized, ephemeral read-only thread,
  ready AgentGov server, six tools, proposal tool present, zero repository/task
  changes, and no model turn.
- Matching-trigger live replay: exactly one turn, one exact proposal-tool call
  start, zero forms, completed terminal state, zero repository/task changes,
  and no retry.
- Matching-trigger focused validation: 69 passed. Full baseline: 755 passed
  with 2 Windows symlink-permission skips.
- Final replay task check: 5 passed, 1 expected paused warning, 1 advisory, and
  no failures. Repository check: 26 passed, 2 warnings, 4 advisories, and no
  failures. The paused scope check failed closed as designed; the admitted-state
  pre-replay scope check had passed 21 checks with 1 advisory.
- Project configuration SHA-256 remained
  `4CCA2D57EDEADDFE52D3E6C4DD4D774192BBDBCAB4E84E07DF73E14A861C0348`;
  `git diff --check` passed. The one-shot bridge and generated schema directory
  were removed.
- Native AgentGov self-review tools were not exposed in this parent session.
  A distinct manual advisory pass found no source, protocol, schema, config,
  installation, task-admission, implementation, Git, release, or deployment
  authority drift in the replay closeout.
- Refreshed the official Codex manual through the `openai-docs` skill and
  generated the current Codex `0.146.0` experimental App Server schema locally.
  Both expose `mcpServer/elicitation/request`, standard typed form mode, and the
  extended-form capability used by the bridge.
- Ran two local no-model direct App Server tool calls with the same valid
  normalized arguments. The first retained only non-error and zero-write state;
  the second retained the allow-listed contract, status, action, decision, and
  repository-modified fields. Neither started a turn, sent a model request, or
  supplied a form response. Both returned normal AgentGov non-admission; the
  second recorded `declined`, action `decline`, and zero repository
  modification. This proves that the Adapter form
  and App Server parser interoperate for valid input, but not live presentation.
- Identified a deterministic evidence blind spot: replay state `call_started`
  does not record completion presence and ignores the structured
  `agentgov.mcp-tool-error` contract. The retained result therefore cannot
  distinguish strict pre-form argument rejection from App Server forwarding or
  UI presentation behavior. No raw event reconstruction was attempted.
- Elicitation-diagnosis focused regression: 69 passed. Full baseline: 755
  passed with 2 Windows symlink-permission skips.
- Diagnosis task and scope while admitted: task 6 passed with 1 advisory;
  combined working-copy scope 22 passed with 1 advisory. Final paused task:
  5 passed, 1 expected warning, 1 advisory, and no failures; final scope fails
  closed solely because the task is paused.
- Post-diagnosis repository check: 26 passed, 2 warnings, 4 advisories, and no
  failures. Project configuration SHA-256 remained
  `4CCA2D57EDEADDFE52D3E6C4DD4D774192BBDBCAB4E84E07DF73E14A861C0348`;
  `git diff --check` passed. The one-shot probe and generated schema were
  removed, and no App Server or AgentGov child process remained.
- Native AgentGov self-review tools remained unavailable in this parent
  session. A distinct manual advisory pass found no requirement, architecture,
  protocol, schema, source, configuration, installation, admission, Git,
  release, or deployment drift; only evidence and status documentation changed.
- Extended the test replay normalizer with unique exact-completion count,
  sorted completion statuses, and explicit `completion_unknown`. Exact
  successful proposal results and native forms preserve their prior semantics.
- Added a strict `agentgov.mcp-tool-error` allow-list: normalized error code,
  exact proposal-tool stage, null or bounded field path, normalized rule, and
  boolean retryability. Duplicate errors collapse, only eight are retained, and
  a truncation flag exposes the bound. Raw messages, arguments, content,
  extensions, and malformed error records remain excluded.
- Added fixtures for completed structured errors, unknown results, duplicate
  completions, multiple call IDs, bounded truncation, invalid stage/path,
  successful results, form state, terminal state, and unrelated-event privacy.
  The historical replay remains unchanged because its raw events were discarded.
- Completion/error normalizer tests: 11 passed. Focused normalizer, MCP,
  documentation, and task regression: 72 passed. Full baseline: 758 passed with
  2 Windows symlink-permission skips.
- Completion/error task and admitted-state scope checks: task 6 passed with
  1 advisory; combined working-copy scope 23 passed with 1 advisory. Repository
  check remained 26 passed, 2 warnings, 4 advisories, and no failures;
  `git diff --check` passed. Final paused task check returned 5 passes, 1
  expected warning, 1 advisory, and no failures; final scope failed closed
  solely because the task was paused.
- Native AgentGov self-review tools were not exposed in this parent session.
  A distinct manual advisory pass confirmed that the state precedence remains
  deterministic, duplicate completion IDs and errors are bounded, raw error
  material is excluded, and the change introduces no requirement,
  architecture, Adapter, protocol, config, install, admission, Git, release, or
  deployment drift.

## Product connection

- Upstream: installed Codex Adapter `1.3.0`, its capability-gated proposal
  review tool, and the existing live-replay contract.
- Current value: discovery and exact invocation are proven, and a valid-input
  no-model probe proves Adapter form generation plus App Server schema parsing.
  The diagnosis also prevents `call_started` from being misreported as a
  localized product failure.
- Downstream: the privacy-bounded completion/error correction is complete.
  Product review must decide whether any later replay is still valuable;
  another replay or product implementation remains separate and unauthorized.
- Project effect: this protects the governance kit's evidence boundary without
  creating the custom product App Server client rejected by ADR-0015.

## Domain-independence capability/authority pressure test

- Goal: assess whether current AgentGov abstractions keep consequential
  capability availability separate from authority and can represent an
  outcome-to-learning-candidate-to-admission sequence without importing a
  consumer domain.
- Non-goals: no consumer integration, business-risk concepts, generic
  executor, runtime security boundary, learning engine, Core schema redesign,
  release change, installation, replay, Git action, or external write.
- Evidence reviewed: ADR-0009, ADR-0013, ADR-0015, repository invariants, the
  development state and trigger schemas, the development governance control
  mapping, the governance model, and Native MCP Adapter documentation.
- Advisory conclusion: capability and authority remain distinct; the current
  state/evidence/decision/transition responsibilities are portable, while the
  concrete contracts correctly remain coding-development-specific. Structural,
  advisory-reasoning, and human-authority gates remain meaningful only as
  separate responsibilities, not a combined authorization score.
- Native MCP conclusion: advertised or permitted tools are not admitted
  transitions. Exact elicited decisions and state revalidation govern the
  writes AgentGov owns. AgentGov cannot currently force host tool selection or
  intercept unrelated capabilities and therefore remains explicitly outside a
  runtime enforcement or security-boundary claim.
- Learning conclusion: an outcome may become evidence for a candidate, but a
  future behavior or authority change requires separate validation and human-
  owned admission. No generic learning contract is justified before observed
  use.
- Decision: no pre-v1 Core change. Added invariant documentation and
  `AG-AUTH-001` as a later architecture validation scenario without changing
  the existing roadmap or implementation.
- Acceptance signals: the record answers all seven requested questions,
  identifies the real mediation boundary, introduces no consumer concepts, and
  makes no unsupported enforcement or learning claim.
- Stop condition reached: implementation stops at documentation because no
  domain-independent inconsistency with existing Core principles was found.
- Focused documentation and task-contract regression passed 36 tests.
  Repository governance reported 26 passes, 2 warnings, 4 advisories, and no
  failures. The new case contains none of the excluded consumer-domain terms,
  and `git diff --check` passed.
- Native AgentGov self-review tools were not exposed in this parent session. A
  distinct manual advisory pass confirmed that the record does not claim
  universal permission modeling, automatic learning admission, forced MCP
  tool selection, independent runtime mediation, or security-boundary
  enforcement, and it changes no requirement, Core behavior, release, or
  external authority.

## Native drift-review form installed-runtime rehearsal admitted

- The human product owner selected plan item 1: install the exact development
  Adapter `1.4.0` into the existing local AgentGov pipx runtime and run one
  bounded Native MCP drift-review form rehearsal in an independent temporary
  repository.
- Goal: prove installed discovery, negotiated seven/five-tool compatibility,
  due-state form emission, human-owned decision handling, state revalidation,
  create-only or zero-write behavior, and privacy-bounded replay evidence.
- Non-goals: no Core or schema redesign, project `.codex/config.toml` change,
  consumer activation, standing external service, generic replay client,
  automated human choice, project or remote Git mutation, commit, push,
  publication, release, deployment, or production execution. A disposable
  empty `git init` is allowed only inside the independent temporary replay
  directory because AgentGov requires a bound Git worktree.
- Authority is limited to the exact local build/install, temporary-repository
  preflight, and one replay described by
  `governance/tasks/p0-native-drift-review-form-installed-rehearsal.json`.
- Source pre-install validation passed 66 focused tests. The exact copied
  `governance_mcp.py` and `drift_review.py` hashes matched source. The offline
  local-only wheel was 437,600 bytes with SHA-256
  `DD78FDC64A235FB5D95FBE02AC5DF6F25F7DCE6384AABE841FDFD1F1DB32B575`
  and passed ZIP integrity.
- No AgentGov MCP process was running. The wheel replaced only the existing
  pipx package through `--force-reinstall --no-deps --no-index`, with no
  dependency download or environment creation. Installed identity is package
  `0.3.0rc1`, Adapter `1.4.0`, protocol `2026-07-28`, seven tools with form
  capability, and five without it. Installed MCP/drift regression passed 40
  tests and both installed module hashes match source.
- Project `.codex/config.toml` remained byte-unchanged with SHA-256
  `4CCA2D57EDEADDFE52D3E6C4DD4D774192BBDBCAB4E84E07DF73E14A861C0348`.
- The disposable repository had no prior review baseline and deterministically
  reported `due` / `initial_review_required` with zero records. Startup-only
  preflights corrected the missing Git-worktree prerequisite and the one-shot
  bridge's outdated list-versus-object tool inventory assumption before any
  tool call.
- The one measured direct App Server replay advertised seven tools, called
  `agentgov_drift_review_record` exactly once, and reached exactly one
  thread-bound `form` request with required `decision` and exact options
  `record_candidate`, `snooze`, and `no_record`. The bridge supplied no human
  decision; record count remained zero and the repository was unchanged.
- The bridge retained no raw prompt, model output, tool arguments, form message,
  or raw event stream. No model turn occurred. This proves installed App Server
  forwarding, not current-Agent tool selection, end-user UI presentation,
  human choice application, or semantic-review quality.
- Documentation/task regression passed 36 tests after replacing the obsolete
  `not installed` assertion with protection for local-only installation,
  unpublished/consumer-inactive status, zero human decision/write, and the
  remaining live-selection/UI evidence gap.
- The first complete baseline exposed only that intentionally stale
  documentation assertion. After correction, the unchanged rerun passed all
  776 tests with 2 platform-limited skips.
- The task is paused after its one authorized replay. Its final task check
  reported 5 passes, 1 expected paused-state warning, 0 failures, and 1
  advisory; the repository check reported 26 passes, 2 warnings, 0 failures,
  and 4 advisories. `git diff --check` passed.
- The three bounded build, replay, and generated-schema temporary directories
  were verified by absolute path and deleted after evidence capture. The local
  Adapter `1.4.0` installation remains; project configuration remains
  unchanged. No commit, push, publication, release, deployment, or consumer
  activation occurred.
- Native AgentGov self-review tools were not exposed in this parent session. A
  distinct manual advisory pass found no requirement or architecture drift:
  the slice remains the admitted P0 native-form installation/forwarding check,
  preserves the human-only decision boundary, and states its remaining live
  Agent-selection, UI-presentation, and human-choice-application gaps rather
  than treating them as completed capability.

## Git transport authentication procedure corrected

- The product owner directed that the prior GitHub authentication blocker must
  not be asked again and explicitly requested that AI Radar's working pattern
  be adapted into the repository Agent instructions.
- A bounded read-only revalidation inspected AI Radar `AGENTS.md` at commit
  `d800adc0c6585eeff2b309e80bffa99aff0e6734`, its HTTPS remote declarations,
  and the non-secret system credential-helper selection. AI Radar uses the
  system Git Credential Manager for direct HTTPS pushes; its Git transport
  does not require a valid GitHub CLI token.
- The reuse decision is recorded in `docs/ai-radar-extraction-map.md` as
  `rewrite-required`. No AI Radar credential, account payload, remote,
  deployment coupling, dual-repository rule, or product workflow was copied.
- `AGENTS.md` now distinguishes direct Git transport authentication from
  GitHub CLI/API authentication. After explicit commit/push authority, an
  invalid `gh auth status` alone cannot block direct Git; the Agent uses the
  configured helper, performs one bounded Git Credential Manager login and
  retry only after a real `git push` authentication failure, and never gains
  force-push, PR, release, deployment, or different-remote authority.
- A documentation contract test protects the distinction and the single-retry
  recovery procedure. README, STATUS, plans, ADRs, release metadata, public
  HTML, localized pages, and user guides were checked and do not require a
  change because this correction changes repository-local Agent execution
  guidance, not AgentGov product behavior, architecture, release identity, or
  end-user commands.
