# Development log — 2026-08-26

## No-retry artifact-driver cleanup-gate replay v1

The product owner asked to continue from the latest formal closeout. Read-only
recovery found `main` synchronized with `origin/main`, no tracked working-tree
delta, and the evidence-gated short-root helper as the latest completed slice.
The remaining product question was whether to validate that gate through one
noninteractive artifact-driver replay.

Resolved alignment journey `mcpj-4d64acbcd0bc4f0e8b4a2902e4f78b0e`
records the product owner's selection of that bounded direction. Native
proposal `prp-5f4dac9a5f9244bb8c5b7cb7d252a0fd` admitted task
`p0-no-retry-artifact-driver-gate-replay-v1`; the product owner then separately
instructed execution. Task-start baseline
`sha256:9aecded3f03e8e93d172950275fe01087c0c8d8f625cd370228c91e8ed05e4a4`
was captured before evidence creation, temporary-root allocation, staging, or
build. The capture boundary does not prove earlier state.

Read-only preflight passed the current 186-path manifest and both persisted
identities, found zero `agv-*` roots, matched both current helper-file
identities and all three required helper APIs, and found one compatible
retained Python 3.11 / pip 24.0 / setuptools 84.0.0 backend plus one
identity-matched retained backend wheel. An initial probe incorrectly treated
a local `build` name as installed distribution metadata and failed before any
allocation or build. A corrected distribution and backend check established
the prerequisite; the defective probe remains disclosed and was not retried
as a build.

One noninteractive process created exactly one verified short root. Exactly
186 manifest inputs were copied with 186 byte matches, zero links or extras,
and matching staged path and content identities. The longest projected path
was 201 under the 240-character gate. Exactly one offline, no-index,
no-dependency, no-build-isolation, no-cache wheel build returned zero. No
retry, repair, alternate backend, dependency installation, or network was
used.

The wheel contains 189 regular members with inventory identity
`sha256:15919824a78e07f1d69e2585a76845af009283164ab7b6235a5fe075c5a8a9c2`.
All 76 expected package and 107 expected data payloads are present with 183
byte matches, zero missing or mismatched managed payloads, six separately
classified generated metadata members, and zero unexpected unmanaged
members. Generated-member identity is
`sha256:7afd3c8d423d038df21d83d86adfd9cfd30564122d312b11d871297c88aebf09`.
The whole-wheel identity is
`sha256:4e973258ed2d7e0fd35a9598cf5a6c9cdb9875a87415522f51c726911b267513`.
It differs from the prior whole archive while both member inventories and all
managed payload bytes agree; archive-byte reproducibility was not an admitted
acceptance signal.

The same driver wrote and durably flushed the sanitized pre-cleanup record at
[`docs/build-validation/no-retry-artifact-driver-gate-replay-v1-2026-08-26.md`](../build-validation/no-retry-artifact-driver-gate-replay-v1-2026-08-26.md).
It computed exact evidence identity
`sha256:75e7ba936e32b1f8b717d16c684161703737bc6d3e873eca604805cfef25edd0`,
created the repository-relative receipt, and called
`remove_short_build_root_after_evidence` without input or TTY control flow.
The gate revalidated the same path and bytes, returned
`cleanup_removed=true`, `reads_stdin=false`, and `depends_on_tty=false`, and a
final observation found zero short roots. The previous closed-input transport
deviation did not recur.

Formal task-declared validation completed. The manifest checker passes with
the same 186 paths and both identities. All 23 focused manifest and short-root
tests pass with 3 Windows privilege-limited symbolic-link skips. The complete
supported Python 3.11 suite passes all 1102 tests with 5 platform-limited skips
in 158.209 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The task JSON parse, zero-short-root
check, and `git diff --check` pass. The task-start scope comparison contains 4
passing and 1 preserved findings with no failure: the pre-existing excluded
`.codex` file is byte-identical, the admitted task record is unchanged, and
only STATUS, the new build-validation record, and this dated log changed after
capture. The new task baseline is the exact create-only boundary artifact.

Distinct native current-Agent advisory review
`srv-9b009882addb50a90ad3594b42222d7a` found the selected requirement,
single-build boundary, same-process evidence and cleanup architecture, scope
preservation, no-network and no-stdin security boundary, payload evidence,
and denied authority consistent. It preserves archive-byte variation,
concurrent adversarial replacement, reusable-driver need, cross-platform,
future-toolchain, repeated-reliability, and external-journey behavior as
unknowns. This is a separate self-review pass, not independent assurance. The
native task-completion-record tool is unavailable, so no completion record is
claimed or fabricated.

No product source, helper, manifest, schema, test, previous record, local
configuration, public CLI, installed package, external Agent, model, consumer
form, credential, Git mutation, publication, release, deployment, or full
journey changed or ran. Cross-platform and future-toolchain behavior, repeated
reliability, adoption, time savings, causal benefit, return on investment, and
the need for a reusable driver remain unknown.

## Minimal noninteractive artifact driver v1

After reviewing the successful one-off replay, the product owner selected a
minimal persisted internal driver before any broader end-to-end journey.
Resolved alignment journey `mcpj-3db912cb51964c5782430d651c223a41`
records that direction. Native proposal
`prp-dcfd28255d964c43b7773589bdb650ba` admitted task
`p0-minimal-noninteractive-artifact-driver-v1`, and the product owner then
separately instructed execution.

Create-only task-start baseline
`sha256:6d4b19f18acf5144013cc3412783047f7aa04bf6a04756da1150cbcfae46ca47`
was captured before implementation. It preserves the existing 2026-08-26
replay closeout as pre-existing work and does not prove earlier state.

The new dependency-free `scripts.artifact_replay_driver` module has no
`__main__.py` or public AgentGov surface. It validates the repository,
normalized manifest and evidence references, existing manifest-check result,
callable action, and bounded evidence form. Only after the manifest passes
does it allocate at most one verified short root and invoke the injected
action once.

The action returns caller-sanitized `ArtifactEvidence`; its text is hidden from
representations. The driver exclusive-creates the evidence file beneath an
existing real repository parent, flushes and `fsync`s it, computes the exact
SHA-256, creates a receipt through the existing helper, then calls the existing
evidence-gated remover. Success requires revalidation, removal, and root
absence. No input or TTY state participates.

Every first deviation is mapped to a bounded reason code. Before validated
evidence authorizes removal, the exact root is preserved through a private
recovery handle and no cleanup success is recorded. The driver does not retry
the action, leak raw exceptions, or silently bypass the evidence gate.

Initial implementation validation passes 12 driver fixture tests with 1
Windows privilege-limited directory-link skip. The fixtures cover exact one-
attempt behavior, manifest and evidence preflight, action failure, malformed
evidence, unsafe and existing targets, exclusive-create races, durable-write
ordering, exact digest binding, changed evidence, receipt and cleanup failure,
root preservation, three stdin states, privacy, and the absent CLI surface.

Formal validation passes. All 24 focused driver and short-root tests pass with
3 Windows privilege-limited symbolic-link skips, all 55 user-documentation
tests pass, and the complete supported Python 3.11 suite passes all 1102 tests
with 5 platform-limited skips in 167.903 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON, privacy, zero-short-root,
whitespace, and `git diff --check` pass.

Final task-start scope is `PASS=10 PRESERVED=3 FAIL=0 TOTAL=13`. The seven
post-capture paths are STATUS, the internal automation contract, the new
validation record, this appended dated log, and the three new driver files.
The prior replay evidence, prior task record, and local MCP configuration are
byte-identical. The create-only new baseline is the exact task boundary.

Distinct native current-Agent advisory review
`srv-d5527863960da8481216eeb9820e332d` found the selected requirement,
internal composition architecture, one-action and evidence-before-cleanup
sequencing, scope preservation, privacy boundary, and denied authority
consistent. It retained future real-caller observation fields,
recovery/concurrency needs, cross-platform adversarial replacement, and
semantic evidence completeness as unknowns. This is a separate self-review
pass, not independent assurance. The native task-completion-record tool is not
exposed in this session, so no completion record is claimed or fabricated.

No real artifact build, retained backend, dependency install,
network, external Agent, model, consumer form, credential, Git mutation,
publication, release, deployment, scheduling, retry, repair, or full journey
occurred or was authorized.

## Real-action artifact-driver replay v1

After the minimal internal driver closeout, the product owner selected one
real-action replay before any broader end-to-end journey. Resolved alignment
journey `mcpj-3c98a91e04bb41a79af117293ec4f37e` records that direction.
Native proposal `prp-8e376c9239d14d94afa3ae93f07a6e08` admitted task
`p0-real-action-artifact-driver-replay-v1`, and the product owner separately
instructed execution.

Create-only task-start baseline
`sha256:bed539e05ec6956f08335ceb4b9dbe3c3d5f55a79c462dad6fe9f6b1b7e69762`
was captured before evidence creation, temporary-root allocation, staging, or
build. It preserves the existing driver, automation contract, predecessor
tasks and evidence, local configuration, and predecessor baselines as
pre-existing work and does not prove earlier state.

Read-only preflight passed the current 186-path manifest and both canonical
identities, found zero short roots, and confirmed the persisted driver and all
three short-root receipt and cleanup APIs. A system-environment probe
incorrectly required separately installed `wheel` distribution metadata and
returned nonzero before allocation. The applicable retained environment was
then verified directly as Python 3.11 / pip 24.0 / setuptools 84.0.0 with its
vendored wheel support, callable wheel backend, and one cached setuptools
wheel whose SHA-256 matched the preceding replay's retained candidate.

Two subsequent host command-construction attempts failed before launching a
Python process: the first contained conflicting Markdown and JavaScript
delimiters, and the second relied on an unavailable host `btoa` function.
Read-only checks after those failures found no evidence file and zero short
roots. Neither reached the driver, allocated a root, invoked the action, or
started a build. They are disclosed as pre-action transport defects and are
not counted as action or build retries.

The corrected foreground invocation called the persisted
`run_artifact_replay` exactly once. Its injected action received the verified
short root, copied all 186 manifest inputs with 186 byte matches and zero
links, and reproduced both staged identities. It ran one offline, no-index,
no-dependency, no-build-isolation, no-cache wheel build with closed standard
input. The build returned zero and emitted exactly one wheel.

The wheel contains 189 regular members. All 76 expected package and 107
expected data payloads are present with 183 byte matches and zero mismatches.
Six generated metadata members remain separately classified and there are
zero unexpected unmanaged members. The member-inventory identity is
`sha256:15919824a78e07f1d69e2585a76845af009283164ab7b6235a5fe075c5a8a9c2`,
the generated-member identity is
`sha256:7afd3c8d423d038df21d83d86adfd9cfd30564122d312b11d871297c88aebf09`,
and the whole-wheel identity is
`sha256:247b1a7fb72ff8ea941d884e1e2e76613730d4c91ba0ab59ee521b9ae74ab217`.

The action returned the sanitized immutable record at
`docs/build-validation/real-action-artifact-driver-replay-v1-2026-08-26.md`.
The driver exclusively wrote and flushed that record, computed evidence
identity
`sha256:77a757a2905f482381b40abda7688472f9812ec599acfe10f473c80f2a95f806`,
created its repository-relative receipt, and called the existing gated
cleanup. The result reports `action_attempts=1`, `evidence_written=true`,
`evidence_revalidated=true`, `cleanup_removed=true`, and `root_absent=true`.
An immediate read-only observation found zero short roots.

Formal task-declared validation passes. All 35 focused driver, manifest, and
short-root tests pass with 4 Windows privilege-limited symbolic-link skips.
The complete supported Python 3.11 suite passes all 1102 tests with 5
platform-limited skips in 159.678 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON, privacy, exact evidence
identity, zero-short-root, whitespace, and `git diff --check` pass. Final
task-start scope is `PASS=6 PRESERVED=9 FAIL=0 TOTAL=15`; only STATUS, the new
immutable validation record, and this appended log changed after capture, and
all nine excluded predecessor paths remain byte-identical.

Distinct native current-Agent advisory review
`srv-99484eb1ff3f5e94b92623f73fe24b27` found the selected real-action
requirement, driver-owned orchestration, evidence-before-cleanup sequence,
payload observation, scope, security, privacy, and denied authority
consistent. It treats the pre-action host defects as separately disclosed
transport evidence rather than action or build retries, while retaining
whether invocation-transport readiness should become a future deterministic
precondition as an explicit question. Cross-platform, concurrent,
future-toolchain, recovery, repeated-reliability, adoption, causal-benefit,
and ROI outcomes remain unknown. This is a separate self-review pass, not
independent assurance. The native task-completion-record tool is not exposed
in this session, so no completion record is claimed or fabricated.

No second action or build, dependency installation, network, retained
background backend, public CLI, installed-package change, external Agent,
model, consumer form, credential, Git mutation, publication, release,
deployment, scheduling, retry, repair, or full governance journey occurred or
was authorized.

## Artifact invocation transport readiness gate v1

After reviewing the real-action replay's three pre-action transport defects,
the product owner selected a deterministic transport-readiness precondition
through resolved alignment journey `mcpj-926e428f144e49d09b57d9921173384d`.
Native proposal `prp-0b90697c46ab4f17b0625fea820a01f2` admitted task
`p0-artifact-invocation-transport-readiness-gate-v1`, and the product owner
separately instructed execution.

Create-only task-start baseline
`sha256:320f314ef368841d045f94ee2dbe1db65dedb6c38502d3cee25b7a6784cda36f`
was captured before implementation. It classifies every pre-existing changed
path and does not prove or authorize earlier work.

The new dependency-free `scripts.artifact_invocation_readiness` package is an
internal checker with no public CLI, installed surface, or generic command
runner. Its request contains normalized encoding length and identity rather
than source or payload bytes, private launcher and backend references,
expected wheel/version identities, and an exact denied-authority mapping.

Every metadata, authority, file-safety, wheel-digest, and initial zero-root
gate occurs before process creation. The sole process path is a module-owned
fixed Python capability probe with isolated mode, closed stdin, captured
output, a bounded environment, and timeout. It can run once only. PASS also
requires matching versions, callable build backend, vendored wheel support,
and a second zero-root observation.

The current allocator creates `agv-<8-lowercase-hex>` roots while the admitted
task retained a 16-character future assumption. The observer conservatively
treats both exact shapes as task-owned, preventing either from being missed;
the allocator itself is unchanged.

Initial validation passes all 37 focused readiness, driver, and short-root
tests with 4 Windows privilege-limited symbolic-link skips. The fixtures cover
PASS privacy, metadata and authority deviations, missing/nonregular/linked
files, wheel mismatch, both root checks and both root shapes, timeout,
exception, nonzero/malformed/mismatched probe results, one-attempt behavior,
fixed probe construction, closed stdin, bounded environment, and the absent
payload/command/CLI surface.

No caller content was accepted or decoded. No real fixed probe, driver call,
root allocation or cleanup, action, build, installation, network, retry,
external Agent, model, Git mutation, publication, release, deployment,
scheduling, repair, or full journey occurred or was authorized. Formal
validation and advisory review remain pending at this point in the session.

Formal task-declared validation then passed. All 55 user-documentation tests
pass, and the complete supported Python 3.11 suite passes all 1102 tests with
5 platform-limited skips in 155.402 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON, privacy/surface, zero-root,
whitespace, and `git diff --check` pass. Final task-start scope is
`PASS=11 PRESERVED=9 FAIL=0 TOTAL=20`; every changed implementation and
closeout path is admitted, and all nine excluded predecessor paths remain
byte-identical. A distinct native current-Agent advisory review remains
pending before completion handoff.

Distinct native current-Agent advisory review
`srv-4e5d7275d5cb68dc7e1776bf685b33e5` found the selected pre-action
requirement, all-non-process-gates-before-one-probe architecture, caller-owned
ordering, scope preservation, fixture coverage, privacy boundary, and denied
authority consistent. It retains cross-platform real-probe behavior,
launcher/backend replacement and final-root observation races, excess output
from a misbehaving selected launcher before post-process size rejection, and
future receipt persistence or enforcement as explicit unknowns. Adoption,
failure reduction, time savings, causal benefit, and return on investment
also remain unknown. This is a separate self-review pass, not independent
assurance. The native task-completion-record tool is not exposed in this
session, so no completion record is claimed or fabricated.

No implementation, validation, or review item remains incomplete within the
admitted fixture-only task. The next product review is whether a separately
admitted caller should require and persist the receipt or keep it ephemeral;
this closeout grants no such downstream authority.

## Minimal artifact invocation caller gate v1

After the readiness-gate closeout, the product owner selected one minimal
fixture-only caller through resolved alignment journey
`mcpj-af1b00ba2376472890d521fe1cfb1f4a`. Native proposal
`prp-cfaca43bb260472687c1496e4a3eab1f` admitted task
`p0-minimal-artifact-invocation-caller-gate-v1`, and the product owner then
separately instructed execution.

Create-only task-start baseline
`sha256:2bffc34f5073cff4df7571421b5710deb05644909d61e383a84ee563f9c25d23`
was captured before implementation. It classifies the four shared included
paths and fifteen excluded predecessor paths, while the broad baseline area
may create only this task's new baseline and all 22 prior baseline files must
remain byte-identical.

The new dependency-free `scripts.artifact_invocation_caller` package is an
internal wrapper with no public CLI, installed surface, scheduler, receipt
store, command runner, backend selector, or build logic. Its frozen private
request composes the existing readiness request and unchanged driver
arguments.

The wrapper calls `check_invocation_readiness` once, then validates the whole
PASS type and normalized report before `run_artifact_replay` is reachable.
Readiness errors, unexpected exceptions, malformed values, schema drift, and
authority drift all stop with zero driver calls. A valid fresh receipt remains
in memory only.

Only then does the wrapper forward the exact repository, manifest, projected
paths, evidence target, and action to the unchanged driver once. Driver
failure never repeats either upstream call. The public first-deviation report
contains only phase, normalized reason, attempts, no-input/no-TTY facts, and
denied authority. An exact driver failure is retained privately so its
existing recovery handle is not lost or disclosed.

Initial validation passes all 46 focused caller, readiness, driver, and short-
root tests with 4 Windows privilege-limited symbolic-link skips. The nine new
caller fixtures cover strict order, exact argument identity, PASS composition,
all readiness-phase call-count classes, malformed and authority-drifted
readiness, driver failure and recovery preservation, malformed and drifted
driver results, invalid requests, privacy, closed/redirected/TTY stdin, absent
persistence, and no CLI or caller-content surface.

No real probe, driver, root allocation or cleanup, action, build,
installation, network, retry, repair, receipt persistence, external Agent,
model, Git mutation, publication, release, deployment, scheduling, public
CLI, or end-to-end journey occurred or was authorized. Formal validation and
advisory review remain pending at this point in the session.

Formal task-declared validation then passed. All 55 user-documentation tests
pass, and the complete supported Python 3.11 suite passes all 1102 tests with
5 platform-limited skips in 181.192 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON, privacy/surface, zero-root,
whitespace, and `git diff --check` pass. Final task-start scope is
`PASS=11 PRESERVED=15 FAIL=0 TOTAL=26`; all seven post-capture implementation
and closeout paths are admitted, and the fifteen excluded predecessor paths
remain byte-identical.

All 23 local baseline files pass internal canonical-digest validation. The 22
pre-existing files have current aggregate identity
`sha256:47515c87204234709058ccb43d99680d2427d7b54306f4d2da60e08abf0f9eed`,
and each last-write timestamp precedes the new caller baseline capture. The
task-start scope helper explicitly excludes untracked `.agentgov` state, so
this supplements scope with a current-Agent action/timestamp audit but does
not overclaim an independently captured Git-layer before/after identity for
those 22 local files.

Distinct native current-Agent advisory review
`srv-581dc4e0320923148ea761e653ae2335` found the selected minimal-caller
requirement, readiness-before-driver composition, complete upstream result
validation, scope preservation, fixture coverage, privacy, private driver-
recovery preservation, and denied authority consistent. It retains real
composed and cross-platform behavior, point-in-time launcher/backend/root
replacement, a pre-capture launcher-output quota, possible direct-driver
bypass, and future receipt persistence or driver-level binding as explicit
unknowns. Adoption, failure reduction, time savings, causal benefit, and
return on investment remain unknown. This is a separate self-review pass, not
independent assurance.

The native task-completion-record tool is not exposed in this session, so no
completion record is claimed or fabricated. No implementation, validation,
or review item remains incomplete within the admitted fixture-only task. The
next product review is whether to harden the launcher's pre-capture output
quota or admit one real composed caller replay; this closeout authorizes
neither.

## Real composed artifact caller replay v1 — stopped before caller

The product owner selected the real composed replay direction through resolved
alignment journey `mcpj-3471ff9782b24074a8cd5c82ccc78703`. Native proposal
`prp-fd9d6f666d704b62924bf13057c62974` admitted task
`p0-real-composed-artifact-caller-replay-v1`, and the product owner separately
instructed execution.

Create-only task-start baseline
`sha256:471ebddaa1c0712dd5662f9660ad7b5bf3354e75c223c21cbc7039ea209a924c`
was captured before any real probe, root, evidence, action, or build. Its
immediate comparison preserved every excluded predecessor path. Read-only
preflight then passed the current 186-path manifest and exact path/content
identities, found one fixed-capability Python 3.12.13 / pip 26.0.1 /
setuptools 83.0.0 launcher, matched the retained backend-wheel digest, confirmed
the new evidence target was absent, and observed zero task-owned short roots.

The one-off replay harness compiled successfully. At runtime, before request
construction, it tried to import `denied_authority_request` from the readiness
package root. The helper is defined in the private readiness module but is not
re-exported by the package. Python therefore stopped at import time. The
harness did not call `run_ready_artifact_replay`; caller, readiness-probe,
driver, action, build, cleanup, and retry counts are all zero. No evidence file
or short root was created.

The admitted task forbids retry or repair after the first deviation. The Agent
did not correct and rerun the harness, invoke a second process attempt, or
perform cleanup. A read-only post-failure observation confirmed the evidence
target remained absent and the short-root count remained zero.

The uncaught import failure emitted a Python traceback containing an absolute
host path in transient terminal output. Repository evidence does not retain
that path, traceback, raw output, source content, credential, environment
value, or process identity. This remains a real privacy deviation in the
execution harness and must not be reported as a successful privacy-bounded
replay.

The real composed replay is incomplete. Any corrected attempt requires a new
human-admitted recovery task, a new task-start baseline, a corrected import
from the defining readiness module, and bounded exception handling that emits
no raw traceback. This log entry grants none of that downstream authority.

Post-failure validation passed. All 57 focused caller, readiness, driver,
manifest, and short-root tests pass with 5 Windows privilege-limited symbolic-
link skips; all 55 user-documentation tests pass; and the complete supported
Python 3.11 suite passes all 1102 tests with 5 platform-limited skips in
177.120 seconds. Task governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3` and
repository governance is `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON,
whitespace, zero-root, and `git diff --check` pass. Final task-start scope is
`PASS=5 PRESERVED=21 FAIL=0 TOTAL=26`: only the admitted status and dated-log
closeout evolved after capture, while all 21 excluded predecessor findings
remain byte-identical.

Distinct native current-Agent advisory review
`srv-f272e93cf8832ce80757187da823d7e7` confirms that the selected replay
outcome was not achieved, the exhausted no-retry boundary was honored, the
incorrect import belongs to the one-off integration harness rather than
evidence of a checker, caller, or driver defect, the transient absolute-path
traceback is a real security deviation, and scope remains bounded. It
recommends that any separately admitted recovery require import-resolution
preflight and outermost bounded exception handling. This is self-review, not
independent assurance, completion, or retry authority.

## Real composed artifact caller replay recovery v1 — stopped before dry source

The product owner selected corrected single replay through resolved alignment
journey `mcpj-16cd8cebb28e4dfeb45359f2c7b3b4e4`. Native proposal
`prp-1a91d6b970764729bca09be5364890c7` admitted recovery task
`p0-real-composed-artifact-caller-replay-recovery-v1`, and the product owner
separately instructed execution.

Create-only recovery baseline
`sha256:739f96c2697c3c38aa91be39aa4c704a4b58949910bed71fafc1265bc8061571`
was captured before harness execution. Immediate scope comparison preserved
the stopped predecessor and every other excluded implementation, task,
evidence, contract, and configuration path. Read-only preflight passed the
186-path manifest and both canonical identities, one compatible fixed
launcher, the retained backend-wheel digest, absent recovery and predecessor
evidence targets, and zero task-owned short roots.

The recovery command constructed one in-memory source and one base64 encoding
for dry and actual modes. Before the encoded source could compile or execute,
PowerShell passed the multiline outer-wrapper argument to `python -c` in a form
that produced invalid Python syntax. The outer wrapper therefore never reached
its own exception boundary. Python emitted a transient syntax-traceback
fragment and PowerShell command-location metadata. No absolute host path was
observed in that fragment, and no raw traceback, command text, source,
environment value, credential, or process detail was persisted in repository
evidence.

The first dry-wrapper deviation exhausted this task's no-second-dry boundary.
The Agent did not repair quoting, change process transport, re-encode, or run
dry or actual mode again. Encoded dry-source executions, actual-wrapper
executions, caller calls, readiness probes, driver calls, actions, builds,
cleanups, and retries are all zero. Both evidence targets remain absent and a
post-failure observation reports zero short roots.

This is the second separately admitted one-off replay attempt to stop in
harness bootstrap before the caller. A third direct replay would repeat an
untested integration pattern. The recommended product review is therefore a
fixture-tested repository-internal bounded harness contract covering exact
Windows process argument transport, source compilation, import resolution,
and outermost exception containment before any further real probe, driver,
action, or build. This log grants no authority for that work.

Post-failure validation passed. All 57 focused caller, readiness, driver,
manifest, and short-root tests pass with 5 Windows privilege-limited symbolic-
link skips; all 55 user-documentation tests pass; and the complete supported
Python 3.11 suite passes all 1102 tests with 5 platform-limited skips in
175.458 seconds. Task governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3` and
repository governance is `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON,
whitespace, zero-root, and `git diff --check` pass. Final recovery scope is
`PASS=5 PRESERVED=22 FAIL=0 TOTAL=27`; only admitted status and log closeout
surfaces evolved after capture.

Distinct native current-Agent advisory review
`srv-2a9d99333bd0ed04c165d010720edcc2` confirms the recovery outcome was not
achieved. It identifies a circular architecture boundary: dry preflight used
the same untested multiline process-argument wrapper it was expected to
validate, so outer Python parsing could fail before bounded exception handling
existed. The review confirms the no-second-dry rule and admitted scope were
preserved, records the transient raw traceback as a security deviation, and
recommends a fixture-tested internal harness that owns process transport from
outside the evaluated source before any further real replay. This is self-
review, not independent assurance or downstream task authority.

## Bounded artifact replay harness v1

### Goal and boundary

Implement the repository-internal transport prerequisite recommended after two
separately admitted one-off composed replay attempts stopped before the caller.
The task adds a dependency-free, fixture-tested dry-then-actual worker boundary;
it does not perform a real replay or invoke caller, readiness, driver, action,
build, evidence, cleanup, network, model, Agent, Git, publication, release, or
deployment behavior.

The product owner admitted proposal
`prp-54cf500a0d5c4d7d80921bf306f111f2`. Task validation then returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and create-only task-start baseline
`sha256:648e75afd1d9a6a59c3719170940470ae6a693f1eed44613f293f22c83d166ba`
was captured before implementation writes.

### Implementation

- Added `scripts.artifact_replay_harness` with a private request, bounded result
  and first-deviation error contract.
- The parent encodes one UTF-8 source once and reuses its exact Base64 value,
  raw and encoded lengths, and SHA-256 identities for dry and actual modes.
- The fixed child boundary is a module argument vector plus JSON stdin, not
  multiline `python -c` source transport.
- The worker validates protocol and identity, compiles with a synthetic name,
  resolves imports during initialization, and invokes one zero-argument source
  entry point with a fixed dry or actual mode global.
- Python-level output is discarded. The parent never returns captured child
  output or exception text and stops on descriptor output, stderr, malformed
  JSON, timeout, launch error, or any strict result-schema deviation.
- Actual runs once only after dry PASS. Neither phase retries.

### Validation and limits

All 9 focused fixture tests pass. They cover real Windows fixed-module/stdin
transport, same-source ordering, syntax, import, initialization and execution
failures, missing or malformed protocol boundaries, timeouts, launch errors,
source-identity mismatch, Python output suppression, descriptor-output
fail-closed behavior, private representations, and zero actual attempts after
dry failure. Fixture source is harmless and does not import or invoke the real
artifact components.

This harness is not a security sandbox and does not grant authority to supplied
source. A real composed replay remains incomplete and needs a separate admitted
task.

Final validation passes. The combined harness, caller, readiness, driver,
manifest, and short-root run passes 66 tests with 5 Windows privilege-limited
symbolic-link skips; all 55 user-documentation tests pass; and the complete
supported Python 3.11 suite passes all 1102 tests with 5 platform-limited skips
in 164.406 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and final captured scope is
`PASS=11 PRESERVED=22 FAIL=0 TOTAL=33`. The admitted task JSON, bounded privacy
and forbidden-component scans, zero short-root observation, whitespace, and
`git diff --check` pass. No real artifact evidence was created.

A distinct current-Agent advisory review found the implementation aligned with
the admitted transport prerequisite and its no-real-replay boundary. It also
confirmed two limits: the harness is containment rather than a security
sandbox, and validation proves the current Python 3.11 Windows boundary but not
every launcher, platform, hostile-source behavior, adoption benefit, or a real
end-to-end replay. This task had no alignment journey, so no native self-review
completion is claimed.

## Bounded artifact replay identity bridge v1

### Direction and boundary

Read-only preparation for the requested real replay found that the existing
harness computed exact source identities but exposed only dry or actual mode to
executed source. The source therefore could not bind the existing readiness
request to its own harness-prepared encoded bytes without using an unrelated
placeholder.

The product owner selected the minimal identity-bridge direction through
resolved alignment journey `mcpj-7f437c4bba4a45689835b0600fc68074`.
Proposal `prp-44c21a236d2a435ba59afdf769013963` admitted the exact fixture-only
task. Create-only task-start baseline
`sha256:5e9dfa727ee336786cada1209ad4657cda8b2369a45e40ee4d0583f1b20b31bd`
was captured before implementation writes.

### Implementation

- The harness contract names one exact source-identity global and four exact
  identity keys.
- Only after strict Base64, length, raw SHA-256, and encoded SHA-256 validation
  does the worker create a `MappingProxyType` over those normalized scalars.
- Dry and actual source receive that read-only mapping alongside the existing
  fixed mode global.
- The parent request retains only source, repository, launcher, and timeout;
  callers cannot inject identity metadata separately.
- The worker verifies that the global still refers to the same mapping after
  source initialization and after its entry point. Mapping mutation fails and
  binding replacement returns `source_identity_context_drift`.

### Validation and limits

All 12 focused harness fixtures pass. New coverage proves exact mapping values,
read-only behavior, absence of parent identity fields, real dry/actual worker
use, initialization and execution replacement rejection, zero actual attempts
after dry deviation, and the pre-existing malformed-identity and no-retry
boundaries. Fixtures remain harmless and do not invoke any real artifact
capability.

This bridge does not execute or authorize a real replay and does not make the
harness a security sandbox. A separately admitted task must still own any real
caller, readiness, driver, action, build, evidence, cleanup, or recovery.

Formal validation passes. The combined harness, caller, readiness, and driver
run passes 46 tests with 2 Windows privilege-limited symbolic-link skips; all
55 user-documentation tests pass; and the complete supported Python 3.11 suite
passes all 1102 tests with 5 platform-limited skips in 176.635 seconds. Task
governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and final captured scope is
`PASS=13 PRESERVED=24 FAIL=0 TOTAL=37`. The admitted task JSON, bounded privacy
and forbidden-component scans, zero short-root observation, whitespace, and
`git diff --check` pass. No real artifact evidence was created.

Distinct native current-Agent self-review
`srv-073a6b49d16c8fb49bd127d411e0ade0` found the requirement, worker-owned
identity architecture, scope, implementation, privacy, and denied-authority
boundaries consistent. It retains the unknown future integration deviation,
cross-platform behavior, and hostile-source containment limits and is advisory
rather than independent assurance. The native task completion-record tool was
not exposed in this session, so no completion record is claimed.

## Real composed artifact identity-bridge replay v1

### Direction, admission, and baseline

The product owner selected one real no-retry replay through resolved alignment
journey `mcpj-703752b5a1ca40108777e476398056fa` and admitted proposal
`prp-e8dff2ec2c774fe28a5b0b0571041121`. Create-only task-start baseline
`sha256:c2c238cb6431a117ce5ac26f3d73cefff2dea985515ee69cef2ef036c65f9423`
was captured before any real replay action.

Read-only preflight passed the current 186-path manifest with path identity
`sha256:cfea3a3632bd75d1f4db51168d257c465987d3fe620fda2449abcd86f42fcd03`
and content identity
`sha256:a986da7096e257b90e984084373f923ad460ff4c95f172a08fe4cf2d1fccacaf`.
It found a compatible fixed Python 3.11 / pip 24.0 / setuptools 84.0.0
launcher, matched the retained backend wheel identity
`sha256:51a52592b3b99e102b609654876bd65f19f999935166d1352678931132b0c670`,
confirmed the new evidence target was absent, observed zero short roots, and
preserved every excluded path.

### First deviation and stop

The one planned replay source and a bounded parent program were held only in
process environment for the outer launch. PowerShell native-argument handling
removed quotes around a path literal when passing that parent program to
Python. Python rejected the parent program during parsing before it could
compile the replay source or call `run_bounded_artifact_replay`.

The parent parser emitted an uncontained ephemeral terminal diagnostic that
included a private host path. No diagnostic, source, host path, environment,
credential, process detail, or traceback was persisted in repository evidence.
This is recorded as a privacy-boundary failure rather than a bounded Harness
result.

Outer parent launches were 1. Harness dry workers, Harness actual workers,
callers, readiness checks, probes, drivers, actions, builds, evidence writes,
cleanup calls, retries, repairs, and alternate transports were all 0. The
planned evidence target remains absent and a final read-only observation found
zero short roots. The first deviation exhausted the task, so no quoting repair,
second launch, transport substitution, implementation change, or manual
cleanup occurred.

### Validation and remaining gap

All 69 related Harness, caller, readiness, driver, manifest, and short-root
tests pass with 5 Windows privilege-limited symbolic-link skips. The complete
supported Python 3.11 suite passes all 1102 tests with 5 platform-limited skips
in 160.749 seconds, and all 55 user-documentation tests pass. Task governance
is `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and final captured scope is
`PASS=5 PRESERVED=29 FAIL=0 TOTAL=34`. Task JSON, bounded persisted-content
privacy, evidence absence, zero short-root, whitespace, and `git diff --check`
checks pass.

Distinct native current-Agent self-review
`srv-3b0886336ee3309263dab41e4283cc93` found the unmet requirement, dynamic
parent-transport architecture gap, ephemeral terminal privacy failure,
preserved scope, zero artifact execution, and denied-authority boundary
consistent. It is advisory rather than independent assurance. The native task
completion-record tool was not exposed in this session, so no completion
record is claimed.

The immutable identity bridge remains validated, but no real execution has yet
shown that the caller consumes it or that the unchanged driver reaches the
evidence-gated cleanup. A future product review may consider a fixed,
fixture-tested repository-internal controller that avoids dynamic parent
`python -c` transport. That review input grants no task or execution authority.

## Interview-ready artifact replay close loop v1

### Direction, admission, and implementation

The product owner selected the fastest bounded implementation-plus-replay
direction through resolved alignment journey
`mcpj-920c2bf73d3d4d26b25e43c178edd60f` and admitted proposal
`prp-04fbe1a7a9af4fd087734cec931e22e6`. Create-only task-start baseline
`sha256:7eca40f485ae4f483063902e773f545ee83bc61c722ee342f848e4856c656ced`
was captured before implementation writes.

Added `scripts.artifact_replay_controller` as a fixed repository-owned module.
It accepts strict JSON configuration on stdin with no source or command field,
constructs the small replay source internally, and calls the existing Harness
directly. The imported controller-owned runtime owns dry preflight and the
actual caller-to-cleanup chain. Bounded results expose attempt counts and stable
reason codes only; the controller never retries.

Six controller fixtures pass. They cover the fixed Harness handoff, internally
constructed source, immutable identity use, strict request shape, bounded JSON,
malformed input, dry behavior, and zero retry after Harness failure. The
combined controller, Harness, caller, readiness, driver, manifest, and
short-root suite passes 75 tests with 5 Windows privilege-limited symbolic-link
skips. The complete supported Python 3.11 suite passes all 1102 tests with 5
platform-limited skips in 172.866 seconds, and all 55 user-documentation tests
pass before the one real replay.

### Sole real replay and first deviation

Fresh preflight passed the 186-path manifest, path identity
`sha256:cfea3a3632bd75d1f4db51168d257c465987d3fe620fda2449abcd86f42fcd03`,
content identity
`sha256:a986da7096e257b90e984084373f923ad460ff4c95f172a08fe4cf2d1fccacaf`,
backend-wheel identity
`sha256:51a52592b3b99e102b609654876bd65f19f999935166d1352678931132b0c670`,
absent evidence target, zero short roots, unchanged excluded paths, task
governance, documentation tests, and whitespace.

The fixed controller was invoked exactly once by module name with JSON stdin.
It reached the Harness, whose single dry worker returned bounded
`harness_source_execution_failed`. Attempt counts are controller 1, dry 1,
actual 0. Caller, readiness, formal probe, driver, action, build, evidence, and
cleanup attempts are all 0. The controller emitted no source, host path,
captured output, environment, credential, process detail, exception text, or
traceback. No retry or implementation repair was performed.

The exact private exception is intentionally unavailable. Static inspection
identifies a likely boundary mismatch: dry preflight calls the distribution
manifest checker, that checker launches bare `git`, and the Harness worker's
bounded environment omits `PATH`. Confirming that hypothesis would itself be a
second execution, so it remains an evidence-bounded diagnosis rather than a
proven runtime cause.

### Closeout and remaining gap

The requested real build evidence file remains absent, and the final short-root
count is 0. The fixed controller closes the earlier PowerShell dynamic-source
transport gap, but the real caller, readiness, build, evidence receipt, and
cleanup gate remain unexercised. A new product review must select a bounded way
to reconcile Git-backed manifest observation with the child environment before
any separately admitted one-attempt replay. This record grants no retry, Git,
publication, release, or deployment authority.

Final governance and integrity checks pass. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and captured task scope is
`PASS=11 PRESERVED=29 FAIL=0 TOTAL=40`. Task JSON, evidence absence, zero short
roots, privacy scanning over admitted changed surfaces, whitespace, and `git
diff --check` pass.

Distinct native current-Agent self-review
`srv-9f7bf9f9a717154b84987913ce47e3e4` found the fixed controller,
validation, scope, privacy, and denied-authority boundaries consistent. It
retained the unmet gated-cleanup proof, likely Git/environment contract
mismatch, suppressed exact runtime exception, cross-platform limits, and
unknown interview and adoption benefit. The review is advisory rather than
independent assurance. The native task completion-record tool was not exposed,
so no completion record is claimed.

## Artifact replay manifest boundary repair v1

### Direction, admission, and baseline

The product owner selected “move manifest preflight outward” through resolved
alignment journey `mcpj-e1b17bdc141b4511bf3f49fb302cdd43`. Proposal
`prp-e6052bc4089d4dedbbf48576447bd5c3` admitted the exact fixture-only repair
task. Create-only task-start baseline
`sha256:af663859440d0f746c11daecab545aa542aac40dbd9b6078ea7c2ea3aaa6672f`
was captured before repair writes.

The selected direction keeps the Harness child's bounded environment unchanged
and does not add a Git executable, command, or `PATH` field. The stopped replay
task remains exhausted; this repair task grants no real replay authority.

### Implementation

- The fixed controller resolves the repository and runs the existing
  Git-backed distribution manifest check before constructing source or calling
  the Harness.
- A manifest exception, malformed result, non-PASS status, count mismatch, or
  digest mismatch returns bounded `manifest_preflight_failed` with controller
  attempts 1 and both Harness attempt counts 0.
- Only the normalized path count, path digest, and content digest from the
  passing observation are placed in the internally constructed runtime
  configuration. The external strict JSON request is unchanged.
- Worker-side replay re-derives distribution paths, compares them with the
  declared manifest, and recomputes path and content identities from regular
  repository files. It invokes no Git subprocess and does not depend on
  executable search.
- Worker-side state or identity drift stops dry execution before caller,
  readiness, driver, action, build, evidence, or cleanup, so actual attempts
  remain zero.

### Validation and remaining boundary

All 8 controller fixtures pass, covering controller-before-Harness ordering,
normalized manifest binding, malformed or drifted manifest failure, no worker
Git process, filesystem revalidation, strict JSON, private representations,
dry behavior, bounded Harness failure, and zero retry. The combined controller,
Harness, caller, readiness, driver, manifest, and short-root suite passes 77
tests with 5 Windows privilege-limited symbolic-link skips. All 55
user-documentation tests pass, and the complete supported Python 3.11 suite
passes all 1102 tests with 5 platform-limited skips in 188.493 seconds. The
current 186-path manifest still passes with path identity
`sha256:cfea3a3632bd75d1f4db51168d257c465987d3fe620fda2449abcd86f42fcd03`
and content identity
`sha256:a986da7096e257b90e984084373f923ad460ff4c95f172a08fe4cf2d1fccacaf`.

No real controller replay, artifact build, evidence write, receipt, cleanup,
network, dependency installation, Git mutation, publication, release, or
deployment occurred. Whether one separately admitted real replay now reaches
readiness, build, evidence, and gated cleanup remains unknown.

Final governance and integrity checks pass. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and captured task scope is
`PASS=13 PRESERVED=30 FAIL=0 TOTAL=43`. Task JSON, zero short roots, zero
private-host-reference matches, real-evidence absence, whitespace, and `git
diff --check` pass.

Distinct native current-Agent self-review
`srv-ec060fa5ad28312f92be1522518821b1` found the selected architecture,
implementation, validation, scope, privacy, and denied-authority boundaries
consistent. It retained the point-in-time Git observation limit, unknown real
replay result, cross-platform limits, and unknown interview benefit. The review
is advisory rather than independent assurance. The native task
completion-record tool was not exposed, so no completion record is claimed.

## Artifact driver filesystem manifest boundary v1

### Direction and admission

Read-only call-chain inspection found that the post-repair actual path still
entered `run_artifact_replay`, whose manifest preflight launched bare Git inside
the Harness worker's intentionally `PATH`-free environment. A direct real
replay would therefore have consumed its sole actual attempt before build.

The product owner selected the controller-bound-facts direction through
resolved alignment journey `mcpj-f8d4eaea8b9e4f24888d0072e44c0f30`.
Proposal `prp-064c96dd4a2b487b867d449acafa843d` admitted the exact
fixture-only repair. Create-only task-start baseline
`sha256:c62f12195e570e63421e6331b488e3dac4155cbc933923308ea9217eb961840f`
was captured before implementation writes.

### Implementation

- The private controller/caller/driver request now carries the exact manifest
  path list, count, path digest, and content digest already admitted by the
  controller.
- The driver re-derives distribution inputs and recomputes both identities from
  repository files immediately before short-root allocation.
- The driver no longer imports or invokes the Git-backed manifest checker.
- Invalid facts or filesystem drift stop with `manifest_preflight_failed`, zero
  action attempts, no root allocation, no evidence, and no cleanup claim.
- The external controller request, Harness environment, offline action,
  evidence format, evidence-gated cleanup, and no-retry semantics are unchanged.

### Validation and remaining boundary

All 30 focused controller/caller/driver tests pass with one Windows
privilege-limited symbolic-link skip. The combined artifact chain passes 78
tests with five platform-limited skips. The complete supported Python 3.11
suite passes all 1102 tests with five skips in 181.228 seconds. The 186-path
manifest and its path/content identities pass unchanged. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; task JSON, captured scope, whitespace, and
`git diff --check` pass.

No real controller replay, build, evidence write, receipt, cleanup, network,
dependency installation, Git mutation, publication, release, or deployment
occurred. Whether a separately admitted real replay now completes readiness,
build, evidence, and gated cleanup remains unknown.

Final captured scope is `PASS=24 PRESERVED=25 FAIL=0 TOTAL=49`; zero short
roots and real-evidence absence were observed, and all 55 user-documentation
tests pass after closeout edits. Distinct native current-Agent self-review
`srv-eb8aa23dc7ebbc322763565fa624c0ca` found the requirement, architecture,
implementation, scope, privacy, and denied-authority boundaries consistent. It
retained point-in-time and cross-platform limits, the unknown real replay
outcome, and lack of adversarial sandbox proof. The review is advisory rather
than independent assurance. The native task completion-record tool was not
exposed, so no completion record is claimed.
