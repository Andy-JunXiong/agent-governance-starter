# Live Codex MCP consumer binding evidence - 2026-08-30

## Goal and admitted boundary

The human resolved alignment journey
`mcpj-6588877d0faf4e7b90b033f1b48ad1f3` in favor of one real MCP consumer
validation. Native proposal `prp-0378bf5dc5dd48d1b61ba598204869d3`
admitted task `p0-live-codex-mcp-consumer-binding-evidence-v1`, and the human
separately replaced the active task pointer after reviewing the exact plan.

The task adds only normalized consumer evidence, its documentation contract
test, current status, and this log. It excludes the project Codex configuration,
the prior MCP parity implementation, runtime behavior, external consumers, Git,
publication, release, and deployment.

## Initial observation

Official OpenAI documentation establishes that Codex supports project-scoped
STDIO MCP configuration and tool allow/deny lists. A read-only Codex CLI query,
a privacy-bounded project-config parse, and the current callable inventory agree
on one enabled six-tool AgentGov binding. The source contract contains eight
form-capable tools; completion and drift review are omitted by the existing
project allow-list. This is classified as a host-binding gap rather than an
Adapter failure.

The current session exercised native proposal review plus alignment start,
update, and resolve. Initial validation then passed all 57 focused documentation
tests and all 1,113 repository tests with five Windows privilege-limited
symbolic-link skips. Native self-review start and complete exercised the final
two configured tool paths and returned five bounded advisory observations. The
review found no requirement, architecture, implementation, privacy, or scope
correction, while retaining eight-tool live exposure, repeated reliability,
and independent review as unknown.

## Validation and closeout boundary

Task governance reports `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and `git diff --check`
passes. The task-start comparison reports `PASS=6 PRESERVED=6 FAIL=0`: all five
task-owned paths are admitted and the six pre-existing excluded changes remain
byte-identical. The raw worktree scope check still reports those six excluded
paths as failures; no exception or ownership transfer is inferred.

The declared focused and full test commands are rerun after this closeout
update before handoff. No completion record is available on the configured
six-tool host. No commit, push, publication, release, or deployment is claimed
or authorized.
