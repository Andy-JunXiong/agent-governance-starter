# Development log - 2026-08-03

## Phase 7 redacted development-event export and portable Monitor input

- Added admitted high-risk task `p0-redacted-development-event-export`. It
  explicitly excludes automatic upload, GitHub Actions mutation, central
  telemetry, source/prompt/validation-output export, governance-file writes,
  Git transitions, release, and deployment.
- Implemented strict `agentgov.development-event-export` 1.0. The create-only
  JSON bundle binds canonical ordered events to an event digest and
  content-derived export ID, validates source count and interval, and fixes
  claim and denied-authority fields.
- Implemented the `metadata_only_v1` redaction profile. It removes actor labels
  and local evidence references while retaining bounded event/task identities,
  timestamps, actor class, governance references, reason codes, outcomes, and
  small counters. Local event validation now also rejects recognized GitHub,
  OpenAI-style, AWS, bearer, JWT, and private-key token shapes in addition to
  credential assignments and absolute paths.
- Added `agentgov export development --repository .`. `--dry-run` renders the
  exact source counts, output, retained metadata, removals, observation scope,
  and authority boundary. A write requires exact `EXPORT` confirmation from an
  interactive terminal and refuses empty or CI input, symbolic links, paths
  outside the repository, tracked/existing output, silent overwrite, and
  oversized metadata.
- Extended `agentgov development-monitor` to schema 1.2. The Monitor now
  accepts `exported_development` from one validated export and `combined` from
  that export plus an actor-validated CI replay directory. Observation data
  reports per-source counts, and every Timeline event identifies whether it
  came from `local_session`, `exported_development`, or `ci_only` input.
- All four scopes remain `partial`. Even `combined` keeps cross-stage discovery
  unavailable because the event contract has no stable finding identity or
  resolution link. The Monitor still does not claim coding-agent consumption,
  requirement or architecture correctness, causal benefit, ROI, approval, or
  downstream authority.
- Added contract, privacy, integrity, immutability, path, confirmation, CLI,
  source-label, combined-input, claim-limit, and schema fixtures. Focused export,
  Monitor, and event-store validation passed before the full regression run.
- Full validation passed 496 source tests with one platform-limited skip.
  Repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`; the
  admitted Phase 7 task returned `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; both new
  CLI help surfaces rendered; and `git diff --check` passed. No commit, push,
  release, deployment, workflow change, artifact upload, hosting, AI Radar
  write, or external-system mutation was authorized.

## Future-0.3 development package identity

- Investigation of the next opt-in GitHub Actions Monitor slice stopped before
  workflow edits because the managed workflow contract requires a published,
  digest-pinned wheel while development source still reported stable 0.2.1.
- Added admitted supporting task `p0-development-version-identity` with an
  explicit boundary against workflow mutation, consumer migration, tagging,
  release, and publication.
- Promoted the single runtime/package version source to `0.3.0.dev0` and the
  bundled compatibility metadata to the matching `development` channel. The
  metadata now admits stable 0.2.1 as a prior compatible version without
  declaring a layout change, migration, or published artifact.
- Kept published stable 0.2.1 documentation and the managed-consumer pins as
  unchanged external facts. Added update regression coverage proving stable
  0.2.1 is not presented as an upgrade from future-0.3 development source.
- Focused package, release-metadata, and update validation passed 36 tests.
  Full validation passed 497 tests with one platform-limited skip. The admitted
  identity task returned `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; its working-copy
  scope returned `PASS=12 FAIL=0 ADVISORY=1`; repository governance remained
  `PASS=16 WARN=2 FAIL=0 ADVISORY=4`; and `git diff --check` passed.
- An isolated temporary source copy built
  `agent_governance_starter-0.3.0.dev0-py3-none-any.whl` with SHA-256
  `7bb2443d1a536cf211896d748d0ae7c44d52a0cd6942c41f0703b99f0ac1920f`.
  A fresh temporary virtual environment installed it with `--no-deps` and
  reported `agentgov 0.3.0.dev0`.
- No GitHub Actions change, consumer migration, commit, push, tag, release,
  artifact upload, or external-system mutation was authorized.

## Public documentation and presentation synchronization

- Re-established the AgentGov product page as the primary public story and
  retained the evidence portfolio as a secondary inspection surface.
- Aligned the portfolio with the existing AgentGov navy, purple, lime, cream,
  typography, navigation, and security posture. Removed its separate theme
  switch and direct external AI Radar call to action.
- Moved AI Radar after the AgentGov lifecycle and cases as a bounded origin
  reference that points to the repository-local extraction and reuse record.
  Independent consumer projects remain absent from the public HTML story.
- Updated README, product HTML, English and Chinese quickstarts, troubleshooting,
  adoption guidance, governance model, dated decision context, sample reports,
  and tests for published stable 0.2.1 versus development source 0.3.0.dev0.
- Unified the published user path on an isolated `pipx` installation followed
  by `agentgov` commands. `PYTHONPATH=src python -m agentgov` remains only for
  explicitly labeled source-development use.
- Added a repository instruction requiring behavior, scope, release, command,
  and journey changes to update all affected current documentation, HTML,
  localized pages, release metadata, and protecting tests in the same slice.
- Full validation passed 507 tests with one platform-limited skip; 46 focused
  documentation and page tests passed; all 12 public HTML files had resolvable
  repository-relative links; CSS braces were balanced; and `git diff --check`
  passed with only the existing Chinese-HTML line-ending notice.
- The admitted documentation-sync task returned
  `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; its combined working-copy scope returned
  `PASS=35 FAIL=0 ADVISORY=1`; and repository governance remained
  `PASS=16 WARN=2 FAIL=0 ADVISORY=4`.

## Published evidence-reference page repair

- Reproduced the public evidence journey reported by the user: GitHub Pages
  served repository `.md` targets as raw Markdown, while `../schemas/...`
  escaped the project base path and returned 404 from the account root.
- Replaced every Portfolio `.md` href with a project-local `.html` target and
  added one AgentGov reference layout and stylesheet; no client-side Markdown
  parser or external runtime was introduced.
- Published both development schemas under `docs/schemas/`, added styled HTML
  schema views, and added byte-identity tests so the public JSON copies cannot
  silently drift from the authoritative `schemas/` files.
- Added structural protection against raw `.md` links, `../` site-root escapes,
  missing relative targets, schema-copy drift, missing source attribution, and
  loss of AgentGov navigation or human-authority wording.
- Full validation passed 511 tests with one platform-limited skip; 11 focused
  Portfolio/reference tests passed; task validation returned
  `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; scope returned
  `PASS=29 FAIL=0 ADVISORY=1`; and repository governance remained
  `PASS=16 WARN=2 FAIL=0 ADVISORY=4`.
- Human-authorized commit `cd25557` was pushed to `main`, and its Pages build
  completed successfully. Live replay proved the Portfolio paths and both
  schema HTML/JSON surfaces returned 200, but also exposed a Jekyll output
  collision: same-name Markdown and HTML proxies targeted one `.html` path, and
  the Markdown output kept the default GitHub theme.
- The working copy correction removes the eleven conflicting HTML proxies and
  declares `layout: reference` plus source attribution directly in each
  authoritative Markdown file.
- Human-authorized follow-up commit `743f3b3` was pushed to `main`; its Pages
  build completed successfully. Live verification confirmed all 13 HTML
  reference/schema views returned 200 with the AgentGov reference layout, both
  schema JSON URLs returned 200 with valid JSON, and the published Portfolio
  contained zero `.md` or `../` hrefs. The reference-page repair is closed.

## End-of-day closeout

- Today's authorized sequence on `main` is complete: `686475c` aligned the
  AgentGov story and development identity, `cd25557` introduced rendered
  reference surfaces, `743f3b3` resolved the Jekyll output collision, and
  `f3514d8` recorded the final public verification.
- Public verification at the closeout commit confirmed the AgentGov product
  page and Portfolio were available, all 13 linked HTML reference/schema views
  returned 200 with the shared layout, both public schema JSON files returned
  valid JSON, and the Portfolio had zero `.md` or `../` links.
- Current sources remain `0.3.0.dev0` development work while the published
  stable and managed-consumer fact remains 0.2.1. No 0.3 release, NYC
  migration, AI Radar runtime/data import, autonomous merge, or deployment was
  performed.
- Product and requirement drift review found no new drift in this slice. The
  public story now follows ADR-0009's development-time governance core, keeps
  PR/CI as a backstop, treats AI Radar only as a bounded origin reference, and
  keeps NYC isolated as an independent consumer project.
- Final closeout validation passed 17 focused public-documentation tests and
  all 511 source tests with one platform-limited skip. Repository governance
  remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed.
- The next session should begin with a separately admitted, minimal opt-in
  GitHub Actions artifact slice for the existing static development Monitor.
  It must preserve redaction and partial-observation claims and must stop for a
  separate packaging decision if a digest-pinned development executable is not
  yet truthfully available.

## Social-link preview repair

- Reproduced the reported LinkedIn card gap. The GitHub Pages product entry had
  description metadata but no canonical URL, Open Graph image, or large-image
  card declaration. The repository URL exposed only GitHub's generated
  Open Graph endpoint rather than a project-owned Social Preview.
- Added a dedicated opaque 1280 x 640 JPEG under 1 MB using the existing
  AgentGov navy, cream, blue, and orange visual language and the exact text
  `AGENT GOVERNANCE` and `From task intent to verified evidence`.
- Added absolute canonical, Open Graph, image dimension/type/alt, and Twitter
  large-card metadata to the product page, Portfolio, and shared reference
  layout. The README now displays the same smaller social asset.
- Added source-level protection for image format, dimensions, size, absolute
  URLs, entry-page metadata, shared reference metadata, and README reuse.
- Focused social-preview and public-page validation passed 21 tests. Full
  source validation passed 515 tests with one platform-limited skip.
- GitHub's repository card still requires a human to upload
  `docs/assets/agentgov-social-preview.jpg` in repository Settings under
  Social preview. LinkedIn may also require a re-scrape after publication
  because its existing blank card can remain cached.
