# Codex repository-context schema differential - 2026-08-31

## Goal and admitted boundary

Resolved alignment journey `mcpj-0b3ac31ba5954a9695921f9a0bdcc798`
selected one corrected local comparison from repository context. Native
proposal `prp-92c93b0be7de4f3d8e4f59f30004f420` admitted task
`p0-codex-repository-context-schema-differential-v1`, and the human separately
entered `REPLACE` to bind that task as active.

The immediate upstream task
`p0-codex-initialize-schema-version-differential-v1` stopped before MCP
response generation. This task authorized exactly one installed process and
one current-source process, the same local form-capable sequence, normalized
comparison, and bounded evidence. It excluded another retry, real Codex,
repair, runtime or package changes, permanent configuration changes, AgentGov
tool calls, stateful workflow, Git, publication, deployment, and release.

## Execution and result

Both bindings started exactly once from the repository working context. Each
exited `0` with two JSON response lines, no non-JSON standard output, empty
standard error, and all configured safe stderr markers false. No process was
retried.

Both reported protocol `2025-11-25`, the same normalized capabilities, and the
same complete eight-tool response. There were no installed-only or
current-only tools and no tool field, description, or input-schema
differences. The installed server reported `1.6.0`; current source reported
`1.7.0`. Aside from that known server-version path, the first comparable
difference was initialize `instructions`, recorded only as encoded length and
fingerprint. Instruction and protocol payload content were not retained.

The result is
`LOCAL_MCP_RESPONSES_MATCH_EXCEPT_INSTRUCTIONS_AND_SERVER_VERSION`. It rules
out the compared tool and capability surfaces at this local fixture boundary,
but does not identify the cause of the upstream real Codex `-32603` closure.
The earlier exit-2 stop is absent here; one run does not prove why.

## Preservation boundary

The permanent Codex configuration retained SHA-256
`4cca2d57edeaddfe52d3e6c4dd4d774192bbdbcab4e84e07df73e14a861c0348`,
and the installed executable retained SHA-256
`7dada88a8ccff3dfa40dd52783719e5aced5b293202979ba3d5b75f027b498e7`.
No package, AgentGov runtime source, permanent configuration, real Codex
client, or AgentGov workflow state changed. Excluded worktree paths retain
their prior ownership.

## Validation and advisory review

All 62 focused documentation tests and all 1,118 repository tests pass, with
five Windows privilege-limited symbolic-link skips. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; whitespace, privacy, and hash checks pass.
The read-only raw scope view reports `PASS=5 FAIL=22 ADVISORY=0` because it
keeps twenty-two pre-existing excluded paths visible as failures. It grants no
exception or ownership transfer.

Native current-Agent advisory self-review
`srv-86f972d5b535ff303eed10e037330bef` completed as a distinct pass over
requirement conformance, implementation, scope, privacy, architecture, and
bounded value. It confirmed the one-start/no-retry boundary, local tool and
capability equality, admitted-path ownership, privacy-bounded retention, and
unchanged runtime/configuration. Real-client cause, repeated reliability,
independent privacy assurance, portability, and external outcomes remain
unknown. No downstream action is authorized by this log.
