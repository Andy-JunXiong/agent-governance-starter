# Development log - 2026-08-18

## Landing-page narrative compression and narrow-width containment

### Goal and boundaries

The human selected a two-task split through native alignment and admitted
`p0-landing-page-narrative-compression-v1` through the native proposal form.
This first task improved only the landing-page product story, directly affected
README entry ordering, protecting tests, current status, and this dated record.
The report generator and both byte-protected sample reports remained excluded.
Git, publication, release, deployment, and the second report-responsive task
remained outside authority.

### Changes

- Reframed the hero around the concrete refund-approval review question and
  linked directly to the existing refund example.
- Reordered navigation and README showcase links around the product journey,
  with the interview-specific entry presented as a guided walkthrough.
- Replaced the long embedded Adapter milestone history with one concise current
  replay-safety slice plus links to the existing evidence portfolio, approved
  product requirements, guided walkthrough, and repository status.
- Compressed the sample-report caveat while retaining the stable `0.2.1`,
  published `0.3.0rc1`, illustrative, and newer-development-source boundaries.
- Removed the malformed Adapter 1.5.0 sentence with the milestone wall while
  keeping the full owner-binding and installation facts on their durable
  detailed documentation surfaces.
- Added explicit minimum-width containment to the hero and evidence grid. Long
  terminal commands now scroll inside the terminal rather than widening the
  document.
- Updated documentation tests to protect the product-first order, concrete hero
  question, concise evidence boundary, milestone separation, and exact
  narrow-width CSS contract.

### Validation

- 64 directly related product-site, portfolio, interview, documentation
  freshness, and user-documentation tests passed.
- The complete supported-Python 3.11 suite passed all 925 tests with 3
  platform-limited skips.
- Local browser validation used 390 x 844 and 1440 x 900 overrides. The narrow
  content viewport was 375 pixels and the document root/body stayed 375 pixels
  wide at the hero, refund example, and evidence sections. The 465-pixel command
  content remained inside the 293-pixel terminal viewport. All six desktop
  navigation entries were visible and the desktop document did not overflow.
- The English and Chinese sample reports remained byte-identical at their
  protected SHA-256 values:
  `fc5acb2392fcbea5787716e2e101d2236d85c76f1a6f76094b9a6b3c9a3cbb2c`
  and
  `09473875c7bd64201e20d22e56f2cf35fc12763e35c1e442e49a0888fa890d69`.
- The recorded `agentgov check governance` command was not supported by the
  current CLI and failed before implementation. Supported `check task` and
  `check repository` commands were used instead; the admitted record was not
  silently rewritten.
- The first scope invocation after pausing was correctly rejected because scope
  reconciliation requires an admitted task. The task was restored to admitted
  only for that check, which accepted all seven current-task paths and rejected
  only the preserved user-owned excluded `.codex/config.toml`. No exception or
  ownership transfer was inferred before the task was paused again.
- `git diff --check` passed.
- Native current-Agent advisory review
  `srv-36e5dd02da20bee026f32390fa72766f` found no correction-required
  requirement, architecture, scope, implementation, security, or data drift.
  It retained the unpublished snapshot and unmeasured reader outcomes as
  explicit unknowns.

### Remaining gaps and stop

The public Pages site still serves the preceding committed snapshot. Online
acceptance awaits separate Git and publication authorization. The two protected
sample reports still have the separately observed mobile-width overflow; their
generator and regeneration policy require the second, separately admitted task.
No evidence establishes improved hiring, interview, or adoption outcomes. Stop
before Git, publication, report regeneration, release, deployment, or follow-on
implementation.

## Generated-report narrow-width containment

### Goal and boundaries

The human chose the report-responsive work as the second half of the previously
aligned two-task split and admitted
`p0-generated-report-narrow-width-containment-v1` through native proposal
review. The task covers the canonical report generator, the two protected
illustrative report snapshots, their tests and current closeout records. It
does not authorize landing-page changes, Git, publication, release, deployment,
or a change to report data, wording, findings, links, or version identity.

### Changes

- Added one canonical CSS containment rule for direct children of the report's
  hero, primary grid, comparison grid and orientation grid.
- Synchronized that exact presentation rule into the protected English and
  Chinese illustrative `0.3.0rc1` reports. A scoped diff confirms that each
  protected artifact changed by exactly one added CSS line and no semantic
  content.
- Kept the existing long command unwrapped and internally scrollable.
- Added a generator-and-snapshot regression test for the exact containment and
  internal-scroll contracts.
- Updated the current protected SHA-256 expectations to:
  `a899b3c6039693fee01b93ca6fb08adf229d2d3854a29322c6b5c5b489e3bec3`
  for English and
  `94136427db9f6ed4390a28283b7c772be27a9a50ed7b13e691618485b294c8ec`
  for Chinese. Earlier status snapshots and dated evidence retain the original
  hashes as historical facts.

### Validation

- All 12 focused HTML-report and interview-documentation tests passed.
- The complete supported-Python 3.11 suite passed all 926 tests with 3
  platform-limited skips.
- Local browser validation covered both languages at 390 x 844 and 1440 x 900.
  At the narrow override, the viewport exposed 375 content pixels; each report's
  root and body stayed at 375 pixels, each orientation section and panel stayed
  at 343 pixels, and the 565-pixel command scroll area remained inside its
  289-pixel command viewport. At desktop width each report retained two
  570-pixel orientation columns without document overflow. Screenshot review
  found no observed clipping or material visual regression.
- Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
  governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; exact hash
  verification passed; and `git diff --check` passed.
- Scope reconciliation accepted all eight paths owned by this task. It rejected
  five preserved changes owned by the preceding landing task and the excluded
  user-owned `.codex/config.toml`; no exception or scope transfer was inferred.

### Native advisory review and stop

The first native review attempt correctly failed with `self-review has already
started for this journey` because the shared split-level alignment journey had
already completed its one review for the landing task. The failure was recorded
without reusing or relabeling that result and formal closeout remained
fail-closed.

The human then selected a fresh active-task journey
`mcpj-8684361306c54feeaa97c09e6e3dcdf1` solely as the distinct native review
context for this already admitted second task. The selection changed no
requirement, implementation, task scope, protected-artifact semantics, Git
state or publication authority. Native current-Agent review
`srv-1da30ad9f242b97c9cc39f1ef7cae6da` found no correction-required
requirement, architecture, scope, implementation, security or data drift. It
retained the unpublished Pages state and unmeasured reader outcomes as explicit
unknowns.

The bounded local implementation and governance closeout are complete, and the
task is paused. Stop before committing, pushing, publishing, releasing,
deploying, or beginning follow-on work. No evidence establishes improved
hiring, adoption or reader-comprehension outcomes.

## Responsive demo publication and online acceptance

### Authority and Git scope

The human explicitly authorized one combined implementation commit and direct
Pages publication for the paused landing and report-responsive tasks, followed
by online responsive acceptance and a real unbriefed-reader comprehension test.
Native alignment journey `mcpj-4b6857a864d74815a981289a9186df21`
separated the human test from the automated publication closeout so no Agent
proxy could be presented as human evidence. Native proposal review admitted
`p0-responsive-demo-publication-closeout` for the exact 14 implementation,
test, task and current-evidence paths.

Read-only preflight confirmed `main`, the expected HTTPS `origin`, credential
helper ownership, and zero local or remote divergence before staging. All 14
admitted paths were staged explicitly. The excluded user-owned
`.codex/config.toml` remained untracked and unstaged. No pull request, release,
force push or non-Pages deployment was created.

### Implementation publication

Commit `6fba1ebe670dc1b45c2a3c6a568655a7a005dba0` (`fix: improve
responsive demo journey`) contains exactly the two completed task slices plus
their publication task and current evidence. It was pushed normally from
`main` to `origin/main`.

The authenticated GitHub Deployments page reports that exact commit as the
active `github-pages` deployment with status `Deployed (completed)` via
`pages-build-deployment #29`, Actions run `32099732000`, job `95597874886`.
The public homepage exposes the new refund-review question and omits the former
embedded milestone wall. Public English and Chinese report bytes match their
repository-protected SHA-256 values:

- English: `a899b3c6039693fee01b93ca6fb08adf229d2d3854a29322c6b5c5b489e3bec3`;
- Chinese: `94136427db9f6ed4390a28283b7c772be27a9a50ed7b13e691618485b294c8ec`.

### Online responsive acceptance

At a 390 x 844 browser override, the live homepage and both live report pages
kept 375-pixel root and body widths. Each report kept a 343-pixel orientation
panel and contained its 565-pixel command scroll area inside a 289-pixel command
viewport. At 1440 x 900, the homepage exposed all six product navigation
destinations and the report retained two 570-pixel columns without document
overflow. Online mobile and desktop screenshots showed no observed clipping,
encoding issue or material visual regression.

Before commit, all 51 focused product-site, user-documentation, report and
interview-documentation tests passed. The complete suite evidence remained 926
passing tests with 3 platform-limited skips. Task and repository governance had
no FAIL. Publication-task scope accepted all 14 intended paths and rejected
only excluded user-owned configuration. Protected hashes, secret-safety review
and `git diff --check` passed.

### Native review, closeout, and real-human boundary

Distinct native current-Agent self-review
`srv-cc8efe8cbf4895e3978e51ee99cc7d6c` found no correction-required drift. It
confirmed the exact implementation publication and online acceptance while
preserving actual reader comprehension and downstream outcomes as unknown. The
publication task is paused after this documentation-only closeout.

The uncoached-reader test remains deliberately separate and pending: no real
human answers were available in this coding session, and no Agent proxy was
substituted. The final handoff provides a neutral public-site-only question
pack. Stop before recording comprehension or choosing a follow-on feature until
genuine reader answers are reviewed.

## Plain-language landing page v1

### Requirement and authority

The human reported that the published landing page remained too complex for an
ordinary reader. After reviewing a supplied simplification brief, the selected
direction was narrowed to one technically literate first-time audience and one
five-part journey: plain-language Hero, concrete refund example, tangible
report explanation, one three-step flow, and a final adoption call to action.
Native proposal review `prp-9f69fcc4f8f1428d9252f45cd0c36026` admitted task
`p0-plain-language-landing-page-v1` for the homepage, its directly related
tests, README current entry, STATUS, this dated log, and the task record. It
authorized no Git or publication action. The unrelated untracked `.codex`
configuration remained excluded and untouched.

### Implementation

The landing-page Hero now says that AgentGov keeps humans in control of
AI-written code, records what the agent was asked to change, checks whether the
work stayed within those limits, and shows what still needs human approval. Its
heading and support contain 31 English words. The former ten-section main
narrative is now four sections and 267 English words: the refund comparison,
facts/gaps/human decisions, one record/check/decide flow, and one final start
CTA.

The main journey no longer embeds the complete finding taxonomy, architecture,
CLI transcript, repository-file inventory, six-step adoption flow, persona
cards, or development milestone prose. The stable wheel remains an exact link
beside the sanitized report identity. Latest reservation/claim/recovery facts
remain in a default-collapsed footer disclosure linked to the evidence
portfolio, preserving public-surface consistency without making those details
an entry requirement. README now leads into the plain-language story while
retaining its detailed governance and PR/CI backstop contracts.

### Validation and current boundary

At a 390 x 844 browser override, the local rendered page kept 375-pixel root
and body scroll widths, a 341-pixel example panel, four main sections, and no
duplicate mobile navigation CTA. At 1440 x 900, it kept a two-column Hero, all
five desktop navigation destinations, and no document overflow. Mobile and
desktop screenshots showed no observed clipping or material regression.

All 75 directly affected product-site, user-documentation, portfolio,
interview, and public-freshness tests pass. The first complete-suite run
executed 926 tests and exposed four historical public-surface assertions. Each
was resolved by preserving the supported README opening, accessible evidence
label, exact stable-wheel link, or default-collapsed evidence boundary; no test
was deleted or weakened. The corrected complete-suite rerun passed all 926
tests with 3 platform-limited skips. Before pause, task governance reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; after pause, the final task check reported
`PASS=2 WARN=2 FAIL=0 ADVISORY=3`, with the expected warning that implementation
is no longer admitted. Repository governance has no FAIL. Pre-pause scope
accepted the task-owned paths and rejected only the explicitly excluded
`.codex/config.toml`; after pause, scope checking correctly refuses because the
task is no longer admitted. `git diff --check` passes.

A distinct bounded current-Agent review found no correction-required
requirement, architecture, scope, implementation, security, or data drift. The
fully specified task did not start an alignment journey, so this review does
not claim native self-review completion or invent a journey handle. It retains
actual first-time-reader comprehension and downstream hiring, adoption, or
conversion outcomes as unknown. The task is paused after this local closeout.

Documentation-only correction task
`p0-plain-language-landing-page-closeout-correction-v1` then separated the
pre-pause task/scope evidence from final paused-state output. Its admitted task
check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance had no
FAIL. Scope accepted the correction's three owned records and rejected six
preserved paths belonging to the completed landing task or excluded `.codex`
configuration. No scope or ownership transfer was inferred. The correction
task is paused without changing the page, tests, README, Git, or publication
state.

The revision remains local and uncommitted. No evidence yet establishes that a
genuinely unbriefed reader understands it better, or that it improves adoption,
hiring, or conversion. Stop before Git, publication, or those outcome claims.

## Plain-language landing publication closeout

### Authority and publication

The human selected the offered publish direction and explicitly authorized one
bounded commit, an ordinary direct push to `origin/main`, and the existing
GitHub Pages publication. The confirmed scope contained the landing page,
directly related tests, README entry, current status, this dated log, and the two
already-admitted task records. The user-owned untracked `.codex` directory was
not staged or modified.

After `git fetch origin`, local and remote were `0 0` divergent. Repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`, `git diff --check`
passed, and a bounded intended-scope scan found no common secret-like patterns.
Commit `a75b4456ee8e9f0a43501cfc00bd9d54ebfcd76b` was created as
`docs: simplify AgentGov landing page` and pushed by ordinary non-force Git to
`origin/main`. Local `HEAD` and `origin/main` then resolved to the same commit.
No pull request, force push, package release, or non-Pages deployment was
created.

### Evidence-bounded online acceptance

The public Pages URL served the new title and Hero. At the available 1730 x
1205 online browser viewport, root and body client widths were 1715 pixels and
their scroll widths were also 1715 pixels. The page retained its two-column
Hero, four main sections, all five desktop navigation destinations, and no
document-level horizontal overflow. A rendered screenshot showed no observed
clipping or material visual regression.

Six internal targets loaded with their expected final URLs and non-empty page
bodies: Quickstart, the evidence boundary, the sample report, the interview
walkthrough, Chinese Quickstart, and recovery evidence.

The committed page content had already passed local browser acceptance at 390
x 844 and 1440 x 900 before publication. The online in-app browser exposed no
supported viewport-resize operation. Its security policy rejected an attempted
isolated 390 x 844 browsing context and explicitly prohibited alternate-browser
or indirect circumvention. This is an acceptance-environment limitation, not
evidence of a product defect, but it also means no online phone-width or actual
device result is claimed.

### Reader and closeout boundary

No genuinely unbriefed human reader participated in this session. Agent
simulation was not substituted for a person, so comprehension, hiring,
adoption, and conversion outcomes remain unknown. The next product review is a
single neutral reader test against the public URL.

Native proposal review `prp-80343b61405146ffa9c38a17600e5586` admitted the
documentation-only task `p0-plain-language-landing-publication-closeout-v1` for
STATUS, this dated log, and its own task record. Its initial task check reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported `PASS=26
WARN=2 FAIL=0 ADVISORY=4`; and scope accepted the task record while rejecting
only excluded `.codex/config.toml`. The task grants no further Git,
publication, release, deployment, PR, or user-configuration authority.

After the documentation edits, task governance remained `PASS=3 WARN=1 FAIL=0
ADVISORY=3`, repository governance remained `PASS=26 WARN=2 FAIL=0
ADVISORY=4`, and `git diff --check` passed. Scope reported `PASS=3 FAIL=1`,
accepting STATUS, this log, and the task record while rejecting only the
excluded, untouched `.codex/config.toml`. That rejection preserved the boundary
and was not treated as an exception. The task was then paused. Its final task
check reports `PASS=2 WARN=2 FAIL=0 ADVISORY=3`; post-pause scope inspection
correctly refuses because implementation is no longer admitted.

## Bilingual Quickstart information-architecture cleanup

### Goal and boundaries

The human asked to implement the reviewed simplification without reopening the
landing-page design. Admitted task `p0-quickstart-ia-cleanup-v3` therefore kept
the existing Hero, report preview, four-section narrative, CTAs, CSS, and
assets. It authorized only a compact first-step clarification on the landing,
a goal-first reorganization of the English and Chinese Quickstarts, directly
related documentation tests, and current closeout records. It authorized no
Git, publication, release, deployment, pull request, or hosting action.

Two earlier task proposals were paused before page edits when inspection found
that their scopes omitted directly affected test or Chinese documentation
surfaces. Their records remain unchanged as fail-closed evidence. The v3 scope
includes every directly affected Quickstart surface and test and explicitly
excludes those paused records, the earlier publication closeout, and
user-owned `.codex` configuration.

### Implementation

The landing workflow label changed from `Record the request` to `Set the
boundaries`, with one compact sentence explaining allowed and excluded work.
No proof strip, terminal transcript, or new landing section was added.

The English HTML, Chinese HTML, and Chinese Markdown Quickstarts now begin with
four goal choices: adopt an existing repository, start a new governed
repository, generate a report, or inspect development features. The ordinary
path then presents stable `0.2.1` installation, inspect and dry-run preview,
adopt or initialize, check, report, human work, next stable guides, and
maintenance. Long automatic-product, Adapter-version, MCP, and replay prose
was replaced in the ordinary path by one development preview with exact links
to the existing evidence owners; provenance was moved, not deleted.

Stable `0.2.1` was exported from its Git tag into an isolated temporary
directory and exercised directly. The published CLI produced real `PLAN`
lines, `SUMMARY CREATE=26 PRESERVE=0`, the note that dry-run changed no files,
and the non-authority note. Those selected real lines are used in both HTML
languages and the Chinese Markdown guide. Temporary fixtures were removed.

Tests were changed semantically rather than deleted: Quickstart tests now
protect the goal-first stable order, compact development boundary, exact deep
links, bilingual authority wording, and the absence of low-level replay
commands from ordinary onboarding. Deep evidence pages still own the detailed
Adapter and reservation/claim/recovery contracts.

### Validation and acceptance

All 55 directly affected tests pass. The complete suite passes all 926 tests
with 3 platform-limited skips. English and Chinese HTML expose the same ten
section sequence, existing public-journey tests validate internal destinations,
and `git diff --check` passes.

At the available 1730 x 1205 local browser viewport, the landing and both
Quickstarts loaded with correct titles, navigation, headings, version boundary,
goal cards, stable command path, human boundary, development links, and
readable English and Chinese text. No material desktop clipping or encoding
regression was observed. The controlled browser offered no viewport resize
operation, so no new phone-width or actual-device Quickstart result is claimed.

Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3` and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
accepted 11 task-owned paths and rejected only four explicitly excluded,
preserved paths: `.codex`, the prior publication closeout, and paused v1 and v2
Quickstart task records. No exception or ownership transfer was inferred. The
task is paused after closeout; its final task check reports `PASS=2 WARN=2
FAIL=0 ADVISORY=3`, and post-pause scope inspection correctly refuses because
implementation is no longer admitted.

### Advisory review and remaining evidence

A distinct bounded current-Agent review found no correction-required
requirement, architecture, scope, implementation, security, or data drift.
Stable, prerelease, development-source, installed, and unproven states remain
separate; passing checks remain non-authoritative; and the landing structure
remains frozen. Because the task was fully specified and did not start an
alignment journey, this does not claim native self-review completion or an
independent reviewer.

Genuine unbriefed-reader comprehension, phone-width behavior, adoption,
hiring, and conversion effects remain unknown. The next product review is one
neutral unbriefed-reader test over the revised Quickstart. This cleanup remains
local and uncommitted and grants no follow-on implementation or external-write
authority.

## Bilingual Quickstart publication closeout

The human explicitly requested publication. Native proposal review first
admitted `p0-quickstart-ia-publication-v1`, but pre-staging review found that it
required one commit while also requiring files inside that commit to record
the commit's own final SHA and post-publication evidence. The task was paused
fail-closed before staging or Git writes. Replacement task
`p0-quickstart-ia-publication-v2` separated source publication from the later
documentation-only evidence closeout.

Before publication, all 55 directly affected tests passed again and the full
suite passed all 926 tests with 3 platform-limited skips. A bounded scan of the
16 intended paths found no common credential or private-key patterns. Task v2
reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; `git diff --check` and staged diff checking
passed. Scope accepted all 16 intended staged paths and rejected only excluded,
untouched `.codex/config.toml`. No exception or ownership transfer was
inferred. A fresh fetch showed local and remote main at `0 0` divergence before
the source commit.

Commit `fb9771d0b30299075f4eb2d58ac82f46243df5c0` (`docs: simplify
bilingual Quickstart`) was created from those 16 paths and pushed by ordinary
non-force Git to `origin/main`. The push used the existing GitHub Pages source;
no PR, package release, force push, Sites deployment, new hosting project, or
non-Pages deployment was created.

The first online read still served the previous landing wording, so the push
was not treated as completed publication evidence. After propagation, the
public landing served `Set the boundaries`. At the available 1730 x 1205
browser viewport, landing root and body client and scroll widths matched at
1715 pixels. The English and Chinese Quickstarts served their expected titles,
headings, four goal choices, stable `SUMMARY CREATE=26 PRESERVE=0` excerpt,
development preview, Adapter link, replay-safety link, and non-empty bodies.
The Chinese screenshot showed no observed clipping or encoding regression.

The controlled browser exposed no supported viewport-resize operation, so no
new online phone-width or actual-device result is claimed. No genuinely
unbriefed reader participated, and no Agent simulation was substituted;
comprehension, adoption, hiring, and conversion effects remain unknown.

A distinct bounded current-Agent review found no correction-required
requirement, architecture, scope, implementation, security, data, or
publication drift. The established direct-main Pages path was preserved, the
PR-oriented GitHub workflow was not introduced, and the Sites hosting skill did
not migrate the project or create hosting metadata. This fully specified task
did not start an alignment journey and therefore does not claim native
self-review or an independent reviewer.

The v2 task is paused after this documentation-only closeout. Its final task
check reports `PASS=2 WARN=2 FAIL=0 ADVISORY=3`; post-pause scope inspection
correctly refuses because implementation is no longer admitted. This closeout
grants no further Git, publication, release, deployment, hosting, PR, or
user-configuration authority.

## Frozen landing product-copy cleanup

The human authorized one small local commit for four visible copy corrections
on the otherwise frozen landing page. Task `p0-landing-product-copy-polish-v1`
initially admitted the page, product-site test, and closeout records. The page
and focused test were updated, and all three product-site tests passed. The
first complete-suite run then identified three directly affected legacy
assertions in public-freshness, interview-documentation, and user-documentation
tests. Because v1 explicitly excluded those paths, it was paused fail-closed
before STATUS, log, staging, or Git action. Replacement v2 admitted the exact
test surfaces while preserving the original product boundary.

The Hero CTA now says `See a concrete example`. Both visible links to the
existing `interview-guide.html` file say `Guided walkthrough`; no file or URL
was renamed. The `Development evidence` disclosure retains its recovery deep
link but explains interrupted work, ownership records, and denied continuation
in plain language. The dashboard remains visibly `Illustrative example`, while
the note beneath it now contains only the existing link to current evidence
and limits. No section, order, CSS rule, responsive behavior, accessibility
attribute, Quickstart, portfolio, governance contract, runtime, or feature was
changed.

Test protection was migrated rather than removed. The landing tests require
the new labels, unchanged link targets, plain-language disclosure, and absence
of the old wording. Public-freshness tests require stable wheel links on
actionable install surfaces while treating the landing as a progressive entry
point. Interview tests keep low-level reservation, claim, recovery, and denied
authority terminology on opted-in evidence surfaces and require the landing's
plain-language recovery link. User-documentation tests keep exact version
boundaries on deeper pages while allowing the landing to compress prerelease
detail.

All 55 directly affected tests pass. The first full-suite run executed 926
tests and exposed only the three legacy landing-density assertions described
above. After their semantic migration, the corrected full suite passes all 926
tests with 3 platform-limited skips. Exact copy counts confirm one concrete
example CTA, two guided-walkthrough labels pointing to the original URL, no old
CTA or interview label, one illustrative label, and both evidence deep links.
`git diff --check` passes.

Before pause, task v2 governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`;
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and scope
accepted every task-owned path while rejecting only excluded, untouched
`.codex/config.toml`. No exception or ownership transfer was inferred. The
final paused task check reports `PASS=2 WARN=2 FAIL=0 ADVISORY=3`; post-pause
scope checking correctly refuses because implementation is no longer admitted.

A distinct bounded current-Agent review found no correction-required
requirement, architecture, scope, implementation, security, or data drift.
Sites guidance kept the existing static page architecture intact; no Sites
hosting was used because the user authorized a local commit only. The task did
not start alignment and does not claim native self-review or an independent
reviewer.

The landing is frozen after the single focused local commit. No push,
publication, release, deployment, PR, hosting, feature development, or
user-configuration action occurred. Genuine unbriefed-reader comprehension
remains unknown. A governed-task demonstration is product-review input only,
not authorization for follow-on work.

## Frozen landing product-copy publication

The human separately authorized publication of the focused landing copy
commit. Native proposal review admitted `p0-landing-copy-publish-v1` for the
existing direct-main GitHub Pages path, online acceptance, and a later
three-file documentation closeout. A fresh fetch showed `origin/main...HEAD`
at `0 1`; source commit `96d6899` was pushed with ordinary non-force Git. No
PR, package release, force push, Sites deployment, new hosting project, or
unrelated user configuration was created or changed.

The first cache-busted online read still served the previous CTA and footer
label, so it was not counted as acceptance. After Pages propagation, the live
English landing showed `See a concrete example`, two `Guided walkthrough`
labels pointing to the unchanged walkthrough URL, `Illustrative example`, the
evidence-and-limits link, and the plain-language interruption and recovery
copy after expanding `Development evidence`. `See a real example` and the
public landing label `Interview walkthrough` were absent. The Quickstart,
walkthrough, sample report, portfolio boundary, and recovery URLs all loaded
with the expected titles or headings and non-empty bodies.

At the available 1730 x 1205 browser viewport, document client width and
scroll width both measured 1715 pixels, with no observed horizontal overflow.
The controlled browser ignored a new-tab viewport option and exposed no
supported resize method. Therefore no new 390px online or actual-device result
is claimed. The final documentation-closeout rerun passes all 926 tests with 3
platform-limited skips, so the responsive test coverage remains fresh and
applicable to the exact published source, but it is not substituted for real
mobile-browser evidence. Task governance reports `PASS=2 WARN=2 FAIL=0
ADVISORY=3`, repository governance reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`,
and JSON plus `git diff --check` pass.

A distinct bounded current-Agent review found no correction-required
requirement, architecture, scope, implementation, security, data, or
publication drift. The public copy preserves progressive disclosure and the
human-authority boundary, and the existing hosting architecture remains
unchanged. This fully specified task did not start alignment and does not
claim native self-review completion or an independent reviewer.

Genuine unbriefed-reader comprehension, phone-width behavior, adoption,
hiring, and conversion effects remain unknown. The next product review is one
neutral unbriefed-reader test on the public landing, ideally using a phone.
That review is not authorization for further copy changes or the separate
governed-task demonstration.
