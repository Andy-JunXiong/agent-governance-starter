# Codex instruction-semantics differential - 2026-08-31

## Goal and admitted boundary

Resolved alignment journey `mcpj-2e6d45b1f46b4f17937714ebdb34c601`
recorded the human selection to compare instruction semantics safely. Native
proposal `prp-bfc4ca91661d47de8b15e363d96f8d5a` admitted task
`p0-codex-instruction-semantics-differential-v1`, and the human separately
entered `REPLACE` to bind that task as active.

The slice authorized one configured installed process and one current-source
process for initialize-only local inspection. It allowed only privacy-safe
counts, fingerprints, fixed-category presence, bounded interpretation, current
status, documentation evidence, and tests. It excluded instruction text,
another real Codex replay, AgentGov tool calls, stateful workflows, runtime or
configuration changes, Git, publication, release, and deployment.

## Execution and result

Preflight found the configured binding and confirmed that its launcher SHA-256
still matched the preceding comparison. The installed and current-source
bindings then each started exactly once. Both exited `0`, returned one JSON
response line, and produced empty standard error. No retry occurred. The
installed binding reported Adapter `1.6.0`; current source reported Adapter
`1.7.0`.

The in-memory, fail-closed analyzer reported
`INSTRUCTION_SEMANTICS_SHARE_16_OF_18_CATEGORIES_CURRENT_ADDS_TWO_INVENTORY_SAFEGUARDS`.
All 19 installed sentence digests were shared, current source added one
sentence digest, and the installed binding added none. Sixteen fixed semantic
categories were shared. Only current source contained the changed-path
classification precondition and immediate inventory-freshness revalidation
categories. No category was installed-only or absent from both bindings.

The result is deterministic for sentence hashes and fixed indicator presence.
Describing the current-source change as an additive inventory safeguard is an
advisory interpretation, not proof of full semantic equivalence. It does not
identify the cause of the real-client `-32603` initialize closure.

## Preservation, validation, and remaining boundary

The analyzer emitted no instruction text, raw request or response, transcript,
absolute path, credential, or local configuration content. The configured
launcher digest remained
`7dada88a8ccff3dfa40dd52783719e5aced5b293202979ba3d5b75f027b498e7`.
No product runtime, installed package, Codex configuration, or external system
changed. Existing `.agentgov` and `.codex` state retains its prior local-only
ownership.

Validation and the distinct advisory current-Agent review are recorded in the
current `STATUS.md` snapshot. Real-client causality, full semantic equivalence,
repeat reliability, portability, independent assurance, adoption, prevented
incidents, time savings, and business benefit remain unknown. A later
real-client compatibility test requires a new product decision and separately
admitted task.

The first complete-suite validation process ended without a recoverable
wrapper summary and is not counted as evidence. The exact command was rerun in
a recoverable session: all 1,120 tests passed with five Windows
privilege-limited symbolic-link skips. All 64 focused documentation tests pass.
Task governance reports `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and
`git diff --check` passes. The raw scope view preserves
`PASS=5 FAIL=1 ADVISORY=0` because the pre-existing excluded
`.codex/config.toml` remains visible rather than being claimed or hidden.

Distinct native current-Agent advisory review
`srv-9b8c338830991ac03cc4274c2ff2720a` found no requirement, scope, privacy,
or authority expansion. It confirmed the one-attempt boundary, fixed-rule-set
limits, retained-data allow-list, and explicit non-causal interpretation. It
kept full semantic equivalence, client causality, independent privacy
assurance, operational usefulness, and business benefit unknown. This is
self-review, not independent assurance or human acceptance.

The native MCP completion-record tool was not exposed in the active Codex
session. The repository fallback `agentgov govern finish` failed closed and
created local evidence `evd-531c943997724b92ab6bf7888abfd9fa` with outcome
`failed` and state `needs_evidence`. Its first task-declared command uses
PowerShell environment syntax; the completion runner's Windows command shell
rejected that syntax before substantive validation and exited `1`. The same
declared command passed when executed directly in PowerShell. A bounded
command-shell reproduction also exited `1`, supporting the shell-mismatch
diagnosis without changing the task record or runner.

Completion reconciliation also preserved the explicitly excluded
`.codex/config.toml` as a scope failure. That file was not modified, hidden,
ignored, admitted, or transferred. The implementation and manual declared
checks are complete, but the development session is not `verified`. A repair
to validation-shell semantics or completion scope handling requires a new
product decision and separately admitted task; this slice claims no exception
or completion authority.
