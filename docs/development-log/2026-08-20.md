# Development log - 2026-08-20

## Governed-refund Landing integration

The product owner reported a one-minute comprehension attempt with all four
answers correct: AgentGov blocked completion validation, the simulated coding
agent modified the files, the demo script restored the out-of-scope file, and
the human retained final acceptance. Codex did not observe the reader, measure
the time, or independently verify the reader condition. The result therefore
meets the user-reported M3 pass criteria but is not independent validation.

Because localization, Landing integration, and bounded closeout were all
reasonable next directions, a native alignment journey preserved the final
product choice for the human. The first start call used an unsupported field
shape and was rejected without state change; the corrected normalized call
offered three stable choices. The first resolution call bound the source digest
rather than the required decision-prompt digest and was also rejected without
state change. The corrected binding recorded the human-selected
`split_new_requirement` direction: keep the walkthrough frozen and treat
Landing discoverability as separate work. Alignment granted no task, Git,
hosting, publication, release, deployment, or scope authority.

Native proposal review then admitted
`p0-governed-refund-walkthrough-landing-link-v1` for only `docs/index.html`,
the walkthrough semantic test, current status, this dated log, and the new task
record. The walkthrough, prior README and task changes, user-owned `.codex`,
site assets, Quickstarts, portfolio, Kernel, schemas, hosting configuration,
Git, and publication remained outside scope.

The Landing refund example now contains one relative button labelled
`Open the 60-to-90-second governed walkthrough`. It reuses the existing button
and actions layout and points to `governed-refund-walkthrough.html`. No CSS,
script, image, walkthrough, maturity, or publication copy changed. The project
has no `.openai/hosting.json`, and the selected local integration explicitly
left publication undecided, so no Sites hosting action occurred.

The existing semantic test now requires exactly one Landing link inside the
refund example and before the report section. All 7 walkthrough/Landing tests
pass, all 54 related walkthrough, user-documentation, and public-freshness
tests pass, and the complete repository suite passes all 938 tests with 3
platform-limited skips in 157 seconds. After the closeout files were written,
the unchanged final full-suite rerun passed the same 938 tests with 3 skips in
164 seconds. Both the local Landing and linked walkthrough return `200 OK`; no
browser screenshot, click, or responsive-device visual review was requested or
performed.

Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Before this
log existed, scope accepted the four then-changed task-owned paths and rejected
nine preserved prior-task or user-owned paths. No exception or ownership
transfer was inferred.

The resolved alignment journey completed native current-Agent self-review with
four advisory observations. It found the selected requirement, relative link,
accessible label, frozen walkthrough, and non-publication boundaries preserved.
It also retained the unknown browser presentation, click-through, adoption, and
conversion effects. This was a separate self-review pass, not independent
evidence. The task is paused for a human visual product review of the local
Landing placement. No commit, push, pull request, hosting, publication, release,
or deployment occurred.

## Landing button contrast correction

The product owner supplied a screenshot of the local Landing refund example in
which the new walkthrough button appeared as a blank white pill. The current
Agent visually inspected the supplied screenshot and identified that the white
button inherited white link text from the dark case section. The product owner
selected the existing-site palette of white background and deep-navy text over
a black button or semantic success color.

Native proposal review separately admitted
`p0-governed-refund-landing-button-contrast-v1` for only the Landing, its
walkthrough semantic test, current status, this dated log, and the new task
record. The walkthrough, global site assets, prior task records, user-owned
`.codex`, README changes, hosting, Git, and publication remained outside scope.

The Landing now defines a local `.case-wrap .button.light` rule with
`background: white`, `color: var(--navy)`, and `border-color: white`. The rule
does not change other light buttons. The link target, copy, accessible label,
placement, responsive rules, and walkthrough source remain unchanged. One
semantic assertion protects all three contrast declarations.

All 7 walkthrough/Landing tests and all 54 related walkthrough,
user-documentation, and public-freshness tests pass. The complete repository
suite passes all 938 tests with 3 platform-limited skips in 164 seconds. Both
local URLs return `200 OK`; no post-correction screenshot, click, or responsive
browser visual test was requested or performed.

Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
accepted all five task-owned paths and rejected ten preserved prior-task or
user-owned paths. No exception or ownership transfer was inferred.

This fully specified correction did not start alignment. A separate bounded
current-Agent advisory review found the scoped selector, requested palette,
unchanged link semantics, and local-only boundary preserved; it does not claim
native self-review or independent visual validation. The task is paused for the
product owner to hard-refresh the local Landing and confirm the label contrast.
No commit, push, pull request, hosting, publication, release, or deployment
occurred.

## Session closeout and direct-main authorization

The product owner ended the development session and explicitly requested that
the related documentation be updated, then committed and pushed directly to
GitHub `main`. Native proposal review admitted
`p0-governed-refund-m2-main-push-closeout-v1` for the exact accumulated
governed-refund M2, correction, Landing, test, documentation, and task-record
paths plus the new closeout task record. User-owned `.codex`, runtime code,
schemas, release metadata, GitHub workflows, hosting, publication, release, and
deployment remain outside scope.

At this pre-push checkpoint, the explicit Git authority has been recorded but
no commit or push result is claimed. The intended workflow is to inspect the
complete diff, confirm remote non-divergence and secret safety, stage only the
15 task-owned paths, push one product commit to `origin/main`, then create and
push one bounded documentation commit recording the concrete Git result. No
pull request or force push is authorized.

### Concrete Git closeout result

The complete suite passed all 938 tests with 3 platform-limited skips in 163
seconds at the pre-commit checkpoint. Task governance reported `PASS=3 WARN=1
FAIL=0 ADVISORY=3`, repository governance reported `PASS=26 WARN=2 FAIL=0
ADVISORY=4`, and scope accepted all 15 intended paths while rejecting only the
excluded user-owned `.codex/config.toml`. All seven task JSON records parsed;
bounded credential-pattern and absolute-host-path scans returned zero matches;
`git diff --check` passed. `git fetch origin` completed and divergence was
`0 0` before commit.

Commit `df8ca89` (`feat: add governed refund walkthrough`) contains exactly the
15 staged paths and 1,245 insertions with 5 deletions. The first ordinary push
from the sandbox failed before contacting the remote because GitHub port 443
was unavailable. The exact same non-force command was retried with approved
external network access and succeeded, advancing `origin/main` from `3e98143`
to `df8ca89`. This was not an authentication recovery and no credential or
remote configuration changed.

This bounded documentation follow-up records the actual result and is being
committed and pushed under the same explicit direct-main authorization. No PR,
force push, hosting, publication, package release, or deployment occurred.
User-owned `.codex` remains untracked and unstaged. The closeout task is paused;
post-correction visual confirmation remains a later human product review, not a
condition retroactively claimed by this Git operation.

## AIRBNB clean-clone automatic-rehearsal preflight

After the product owner kept the corrected governed-refund Landing button,
native alignment selected the strategic independent non-NYC automatic
rehearsal. The product owner selected AIRBNB as the consumer. Read-only
inspection found its existing `master` worktree at committed HEAD `d706155`
with one modified README and one untracked prior-replay task. A second native
alignment decision therefore selected a temporary clean AIRBNB clone instead
of cleaning, resetting, or overwriting that preserved worktree.

Native proposal review admitted
`p0-airbnb-clean-clone-preflight-v1` for only its task record, current status,
this dated log, and one new sanitized experiment record. The separate take-up
then authorized one local temporary clone and read-only prerequisite checks,
but no live Codex turn, AIRBNB task admission, consumer source change,
installation, repair, Git operation, publication, release, deployment, or
cleanup.

The temporary clone used copied Git objects, detached at exact AIRBNB commit
`d70615527d9acdde3893ce645d1923606173acf6`, removed its local remote, and
reported a clean working tree with the committed AIRBNB governance rules,
Codex configuration, and task-directory placeholder. The source AIRBNB
worktree retained its original branch, HEAD, modified README, untracked prior
task, and measured blob identities. The clone remains in the operating-system
temporary area because cleanup was not authorized; no absolute path is
retained in repository evidence.

Read-only discovery found one retained `0.3.0rc1` wheel matching SHA-256
`78D6A216FE7344A2E3DA01086A7A92B9F3310BE61DAD2E690B0705003221FDE6`.
Without installation, direct artifact loading and an in-memory MCP handshake
reported Adapter `1.5.0`, AgentGov protocol `2026-07-28`, seven form-capable
tools, the owner-free proposal input, and a real task directory in the clone.

The committed AIRBNB Codex configuration calls `agentgov`, but the existing
launcher failed before package startup because its Python binding could not be
resolved. No standard pipx AgentGov environment or usable pipx command was
available, and the available Python 3.11.9 interpreter contained neither
AgentGov nor pipx. The preflight therefore stopped with live-replay readiness
`BLOCKED` at the runtime-binding prerequisite. It did not install the retained
wheel, repair the launcher, change AIRBNB configuration, or start a model
session. Exact evidence and limits are recorded in
`docs/experiments/airbnb-clean-clone-preflight-2026-08-20.md`.

All 13 task-contract and 36 user-documentation tests passed. The complete
repository suite passed all 938 tests with 3 platform-limited skips in 180
seconds. Task governance before pause reported `PASS=3 WARN=1 FAIL=0
ADVISORY=3`; repository governance reported `PASS=26 WARN=2 FAIL=0
ADVISORY=4`. Scope accepted the four exact task-owned Starter paths and
rejected only pre-existing excluded `.codex/config.toml`; no exception or
ownership transfer was inferred. The task JSON parsed, bounded credential and
absolute-host-path scans returned zero matches, and `git diff --check` passed.

Native current-Agent advisory review
`srv-6030fa385782ccd98453cb047099e252` found no correction-required
requirement, scope, implementation, or privacy drift. It confirmed that direct
wheel loading proves the named artifact contents but not Codex host launch,
and that `BLOCKED` is the accurate fail-closed readiness result. The review is
a separate self-review pass, not an independent audit. The task is paused;
isolated offline runtime preparation and the later live replay remain separate
unadmitted requirements.

## AIRBNB isolated-runtime preparation

The product owner separately took up the admitted
`p0-airbnb-isolated-runtime-preparation-v1` task. Preconditions reconfirmed the
retained clean clone at exact AIRBNB commit `d706155` with no changes or
remotes, the original AIRBNB worktree with its two existing uncommitted paths
and measured identities unchanged, the exact retained `0.3.0rc1` wheel with
its recorded SHA-256, and an available Python 3.11.9 interpreter.

Exactly one fresh virtual environment was created under the operating-system
temporary area. The retained wheel was installed into it using `--no-index
--no-deps`; the isolated interpreter reports distribution `0.3.0rc1`, and no
package index or dependency resolution was used. No global Python, pipx
environment, launcher, user configuration, existing environment, persistent
PATH, or repository file was altered by installation.

From the clean AIRBNB clone, one process prepended only the isolated scripts
directory to PATH and invoked the unchanged configured command name
`agentgov`. The isolated launcher had first resolution priority and reported
`agentgov 0.3.0rc1`. A no-model JSON-RPC discovery, MCP initialize, and
tools/list exchange then reported Adapter `1.5.0`, AgentGov protocol
`2026-07-28` among the supported versions, negotiated MCP protocol
`2025-11-25`, exactly seven expected form-capable tools, and no Agent-supplied
proposal owner. No tool call or external model turn occurred.

Post-checks found the temporary clone still clean at the same commit, without
remotes, and with byte-identical committed Codex configuration. The original
AIRBNB worktree retained its branch, HEAD, two-path status, measured blobs, and
configuration identity; the pre-existing global launcher also retained its
identity. Both temporary clone and runtime remain retained because cleanup was
not authorized, and no absolute temporary path is recorded.

This clears the earlier executable-binding prerequisite for a later live
replay when the process-local binding is supplied. It does not prove a fresh
Codex session will inherit that binding, admit an AIRBNB-local task, or execute
the live replay. No network request, consumer source or configuration change,
consumer validation, global repair, Git operation, publication, release,
deployment, or cleanup occurred.

All 13 task-contract tests and all 36 user-documentation tests passed. The
complete repository suite passed all 938 tests with 3 platform-limited skips
in 170 seconds. Before pause, task governance reported `PASS=3 WARN=1 FAIL=0
ADVISORY=3`, and repository governance reported `PASS=26 WARN=2 FAIL=0
ADVISORY=4`. Scope accepted all four task-owned Starter paths and rejected only
the preserved user-owned `.codex/config.toml` and the two excluded prior-
preflight records. No exception or ownership transfer was inferred. The task
JSON parsed, bounded credential and absolute-host-path scans returned zero
matches, and `git diff --check` passed.

This fully specified task did not start a new alignment journey. A distinct
bounded current-Agent advisory review found no correction-required requirement,
scope, implementation, privacy, or authority drift. It retained the unknown
fresh-Codex-session binding outcome and does not claim native self-review
completion or independent audit status. The task is paused for product review
of whether to admit exactly one live uncoached AIRBNB replay. That possible
next task would still require AIRBNB-local admission and explicit approval for
any external model/context transmission. No commit or push occurred.
