# Development log - 2026-08-16

## Adapter 1.5 installation task closeout continuation

A fresh host restored the native AgentGov connection. The product owner chose
a new exact-task alignment binding because the old foreground journey did not
survive Adapter restart. Native current-Agent advisory review
`srv-c2faed363079120c1eaf5023fc1de2e4` found no correction-required
implementation or scope drift, and the installation task was paused. The
stable 2026-08-15 log retains the detailed continuation evidence.

## AIRBNB owner regression replay

The product owner selected one narrow owner-attribution replay rather than a
combined lifecycle run, no-model repetition, or stop. Native proposal review
admitted task `p0-airbnb-adapter-1-5-owner-regression-replay-v1` for sanitized
starter-repository evidence only. AIRBNB write authority remained consumer
local.

The fresh host returned a completion view claiming the existing README heading
wording and no test need. The product owner reported that no native task form
appeared. Read-only AIRBNB inspection found only the earlier one-heading diff
and old 2026-08-15 admitted task, whose owner and decider remain
`current-agent`; no new task or `.agentgov` lifecycle state exists. File times
remained on 2026-08-15 and `git diff --check` passed. No new repository effect
can be attributed to this replay.

Harness Contract v1 records `preexisting_replay_state_not_cleared` at
`session_start` as First Deviation. Later missing native proposal evidence,
absent form, absent new task, Completion Verified, and Bounded Handoff remain
visible but do not replace that earlier mismatch. The single replay is
consumed. No cleanup, reset, retry, second replay, correction, Git operation,
publication, release, deployment, or follow-on work is authorized.

## Validation and advisory closeout

- All 25 Harness, 28 user-documentation, and 13 task-contract tests passed.
- The complete suite passed all 834 tests with 2 platform-limited skips.
- Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
  governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- Both new JSON documents parsed, the intended-path credential-pattern scan
  found no match, and `git diff --check` passed.
- Scope reconciliation accepted nine current-task paths. Its two failures are
  the pre-existing excluded 2026-08-15 log change and human-owned
  `.codex/config.toml`; no exception was inferred.

Native current-Agent advisory review
`srv-30ea1e5eff4681e6a2edc7dba6d30573` found no correction-required evidence,
privacy, implementation, or scope drift. It confirmed that the result is an
unavailable owner-binding measurement caused by contaminated replay
preconditions, not evidence that Adapter `1.5.0` succeeded or failed in a real
consumer proposal. It identified a possible future clean-target preflight as a
product-review question only. Task
`p0-airbnb-adapter-1-5-owner-regression-replay-v1` is paused, and no downstream
action is authorized.

## Clean-target replay preflight

The product owner reviewed three directions and selected a dedicated
read-only replay-preflight contract, evaluator, and CLI instead of extending
`doctor` or admitted-task scope. Native task proposal review admitted
`p0-clean-target-replay-preflight-v1` for the exact starter-repository slice.
No consumer cleanup, mutation, replay, Adapter installation, Git operation,
publication, release, or deployment was admitted.

Development source now provides `agentgov.replay-preflight-plan` version `1.0`,
its strict JSON Schema, a dependency-free evaluator, and
`agentgov check replay-preflight`. The evaluator binds an expected Git
revision, exact target paths and explicit byte or text preconditions, exact
related-task absence, strict local Adapter identity and protocol metadata, and
one `rpf-` correlation marker lookup. It reports `READY`, `BLOCKED`, or
`UNKNOWN`; every non-ready result blocks replay consumption, while every
result retains an all-false cleanup, replay, task, Git, release, deployment,
and publication authority boundary. `READY` proves only the named
prerequisites.

The implementation uses read-only Git commands, includes both rename
endpoints and untracked paths, rejects duplicate JSON keys and unsafe paths,
and treats symbolic links, missing Git facts, and untrustworthy Adapter
metadata as `UNKNOWN`. Correlation lookup is intentionally read-only: it does
not create or reserve a marker and cannot eliminate a time-of-check and
time-of-use race. Existing `doctor`, admitted-task scope, Harness, Adapter, and
governance MCP meanings remain unchanged.

### Validation and advisory closeout

- All 25 replay-preflight and CLI tests passed; the broader replay, CLI,
  user-documentation, and public-freshness set passed all 64 tests, and the
  final post-closeout focused set passed all 78 tests.
- The complete suite passed all 852 tests with 2 platform-limited skips.
- Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`;
  after the intentional pause it reported
  `PASS=2 WARN=2 FAIL=0 ADVISORY=3`. Repository governance reported
  `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- The task and Schema parsed as JSON, the intended-diff credential-pattern
  scan found no match, and `git diff --check` passed.
- Pre-pause scope reconciliation accepted all 11 then-current task paths. Its
  only failure was the pre-existing, excluded, human-owned
  `.codex/config.toml`; no exception was inferred and the path remains
  untouched. The later `STATUS.md` and dated-log closeout changes are both
  explicitly included by the task; the paused state correctly rejects a new
  admitted-task scope comparison.

Native current-Agent advisory review
`srv-a55b5f23db61a1fe7c1ec440a132e10c` found no correction-required
requirement, architecture, security, implementation, or scope drift. It kept
two bounded unknowns visible: no real consumer preflight has run, and the
read-only correlation lookup does not reserve an identifier. Task
`p0-clean-target-replay-preflight-v1` is paused. A disposable-consumer
preflight rehearsal without launching a replay is a future product-review
candidate only and grants no follow-on authority.

## Disposable-consumer replay preflight rehearsal

The product owner admitted task
`p0-disposable-consumer-preflight-rehearsal-v1` through native proposal review
for one synthetic local consumer, exactly one development-source preflight,
and sanitized starter-repository evidence. No real consumer, Adapter
installation, model session, replay, network resource, Git remote,
publication, release, or deployment was in scope.

The rehearsal created one no-remote temporary Git repository with a committed
README target, an empty tracked task-directory placeholder, strict local
Adapter `1.5.0` / protocol `2026-07-28` metadata, one absent related task, and
one unused correlation identifier. The explicit plan denied every cleanup,
task, repository-write, Git, replay, publication, release, and deployment
authority.

The replay-preflight CLI was invoked exactly once. Its machine-readable report
exited `0` with status `READY`, `preconditions_ready: true`, six PASS findings,
and no reason codes. Read-only post-check inspection confirmed that the target
remained clean, no task or marker was created, and the repository still had no
remote. No replay followed. The temporary setup remains in the operating-
system temporary area because cleanup was not authorized; its absolute path
is not retained in project evidence.

The task's initially declared import-style focused-test command failed because
the repository's `tests` directory is not an importable package. Equivalent
file-pattern discovery passed all 15 replay-preflight tests and all 10 CLI
tests without repeating the one-shot preflight. The sanitized experiment
record is
`docs/experiments/disposable-consumer-preflight-2026-08-16.md`.

The complete suite under supported Python 3.11.9 passed all 852 tests with 2
platform-limited skips. The PATH-default Python 3.9.7 attempt is retained as
failed environment evidence: 623 tests ran with 27 compatibility errors, 7
failures, and 2 skips. Pre-pause task governance reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Initial scope reconciliation accepted the
three then-current task files and rejected only the pre-existing human-owned
`.codex` configuration, social cover image, and unrelated AIRBNB task record;
no exception was inferred.

After `STATUS.md` joined the closeout, pre-pause scope reconciliation accepted
all four current-task paths and rejected the same three preserved user-owned
paths. Targeted documentation and task-contract tests passed all 53 tests.
The task JSON parsed, the bounded secret-like credential-pattern inspection
found no matching value, and `git diff --check` passed.

Because this fully specified low-risk task did not start an alignment journey,
the current Agent performed a separate bounded advisory review without
claiming native self-review completion. Requirement, authority, privacy, and
scope evidence showed no correction-required drift. It retained the declared
test-command mismatch and PATH-default Python 3.9 incompatibility as explicit
environment and workflow evidence. Task
`p0-disposable-consumer-preflight-rehearsal-v1` is paused so the single check
cannot be reused. No cleanup, second preflight, replay, real-consumer change,
Git operation, publication, release, deployment, or follow-on work is
authorized.

Post-pause task governance reported `PASS=2 WARN=2 FAIL=0 ADVISORY=3`; final
repository governance remained `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.

## Human-controlled replay correlation reservation

The product owner resolved native alignment in favor of a small correlation-
reservation layer instead of restart-safe journey persistence, further
exploration, or stopping. Native proposal review then admitted task
`p0-human-controlled-replay-correlation-reservation-v1` for one dependency-
free local contract, CLI, Schema, test, and documentation slice. No Adapter
installation, real-consumer marker, replay, Harness semantic change, Git
operation, publication, release, or deployment was admitted.

Development source now exposes strict version `1.0` preview, marker, and result
contracts through `schemas/replay-correlation-reservation-v1.schema.json` and
the low-level `agentgov reserve replay-correlation` command. Default execution
is read-only and shows the exact marker. `--apply` is terminal-only, requires
exact interactive `RESERVE`, and repeats the same preflight immediately before
exclusive file creation. The registry directory must already exist, so
missing or unsafe registry state creates no scaffolding. Cancellation,
redirected input, malformed plans, non-READY facts, stale target or revision,
Adapter mismatch, existing markers, and exclusive-create races do not create
or overwrite the requested marker.

One successful apply records exactly one bounded marker containing the plan
digest, correlation identifier, expected and observed revision, Adapter
identity, protocol, and denied authority. It does not admit a task, authorize
or launch a replay, or grant cleanup, Git, publication, release, or deployment
authority. Preflight, reservation, separately authorized replay, and Harness
Contract v1 remain distinct. No Harness schema, evaluator, fixture, or First
Deviation rule changed, and no automatic reservation-to-Harness field mapping
is claimed.

### Validation and advisory closeout

- All 15 reservation, 15 replay-preflight, 11 CLI, and 40 targeted
  documentation tests passed.
- The complete suite under supported Python 3.11.9 passed all 868 tests with 2
  platform-limited skips.
- Before pause, task governance reported
  `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported
  `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- The reservation Schema and task parsed as JSON, the bounded credential-
  pattern scan found no match, and `git diff --check` passed.
- Pre-pause scope reconciliation accepted all 13 current-task paths and
  rejected five preserved user-owned or preceding-task paths: `.codex`
  configuration, the social cover image, the AIRBNB task, and the earlier
  disposable-preflight task and experiment record. No exception was inferred.

Native current-Agent advisory review
`srv-6a3f3d55584e3cfce3da1893f9851d46` found no correction-required authority,
security, architecture, implementation, or scope drift. It retained bounded
unknowns for host-native confirmation, reservation consumption, the exact
Harness-evidence mapping, process-crash behavior, and network-filesystem
exclusive-create semantics. Task
`p0-human-controlled-replay-correlation-reservation-v1` is paused. No install,
real-consumer marker, replay, Git action, publication, release, deployment, or
follow-on work is authorized.

## Bounded session publication closeout

At the end of the session, the human product owner explicitly authorized an
update to the related closeout documentation, one bounded commit, and a
non-force direct push to `origin/main`. Native proposal review admitted task
`p0-record-and-publish-2026-08-16-session-closeout` for the repository writes
needed to record that decision and validate the exact publication set. The
task record itself grants no Git or publication authority; those actions rely
only on the human's explicit instruction.

The intended set contains the disposable-consumer preflight evidence, the
human-controlled replay correlation-reservation implementation, their tests
and documentation, and the closeout task record. The existing local `.codex`
configuration, social cover image, and misdirected AIRBNB task record are
excluded and must remain unstaged. The direct-main instruction does not
authorize a pull request, force-push, release, deployment, or any additional
external action.

This entry records the reviewed authorization and pre-publication boundary;
it does not claim that a future Git command succeeded. The observed commit and
push result is established by Git history and reported in the completion
handoff after the commands finish.

Final publication-closeout validation passed all 79 focused reservation, CLI,
documentation, and task-contract tests. The supported Python 3.11.9 suite then
ran all 868 tests in 280.647 seconds with 2 platform-limited skips and no
failure. Before pause, the closeout task reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance remained
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope accepted all 16 intended paths and
rejected only the three explicitly excluded local paths. JSON parsing and Git
diff checks passed.

A distinct bounded current-Agent review found no correction-required scope,
security, documentation, or publication-boundary drift. It confirmed that the
excluded local files remain outside the intended commit and that the task does
not itself authorize Git. The closeout task is paused before Git operations;
the human's explicit direct-main instruction remains the sole authority for
the one bounded commit and non-force push.
