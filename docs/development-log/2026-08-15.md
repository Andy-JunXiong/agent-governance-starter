# Development log - 2026-08-15

## Compact Documentation Index Candidate v1

### Goal, alignment, admission, and boundaries

The product owner reviewed the completed logical archive-index candidate and
selected a readability-first direction through the native alignment journey.
The exact low-risk task `p0-compact-documentation-index-candidate-v1` was then
admitted through native proposal review and taken up in a separate human
instruction.

The slice changes only the proposed Markdown presentation: each eligible
record shows its date, title, and stable relative link. Per-record source
SHA-256 values remain in JSON entries and terminal diagnostics, and the
candidate-content before/after digests remain unchanged in role. Creating or
updating `INDEX.md`, adding a writer, changing the CLI or schema shape,
scheduling, reminders, moving or rewriting logs, Git, publication, release,
deployment, and external actions remain excluded.

### Implementation

- The dependency-free candidate renderer no longer appends a source hash to
  each Markdown record bullet.
- The strict `agentgov.documentation-archive-plan` 1.0 JSON shape and all
  entry hashes remain intact.
- Focused tests now require compact candidate content and retained
  machine-readable hashes.
- README and the dedicated guide explain the human-facing versus
  machine-verifiable evidence split without presenting usefulness as an
  objective result.

### Validation

- The 10 archive-planner tests passed after first demonstrating the two new
  compact-rendering assertions failed against the previous implementation.
- All 24 user-documentation and 13 task-contract tests passed.
- The complete Python suite passed all 821 tests with 2 platform-limited
  skips.
- The acceptance run through 2026-08-15 returned `pass`, 16 eligible entries,
  16 candidate links, no inline source hash, and a `create` action before this
  session record existed. The final closeout run returned 17 entries and links
  with this record first. Both runs retained valid JSON source hashes, created
  no `INDEX.md`, and left the complete development-log SHA-256 snapshot
  unchanged.
- The admitted task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
  repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- The admitted-state scope check accepted all eight task paths and found one
  failure only for the pre-existing human-owned `.codex/config.toml`, which
  the task excludes and this work left untouched.
- `git diff --check` passed. User-reported validation: none.

### Product connection and remaining review

This refinement builds directly on Documentation Archive and Index Plan v1.
In the short term it makes the proposed navigation artifact easier to scan
without discarding integrity evidence. The long-term benefit is unknown until
the product owner reviews the compact result and decides whether a maintained
index is useful.

No work is incomplete inside the admitted slice. The next product review is
whether the compact candidate merits a separately admitted create/update
workflow or no follow-on. A writer, concurrency policy, scheduling, Git,
publication, release, and deployment remain undecided and unauthorized.

### Native current-Agent self-review

The resolved alignment journey completed a distinct medium-risk native
current-Agent self-review using only the admitted task, implementation,
focused tests, README, guide, Status, and this dated evidence. It found no
correction-required requirement, architecture, scope, implementation, or data
drift.

The review confirmed compact human-facing Markdown, retained machine-readable
source hashes and candidate digests, the unchanged strict 1.0 shape, the
dependency-free renderer, admitted path boundaries, and the absence of an
applied index. It retained two product uncertainties: only the product owner
can establish whether the compact candidate is useful, and writer concurrency,
update safety, and scheduling architecture remain undecided. No user-reported
rendered Markdown or browser review was supplied. The review grants no task,
scope, exception, Git, publication, release, deployment, or external authority.

## Safe Documentation Index Writer v1

### Goal, alignment, admission, and boundaries

After reviewing the compact candidate, the product owner confirmed that the
directory index is needed and selected an explicit safe writer through a new
native alignment journey. Native proposal review admitted the exact low-risk
task `p0-safe-documentation-index-writer-v1`, and the product owner took it up
in a separate instruction.

The slice may create or update only `docs/development-log/INDEX.md`. Every
dated log remains at its stable path and must stay byte-identical during an
apply. Automatic closeout refresh, scheduling, reminders, source-log writes,
schema-shape changes, Git, publication, release, deployment, and external
actions remain excluded.

### Implementation

- `agentgov plan documentation-archive ... --apply` keeps ordinary planning
  read-only, rejects JSON/apply combinations, and requires a real interactive
  terminal plus the exact phrase `APPLY INDEX` before a create or update.
- Apply regenerates and compares the complete immutable plan, including the
  through-date, ordered entry metadata and source hashes, findings, action,
  candidate content, and target before/after digests.
- An absent index uses exclusive creation. An update writes and flushes a
  same-directory temporary file, revalidates again, preserves target mode, and
  uses `os.replace`; the temporary path is removed after success or failure.
- No-op plans return without prompting or writing. Failed, unsafe, stale,
  non-interactive, declined, mismatched, and interrupted paths remain bounded.
- The strict `agentgov.documentation-archive-plan` 1.0 JSON shape remains
  unchanged and continues to deny apply authority; the explicit CLI action is
  a separate human-confirmed boundary.

### Validation and real-repository apply

- All 16 archive planner/writer tests passed, covering confirmed create and
  update, no-op, non-interactive and incorrect confirmation, JSON/apply
  refusal, interrupted input, stale source and target, unsafe target, injected
  replacement failure, temporary cleanup, and complete dated-log preservation.
- All 24 user-documentation and 13 task-contract tests passed.
- The complete Python suite passed all 827 tests with 2 platform-limited
  skips before the real repository index was created.
- The admitted task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
  repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- The final admitted-state scope check accepted all 11 task paths and found one
  failure only for the pre-existing human-owned
  `.codex/config.toml`, which remains excluded and untouched.
- Under the admitted human take-up, the current Agent invoked the same exact-
  plan-revalidating apply core and created the 17-link index with SHA-256
  `8d4017d8af05b7d975742a79b7263b6f63c339e8b9768002a974136a6c8504c4`.
  A follow-up plan returned `none`; all 17 dated-log hashes were unchanged and
  no temporary file remained.
- `git diff --check` passed. The product owner then ran the documented
  PowerShell apply command in the repository. Supplied screenshots show
  `STATE pass`, 17 dated records through 2026-08-15, `CHANGE none`, and
  `UNCHANGED` with index digest
  `8d4017d8af05b7d975742a79b7263b6f63c339e8b9768002a974136a6c8504c4`.
  Unicode rendered normally in that terminal. The no-op correctly required no
  `APPLY INDEX` confirmation. The screenshots do not establish a separately
  displayed numeric exit code, confirmed create or update, or other-platform
  behavior.

### Product connection and remaining review

The writer builds directly on the read-only archive planner and compact
candidate. In the short term, the repository now has one navigable index and a
repeatable guarded maintenance command without sacrificing historical logs.
The long-term benefit of automating refresh is unknown until manual use shows
whether explicit maintenance is burdensome.

No work is incomplete inside writer v1. The next product review is whether the
manual writer is sufficient after ordinary use. Automatic closeout refresh,
scheduling, arbitrary external-writer concurrency control, Git, publication,
release, and deployment remain undecided and unauthorized.

### Native current-Agent self-review

The resolved alignment journey completed a distinct medium-risk native
current-Agent self-review using only the admitted task, implementation, CLI,
tests, README, guide, generated index, Status, and this dated evidence. It
found no correction-required requirement, architecture, scope,
implementation, data, or security drift. The review confirmed that plan 1.0
remains read-only, the CLI owns exact interactive confirmation, the core owns
complete plan revalidation, create is exclusive, update is an atomic
same-directory replacement, and the real index was created without changing
any dated log.

At the time of that review, the product owner had not yet exercised the CLI
manually. The later screenshot-backed PowerShell no-op run recorded above
supersedes only that unknown. User-confirmed create or update and other-platform
behavior remain untested; crash durability still depends on platform
filesystem behavior, an arbitrary external writer can still race the final
replacement boundary, and automatic refresh has not been selected. Review result
`srv-1b9cd6006b37ecf7616915386030e62c` granted no task, Git, publication,
release, deployment, or follow-on authority.

## AIRBNB uncoached README heading replay evidence

### Goal, alignment, and boundaries

The product owner selected AIRBNB through resolved alignment journey
`mcpj-9e1b688cc0f84948bcc61737802a53b1` and chose one genuine low-risk README
heading correction for a fresh uncoached automatic-governance replay. The new
Codex session received only the ordinary usability request, with no AgentGov,
MCP, task-record, protocol-JSON, lifecycle-command, or confirmation-word
coaching.

This AgentGov task records evidence only. It excludes AIRBNB modification,
another replay, AgentGov runtime or schema changes, an Adapter or validator
fix, Git, publication, release, deployment, NYC, and external actions.

### Evidence and First Deviation

- Codex-read consumer facts show one README heading diff, unchanged command
  blocks, one new admitted task record, and a passing `git diff --check`.
- The compact task structurally reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, but
  persisted `current-agent` as both `owner` and `decided_by`.
- The product owner separately reported that the native form appeared and that
  they personally accepted it. No retained event independently authenticates
  human origin, so the evidence keeps `human_origin_assurance: unavailable`.
- The task remained `admitted`; no `.agentgov` lifecycle evidence, Completion
  Verified, or Bounded Handoff was present when the consumer was inspected.

The new sanitized Harness Contract v1 fixture contains 13 expected and
observed stages. It derives the earliest mismatch at
`proposal_materialization` with code `human_owner_misattributed`. The later
automatic-lifecycle termination remains visible without replacing that First
Deviation.

### Implementation and validation

- Added the privacy-bounded observed fixture and a source-separated experiment
  record.
- Extended the Harness guide and tests to cover the new identity deviation,
  the user-reported form fact, later missing Completion Verified and Bounded
  Handoff, and honest repository-write terminal facts.
- All 24 focused Harness tests, all 25 user-documentation tests, all 13
  task-contract tests, and the complete 829-test suite passed; the full suite
  retained 2 platform-limited skips.
- The admitted task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
  repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- Both new JSON documents parsed, the intended-path credential-pattern scan
  found no match, and `git diff --check` passed.
- The final admitted-state scope check accepted all 8 task paths. Its only
  failure was the pre-existing human-owned `.codex/config.toml`, which is
  explicitly excluded and remains untouched.

### Product connection

The replay improves on the 2026-08-14 baseline: automatic tool selection,
native form presentation, human acceptance, task creation, bounded
implementation, and diff validation were reached. The complete independent
automatic-rehearsal gate still did not pass because durable human identity and
the completion/handoff continuation remained incomplete.

The next correction is not selected. Identity materialization and lifecycle
continuation are separate product-review candidates, and this evidence task
authorizes neither.

### Native current-Agent self-review

The resolved alignment journey completed a distinct medium-risk native
current-Agent self-review using only the admitted task, fixture, experiment,
Harness guide, focused tests, Status, and this dated evidence. It found no
correction-required requirement, architecture, scope, implementation, security,
or data drift. Review result `srv-da7a2ae15324c626c4428df9b7e1b05e`
confirmed that the existing Harness Contract v1 is sufficient, that the
source-channel distinction and denied authority are preserved, and that the
evidence does not claim the automatic journey passed.

The review retained the important unknowns: no retained event independently
authenticates who clicked the form; the cause and recurrence of the accountable-
owner misattribution are not established; the reason the journey stopped before
Completion Verified and Bounded Handoff is unknown; and passing local tests do
not prove external runtime behavior. The review grants no task, correction,
replay, scope expansion, Git, publication, release, deployment, or external
authority.

## Native human-owner role binding v1

### Goal, decision, admission, and boundaries

After the AIRBNB replay evidence closed, the product owner selected the
earliest deviation through resolved native alignment journey
`mcpj-d81a91f706ff44b6ad814fde2ee2d9d9`: correct human identity
materialization before addressing automatic Completion Verified or Bounded
Handoff continuation. Native proposal review then admitted exact task
`p0-native-human-owner-binding-v1`, and the product owner separately took it up.

The task changes only the development native MCP proposal boundary, its tests,
Adapter identity, ADR and affected user/status documentation. It excludes the
generic task-proposal implementation and schemas, installed runtime, project
configuration, AIRBNB, NYC, another live replay, lifecycle continuation, Git,
publication, release, deployment, and external actions.

### Diagnosis and implementation

The read-only diagnosis localized the observed misattribution. Adapter `1.4.0`
accepted `owner` in Coding Agent-controlled native tool arguments. The shared
task builder then consistently used that value for both the durable task
`owner` and `decided_by`, so a real native form acceptance could still persist
`current-agent` in both fields.

Development Adapter `1.5.0` removes `owner` from the capability-gated native
tool schema and exact parser. Native preparation injects the Adapter-owned
canonical role `Human product owner` before rendering the complete reviewed
plan. Only the bound `accept` plus `admit` decision can persist that plan. An
Agent-supplied owner now fails before elicitation and writes nothing. The
generic `agentgov.task-proposal` 1.0 contract, reference materializer, terminal
`ADMIT` fallback, development-task schema, and task validator are unchanged.

This is human-role mediation, not cryptographic proof of the individual's
identity or account. The historical AIRBNB fixture therefore remains unchanged
and continues to report human-origin assurance as unavailable for that run.

### Validation

- All 31 focused MCP tests pass, including the new Agent-supplied-owner
  pre-elicitation rejection and exact `Human product owner` / `decided_by`
  admitted-document assertions.
- All 26 user-documentation and 13 task-contract tests pass. The complete
  Python suite passed all 831 tests with 2 platform-limited skips.
- The admitted task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
  repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- The task JSON parsed, the added-line credential-pattern scan found no match,
  and `git diff --check` passed. A broader intended-file scan found only three
  existing negative-test sentinel strings; none was added or changed.
- The admitted-state scope check accepted every current task path. Its five
  failures were only pre-existing excluded or outside-scope AIRBNB evidence
  files and the untouched human-owned `.codex/config.toml`. No exception was
  inferred or granted.

### Product connection and remaining review

The correction builds directly on Harness Contract v1's
`human_owner_misattributed` First Deviation. In the short term it removes Agent
control over the native accountable-owner field. Whether this improves the
fresh consumer journey is unknown until a separately authorized installation
and replay. The next product review is whether to install exact Adapter `1.5.0`
source and authorize one fresh AIRBNB replay; neither action is authorized by
this source task. The separate completion/handoff continuation gap remains
deferred.

### Native current-Agent self-review

The resolved alignment journey completed a distinct medium-risk native
current-Agent self-review using only the admitted task, Adapter source, focused
tests, ADR, affected guides and public pages, Status, and this dated record. It
found no correction-required requirement, architecture, scope, implementation,
security, or data drift. Review result
`srv-855de7db8cc8b2d4dd6eafb0b2dc691a` confirmed the native-only boundary,
Adapter-owned role binding, zero-write hostile-input behavior, unchanged
generic contracts, denied downstream authority, and explicit version/status
synchronization.

The review retained four bounded unknowns: source tests do not establish
installed or consumer-host behavior; no live replay yet shows whether an
uncoached Agent uses the owner-free input; native form mediation does not
cryptographically authenticate the individual operator; and the separate
completion/handoff continuation gap remains unexplained. The review grants no
installation, configuration change, consumer modification, replay, task, Git,
publication, release, deployment, or external authority.

## Adapter 1.5.0 local installation and no-model preflight

### Goal, non-goals, acceptance, and stop conditions

The human admitted task `p0-install-adapter-1-5-offline-preflight` and then
explicitly authorized installation. The goal was to install the exact reviewed
Adapter `1.5.0` module into the existing local AgentGov pipx development
runtime, retain verified rollback backups, repair only the exposed launcher,
and exercise installed proposal discovery and human-owner materialization with
no model.

The task excluded package download or build, a new environment, project-config
changes, source behavior changes, AIRBNB or NYC modification, consumer replay,
Git, publication, release, deployment, and follow-on lifecycle work. Stop
conditions included backup collision, unexpected target identity, failed
post-copy verification without successful rollback, or any need to expand the
two-file runtime mutation.

### Installation evidence

Before replacement, the reviewed source module SHA-256 was
`9452D42C33ED9E52615E9B44FA0D7982C1C89F5E2B8C5AC5AD7E110DC8AAA610`;
the installed Adapter `1.4.0` module was
`7E84D0D828DC0A56E002E132D7D6A123E8E032902799B482929C112273D63188`;
the broken exposed launcher was
`E8E99022E267C23A8976B9DBBB25C6CD1B44F1100B7E944E2E0966277C126B10`;
and the working inner launcher was
`7DADA88A8CCFF3DFA40DD52783719E5ACED5B293202979BA3D5B75F027B498E7`.
The project `.codex/config.toml` hash was and remains
`4CCA2D57EDEADDFE52D3E6C4DD4D774192BBDBCAB4E84E07DF73E14A861C0348`.

Create-only backups of the installed module and exposed launcher were made in
their existing pipx locations and verified byte-identical. The first
replacement attempt found the exposed launcher locked; both runtime targets
were restored successfully and the backups were retained. Process inspection
isolated two wrapper `agentgov.exe` processes and their Python children. After
the human explicitly authorized stopping the wrappers and continuing, only
the two wrappers were stopped. The module replacement then matched the
reviewed source hash, the exposed launcher matched the working inner launcher,
and both original backups remained intact.

The unchanged configured command starts and reports `agentgov 0.3.0rc1`. An
import from outside the source tree loads the pipx module as Adapter `1.5.0`,
canonical owner `Human product owner`, and protocol `2026-07-28`.

### Installed no-model preflight

One automatically removed temporary Git repository exercised the installed
pipx runtime without a model or network call. Form-capable discovery returned
seven tools and non-form discovery returned the five base tools. The proposal
schema omitted `owner`. Hostile `owner=current-agent` arguments returned
`tool_arguments_invalid` before elicitation and created no task. A separate
bound `accept` plus `admit` fixture emitted one elicitation and created exactly
one task whose `owner` and `decided_by` were both `Human product owner`.

The sanitized evidence record is
`docs/experiments/adapter-1-5-installed-preflight-2026-08-15.md`.

### Repository validation and remaining review

- All 27 user-documentation and 13 task-contract tests pass.
- The complete Python suite passed all 832 tests with 2 platform-limited skips.
- The task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
  governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- The admitted-state scope check accepted all 13 current-task paths. Its eight
  failures were pre-existing excluded or outside-scope source, harness,
  AIRBNB, and untouched human-owned `.codex/config.toml` paths. No exception
  was inferred or granted.
- The task JSON parsed, the intended-path credential-pattern scan found no
  match, and `git diff --check` passed.

The direct file repair does not prove a future wheel or published package has
the same bytes. The reason the exposed launcher became invalid remains
unknown. Live Agent selection, UI presentation, personal identity
authentication, and consumer effectiveness remain untested. A distinct native
current-Agent advisory self-review and task pause remain before formal
closeout. No backup removal, rollback, another installation, project-config
change, consumer replay, Git, publication, release, deployment, or follow-on
implementation is authorized.

### Session stop and Git checkpoint authorization

The required native current-Agent self-review was attempted twice against the
resolved installation journey. Both calls returned `Transport closed` after
the approved wrapper-process stop. Read-only local diagnosis confirmed that
`agentgov_governance` remains enabled and still points to the unchanged project
command, while the running Codex CLI exposes no in-session MCP reload command.
Starting a standalone server cannot bind it back into the current host tool
channel. No review result was fabricated, and the installation task remains
`admitted` rather than paused or completed.

The human then ended the day's session and separately authorized updating the
related documentation, committing the exact reviewed daily change set, and
performing one ordinary non-force push directly to `origin/main`. That Git
authorization is limited to the AIRBNB evidence, Adapter `1.5.0` source
correction, local installation evidence, their admitted task records, and
their synchronized documentation and tests. The human-owned untracked
`.codex/config.toml` is excluded. No PR, backup removal, replay, publication,
release, deployment, or other external action is authorized. This Git
checkpoint preserves an honest blocked state; it does not satisfy the missing
native review or close the admitted installation task.
