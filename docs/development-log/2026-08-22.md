# 2026-08-22 development evidence

## Evidence Freshness real-use review v1

The product owner asked to start the day's work and selected the recommended
direction: apply Evidence Freshness v1 to one real repository-referenced
evidence item. Native alignment journey
`mcpj-ff96d945ab4941f297ac8a4324c062f0` recorded that human-selected center.
Native proposal review then admitted exact task
`p1-evidence-freshness-real-use-v1` through proposal
`prp-bf6363479a644bb5b241c650609f523a`. The product owner separately instructed
the Agent to begin execution after admission.

The bounded trial uses the bundled `0.3.0rc1` source compatibility baseline as
the real evidence item. The freshness record references `release/current.json`
and `docs/releases/0.3.0rc1.md`, and cites `docs/release-channels.md` as policy.
It deliberately does not describe the bundled source manifest as the immutable
public release manifest. No raw evidence content, host path, prompt, credential,
or private data is stored.

The declared event vocabulary is:

- `bundled-compatibility-baseline-changed` for a change to compatibility facts
  in the bundled baseline;
- `release-candidate-notes-corrected` for a correction to facts in the
  referenced release-candidate notes;
- `release-channel-policy-changed` for a cited policy change that requires the
  record to be reconsidered.

Each event identifies one referenced dependency and reconsideration reason, so
the vocabulary is understandable within this repository. External-consumer
understanding remains unknown. Tests exercise each exact match and a similar
undeclared name; the checker still performs no event discovery, inference,
refresh, or automatic integration.

Initial validation passed all 59 focused Evidence Freshness and user-
documentation tests in 1.212 seconds. The explicit command for the real record
reported `PASS` as of 2026-08-22 with reason `current`. Complete-suite,
governance, scope, privacy, JSON, diff, and advisory-review evidence remains to
be appended after it is observed.

After the current-status and documentation-owner assertions were added, the
next 59-test run found one documentation failure: the strategic plan described
the three events in prose but did not preserve the exact identifiers protected
by the test. The plan was corrected to name the identifiers; the test was not
weakened.

The first complete-suite run then passed 970 of 971 tests with three platform-
limited skips in 162.427 seconds. Its only failure was an existing adoption-
evidence contract requiring current status to retain the phrase `genuinely
unbriefed`. The new current snapshot had removed the preceding user-reported
README-reader boundary while replacing the old active slice. Status was
corrected to retain that evidence as user-reported only, without reader answers
or a comprehension-benefit claim; the test was not weakened.

After that correction, the adoption contract plus the Evidence Freshness and
user-documentation suites passed all 60 selected tests. The final supported
Python 3.11 full suite then passed all 971 tests with 3 platform-limited skips
in 155.149 seconds.

The admitted task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3` and the
repository check reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
reconciliation passed all seven task-owned paths and retained one failure for
the pre-existing user-owned `.codex/config.toml`, which is explicitly excluded
and remains untouched. The new task and freshness JSON parse, the bounded
added-content privacy scan and new-JSON ASCII scan pass, and
`git diff --check` passes.

One initially added documentation-test edit was outside the task's admitted
path list. The scope checker exposed it, and that nonessential edit was removed
without weakening the existing suite. No out-of-scope implementation change is
retained.

Native current-Agent self-review
`srv-36cf93af5425a1414a12bcd1d6deb006` completed as a distinct advisory pass
over the task, record, tests, specification, plan, status, and dated evidence.
It found the real evidence identity, standalone architecture, exact-match
implementation, retained scope, validation, privacy, and authority boundaries
consistent. It retained one recommended product-review question: whether an
uncoached external consumer can map a real baseline, notes, or policy change to
the intended event name without repository-specific coaching. The self-review
is not independent assurance, human acceptance, or authority for Git,
publication, release, deployment, automatic integration, or follow-on work.

After advisory write-back and the strategic plan date update, the adoption
boundary plus focused Evidence Freshness and user-documentation suites passed
all 60 selected tests in 0.275 seconds, and `git diff --check` passed. Final
scope reconciliation remained `PASS=7 FAIL=1`: every task-owned path passed,
and the sole failure remained the unchanged, pre-existing, explicitly excluded
`.codex/config.toml`.

The task changes no checker behavior, schema, release metadata, release notes,
source code, workflow, publication, or external system. The user-owned
`.codex` directory remains excluded and untouched. No Git operation,
publication, release, deployment, automatic integration, or external write is
authorized.

## Evidence Freshness vocabulary rehearsal page v1

The product owner found the prose-only rehearsal instructions too broad and
effortful to use, and asked for a page. Native alignment journey
`mcpj-a7847c55e281431e94530dde10548490` recorded the human-selected direction.
Native proposal review then admitted exact task
`p1-evidence-freshness-vocabulary-rehearsal-page-v1` through proposal
`prp-c1d2bf7439714aba9bd035fc13bc06b3`; the product owner separately instructed
the Agent to begin building the page.

The implementation is one responsive Chinese HTML file in the existing static
documentation site. It provides a clear start, material, answer, and result
journey; uses only in-memory state; and records eligibility, UTC timestamps,
elapsed seconds, six raw answers, participant questions, first uncertainty,
extra-source and assistance disclosures, assistance notes, and confidence. It
uses the Clipboard API when available and keeps the generated result visible
for manual selection and copying when programmatic clipboard access fails.

The page deliberately contains no answer key, score, automatic pass/fail,
analytics, persistence, cookie, network request, repository write, upload, or
publication behavior. Its timer is an observational convenience rather than a
trusted measurement. The facilitator is told not to provide content help; any
assistance is disclosed instead of silently invalidating the raw record.

All five focused page contract tests pass. They check the local self-contained
and responsive structure, required controls and raw-result fields, timer and
progress contract, absence of scoring/persistence/network logic, and visible
manual-copy fallback. The supported Python 3.11 full suite then passed all 976
tests with 3 platform-limited skips in 147.596 seconds.

The admitted task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3` and the
repository check reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
reconciliation passed all five current-task-owned paths and retained six
failures for pre-existing prior-task or user-owned changes. Those findings do
not grant an exception: the excluded and unowned paths remain outside this
task. Task JSON parsing, bounded privacy and external-resource scans, and
`git diff --check` passed.

Native current-Agent self-review
`srv-cd2ae52eb94b3f6747f7369f1c6a8351` completed as a distinct advisory pass.
It found the requirement, self-contained static-page architecture, task-owned
scope, implementation, privacy, and authority boundaries consistent. It
retained three unknowns: whether a genuinely unbriefed participant understands
the vocabulary without coaching, how the page behaves in that participant's
live browser, and whether public-site navigation is ever desirable. The review
is not independent assurance, human acceptance, or authority for Git,
publication, release, deployment, or follow-on work. The current callable
governance inventory does not expose `agentgov_task_completion_record`, so no
native completion record is fabricated.

The pre-existing completed-task changes and user-owned `.codex` state remain
outside this task's ownership. No Git operation, navigation change,
publication, hosting, release, deployment, participant recruitment, or
external write is authorized.

## Evidence Freshness rehearsal Sites publication v1

The product owner asked to publish the previously delivered local rehearsal
page after reviewing the recommendation for an unlisted test link. Native
proposal review admitted exact task
`p1-evidence-freshness-rehearsal-sites-publish-v1` through proposal
`prp-c68f38d19d454d8a9f0ea589f9a7481a`. The reviewed task makes the access
assumption explicit: the test URL is public to anyone who receives or discovers
it, but is not added to formal repository navigation and is not described as a
formal product launch.

The implementation adds one standalone Sites project under
`sites/evidence-freshness-rehearsal`. Its participant-facing HTML is
the validated local page plus one hosting-only `noindex, nofollow` directive.
The root route redirects to that file and supplies Chinese metadata. Starter
preview, database, account, example, and unused styling capabilities were
removed. A dedicated contract test protects that exact bounded parity, the
root journey, non-indexing metadata, hosting-metadata limits, and removal of
those unrequested capabilities.

The first generated build failed because the starter's package scripts used a
POSIX environment-variable prefix that Windows command execution does not
recognize. The project scripts were corrected to call the same vinext commands
directly; no test was weakened. A later source review found unused starter
authentication and loading-skeleton test files, so both were removed before
the final deployment-source state was pushed.

The standalone site build then succeeded and all 10 focused local-page and
hosted-copy tests passed. Sites version 1 reached `succeeded`, public access was
enabled, and the production test URL became
`https://evidence-freshness-rehearsal.maki83794676.chatgpt.site`. An external
HTTP request followed the root redirects to a final `200 text/html` response;
the response contains the expected start and raw-result controls. The
PowerShell response decoder rendered the UTF-8 Chinese title bytes as mojibake,
so this check establishes reachability and control presence, not participant
browser rendering.

A distinct bounded current-Agent advisory pass then found that the
`noindex, nofollow` metadata existed only on the redirecting application shell,
not the final participant HTML. The hosted page and parity test were corrected
without changing the validated interaction, rebuilt, and pushed as Sites
version 2. Version 2 reached `succeeded`; its public response returns `200` and
contains the expected controls plus the final-page robots directive. The
review treats `noindex` as a crawler request rather than a secrecy control.

The first redirect-chain check also observed a Cloudflare `__cf_bm` response
cookie. It is provider bot-management infrastructure, not app-owned analytics
or participant-answer storage, and a later final-response check did not expose
it. The page therefore makes no infrastructure-level cookie-free claim while
retaining its bounded claim that no participant answer is submitted or stored
by the application.

The exact validated site source was committed and pushed only to the
Sites-managed source destination required for this publication. The parent
repository was not committed or pushed. The temporary nested Git metadata and
deployment archive were removed after Sites accepted the version. No source
credential or token was persisted in repository content.

The current callable governance inventory does not expose
`agentgov_task_completion_record`, so no native completion record is
fabricated. The final supported Python 3.11 full suite passed all 981 tests with
3 platform-limited skips in 160.046 seconds. The admitted task check reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3` and the repository check reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope reconciliation passed all 19
current-task-owned paths and retained 9 failures for pre-existing prior-task or
user-owned changes; no exception or ownership transfer is inferred.
Deployment JSON parsing, bounded publishing privacy and hosting-metadata scans,
and `git diff --check` passed.

The genuinely unbriefed participant result remains unknown; publication makes
that review possible but does not itself prove comprehension, usability,
adoption, or product value.

## Evidence Freshness friendly source material v1

The product owner reviewed the published rehearsal and identified a product
validity problem: the measured section showed an Agent-created record summary
and abbreviated rules instead of the source material. The owner then clarified
that replacing the summary with raw JSON alone would still be too difficult to
read. Native proposal review admitted exact task
`p1-evidence-freshness-friendly-source-material-v1` through proposal
`prp-62f6656825b244e9a20d6bad6a26edad`; the owner separately instructed the
Agent to implement and republish the corrected design.

The corrected page uses three layers. Material A presents every field and
value from the repository-owned freshness record through Chinese thematic
groups while retaining each original key path, identifier, date, path, null,
and empty-array value. Material B follows every top-level section of the
repository-owned Evidence Freshness specification in a structured Chinese
reading layout, including the status table and command, privacy, and authority
boundaries. Material C provides labeled disclosures containing the exact
original JSON and exact complete English specification so the friendly
transformation can be audited. The old `真实记录摘要` and `规则速读` labels and
their synthetic presentation were removed.

The interaction remains deliberately unscored and privacy bounded. The page
has no answer key, automatic pass/fail, answer submission, analytics,
persistence, account, or app-owned cookie behavior. Its header and footer now
say that answers are not uploaded or persisted rather than inaccurately
describing the publicly hosted page itself as offline. The hosted page still
differs from the validated local page only by the bounded
`noindex, nofollow` directive.

Eight local-page tests and five hosted-site tests pass. New assertions compare
both embedded audit payloads byte for byte with their repository-owned source
files, require every original JSON key path and every source-specification
section, protect all five status meanings, and reject the superseded synthetic
labels. The standalone build succeeds. The supported Python 3.11 full suite
passed all 984 tests with 3 platform-limited skips in 151.687 seconds.

The exact corrected Sites source was pushed only to the existing Sites-managed
source destination at commit `6b37d268e0c3b19fb1cafa6e34a2908e113d6381`.
The deployment archive was rebuilt from that exact pushed source state. Sites
version 3 reached `succeeded` at
`https://evidence-freshness-rehearsal.maki83794676.chatgpt.site`. An external
request followed the root redirects to a final `200` response containing the
original event-key path, both exact-source disclosure identifiers, and the
final-page robots directive; the old summary label was absent. The temporary
clone and archive were then removed, and the parent repository was not
committed or pushed.

The admitted task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope reconciliation
passed all 7 current-task-owned changed paths and retained 22 failures for
pre-existing prior-task or user-owned paths. Those failures grant no exception
or ownership transfer.

A distinct bounded current-Agent advisory review found the friendly layer
faithful to the two declared sources and the audit payloads exact. It retained
three unknowns rather than treating the build as product proof: whether the
Chinese organization unintentionally coaches a test answer, whether a
genuinely unbriefed participant understands the material, and how comfortable
the longer reading section feels in a live browser. The current callable
governance inventory does not expose `agentgov_task_completion_record`, so no
native completion record is fabricated. No access change, formal navigation,
custom domain, analytics, participant collection, release, or follow-on work
is authorized.

## Evidence Freshness concept map v1

The first genuinely unbriefed participant completed Sites version 3 in 342
seconds without page-external material or observer assistance and reported
confidence 4/5. An advisory source comparison found two answers aligned with
the source: the release-note correction event and the distinction between a
due review and invalid evidence. Four answers exposed concrete comprehension
gaps: two supplied repository paths where event identifiers were requested,
one treated a similar but non-member event name as invalidating, and one said
the checker automatically discovers changes. This one result is bounded
product evidence, not a general comprehension rate, formal score, or claim
about the participant.

The product owner accepted the recommendation to add one relationship map and
explicitly instructed the Agent to implement it. Native proposal review
admitted exact task `p1-evidence-freshness-concept-map-v1` through proposal
`prp-eedefa814ead40b08fb14a6bccb0bc2e`.

The revised material begins with three differentiated roles. Reference paths
answer where evidence or policy lives; declared invalidating event identifiers
name changes that require reconsidering the record; observed events are facts
provided by an accountable producer and are not discovered or filled by the
checker. A responsive four-step flow connects referenced dependency, declared
event, producer-supplied observation, and exact-name membership. The concept
map deliberately contains none of the three concrete answer event identifiers,
so it teaches the structural distinction without becoming a literal event-
name answer list.

The complete friendly record, structured specification, exact JSON, and exact
English specification remain present. No answer key, score, automatic
pass/fail, persistence, answer submission, access-policy change, application
architecture change, or source-contract change was introduced.

All 14 focused page and hosted-site tests pass. The new contract test protects
the three information roles, source key names, producer boundary, absence of
automatic discovery, exact-match rule, responsive placement before the full
record, and exclusion of concrete answer identifiers from the concept map.
Both exact-source parity tests continue to pass. The standalone site build
succeeds, and the supported Python 3.11 full suite passed all 985 tests with 3
platform-limited skips in 157.141 seconds.

The exact validated page source was pushed only to the existing Sites-managed
source destination at commit `1432588890ee8d4043926a1298809be4f57cc472`.
Sites version 4 reached `succeeded` at the existing public URL. An external
request followed redirects to a final `200` response containing the concept-
map identifier, all three role markers, both source-audit identifiers, and the
robots directive. The temporary clone and deployment archive were removed;
the parent repository was not committed or pushed.

Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and scope
reconciliation passed all 7 current-task-owned changed paths while retaining
23 failures for pre-existing prior-task or user-owned paths. No exception or
ownership transfer is inferred.

A distinct bounded current-Agent advisory review found the new conceptual
model faithful to the declared sources and the exact audit payloads unchanged.
It retained three product unknowns: whether the diagram still over-coaches the
concept questions, whether a different unbriefed participant will distinguish
paths from events, and whether the extra section worsens scrolling in a live
browser. The current callable governance inventory does not expose
`agentgov_task_completion_record`, so no native completion record is
fabricated. The next product review is a second run with a different genuinely
unbriefed participant, not independent authorization for another code change.

## Evidence Freshness release-review pilot v1

After the concept-map revision, the product owner supplied a second genuinely
unbriefed rehearsal result. The participant completed the six questions in 310
seconds without outside material or observer assistance, reported confidence
5/5, and gave six answers aligned with the source. The owner reported that the
participant had genuinely understood the rules. This is bounded evidence from
one participant, not a general comprehension rate or proof of operational
benefit.

Alignment journey `mcpj-d88b8b58f39a48b5842f470523b6739d` resolved the next
direction at 2026-08-22T10:31:35.870Z. The product owner selected a read-only,
non-blocking release-review integration pilot. Native proposal review then
admitted exact task `p1-evidence-freshness-release-review-pilot-v1` through
proposal `prp-7441a2fdb36542b9ada70c4a1be58d9b`; the owner separately instructed
the Agent to continue implementation.

The release-review command now accepts paired optional `--freshness-record` and
`--freshness-as-of` inputs. It uses the existing Evidence Freshness checker and
writes a separate strict `evidence-freshness.json` sidecar plus a labeled
Markdown section. The separation deliberately preserves the existing strict
`review.json` 1.0 contract, gate results, review state, human-decision boundary,
and exit behavior. Every freshness status remains advisory and non-blocking.
The sidecar contains a portable source-relative record reference and normalized
result only; it copies no raw record, discovers or changes no observed event,
and grants no release authority. Missing pairs, malformed content, invalid
dates, outside-repository paths, and symbolic links fail before any bundle is
created.

The first full regression run failed one exact source-parity test because the
pilot guidance had initially been placed in the standalone Evidence Freshness
specification embedded by the published rehearsal. The integration guidance
was moved to its owning release-review document, restoring the standalone core
contract and published-source boundary. No test was weakened or deleted.

All 65 focused release-review, user-documentation, and rehearsal-source-parity
tests then passed. The supported Python 3.11 full suite passed all 991 tests
with 3 platform-limited skips in 161.245 seconds. Task governance reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope reconciliation passed all 11
current-task-owned changed paths and retained 26 failures for pre-existing
prior-task or user-owned paths; no exception or ownership transfer is inferred.

Native current-Agent self-review
`srv-a6daed8a4dc1add1f98e21a9b6eb746f` completed as a distinct separate pass.
It found the paired-input requirement, sidecar architecture, documentation
ownership, atomic-failure behavior, privacy boundary, and unchanged authority
behavior consistent. It retains three explicit unknowns: usefulness to an
actual release reviewer, any future promotion into the main review contract,
and suitability as a release gate. The current callable governance inventory
does not expose `agentgov_task_completion_record`, so no native completion
record is fabricated.

No parent-repository commit, push, publication, release, deployment, CI change,
automatic event discovery, or release gate was performed or authorized. The
next product review is one actual release-review use of the sidecar, not
independent authority for follow-on implementation.

### First real reviewer outcome

The first real run downloaded the published `0.3.0rc1` wheel and immutable
manifest into a temporary directory and verified their published SHA-256
digests before use. It ran the release-review flow against the Airbnb consumer
repository without modifying either repository. All seven collection gates
reported `PASS`; the Evidence Freshness sidecar reported `PASS` as of
2026-08-22 with no active expiry or invalidating condition; the human release
decision remained pending. No Git, tag, push, publication, release, deployment,
or consumer upgrade was performed.

The product owner then answered yes to all three bounded usefulness questions:
the freshness result was easy to find, the distinction between current evidence
and release authorization was easy to understand, and the information helped
the review. This supports retaining the non-blocking pilot for that reviewer
and run only. It does not prove broad usefulness, support a governance coverage
percentage, or authorize freshness as a release gate.

The run exposed a separate presentation question. The main gate table reported
that the consumer check and status rendering completed successfully, while the
detailed Airbnb status reported incomplete adoption with
`PASS=4 WARN=7 FAIL=0 ADVISORY=1`. A future product review should decide whether
the main review needs a clearer distinction between successful evidence
collection and consumer governance completeness. That observation grants no
new implementation authority.

## Release-review consumer governance summary v1

The first real Evidence Freshness release review exposed a presentation
ambiguity outside the freshness result itself. All seven collection gates
reported `PASS`, while the detailed Airbnb consumer status reported incomplete
adoption with `PASS=4 WARN=7 FAIL=0 ADVISORY=1`. A reviewer could therefore
mistake successful evidence collection for complete consumer governance.

Alignment journey `mcpj-f6d2a0dc064c4c4aa7ca9ef1021d388e` offered three
directions. The product owner selected the recommended Markdown-only correction
and rejected an immediate machine-contract version change or separate broader
redesign. Native proposal review admitted exact task
`p1-release-review-consumer-governance-summary-v1` through proposal
`prp-ec3bfba7a4e54d2bb6ed1ce59434270f`; the owner then separately instructed the
Agent to execute it.

The release-review implementation now strictly reads the Adoption row and
four-column Findings table from the already-collected `consumer-status.md`.
Generated `REVIEW.md` displays that normalized summary immediately after the
collection gates and says that a `PASS` `consumer-status` gate means rendering
completed successfully, not that consumer governance is complete. Only
`configured` or `incomplete` Adoption states and non-negative decimal counts
are accepted. Missing, duplicate, malformed, or unsupported table evidence
fails before the create-new-only output is committed rather than being guessed.

The summary is passed only to the Markdown renderer. It is not added to the
strict `review.json` 1.0 document, does not become a gate, and does not change
review state, exit behavior, the pending human decision, consumer contents, or
authority. Focused tests cover configured and incomplete states, aggregate
counts, exact explanatory wording, malformed evidence, atomic output, unchanged
machine output, and continued freshness rendering. All 59 focused release-
review and user-documentation tests pass.

A second real review used the same digest-verified published `0.3.0rc1` wheel
and immutable manifest against the Airbnb consumer repository. All seven
collection gates and the non-blocking freshness result passed. The corrected
main page displayed `Adoption: incomplete` and
`PASS=4 WARN=7 FAIL=0 ADVISORY=1`. Its `review.json` SHA-256 was identical to
the preceding real bundle, proving that the presentation correction did not
alter the machine review document. The real run's supported Python 3.11 source
suite passed all 994 tests with 3 platform-limited skips in 166.073 seconds.

Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope reconciliation
passed all 8 current-task-owned paths and retained 30 failures for pre-existing
prior-task or user-owned paths. Task JSON parsing, bounded privacy scanning,
and `git diff --check` passed.

Native current-Agent self-review
`srv-a8480daf7e60bc2800fdb874e42aafe1` completed as a distinct separate pass.
It found the requirement, Markdown-only architecture, strict parser, scope,
aggregate-data boundary, and unchanged machine and authority contracts
consistent. It retains two explicit unknowns: usefulness across other consumers
and compatibility when a future candidate intentionally changes the status
Markdown format. The current callable inventory does not expose
`agentgov_task_completion_record`, so no native completion record is fabricated.

Neither repository was modified by the real review, and no consumer project
workflow, Git operation, tag, push, publication, release, deployment, upgrade,
warning gate, or machine-contract change was performed. One human inspection
of the corrected page remains product validation rather than implementation
work or follow-on authority.

### Session stop and Git authorization

The product owner ended work for the day before the corrected real review page
received post-change human usability feedback. That review therefore remains a
clearly identified next-session product check rather than an inferred success.

The owner explicitly authorized updating the related closeout records,
committing today's bounded Evidence Freshness, rehearsal-page, Sites-source,
release-review, tests, task evidence, and documentation changes, and pushing
the resulting commit ordinarily to `origin/main`. User-owned `.codex` state is
excluded from the intended Git scope. This authorization does not include
force-push, pull-request creation, release, deployment, access changes, or any
other external action.
