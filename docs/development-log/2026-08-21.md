# Development log - 2026-08-21

## AIRBNB live uncoached governance replay evidence

The product owner approved one external-model AIRBNB replay using the retained
temporary clean clone and isolated AgentGov runtime. A fresh interactive Codex
CLI process used a process-local PATH plus a one-time trust override, allowing
the unchanged project-level configuration to load the required
`agentgov_governance` server without adding a persistent trust entry for the
temporary clone.

The coding Agent received one normalized README-heading requirement without
instructions about how to invoke governance. User-provided terminal evidence
shows it calling native proposal review, presenting the exact task-admission
form, and waiting for the human decision. The product owner admitted the task.
Only then did the Agent verify the task record and pre-edit state, replace the
single level-three heading, run its bounded Git checks, and disclose a bounded
current-Agent review.

Independent read-only repository measurement after the handoff found the
consumer task owned by `Human product owner`, in `admitted` state, and
scoped only to `README.md` plus the tool-managed task record. The clone
contained exactly those two changes. Repository-normalized diff reported one
deleted and one added heading line, and `git diff --check` passed. The clone
remained detached at exact commit `d70615527d9acdde3893ce645d1923606173acf6`
with no remotes. No commit or push occurred.

After completion, the parent Agent gave incorrect advice to rewrite the
existing human admission decision as paused. When the product owner relayed
that request, the live coding Agent attempted native proposal review. AgentGov
rejected the normalized request because it would mutate an existing human
admission decision and rationale. The Agent respected the fail-closed result,
performed only read-only checks, and stopped. No follow-up task record,
decision mutation, further README edit, commit, or push resulted. The parent
Agent corrected its guidance.

The original AIRBNB worktree retained its prior branch, committed HEAD,
modified README, untracked prior task, and measured blob identities. The
retained clone and runtime were not cleaned up. User-provided terminal behavior
and current-Agent repository measurements remain explicitly separate evidence
sources; no raw prompt, response, transcript, screenshot, source content,
credential, private data, or absolute host path is stored.

Native Starter proposal review then admitted
`p0-airbnb-live-uncoached-replay-evidence-v1` for only its task record, one
sanitized experiment record, this dated log, and current status. The proposal
created only the task record and did not start implementation. The product
owner separately took up the admitted evidence task. Prior Starter records,
user-owned `.codex`, product source, tests, Git, publication, release, and
deployment remain outside scope.

All 13 task-contract tests and all 36 user-documentation tests passed. The
complete Starter suite passed all 938 tests with 3 platform-limited skips in
183 seconds. Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`,
and repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
Scope accepted all four task-owned Starter paths and rejected only six
preserved excluded paths from prior tasks or user-owned `.codex`; no
exception or ownership transfer was inferred. The task JSON parsed, bounded
credential and absolute-host-path scans returned zero matches, and
`git diff --check` passed.

This fully specified evidence task did not start a new alignment journey. A
distinct bounded current-Agent advisory review found no correction-required
requirement, scope, evidence, privacy, or authority drift. It retained the
unknown generality beyond this narrow replay and the unresolved supported
completion-marker workflow. The review does not claim native self-review
completion or independent audit status.

The evidence work is complete and stopped without altering its original human
`admitted` decision. No new task-state transition, commit, push, cleanup,
publication, release, or deployment is claimed.

## Native append-only task-completion recording v2

The product owner selected the append-only completion direction through native
alignment journey `mcpj-8661e24241e34a54a3b104228f313308`. Native proposal
review first admitted
`p0-native-append-only-task-completion-record-v1`. Pre-implementation scope
review then found that v1 omitted the repository and generated Agent guidance
and Codex MCP templates required by its own acceptance signals. No v1 field was
rewritten. Native proposal review admitted the cumulative successor
`p0-native-append-only-task-completion-record-v2`, and the product owner
separately took up that exact task.

Development Adapter `1.6.0` adds exactly one base tool,
`agentgov_task_completion_record`. It accepts only one safe repository-relative
task-record path, revalidates human admission and the complete Git scope,
retains a matching active-session comparison base or uses current HEAD for a
fully attributable sessionless snapshot, runs only the task-declared
validation commands, and reuses the existing validation evidence,
`validation.completed`, completion reconciliation, and
`completion.reconciled` contracts. Fresh passing evidence returns `verified`;
failed or validation-mutated evidence returns `needs_evidence`. Malformed,
non-admitted, active-task-mismatched, unrelated, and out-of-scope calls fail
before local evidence is written. Repeated successful calls create distinct
append-only evidence and events while leaving the task, HEAD, and index
unchanged.

Repository and generated Agent guidance now route normal admitted-task
closeout through the tool without treating it as human acceptance. The
generated Codex MCP template enables it. Form-capable discovery is eight tools
and base discovery is six. Official Codex configuration documents a 60-second
default per-tool timeout and a server-level override; the generated config now
uses 1,800 seconds because this repository's declared suite takes more than 60
seconds. README, MCP, task, lifecycle, automation, product
requirements, and ADR surfaces identify the capability as development source
only and preserve historical Adapter `1.5.0` and AIRBNB facts. Strategic
direction did not change, so `DEVELOPMENT_PLAN.md` required no content change.

All 105 focused MCP, evidence, initializer, user-documentation, and
public-freshness tests passed. The complete repository suite passed all 942
tests with 3 platform-limited skips in 202 seconds. These results precede the
final task/repository/scope checks and native self-review recorded below.

The result grants no human requirement acceptance, architecture correctness,
exception, source-change, scope-expansion, Git, publication, release, or
deployment authority. It neither starts nor hands off a development session.
No installed runtime, retained AIRBNB clone, AIRBNB worktree, consumer
configuration, temporary resource, Git history or remote, package, public
page, release, deployment, or external system was changed.

Final task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
accepted all 21 v2-owned changed paths and rejected only 8 explicitly excluded
paths belonging to prior tasks or user-owned `.codex`; those paths were
preserved, not reclassified as exceptions. Both task records parsed as JSON,
bounded secret-like and absolute-host-path scans returned no matches, and
`git diff --check` passed.

The resolved alignment journey then completed native current-Agent self-review
result `srv-79e87ea1acfd031ab3f6a8f62e34fc6d`. The separate advisory pass found no
correction-required requirement, architecture, or scope drift. It retained the
unknown installed-runtime, live-consumer, cross-host, higher-risk,
committed-during-task, and non-Git behavior. It also retained the limit that a
sufficiently long multi-command validation set may exceed the generated
1,800-second client timeout. The review made zero AgentGov model or network
calls, retained no materializer context, granted no authority, and is not an
independent audit.

The v2 implementation is complete and stopped. The v1 and v2 human admission
decisions remain unchanged. No task-state rewrite, session start or handoff,
commit, push, installation, consumer replay, publication, release, deployment,
or external action is claimed.

After the timeout correction and self-review record were incorporated, the
five exact focused commands still passed 105 tests and the final exact verbose
suite passed all 942 tests with 3 platform-limited skips in 200 seconds.

## Native-completion isolated AIRBNB replay attempt

The product owner selected a live uncoached isolated AIRBNB replay through
resolved alignment journey `mcpj-26130afbf8444dadab2f3e42246e0de7`. Native
proposal review admitted
`p0-native-completion-airbnb-isolated-live-replay-v1`; its task check reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and the product owner separately started the
task.

Read-only preflight found the original AIRBNB worktree at its previously
measured commit with its existing README and task changes preserved. The prior
temporary clean clone had also been used by the earlier replay and therefore
was not reused. A fresh operating-system temporary root received a bounded
source staging copy, a new Python 3.11.9 virtual environment, and a new AIRBNB
clone detached at exact commit
`d70615527d9acdde3893ce645d1923606173acf6` with zero remotes. Four
task-critical source/template hashes matched between Starter and staging, and
the new clone was clean.

The exact offline installation attempt used no index, no dependencies, and no
build isolation. It stopped during metadata preparation. The new environment
contained `pip 24.0` and `setuptools 65.5.0`, while Starter declares
`setuptools>=69`; the older backend rejected current project metadata before
building or installing a package. The runtime contains no AgentGov package or
`agentgov` command. No compatible build dependency was found in the checked
local cache, and the task did not authorize a download, metadata correction,
manual wheel construction, or alternate-path retry.

A second deterministic readiness finding showed that the AIRBNB commit's
Codex `enabled_tools` allow-list contains the prior seven form-capable tools
and omits `agentgov_task_completion_record`; its Agent guidance still describes
five base tools. Official Codex configuration defines `enabled_tools` as the
server tool allow-list. This was not repaired or overridden. Because
installation had already failed, no MCP discovery, Codex process, external
model turn, form, consumer task, edit, completion record, or current-Agent
consumer review occurred.

Starter source hashes, HEAD, index, remotes, and prior dirty state; original
AIRBNB state; the retained clean clone; and retained runtimes remained
unchanged. The new temporary source, empty runtime, and clean remote-free clone
are retained. Sanitized evidence contains no raw prompt, response, transcript,
screenshot, source content, credential, private data, temporary absolute path,
or external-model payload.

The attempt is `BLOCKED_BEFORE_INSTALLATION` and complete as a bounded failure
record. It stops without retry, cleanup, Git operations, publication, release,
deployment, consumer activation, or external-system change. A later product
review must decide whether to admit an offline build-bootstrap and explicit
consumer-configuration-binding task; this record grants neither.

The closeout validation then passed both focused documentation suites with 37
and 12 tests. The exact full repository suite passed all 944 tests with 3
platform-limited skips in 199.247 seconds. The admitted task check reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed. The
cumulative working-copy scope check admitted 9 task-owned paths and rejected
22 pre-existing excluded paths. Those paths were preserved; the result grants
no exception or ownership transfer.

Resolved journey `mcpj-26130afbf8444dadab2f3e42246e0de7` completed a
distinct native current-Agent self-review as
`srv-6fbc48d394502804b530a4c0e7b781db`. The advisory pass confirmed that the
fail-closed stop matched the admitted requirement and found no
correction-required scope drift. It retained the compatible offline build
dependency, live installed completion journey, and explicit AIRBNB tool
binding as unknown. It made zero AgentGov model or network calls, granted no
authority, and is not an independent audit.

## Final development closeout and direct-main handoff

The product owner then explicitly asked to stop for the day, update the
related documentation, commit the confirmed work, and push it directly to
GitHub `main`. Native proposal review admitted exact local closeout task
`p0-2026-08-21-development-closeout-main-push-v1`, and the product owner
separately started it. That task owns only its task record, this append-only
log, and `STATUS.md`; it changes no product behavior and rewrites no prior task
decision.

Git preflight found the current branch at `main`, local HEAD and the locally
known `origin/main` both at `f509a724e2477c8f59811a44ad1b50d08c7eaf75`, and
the configured fetch and push remote at the expected GitHub repository. The
intended commit payload is the explicitly named cumulative non-`.codex`
working-copy scope from today's previously admitted tasks plus this closeout
record. The user-owned `.codex` directory remains excluded and unstaged. The
human authorization permits one ordinary non-force commit and direct push to
`origin/main`; it does not authorize a pull request, force-push, release,
deployment, temporary-resource cleanup, or broader external action.

The final commit identity and push result are necessarily observed after this
record is included in the payload. They are therefore reported in the direct
human completion handoff rather than predicted in this append-only evidence.

Final closeout validation passed the exact full repository suite with all 944
tests successful and 3 platform-limited skips in 198.835 seconds. The closeout
task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance
reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed.
The narrow closeout scope check admitted its three owned records and rejected
29 preserved paths owned by earlier tasks or the user; no exception or
ownership transfer was inferred. Its task JSON parsed, and bounded secret-like
and absolute-host-path scans returned zero matches.

A distinct bounded current-Agent advisory pass found the documentation-only
requirement, accumulated-scope attribution, validation evidence, direct-main
authority, and `.codex` exclusion consistent. It retained remote divergence
and Git transport as unknown until fetch and push. This fully specified task
started no alignment journey, so the pass does not claim native self-review
completion or an independent audit and grants no additional authority.

## Native-completion AIRBNB end-to-end recovery

The product owner selected the single end-to-end recovery direction through
resolved native alignment journey `mcpj-ae9d3edf5fe34791beacced68b6e59be`.
Native proposal review admitted
`p0-native-completion-airbnb-end-to-end-recovery-v1`, and the product owner
separately started it.

A fresh temporary source archive matched Starter HEAD and five task-critical
source, metadata, and generated-template hashes. A fresh Python 3.11.9 runtime
reproduced the earlier `setuptools 65.5.0` gate. The product owner then approved
exactly one dependency command. It downloaded only `setuptools 84.0.0`, whose
wheel SHA-256 is
`51A52592B3B99E102B609654876BD65F19F999935166D1352678931132B0C670`.
The runtime installed that backend offline, built exact staged source, and
installed `agent-governance-starter 0.3.0rc1`. The AgentGov wheel SHA-256 is
`DD0ACA8F5030EB0CB3036B6B82CAF00754C80854D2E3B6DABDC58C69EE0D87EC`.
Installed identity reported Adapter `1.6.0`, eight form-capable tools, six base
tools, and native task completion.

The first local clone command was rejected before object copying by Git's
cross-identity safe-directory protection. Its unused target was retained. A
second fresh target used one exact process-local safe-directory exception,
detached at AIRBNB commit `d70615527d9acdde3893ce645d1923606173acf6`, removed
its remote, and began clean. Only its Codex configuration and Agent guidance
were changed: the allow-list gained task completion, the timeout became 1,800
seconds, the base-tool count became six, and append-only completion guidance
was added. Direct no-model discovery reported MCP `2025-11-25`, Adapter `1.6.0`,
eight/form and six/base tools, and completion input `task_path`.

Two no-model trust-override shapes did not cause `codex mcp list` to merge the
project layer; neither changed user configuration. The corrected inline-table
worktree override loaded the required project server with the 1,800-second
timeout while leaving user configuration byte-unchanged. A pre-model TUI
process was then stopped at its interactive trust prompt rather than persisting
trust. No model turn occurred in those preflights.

The product owner explicitly approved the one eligible live Codex launch. It
used the fresh runtime on process-local PATH and the exact inline worktree
override. Codex identified the repository and default model, but `thread/start`
failed during TUI bootstrap because the required AgentGov MCP handshake closed
while producing the initialize response (`-32603`). No usable session or Agent
turn, native form, consumer task, README edit, validation, completion record,
or current-Agent consumer review followed. The attempt stopped without MCP
repair, coaching, or a second live replay and is
`BLOCKED_BEFORE_MODEL_MCP_INITIALIZATION`.

The disposable clone remains detached, remote-free, and changed only at the
two pre-replay binding paths. Original AIRBNB, prior clones and runtimes,
Starter source, HEAD, index, remotes, user Codex configuration, and credentials
remain unchanged. The bootstrap, artifact, runtime, staging area, unused clone
target, and bound clone are retained. No raw prompt, response, transcript,
screenshot, source content, credential, private data, temporary absolute path,
or external-model payload is recorded. No second download, retry, cleanup,
commit, push, pull request, publication, release, deployment, original-consumer
mutation, or broader activation occurred.

The exact complete Starter suite then passed all 946 tests on the fresh Python
3.11.9 runtime, with 3 platform-limited skips, in 258.165 seconds. A preceding
full-suite invocation had resolved to unsupported Python 3.9.7 and ended after
713 executed tests with 7 failures and 32 errors caused by unavailable
language and standard-library features. No test was weakened or deleted, and
the supported-runtime result is the product validation signal. Final task,
scope, repository, diff, privacy, native completion-record, and current-Agent
self-review outcomes follow in the task closeout evidence.

After that evidence write-back, the final exact suite again passed all 946
tests with 3 platform-limited skips in 261.439 seconds. Focused documentation
suites passed 38 and 13 tests. The task check returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed. Scope
reconciliation admitted all 9 task-owned paths and retained 3 failures for the
pre-existing, task-excluded `.codex` configuration, social-cover asset, and
AIRBNB handoff task; no exception or ownership transfer was inferred. The task
JSON parsed, and bounded secret-like and host-absolute-path scans returned zero
matches.

The current Agent's callable governance inventory did not expose
`agentgov_task_completion_record`; no Starter completion record was
fabricated. Native current-Agent self-review
`srv-4f6ab60826ed1c5b3755bf0f627faa7b` then completed as a distinct advisory
pass with requirement, architecture, scope, implementation, and security
observations. It found the bounded failure classification, admitted-path
ownership, validation, privacy, and stop boundaries consistent. It retained
the initialize root cause, host/runtime attribution, later proposal-to-
completion path, and dependency provenance beyond the recorded digests as
unknown. The review granted no task, scope, exception, Git, release,
deployment, or external authority.

## Installed Codex-to-AgentGov initialize diagnostic

The product owner selected diagnose-initialization-first through resolved
alignment journey `mcpj-d3402c1828a34ea3bca54073af84e7bc`. Native proposal
review admitted `p0-installed-codex-agentgov-initialize-diagnostic-v1`, and the
product owner separately started it.

Read-only identity checks found the retained Python 3.11.9 runtime and AgentGov
`0.3.0rc1`, the remote-free AIRBNB clone at
`d70615527d9acdde3893ce645d1923606173acf6`, its two existing binding changes,
the original AIRBNB identities, and the user Codex configuration unchanged.
The bare configured launcher resolves to the retained runtime when its Scripts
directory is placed first on the diagnostic parent process `PATH`.

One initial harness invocation passed the modified `PATH` only to a child after
Windows executable resolution had selected an older global Adapter `1.4.0`.
That seven-tool observation was rejected as a harness error. The corrected
direct stdio probe resolved the retained launcher before process creation and
passed with protocol `2025-11-25`, Adapter `1.6.0`, eight tools, task completion
present, exit zero, and no stderr.

Following the official Codex App Server lifecycle, the host connection
initialized successfully and no `turn/start` was sent. The installed Codex
schema rejected the official example's `workspaceWrite` sandbox value with
`-32600`; the request created no thread. Correcting only that value to
`workspace-write` on the same connection made `thread/start` succeed. One
empty sandbox-local thread was created, and required server
`agentgov_governance` moved from `starting` to `ready` with no MCP error. No
turn or item event occurred.

App Server's automatic featured-plugin and curated-plugin refresh attempts
failed in the restricted environment and installed nothing. After the empty
thread became ready, an automatic Responses WebSocket attempt failed TLS
validation before connecting. No turn, prompt, model payload, Agent response,
tool call, or source content was sent. The exact diagnostic Codex and AgentGov
processes were terminated, while the empty sandbox thread artifact was
retained because cleanup was out of scope.

The outcome is
`INITIALIZE_HANDSHAKE_NOT_REPRODUCED_IN_BOUNDED_APP_SERVER`. AgentGov process
startup, direct MCP initialize, Codex App Server initialize, required-MCP
startup, and empty thread creation all passed. The evidence therefore narrows
the prior live TUI `-32603` away from the retained binary and generic sandboxed
App Server integration, but cannot distinguish user-host Codex-home behavior,
TUI-specific behavior, command-resolution drift, or a transient condition.
No repair, unsandboxed probe, dependency download, model turn, second live
replay, cleanup, Git operation, publication, release, deployment, or broader
activation followed.

The first complete Starter-suite pass then found one documentation consistency
failure because current status no longer retained the historical replay
outcome identifier. Status was corrected without weakening a test. The final
Python 3.11.9 suite passed all 946 tests with 3 platform-limited skips in
260.078 seconds, and the focused user-documentation suite passed all 38 tests.
Task governance returned `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository
governance returned `PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and
`git diff --check` passed. Scope reconciliation admitted the 4 diagnostic-
owned paths and retained 10 failures for explicitly excluded prior-task or
user paths; no exception or ownership transfer was inferred.
After advisory evidence write-back, the focused 38-test suite and diff check
passed again. The task JSON parsed, and bounded host-absolute-path and
recognized credential-marker scans returned zero matches.

The current Agent's callable governance inventory did not expose
`agentgov_task_completion_record`; no Starter completion record was
fabricated. Native current-Agent self-review
`srv-47c9c47323b97e67fc4cd11c689df32a` completed as a separate advisory pass
with requirement, architecture, scope, implementation, and security
observations. It found the outcome, harness classification, ownership,
validation, privacy, and stop boundaries consistent. The earlier user-host
TUI failure cause, host and TUI differences, command-resolution drift,
transient conditions, and future Codex schema behavior remain unknown. The
review granted no downstream authority.

## User-host Codex-to-AgentGov no-model comparison

Resolved alignment journey `mcpj-f565c4f45c9149588cece61d2f7afcd3`
recorded the product owner's selection of the user-host no-model comparison.
Native proposal review admitted
`p0-user-host-codex-agentgov-no-model-comparison-v1`, and the product owner
separately started it.

Read-only preflight confirmed the retained Python 3.11.9 and AgentGov
`0.3.0rc1` runtime, the remote-free AIRBNB clone at
`d70615527d9acdde3893ce645d1923606173acf6`, its two existing binding changes,
the unchanged user Codex configuration hash, and Codex
`0.149.0-alpha.4`. Process-local `PATH` resolved the clone's bare AgentGov
command to the retained launcher. Original AIRBNB HEAD and its two checked
binding hashes matched the preceding record; its full status command retained
the existing local AgentGov-directory permission warning.

The product owner separately approved exactly one user-host Codex App Server
launch using process-local runtime and worktree-trust inputs. Connection
initialize identified the real user Codex home. The host then recognized the
required project AgentGov server, but the single `thread/start` failed with
the same `-32603` initialize-response connection closure as the earlier live
TUI launch. No `thread/started`, `turn/start`, turn, item, prompt, model
payload, Agent response, or tool-call event occurred. The exact diagnostic
process was terminated immediately; no retry, second launch, or repair
followed.

The outcome is `USER_HOST_INITIALIZE_FAILURE_REPRODUCED_NO_MODEL_TURN`. The
same Codex build, retained runtime, clone binding, and no-turn lifecycle passed
in the preceding sandboxed App Server diagnostic. The reproducible deviation
is therefore localized to the real user-host Codex-home or host-process
context. Child-process command resolution, inherited host environment,
user-home state, and other TUI/user-host-specific conditions remain
undistinguished.

The user configuration and retained clone identities stayed unchanged. No new
user-Codex-home file was observed from the startup-time cutoff. Five existing
shared files were modified after the cutoff, but concurrent pre-existing Codex
processes make attribution impossible; no contents or session identities were
inspected. All pre-existing processes were preserved. No raw interaction,
path, credential, private data, source content, installation or thread
identity, model payload, or unbounded host log was retained. No repair,
launcher pinning, dependency download, cleanup, Git operation, publication,
release, deployment, or broader activation occurred.

The exact Python 3.11.9 repository suite passed all 946 tests with 3 platform-
limited skips in 268.686 seconds. Task governance returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed. Scope
reconciliation admitted the 4 task-owned paths and retained 12 failures for
explicitly excluded prior-task or user-owned paths; no exception or ownership
transfer was inferred. The task JSON parsed, and bounded host-absolute-path
and recognized credential-marker scans returned zero matches.

The current Agent's callable governance inventory did not expose
`agentgov_task_completion_record`; no Starter completion record was
fabricated. Native current-Agent self-review
`srv-d0e389eb2f38b3cbe6e47e346f2db0e6` completed as a distinct advisory pass
with requirement, architecture, scope, implementation, and security
observations. It found the retained-child conclusion, observer fallback,
ownership, validation, privacy, attribution limits, and stop boundaries
consistent. Complete child environment, causal user-home state, the
initialize-response code path, shared-file attribution, and possible
unobserved background network attempts remain unknown. The review granted no
downstream authority.

The current Agent's callable governance inventory did not expose
`agentgov_task_completion_record`; no Starter completion record was
fabricated. Native current-Agent self-review
`srv-0022f90a035fd6310dc67aa983c2cbe7` completed as a distinct advisory pass
with requirement, architecture, scope, implementation, and security
observations. It found the single-launch result, differential classification,
ownership, validation, privacy, and stop boundaries consistent. Child-process
executable identity, inherited environment, user-home state, the shared-file
modifications, and possible unobserved background network attempts remain
unknown. The review granted no downstream authority.
After advisory evidence write-back, the focused documentation suites passed
38 and 13 tests, and the diff and privacy checks passed again.

## User-host AgentGov child identity diagnostic

Resolved alignment journey `mcpj-1fd2da6fc67e4fd28e356ccc5bef7587`
recorded the product owner's selection of child-identity attribution. Native
proposal review admitted
`p0-user-host-agentgov-child-identity-diagnostic-v1`, and the product owner
separately started it.

Fresh preflight confirmed the retained Python 3.11.9 and AgentGov `0.3.0rc1`
runtime, the remote-free AIRBNB clone and two existing binding changes, the
original AIRBNB checked identities, unchanged user Codex configuration, and
Codex `0.149.0-alpha.4`. The process-local parent environment placed the
retained runtime first on `PATH`, had no `CODEX_HOME` or `PYTHONPATH` override,
and retained the existing `ELECTRON_RUN_AS_NODE` flag. Complete environment
values were not retained.

Persistent or event-based Windows tracing was unavailable and was not
enabled. A hidden 800-millisecond PowerShell child proved that transient CIM
polling could filter an exact parent relationship, then exited normally
without Codex, MCP, network, or repository activity.

The product owner separately approved exactly one user-host App Server
startup. The in-memory observer retained only normalized descendant facts. The
real user Codex-home connection initialized, then the only `thread/start`
reproduced the required-MCP initialize-response closure and `-32603`. No
`thread/started`, `turn/start`, turn, item, prompt, model payload, Agent
response, or tool call occurred.

The diagnostic Codex process started the retained `agentgov.exe`; its digest
matched the preflight value and its arguments matched
`adapter governance-mcp --host-profile codex`. That launcher started the
retained `python.exe`, whose digest was also recorded in the sanitized
experiment. The older global AgentGov process was unrelated and was not
selected. The exact diagnostic processes exited after the first result; no
retry, wrapper, second launch, or repair followed.

The outcome is
`RETAINED_AGENTGOV_CHILD_CONFIRMED_USER_HOST_FAILURE_PERSISTS`. Accidental
global-launcher selection is ruled out. The remaining reproducible boundary is
the real user Codex-home or host-environment context while running the intended
retained executable. The child's complete inherited environment and causal
user-home state remain unknown because obtaining them would require a
prohibited wrapper, debugger, persistent trace, raw log, or wider inspection.

User configuration, clone identities, and pre-existing processes were
preserved. A metadata-only five-minute user-home window found 18 recently
modified and 7 recently created files across bounded extension categories,
but multiple concurrent Codex processes prevent attribution. No name, path,
content, session identity, installation identity, raw command line, complete
environment value, or broad log was retained. No cleanup, Git operation,
publication, release, deployment, or broader activation occurred.

The exact Python 3.11.9 repository suite passed all 946 tests with 3 platform-
limited skips in 261.213 seconds. Task governance returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed. Scope
reconciliation admitted the 4 task-owned paths and retained 14 failures for
explicitly excluded prior-task or user-owned paths; no exception or ownership
transfer was inferred. The task JSON parsed, and bounded host-absolute-path
and recognized credential-marker scans returned zero matches.

## Disposable Codex-home initialize differential

Native proposal review admitted
`p0-disposable-codex-home-initialize-differential-v1` through proposal
`prp-0175f96bf9bb407d8e4f5a3328615e45`, and the product owner separately
started it. The task permitted one separately approved user-host App Server
startup with a fresh disposable Codex home and stopped before repair, retry,
model interaction, cleanup, Git, release, deployment, or broader activation.

Preflight matched Starter HEAD, Codex `0.149.0-alpha.4`, retained Python
`3.11.9`, AgentGov `0.3.0rc1`, the remote-free AIRBNB clone and its two
binding changes, project files, user configuration identity, and pre-existing
process counts. The diagnostic put the retained runtime first on process-local
`PATH`, used the same project binding and one-time trust override, assigned a
fresh operating-system temporary directory as `CODEX_HOME`, and removed
`PYTHONPATH` and `ELECTRON_RUN_AS_NODE` from the child. It changed no
persistent configuration.

The product owner separately approved the one startup. Connection initialize
succeeded, but the only `thread/start` reproduced the required-MCP
initialize-response closure and `-32603`. No `thread/started`, `turn/start`,
turn, item, prompt, model payload, Agent response, or tool-call event occurred.
The exact diagnostic process exited, and pre-existing Codex, AgentGov, and
Python counts were preserved.

The outcome is
`DISPOSABLE_CODEX_HOME_REPRODUCED_USER_HOST_INITIALIZE_FAILURE`. Existing
user-home files and persistent user configuration are not necessary for the
failure to reproduce under the matched launch. Cleanup or migration of the
existing Codex home is therefore not a justified repair. The remaining
unknown lies in a home-independent difference between the passing sandboxed
App Server path and the failing user-host App Server/project-MCP initialization
path; this task did not widen observation to find it.

The disposable home remains without cleanup or content inspection. Its path,
generated names, contents, session identity, and internal metadata were not
retained. Real user configuration, retained runtime and clone identities,
Starter Git state, remotes, credentials, and pre-existing processes were
preserved. No raw protocol response, prompt, transcript, source content,
private data, absolute host path, process identity, raw command line, complete
environment value, or external-model payload was retained.

Focused documentation suites passed 38 and 13 tests. The exact supported
Python 3.11.9 repository suite passed all 946 tests with 3 platform-limited
skips in 254.303 seconds. Task governance returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed. Scope
reconciliation admitted all 4 task-owned paths and retained 16 failures for
explicitly excluded prior-task or user-owned paths; no exception or ownership
transfer was inferred. The task JSON parsed, and bounded absolute-host-path
and recognized credential-marker scans returned zero matches.

This fully specified task did not start an alignment journey. A distinct
bounded current-Agent advisory pass found the one-variable inference,
admitted-path ownership, validation, privacy, and stop boundaries consistent;
it does not claim native self-review completion or independent-audit status.
The initialize-response code flow, complete child environment,
home-independent host state, disposable-home contents, and possible
background network attempts remain unknown. The current callable governance
inventory did not expose `agentgov_task_completion_record`, so no completion
record was fabricated. No downstream authority was granted.

## Final diagnostic closeout and direct-main handoff v2

Native proposal review admitted
`p0-2026-08-21-diagnostic-closeout-main-push-v2` through proposal
`prp-ca8346bebd1242acb6b5b27a79f6c90b`, and the product owner separately
started it. The product owner explicitly authorized one ordinary non-force
commit on `main` and direct push to `origin/main` after final validation.

The exact cumulative commit scope contains 18 paths: the closeout task record;
five previously admitted diagnostic task records; five sanitized experiment
records; `README.md`, `STATUS.md`, this dated log, the governance MCP adapter
guide, automatic-governance product requirements, and two documentation test
files. This scope contains no new uncommitted product-source implementation;
it records the installed Adapter `1.6.0` recovery, the live user-host
initialize failure, the correct retained child identities, and the disposable-
home differential.

The user-owned `.codex` directory, social-cover asset, and external AIRBNB
consumer task record remain excluded, unstaged, and unchanged. No ownership
transfer or scope exception is inferred. The task authorizes no pull request,
force-push, pull, rebase, reset, release, deployment, cleanup, or broader
external action.

The focused documentation suites passed 38 and 13 tests. The exact supported
Python 3.11.9 repository suite passed all 946 tests with 3 platform-limited
skips in 266.486 seconds. Task governance returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed. Scope
reconciliation admitted all 18 intended paths and retained exactly 3 failures
for the explicit exclusions; no exception or ownership transfer was inferred.
All included task JSON records parsed. An initial generic drive-path scan
matched only the documentation test's deliberate `C:\\Users` rejection
literal; the concrete user-bearing host-path scan and credential-marker scan
returned zero matches.

This fully specified closeout did not start an alignment journey. A distinct
bounded current-Agent advisory pass found the documentation-only requirement,
cumulative ownership, blocked-integration wording, validation, exclusions,
Git authority, and stop boundaries consistent; it does not claim native
self-review completion or independent-audit status. Remote divergence, Git
transport, and branch-protection acceptance remain unknown until fetch and
push. The current callable governance inventory did not expose
`agentgov_task_completion_record`, so no completion record was fabricated.

Remote-divergence, staged-scope, commit, and push checks follow after this
append-only evidence enters the intended payload. The final commit identity
and remote result will be reported in the direct human handoff rather than
predicted here.

## README product-entry refactor backlog record

Native proposal review admitted
`p1-record-readme-product-entry-refactor-backlog` through proposal
`prp-b61ca3d6797c4cdcb51c4002617bc0b2`, and the product owner separately
requested this exact record, documentation update, commit, and direct push to
`main`. This slice records a future product item only; it does not change
README, start the refactor, decide its schedule, or authorize later
implementation.

The read-only review found that README currently has 1,769 lines, approximately
11,530 words, 27 second-level sections, 48 code blocks, and 124 links. The
`Runnable CLI example` section alone spans 819 lines, while `Project navigation`
spans 188. The problem is responsibility mixing rather than length alone:
product entry, command reference, architecture meaning, and development
evidence currently share one reading path.

The recorded P1 item requires section-by-section classification as Product,
Reference, Architecture, or Evidence before migration. It forbids moving the
819-line monolith into a new catch-all CLI document, prefers existing topical
documents, retains one README architecture diagram, moves interview framing to
reviewer navigation, reduces the first screen to three primary routes, and
preserves distinct stable, published-prerelease, and development-source facts.
Unique evidence is preserved under its owning source rather than deleted or
duplicated.

The primary future acceptance signal is reader comprehension: within two to
three minutes a new reader should be able to explain the product, problem,
stable capability, development-source difference, shortest runnable path, and
where to read deeper material. A 400–650-line or 3,000–4,500-word result is a
review signal only, not a hard target. Real unbriefed-reader comprehension
remains unknown until future human evidence exists.

Validation, scope reconciliation, privacy review, advisory review, and exact
Git publication evidence follow below after their observed results are
available. User-owned `.codex`, the social-cover asset, and the external AIRBNB
task record remain excluded and unchanged.

The first declared focused command named a nonexistent
`tests.test_development_plan` module. Its 49 real user-documentation and status
tests passed, but unittest returned an operational import error for that
invalid target. The task-owned command was corrected without changing scope or
acceptance meaning to use the existing public-documentation-freshness suite,
which protects strategic-plan ownership and cross-surface consistency. The
invalid run is retained as diagnostic evidence and is not counted as a product
test pass.

The corrected focused command passed all 62 user-documentation, status, and
public-documentation-freshness tests. Task governance returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and scope reconciliation admitted all 4
task-owned paths while retaining 3 failures for the explicit exclusions. Task
JSON parsing, bounded concrete-host-path and credential-marker scans, full diff
review, and `git diff --check` passed.

This fully specified documentation record did not start an alignment journey.
A distinct bounded current-Agent advisory pass found the Product, Reference,
Architecture, and Evidence ownership split; unique-evidence preservation;
reader-centered acceptance; nonbinding size heuristic; exact scope; and
not-started implementation boundary consistent. It does not claim native self-
review or independent evidence. The current callable governance inventory does
not expose `agentgov_task_completion_record`, so no completion record was
fabricated.

The product owner authorized one ordinary non-force commit on `main` and direct
push to `origin/main` for these 4 exact task files after final validation. That
authorization does not include README implementation, a pull request, force-
push, release, deployment, cleanup, or any excluded file. Exact commit and
remote results will be reported in the direct handoff rather than predicted in
this pre-commit record.

## README product-entry refactor v1

Resolved native alignment journey `mcpj-062f3f4579004d63b86389978fdfb10c`
records the product owner's choice to prioritize the README product entry.
Native proposal review admitted `p1-readme-product-entry-refactor-v1` through
proposal `prp-888c9e3c6cb847919b6ac3e3fdec9841`. The task permits only the
README, its two documentation contract test files, the strategic plan, current
status, this dated log, and its own task record. It grants no Git, publication,
release, deployment, external-consumer, or excluded-file authority.

Goal: turn README from a mixed product/reference/architecture/evidence
monolith into a concise product entry. Non-goals: no new catch-all reference,
no rewrite of historical evidence, no public-site or localized-page change,
and no claim that unbriefed-reader comprehension has been proved. Acceptance
requires three primary first-screen routes, one README architecture diagram,
early and honest release-channel distinctions, reuse of existing topical
documents, and tests that protect each fact at its owning surface. The stop
condition is any section or unique claim without a confirmed owner.

The required pre-migration inventory was completed before editing README:

| Existing README section | Responsibility | Confirmed retained role or destination |
|---|---|---|
| `Interview snapshot` | Product | Rename to a neutral product overview; route interview framing to `docs/interview-guide.md` and evidence review to `docs/portfolio.html`. |
| `Why this exists` | Product | Retain a concise problem statement in README. |
| `Architecture at a glance` | Architecture | Retain one overview diagram; route durable detail to `docs/governance-model.md`, ADR-0013, and ADR-0016. |
| `What makes it different` | Product | Retain a compact comparison focused on deterministic facts, advisory judgment, and human authority. |
| `What this project demonstrates` | Product | Compress to current capability groups and route detail to existing topical guides. |
| `Scope boundaries` | Product | Retain concise non-goals and authority limits. |
| `Runnable CLI example` | Reference | Keep only the shortest stable first-use path; route adoption, development lifecycle, replay, Monitor, archive-index, and integration commands to their existing guides. |
| `Example findings` | Reference | Keep the four finding meanings; route rendered detail to the existing sample report. |
| `Detailed architecture` | Architecture | Remove the duplicate diagram and route to `docs/governance-model.md` and `docs/adr/`. |
| `Project status and non-goals` | Evidence | Keep an early release-channel summary and route current facts to `STATUS.md` plus `docs/release-channels.md`. |
| `Design principles` | Architecture | Compress into the overview and route durable meaning to `docs/governance-model.md` and the ADRs. |
| `Current scope` | Product | Retain a short supported-versus-not-supported boundary. |
| `Project navigation` | Reference | Replace the 188-line inventory with a compact responsibility-based documentation map over existing documents. |
| `Repository layout` | Reference | Retain only the paths needed to orient a contributor. |
| `Development setup` | Reference | Retain the baseline local validation command. |
| `Development task contract preview` | Reference | Route to `docs/development-task-contract.md`, scope, evidence, and session guides. |
| `Clean-repository adoption path` | Reference | Route to the existing web Quickstart and keep one minimal verified command sequence. |
| `Existing-repository inspection` | Reference | Route to `docs/existing-repository-adoption.md` and `docs/generated-files-guide.md`. |
| `Capability check` | Reference | Route to CLI help, `docs/governance-model.md`, and the capability/evaluation references already in the repository. |
| `Templates` | Reference | Route to `templates/README.md`. |
| `Agent operating protocols` | Reference | Route to `agent-skills/README.md`; retain the advisory and authority boundary in the product summary. |
| `Repository check` | Reference | Keep one Quickstart invocation and route semantics to `docs/governance-model.md`. |
| `Capability artifacts and source drift` | Reference | Route to the review-artifact model in `docs/governance-model.md` and troubleshooting. |
| `Repository report formats` | Reference | Route to the sample report, report schema, consumer CI, and CLI help. |
| `Evaluation readiness` | Reference | Route to `evaluation/README.md` and `evaluation/readiness-policy.md`. |
| `Reference implementation` | Evidence | Retain the independent-source boundary and route extraction decisions to `docs/ai-radar-extraction-map.md`. |
| `License` | Product | Retain the MIT license link. |

No section lacked a destination. Detailed lifecycle, native Adapter, replay,
AIRBNB, NYC, installation-recovery, and other chronological claims already
have stable owners in the linked architecture/reference documents,
`STATUS.md`, and dated development or experiment records. Their README copies
may therefore be removed only while the tests continue asserting the claims at
those owning surfaces. This inventory is append-only migration evidence, not a
new product reference or maintenance registry.

The implementation replaced the 1,769-line, approximately 11,530-word mixed
surface with a 366-line, approximately 1,993-word product entry. It now has 12
second-level sections, 55 Markdown links, exactly three primary first-screen
routes, and one Mermaid architecture diagram. `Interview snapshot` became the
neutral `Product overview`; stable `0.2.1`, published prerelease `0.3.0rc1`,
and current development source appear before the deeper story, including the
development-only `repository:git-access` doctor distinction. One stable
install and first-use path and one governed example remain directly runnable.

The former 819-line command sequence and 188-line project-navigation inventory
were not moved into another document. README now routes detailed adoption,
development lifecycle, replay, Monitor, MCP, architecture, reviewer, release,
and evidence needs to the existing topical documents recorded in the
inventory. AIRBNB, NYC, Adapter installation, replay failure, and other
chronological facts remain at their existing `STATUS.md`, dated-log, and
experiment paths. The public HTML and localized pages were checked as separate
owners and remain unchanged because their product journeys and release facts
did not change.

The first focused post-refactor test run produced 19 failures and one error,
all from old tests that required detailed version, replay, command, or evidence
facts to be duplicated in README. No failing test was deleted or bypassed.
Those assertions now require README to link to the correct owner and continue
requiring every exact claim at `STATUS.md`, the relevant topical guide, ADR, or
dated experiment. A new product-entry contract requires exactly three top
routes, one architecture diagram, early release-channel distinctions, owner
links, removal of the old mixed-responsibility headings, and absence of known
historical-detail copies. The corrected focused user-documentation, public-
documentation-freshness, and status suite passes all 63 tests.

### Full-suite ownership correction

The first full repository suite after that focused correction ran 950 tests
with 3 platform-limited skips and produced 47 failures. Every failure was an
obsolete README-copy contract in seven additional documentation-test modules;
no CLI, schema, runtime, release, or consumer behavior failed. Because those
files were outside the first task, no corrective edit began until native
proposal review admitted the cumulative 15-path task
`p1-readme-product-entry-test-owner-migration-v1` through proposal
`prp-e99afc2fc27a4bd8934fec53ca806022`.

The correction preserves deep evidence assertions at their owning status,
guide, portfolio, pilot, report, or asset surfaces while making README tests
protect entry behavior: exact top routes, concise product and release meaning,
existing owner links, one overview diagram, authority boundaries, and the
absence of migrated chronology. The final README is 376 lines, approximately
2,044 words, 12 second-level sections, 61 Markdown links, exactly three primary
first-screen routes, and one Mermaid architecture diagram. The cumulative
focused suite now passes all 126 tests. Full closeout validation follows this
record; no commit, push, publication, release, deployment, or external action
is authorized.

### Product-entry closeout validation and advisory review

The corrected focused suite passed all 126 tests. The exact supported Python
3.11 full suite then passed all 950 tests with 3 platform-limited skips in
149.463 seconds. Both task records parsed as JSON; the cumulative task check
reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; all 61 README Markdown references were
checked; bounded concrete-host-path and credential-marker scans passed; and
`git diff --check` passed.

The cumulative scope report passed all 14 changed or new work paths and failed
only on pre-existing user-owned `.codex/config.toml`, which the task explicitly
excludes. That file was already untracked before alignment and remains
untouched. It was not hidden, staged, removed, admitted, or treated as an
exception. The current deterministic scope contract cannot distinguish that
pre-existing excluded untracked path from one created by the task, so the
single scope failure remains visible.

Resolved alignment journey `mcpj-062f3f4579004d63b86389978fdfb10c`
received distinct native current-Agent self-review
`srv-c9b13cc2e817573caae8a65c26fd0f8a`. The advisory pass found the selected
requirement, documentation ownership, architecture boundary, implementation,
and security boundary consistent with the allowed evidence. It retained two
unknowns: the scope check's treatment of the pre-existing `.codex` path and
whether a genuinely unbriefed reader can meet the two-to-three-minute
comprehension target. The review grants no human acceptance, scope exception,
Git, publication, release, deployment, or follow-on authority. The callable
tool inventory exposes no `agentgov_task_completion_record`, so none was
fabricated.
