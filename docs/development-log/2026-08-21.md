# Development log - 2026-08-21

## AIRBNB live uncoached governance replay evidence

The product owner approved one external-model AIRBNB replay using the retained
temporary clean clone and isolated AgentGov runtime. A fresh interactive Codex
CLI process used a process-local PATH plus a one-time trust override, allowing
the unchanged project-level configuration to load the required
`agentgov_governance` server without adding a persistent trust entry for the
temporary clone.

The coding Agent received one normalized README-heading requirement without
instructions about how to invoke governance. User-provided terminal evidence
shows it calling native proposal review, presenting the exact task-admission
form, and waiting for the human decision. The product owner admitted the task.
Only then did the Agent verify the task record and pre-edit state, replace the
single level-three heading, run its bounded Git checks, and disclose a bounded
current-Agent review.

Independent read-only repository measurement after the handoff found the
consumer task owned by `Human product owner`, in `admitted` state, and
scoped only to `README.md` plus the tool-managed task record. The clone
contained exactly those two changes. Repository-normalized diff reported one
deleted and one added heading line, and `git diff --check` passed. The clone
remained detached at exact commit `d70615527d9acdde3893ce645d1923606173acf6`
with no remotes. No commit or push occurred.

After completion, the parent Agent gave incorrect advice to rewrite the
existing human admission decision as paused. When the product owner relayed
that request, the live coding Agent attempted native proposal review. AgentGov
rejected the normalized request because it would mutate an existing human
admission decision and rationale. The Agent respected the fail-closed result,
performed only read-only checks, and stopped. No follow-up task record,
decision mutation, further README edit, commit, or push resulted. The parent
Agent corrected its guidance.

The original AIRBNB worktree retained its prior branch, committed HEAD,
modified README, untracked prior task, and measured blob identities. The
retained clone and runtime were not cleaned up. User-provided terminal behavior
and current-Agent repository measurements remain explicitly separate evidence
sources; no raw prompt, response, transcript, screenshot, source content,
credential, private data, or absolute host path is stored.

Native Starter proposal review then admitted
`p0-airbnb-live-uncoached-replay-evidence-v1` for only its task record, one
sanitized experiment record, this dated log, and current status. The proposal
created only the task record and did not start implementation. The product
owner separately took up the admitted evidence task. Prior Starter records,
user-owned `.codex`, product source, tests, Git, publication, release, and
deployment remain outside scope.

All 13 task-contract tests and all 36 user-documentation tests passed. The
complete Starter suite passed all 938 tests with 3 platform-limited skips in
183 seconds. Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`,
and repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
Scope accepted all four task-owned Starter paths and rejected only six
preserved excluded paths from prior tasks or user-owned `.codex`; no
exception or ownership transfer was inferred. The task JSON parsed, bounded
credential and absolute-host-path scans returned zero matches, and
`git diff --check` passed.

This fully specified evidence task did not start a new alignment journey. A
distinct bounded current-Agent advisory review found no correction-required
requirement, scope, evidence, privacy, or authority drift. It retained the
unknown generality beyond this narrow replay and the unresolved supported
completion-marker workflow. The review does not claim native self-review
completion or independent audit status.

The evidence work is complete and stopped without altering its original human
`admitted` decision. No new task-state transition, commit, push, cleanup,
publication, release, or deployment is claimed.

## Native append-only task-completion recording v2

The product owner selected the append-only completion direction through native
alignment journey `mcpj-8661e24241e34a54a3b104228f313308`. Native proposal
review first admitted
`p0-native-append-only-task-completion-record-v1`. Pre-implementation scope
review then found that v1 omitted the repository and generated Agent guidance
and Codex MCP templates required by its own acceptance signals. No v1 field was
rewritten. Native proposal review admitted the cumulative successor
`p0-native-append-only-task-completion-record-v2`, and the product owner
separately took up that exact task.

Development Adapter `1.6.0` adds exactly one base tool,
`agentgov_task_completion_record`. It accepts only one safe repository-relative
task-record path, revalidates human admission and the complete Git scope,
retains a matching active-session comparison base or uses current HEAD for a
fully attributable sessionless snapshot, runs only the task-declared
validation commands, and reuses the existing validation evidence,
`validation.completed`, completion reconciliation, and
`completion.reconciled` contracts. Fresh passing evidence returns `verified`;
failed or validation-mutated evidence returns `needs_evidence`. Malformed,
non-admitted, active-task-mismatched, unrelated, and out-of-scope calls fail
before local evidence is written. Repeated successful calls create distinct
append-only evidence and events while leaving the task, HEAD, and index
unchanged.

Repository and generated Agent guidance now route normal admitted-task
closeout through the tool without treating it as human acceptance. The
generated Codex MCP template enables it. Form-capable discovery is eight tools
and base discovery is six. Official Codex configuration documents a 60-second
default per-tool timeout and a server-level override; the generated config now
uses 1,800 seconds because this repository's declared suite takes more than 60
seconds. README, MCP, task, lifecycle, automation, product
requirements, and ADR surfaces identify the capability as development source
only and preserve historical Adapter `1.5.0` and AIRBNB facts. Strategic
direction did not change, so `DEVELOPMENT_PLAN.md` required no content change.

All 105 focused MCP, evidence, initializer, user-documentation, and
public-freshness tests passed. The complete repository suite passed all 942
tests with 3 platform-limited skips in 202 seconds. These results precede the
final task/repository/scope checks and native self-review recorded below.

The result grants no human requirement acceptance, architecture correctness,
exception, source-change, scope-expansion, Git, publication, release, or
deployment authority. It neither starts nor hands off a development session.
No installed runtime, retained AIRBNB clone, AIRBNB worktree, consumer
configuration, temporary resource, Git history or remote, package, public
page, release, deployment, or external system was changed.

Final task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
accepted all 21 v2-owned changed paths and rejected only 8 explicitly excluded
paths belonging to prior tasks or user-owned `.codex`; those paths were
preserved, not reclassified as exceptions. Both task records parsed as JSON,
bounded secret-like and absolute-host-path scans returned no matches, and
`git diff --check` passed.

The resolved alignment journey then completed native current-Agent self-review
result `srv-79e87ea1acfd031ab3f6a8f62e34fc6d`. The separate advisory pass found no
correction-required requirement, architecture, or scope drift. It retained the
unknown installed-runtime, live-consumer, cross-host, higher-risk,
committed-during-task, and non-Git behavior. It also retained the limit that a
sufficiently long multi-command validation set may exceed the generated
1,800-second client timeout. The review made zero AgentGov model or network
calls, retained no materializer context, granted no authority, and is not an
independent audit.

The v2 implementation is complete and stopped. The v1 and v2 human admission
decisions remain unchanged. No task-state rewrite, session start or handoff,
commit, push, installation, consumer replay, publication, release, deployment,
or external action is claimed.

After the timeout correction and self-review record were incorporated, the
five exact focused commands still passed 105 tests and the final exact verbose
suite passed all 942 tests with 3 platform-limited skips in 200 seconds.

## Native-completion isolated AIRBNB replay attempt

The product owner selected a live uncoached isolated AIRBNB replay through
resolved alignment journey `mcpj-26130afbf8444dadab2f3e42246e0de7`. Native
proposal review admitted
`p0-native-completion-airbnb-isolated-live-replay-v1`; its task check reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and the product owner separately started the
task.

Read-only preflight found the original AIRBNB worktree at its previously
measured commit with its existing README and task changes preserved. The prior
temporary clean clone had also been used by the earlier replay and therefore
was not reused. A fresh operating-system temporary root received a bounded
source staging copy, a new Python 3.11.9 virtual environment, and a new AIRBNB
clone detached at exact commit
`d70615527d9acdde3893ce645d1923606173acf6` with zero remotes. Four
task-critical source/template hashes matched between Starter and staging, and
the new clone was clean.

The exact offline installation attempt used no index, no dependencies, and no
build isolation. It stopped during metadata preparation. The new environment
contained `pip 24.0` and `setuptools 65.5.0`, while Starter declares
`setuptools>=69`; the older backend rejected current project metadata before
building or installing a package. The runtime contains no AgentGov package or
`agentgov` command. No compatible build dependency was found in the checked
local cache, and the task did not authorize a download, metadata correction,
manual wheel construction, or alternate-path retry.

A second deterministic readiness finding showed that the AIRBNB commit's
Codex `enabled_tools` allow-list contains the prior seven form-capable tools
and omits `agentgov_task_completion_record`; its Agent guidance still describes
five base tools. Official Codex configuration defines `enabled_tools` as the
server tool allow-list. This was not repaired or overridden. Because
installation had already failed, no MCP discovery, Codex process, external
model turn, form, consumer task, edit, completion record, or current-Agent
consumer review occurred.

Starter source hashes, HEAD, index, remotes, and prior dirty state; original
AIRBNB state; the retained clean clone; and retained runtimes remained
unchanged. The new temporary source, empty runtime, and clean remote-free clone
are retained. Sanitized evidence contains no raw prompt, response, transcript,
screenshot, source content, credential, private data, temporary absolute path,
or external-model payload.

The attempt is `BLOCKED_BEFORE_INSTALLATION` and complete as a bounded failure
record. It stops without retry, cleanup, Git operations, publication, release,
deployment, consumer activation, or external-system change. A later product
review must decide whether to admit an offline build-bootstrap and explicit
consumer-configuration-binding task; this record grants neither.

The closeout validation then passed both focused documentation suites with 37
and 12 tests. The exact full repository suite passed all 944 tests with 3
platform-limited skips in 199.247 seconds. The admitted task check reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed. The
cumulative working-copy scope check admitted 9 task-owned paths and rejected
22 pre-existing excluded paths. Those paths were preserved; the result grants
no exception or ownership transfer.

Resolved journey `mcpj-26130afbf8444dadab2f3e42246e0de7` completed a
distinct native current-Agent self-review as
`srv-6fbc48d394502804b530a4c0e7b781db`. The advisory pass confirmed that the
fail-closed stop matched the admitted requirement and found no
correction-required scope drift. It retained the compatible offline build
dependency, live installed completion journey, and explicit AIRBNB tool
binding as unknown. It made zero AgentGov model or network calls, granted no
authority, and is not an independent audit.

## Final development closeout and direct-main handoff

The product owner then explicitly asked to stop for the day, update the
related documentation, commit the confirmed work, and push it directly to
GitHub `main`. Native proposal review admitted exact local closeout task
`p0-2026-08-21-development-closeout-main-push-v1`, and the product owner
separately started it. That task owns only its task record, this append-only
log, and `STATUS.md`; it changes no product behavior and rewrites no prior task
decision.

Git preflight found the current branch at `main`, local HEAD and the locally
known `origin/main` both at `f509a724e2477c8f59811a44ad1b50d08c7eaf75`, and
the configured fetch and push remote at the expected GitHub repository. The
intended commit payload is the explicitly named cumulative non-`.codex`
working-copy scope from today's previously admitted tasks plus this closeout
record. The user-owned `.codex` directory remains excluded and unstaged. The
human authorization permits one ordinary non-force commit and direct push to
`origin/main`; it does not authorize a pull request, force-push, release,
deployment, temporary-resource cleanup, or broader external action.

The final commit identity and push result are necessarily observed after this
record is included in the payload. They are therefore reported in the direct
human completion handoff rather than predicted in this append-only evidence.

Final closeout validation passed the exact full repository suite with all 944
tests successful and 3 platform-limited skips in 198.835 seconds. The closeout
task check reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance
reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed.
The narrow closeout scope check admitted its three owned records and rejected
29 preserved paths owned by earlier tasks or the user; no exception or
ownership transfer was inferred. Its task JSON parsed, and bounded secret-like
and absolute-host-path scans returned zero matches.

A distinct bounded current-Agent advisory pass found the documentation-only
requirement, accumulated-scope attribution, validation evidence, direct-main
authority, and `.codex` exclusion consistent. It retained remote divergence
and Git transport as unknown until fetch and push. This fully specified task
started no alignment journey, so the pass does not claim native self-review
completion or an independent audit and grants no additional authority.
