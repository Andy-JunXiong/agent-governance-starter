# Development log - 2026-08-07

## Reviewed requirement: remaining MCP alignment rejection

The human product owner reviewed the 2026-08-06 closeout and explicitly asked
the Agent to perform the recommended bounded diagnosis. Routing hygiene paused
`p0-mcp-tool-selection-guidance` and admitted only
`p0-mcp-alignment-drift-semantics-contract`.

Normalized contract comparison, without retaining or reconstructing the live
payload, found one exact Adapter/Core mismatch. The MCP input schema allowed
`deterministic` semantics for every drift kind, while Core requires business,
requirement, and architecture drift to remain advisory. Core's exact rejection
was not in the safe classification table, so the host received the observed
unclassified non-retryable error.

The bounded correction makes that conditional rule visible in the MCP schema,
classifies the exact Core rejection as `drift.semantics` /
`advisory_required`, and retains Core's rule unchanged. Three normalized
fixtures cover business, requirement, and architecture drift. Each fixture
proves the rejected start is atomic and an advisory retry succeeds through the
same MCP server process. Validation and runtime replay evidence follow after
the implementation checks complete.

## Validation closeout

- MCP and current-task focused regression passed 29 tests.
- The official Python 3.12 full suite passed 728 tests with two
  platform-limited skips.
- The admitted task reported `PASS=6 WARN=0 FAIL=0 ADVISORY=1`.
- Repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
- All 56 repository schemas parsed, source/tests compiled, and
  `git diff --check` passed.
- Combined working-copy scope reported `PASS=11 FAIL=2 ADVISORY=1`. Both
  failures are preserved prior-session files explicitly excluded from this
  requirement: `.codex/config.toml` and the user's social-cover image.

The bounded development correction is complete and remains admitted for human
review. No installed runtime, live replay, Git write, publication, release,
deployment, NYC work, other host, or consumer repository was touched. The next
possible connection is one fresh installed-runtime Codex replay, but it is not
admitted or started by this closeout.

## Reviewed next requirement: installed-runtime replay

The human product owner reviewed the correction handoff and explicitly
requested the next step. Routing hygiene paused
`p0-mcp-alignment-drift-semantics-contract` and admitted only
`p0-live-codex-mcp-drift-retry-replay`.

This requirement first rebuilds and hash-records the exact current source,
updates only the existing isolated AgentGov pipx runtime, and probes Adapter
`1.2.1`, its five tools, conditional drift schema, and retryable
`advisory_required` behavior. Because this active session is not fresh, it must
stop after preflight. The measured journey must begin in a newly created Codex
session with an ordinary multi-direction request and no named-tool coaching.

## Installed runtime prepared

The exact current working source was built locally with official Python
3.12.10 into `agent_governance_starter-0.3.0rc1-py3-none-any.whl`. Its SHA-256
was `445FF0A3A803D1FC644A4566E4D2B68365DBA9C97C758C0876376012ED87C55D`.
The artifact remains a local development build and is not the immutable
published `v0.3.0rc1` artifact.

The first pipx replacement attempt could not read the restricted workspace
path. A sandbox-local copy to the user temporary directory retained the exact
hash. The next attempt found a Windows lock. Elevated read-only inspection
identified exactly two AgentGov governance-MCP child-process chains, six
processes in total. Only those six child processes were stopped; VS Code and
unrelated project processes were not targeted. Installation then succeeded in
the existing isolated pipx environment using Python 3.12.10.

The installed executable reported Adapter `1.2.1`, MCP protocols `2026-07-28`,
`2025-11-25`, and `2025-06-18`, and exactly five tools. Its alignment-start
schema required advisory semantics for business, requirement, and architecture
drift. In one installed foreground process, a normalized deterministic
architecture call returned retryable `drift.semantics` /
`advisory_required`; a corrected advisory retry succeeded, entered
`exploring`, and returned a valid journey handle. No rejected value was
recorded.

This is installation and protocol preflight evidence only. This active session
is not eligible to perform or claim the fresh uncoached replay. No publication,
release, deployment, NYC work, consumer mutation, other host, or second Agent
occurred.

Final preparation validation kept the exact installed prerequisite source,
tests, and MCP documentation inside the replay task's cumulative envelope. The
task reported `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The final combined scope result is recorded
as `PASS=12 FAIL=2 ADVISORY=1`; both failures are the preserved host-local
Codex config and user-owned social-cover image explicitly excluded from the
task. `git diff --check` passed.

## Fresh replay result: second alignment rejection

The human started the required fresh Codex session with an ordinary request.
The Agent selected the required alignment journey, received one retryable drift
error, corrected it, and then received `alignment_rejected_internal` with
`unclassified`. It followed fail-closed guidance, changed no files, and
preserved the existing working copy. Implementation, human direction selection,
and self-review did not begin. The replay outcome remains `FAIL`.

The product owner reviewed that result and explicitly authorized the bounded
next diagnosis. Routing hygiene paused
`p0-live-codex-mcp-drift-retry-replay` and admitted only
`p0-mcp-alignment-stable-options-diagnostic`.

Normalized contract comparison found the next schema/Core mismatch without
using the live payload. MCP permits `unknowns=[]` with fewer than two candidate
resolutions or a null recommendation, while Core rejects such a context with
`a context without unknowns requires stable recommended resolutions`. That
message is not safely classified by Adapter `1.2.1`, matching the observed
second-layer unclassified result after drift validation advanced.

Development Adapter `1.2.2` now makes the cross-field rule visible in the MCP
start schema. With `unknowns=[]`, the host must supply at least two candidate
resolutions and a non-null recommendation. Direct calls receive precise
retryable diagnostics: `candidate_resolutions` / `stable_options_required` or
`recommended_resolution_id` / `recommendation_required`. The Adapter performs
those checks before creating a journey; two rejected calls followed by a fully
corrected call in one server process reach `ready_for_decision`.

The MCP and current-task focused regression passed 30 tests. The official
Python 3.12 full suite passed 729 tests with two platform-limited skips. The
admitted task reported `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository governance
reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; all 56 schemas parsed,
source/tests compiled, and `git diff --check` passed. Combined working-copy
scope reported `PASS=14 FAIL=2 ADVISORY=1`; both failures remain the explicitly
excluded host-local Codex config and user-owned social-cover image.

A distinct final diff review found no new privacy, authority, atomicity, or
compatibility issue. The development correction is complete and remains
admitted for human review. Adapter `1.2.2` has not been built or installed, and
no new live replay, Git write, publication, release, deployment, NYC work,
consumer mutation, other host, or second Agent occurred.

## Reviewed next requirement: Adapter 1.2.2 installed replay

The human product owner reviewed the completed stable-options correction and
explicitly requested the next step. Routing hygiene paused
`p0-mcp-alignment-stable-options-diagnostic` and admitted only
`p0-live-codex-mcp-stable-options-replay`.

This active session may build, hash, install, and probe the exact corrected
source. It cannot supply the measured replay because it already knows the tool
contract and expected results. The uncoached journey remains reserved for a
newly created Codex session after installed-runtime preflight succeeds.

## Adapter 1.2.2 installed runtime prepared

The exact current working source was built locally with official Python
3.12.10. The local-only `0.3.0rc1`-identified wheel had SHA-256
`329790D30064103669BA231302FEF87F92C190D5401E9C4817815736825BACB8` and is
not the immutable published `v0.3.0rc1` artifact.

The hash-matched temporary handoff was installed into the existing isolated
pipx environment. Read-only inspection first identified exactly two AgentGov
governance-MCP child-process chains, six processes total; only those six were
stopped to release the Windows lock. VS Code and unrelated project processes
were not targeted. Pipx used Python 3.12.10 and replaced only the existing
AgentGov environment and launcher.

Installed discovery reported Adapter `1.2.2`, three supported MCP protocol
versions, and exactly five tools. Its no-unknown schema condition reported
candidate `minItems=2` and a string recommendation. In one installed
foreground process, missing options returned retryable
`stable_options_required`, missing recommendation returned retryable
`recommendation_required`, and the fully corrected call entered
`ready_for_decision` with a valid journey handle.

This is installed-runtime preflight evidence, not the fresh uncoached replay.
The active session stops at this boundary. No Git write, publication, release,
deployment, NYC work, consumer mutation, other host, or second Agent occurred.

Preparation closeout retained the previously completed 729-test Python 3.12
source baseline with two platform-limited skips. The current replay task and 13
task-contract tests passed; the task reported
`PASS=6 WARN=0 FAIL=0 ADVISORY=1`, repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and combined working-copy scope reported
`PASS=15 FAIL=2 ADVISORY=1`. Both scope failures remain the explicitly excluded
host-local Codex config and user-owned social-cover image. `git diff --check`
passed, and all temporary wheel handoff files were removed after installation.

## Adapter 1.2.2 fresh replay result and reviewed audit

The required new Codex session again selected governance and stopped
fail-closed without modifying the repository when Alignment Start returned an
unclassified Core rejection. Existing changes were preserved; no commit,
publication, release, or deployment occurred. Human direction selection,
implementation, and self-review did not begin. The replay outcome is `FAIL`.

After three successive live runs exposed one hidden validation rule at a time,
the product owner explicitly requested the recommended complete Alignment Start
contract parity audit. Routing hygiene paused
`p0-live-codex-mcp-stable-options-replay` and admitted only
`p0-mcp-alignment-start-contract-parity-audit`. The audit uses normalized
fixtures and a documented reachable-rule denominator; it will not read or
reconstruct the failed session's raw payload.

## Complete Alignment Start parity implementation

The audit denominator is eleven reachable validation families: ten
model-authored input families and one Adapter/Core-generated state family. It
is documented in `docs/governance-mcp-adapter.md` with applicability and
failure-class rules; no coverage percentage is claimed.

Development Adapter `1.2.3` now validates the complete model-authored Start
boundary before invoking Core. The public schema also adds the Core success
signal minimum, full assumptions cardinality/text/uniqueness contract, and the
`adopt_new_center` versus non-adopt center-patch condition. Thirty normalized
repairable-input fixtures cover envelope, subject, center, drift, assumptions,
unknowns, candidates, recommendation, readiness, and privacy/path families.
Every fixture returns a bounded field/rule, is retryable, and never becomes
`unclassified`.

After Adapter validation, any remaining Core rejection is necessarily in the
generated-state family for this contract version and returns only
`alignment_rejected_internal`, no field path, `unclassified`, and
`retryable=false`. A synthetic future Core invariant proves that its cause text
is not returned. Existing successful Codex/Claude fixtures and the bounded
update path remain compatible; the audit makes no claim about semantic
correctness or real host behavior.

Validation closed with 20 focused MCP tests and 13 current-task contract tests
passing. The official Python 3.12 suite passed all 732 tests with two
platform-limited skips. The admitted task reported
`PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; all 56 JSON schema files parsed,
source/tests compiled, and `git diff --check` passed. Combined working-copy
scope reported `PASS=16 FAIL=2 ADVISORY=1`; both failures are the explicitly
excluded host-local Codex config and user-owned social-cover image.

A distinct advisory review found no new privacy, authority, atomicity,
schema-loosening, or compatibility issue in the bounded Start change. Adapter
`1.2.3` exists only in the development source: it has not been built or
installed, and no fresh uncoached replay, Git write, publication, release, or
deployment occurred. The next candidate requirement is therefore a reviewed
build/install/preflight followed by a fresh-session replay, subject to product
owner selection.

## Reviewed next requirement: Adapter 1.2.3 installed replay

The human product owner reviewed the completed parity audit and explicitly
requested the recommended next step. Routing hygiene paused
`p0-mcp-alignment-start-contract-parity-audit` and admitted only
`p0-live-codex-mcp-alignment-start-parity-replay`.

The bounded requirement may build and hash the exact development source,
replace only the existing isolated pipx AgentGov runtime, run installed
protocol preflight, and create one fresh uncoached Codex process. The replay
will receive an ordinary multi-direction request without AgentGov, MCP,
alignment, self-review, protocol, or tool-name coaching. Only normalized
outcomes may be retained.

## Adapter 1.2.3 installed preflight and external-replay blocker

The exact source was built without network access using Python 3.12.10,
setuptools 83.0.0, wheel 0.47.0, and build 1.5.0. The local-only
`0.3.0rc1`-identified wheel had SHA-256
`F3F2B45B21636556FFD034C9C91370FEB790D794EDE2B2488568A8B1ADE9CECA` and is
not the immutable published artifact. Two precisely identified AgentGov
governance-MCP launcher/child chains, four processes total, were stopped to
release the existing pipx lock; no unrelated process was targeted. The wheel
replaced only the existing Python 3.12.10 AgentGov pipx environment, and all
temporary wheel files were removed.

Installed Adapter `1.2.3` reported exactly five tools. A single foreground
protocol probe confirmed the Start schema's center/assumptions constraints,
retryable `center.success_signals` / `min_items`, and a corrected
`ready_for_decision` result. An installed-package synthetic future Core
failure confirmed private, non-retryable `alignment_rejected_internal` /
`unclassified` behavior.

A fresh ephemeral read-only Codex process then exited before tool discovery
because the sandbox could not reach the external service; it is not counted as
an eligible replay. The required unsandboxed retry was denied at the data-egress
boundary because it may transmit repository instructions and context to the
external Codex service. No raw replay material was retained and no repository
file was changed by the attempted process. The active task remains admitted
but cannot continue until the human explicitly approves that external
transmission risk.

## External replay measurement invalidated

The human explicitly approved one external Codex transmission. A fresh,
ephemeral, read-only session completed with exit code zero and a final response
matching the human-selection-boundary shape. The initial normalizer was too
broad: it scanned all event text, including historical project documents, and
therefore emitted contradictory readiness and internal-rejection markers.

Because the privacy plan intentionally discarded raw events, the actual MCP
event sequence cannot be reconstructed. The run is recorded as
`INVALID_MEASUREMENT`; it is not evidence of success or Adapter failure. No
repository mutation or raw replay material was retained. A corrected parser
would inspect only completed `mcp_tool_call` events, but a second external
session was denied because the user's approval covered one transmission. The
task now requires separate explicit approval before sending repository context
again.

## Event-scoped external replay passed

The human separately approved one additional external transmission. A new
fresh, ephemeral, read-only Codex session was measured only through completed
`mcp_tool_call` events and its final Agent message. Historical repository text
and ordinary command output were excluded from classification.

The session exited zero with no operational error and completed two real
`agentgov_alignment_start` calls. The first returned a retryable input
diagnostic; the corrected second call entered `ready_for_decision`. The final
message stopped at the human-owned direction-selection boundary. No internal
or unclassified rejection, Alignment Update, Alignment Resolve, self-review,
or repository mutation occurred. Raw events were not retained.

The replay verdict is `PASS_HUMAN_SELECTION_BOUNDARY`. It validates installed
Adapter `1.2.3` through Alignment Start and closes the repeated unclassified
Start-rejection investigation. Post-selection resolution, implementation,
validation, and current-Agent self-review remain outside this completed slice
and require product-owner review before becoming the next requirement.

Replay closeout passed 20 focused MCP tests and 13 current-task contract tests.
The admitted task reported `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; `git diff --check`
passed. Combined working-copy scope reported `PASS=17 FAIL=2 ADVISORY=1`;
both failures remain the explicitly excluded host-local Codex config and
user-owned social-cover image.

A distinct advisory review found no retained raw replay material, repository
mutation, authority expansion, or claim beyond the measured Start/human-choice
boundary. No commit, push, publication, release, deployment, other host,
second Agent, or post-selection implementation occurred.

## Human-selected post-selection contract slice

The product owner selected the smallest deterministic continuation after the
successful Alignment Start replay. Development Adapter `1.2.4` now validates
Alignment Resolve, active-Agent self-review start, and self-review completion
before state mutation. Repairable binding, offered-option, normalized-list,
repository-relative evidence, request-binding, and observation failures return
only bounded stage, field, rule, and retryability metadata. Corrected calls can
continue in the same foreground journey.

This slice adds no model, credential, network call, host, consumer mutation, or
project authority. It does not prove live post-selection implementation or
self-review behavior; that remains a separately reviewed future requirement.

Focused validation passed 22 MCP tests and 13 current-task tests. The final
official Python 3.12 suite passed all 734 tests with two platform-limited skips.
The admitted task reported `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; `git diff --check`
passed. Combined working-copy scope reported `PASS=18 FAIL=2 ADVISORY=1`, with
only the two pre-existing excluded host-local files failing.

A distinct medium-risk current-Agent advisory review found one schema/runtime
cardinality mismatch for self-review reason codes and evidence references. The
published schema was synchronized with the already enforced runtime limits and
the focused and full suites passed again. The review found no remaining
privacy, authority, atomicity, compatibility, or product-claim issue in the
bounded slice.

That conclusion was superseded by a later independent read-only installation-
gate audit. Three direct probes showed that a non-identifier reason code, a
241-character evidence path, and 21 observation assumptions passed the Adapter
boundary but were rejected later as generic, non-retryable Core failures. A
complete comparison also found missing Adapter checks for evidence allow-list
membership and duplicate observations.

Development Adapter `1.2.4` now aligns reason identifiers with the 120-character
portable Core identifier, limits evidence paths to 240 characters, limits
observation assumptions and unknowns to 20 items, rejects evidence outside the
exact self-review allow-list, and rejects duplicate observations before Core
state mutation. The public MCP schema mirrors those constraints. Installation,
external replay, commit, publication, release, and deployment did not occur in
this correction.

Renewed validation passed 22 focused MCP tests, 13 current-task tests, and all
734 Python 3.12 tests with two platform-limited skips. The admitted task
reported `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; `git diff --check` passed. Combined scope
reported `PASS=18 FAIL=2 ADVISORY=1`, limited to the two previously excluded
host-local files. Five independent failure probes returned the expected field,
rule, stage, and `retryable=true`, left self-review state unchanged, and a
corrected completion succeeded in the same process. The development source is
ready for a separately authorized installation and installed-runtime preflight;
it still has no new live-host evidence.

## Adapter 1.2.4 installation and installed-runtime preflight

The exact corrected source was built offline with Python 3.12.10. The local
`0.3.0rc1` wheel had SHA-256
`09CC8C54A8613E1E3100F60850EBE7BD5DF53668CCD37FBAEBBAF2C8A73BF362`
and passed ZIP integrity validation. No governance-MCP process was running, so
no process termination was required. The wheel replaced only the existing
AgentGov pipx environment; both wheel staging directories were then removed.

Installed STDIO discovery returned protocol `2026-07-28`, Adapter `1.2.4`, and
exactly five tools. Its schemas exposed the 120-character reason identifier,
240-character evidence path, 20-item assumptions/unknowns limit, and unique
observations. One installed-package foreground journey reproduced all five
repairable failures with the expected field/rule metadata and
`retryable=true`, preserved state after every rejection, and completed after a
corrected retry. AgentGov model and network call counts were both zero. No
external Codex replay, commit, push, publication, release, deployment, other
host, or consumer mutation occurred.

## Current-session live post-selection replay

The human product owner identified this already-running ephemeral Codex session
as the single approved external replay, prohibited another Codex session, Agent,
host, or external replay, and selected the recommended live post-selection
direction. The native MCP journey reached `ready_for_decision`, recorded the
exact human `return_to_center` selection through Alignment Resolve, and retained
the denied authority boundary. The session then continued only with bounded
repository validation and evidence closeout; it did not start another process,
modify NYC, or gain Git, release, publication, deployment, or consumer authority.

The event-scoped host observed six governance tool-call completions. Two initial
Alignment Start attempts failed before the successful transition: the first
carried no safe Adapter classification marker and is not counted as a typed
diagnostic; the second returned `alignment_invalid_field`; the corrected third
entered `ready_for_decision`. Alignment Resolve, self-review start
(`materialization_required`), and self-review completion (`completed`) then
succeeded in the same ephemeral process. Raw events and the original prompt
were not retained.

The default Python 3.9 command failed before test execution because that runtime
lacks `tomllib`, and the Windows launcher had no registered Python 3.12. The
exact Python 3.12 runtime behind the installed AgentGov pipx environment was
therefore used. All 22 focused MCP tests and 13 task-contract tests passed. An
initial full-suite run reached the 120-second tool timeout with no observed test
failure and was not counted; the unchanged rerun completed all 734 tests in
252.148 seconds with two platform-limited skips.

The admitted task check reported `PASS=6 WARN=0 FAIL=0 ADVISORY=1` and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`;
`git diff --check` passed. During the live turn, combined working-copy scope
reported `PASS=19 FAIL=4 ADVISORY=1`: the excluded Codex config, two transient
`.tmp-replay` bridge files, and the excluded social-cover image. After the
ephemeral process exited, the outer host extracted only the normalized result
and deleted the two bridge files. A temporary admitted-state, read-only final
scope revalidation then reported `PASS=19 FAIL=2 ADVISORY=1`, leaving only the
pre-existing excluded Codex config and social-cover image. The task decision
was immediately restored to `paused`; no exception or new implementation was
authorized.

This evidence proves the current Codex host can bind a human-owned direction to
Alignment Resolve against installed Adapter `1.2.4` and that the exact working
source remains regression-clean. It does not prove automatic host-side proposal
generation, another MCP host, NYC adoption, release behavior, or deployment.
The distinct native current-Agent review then completed with one materializer
invocation, zero AgentGov model calls, zero AgentGov network calls, and no
Adapter context retention. Its three advisory observations confirmed the
bounded live claim, retained the four then-active scope failures without an
exception, and left the next requirement undecided for product-owner review.
The two transient bridge-file findings were removed only afterward by the outer
host cleanup described above.
After the evidence-only edits, 41 affected documentation, status, and task-
contract tests passed and `git diff --check` remained clean.

## Human-selected automatic task-proposal slice

After reviewing the completed Adapter 1.2.4 journey, the human product owner
selected option 1: automatic proposal generation. The admitted bounded goal is
to let a host Coding Agent materialize one ordinary-language work request into
the existing strict low-risk task-proposal and read-only admission preview.
The existing exact human `ADMIT` boundary remains unchanged. This selection
does not authorize a model, credential, network call, host installation, raw
conversation retention, automatic admission, repository implementation beyond
the admitted slice, Git operations, publication, release, or deployment.

The implemented `ReferenceTaskProposalAdapter` invokes one replaceable host
materializer for ordinary request text, accepts only the bounded
`TaskProposalDraft`, creates proposal identity and all privacy/authority fields,
and hands the normalized record to the existing read-only admission planner.
Its result cannot admit or apply a task. The existing exact interactive
`ADMIT` flow remains the only tested write boundary. Direct normalized-draft
preparation truthfully reports zero materializer calls, while ordinary request
preparation reports one. Host materializer exceptions are suppressed behind a
bounded Adapter error and corrected input can be retried through a fresh call.

Eight focused reference-Adapter tests and all 16 existing task-proposal tests
passed (one Windows symlink test skipped for platform privilege). Thirteen
current-task contract tests and 17 user-documentation tests passed. The final
Python 3.12 suite passed all 742 tests in 253.489 seconds with two platform-
limited skips. The task check reports `PASS=6 WARN=0 FAIL=0 ADVISORY=1` and
repository governance reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.
Working-copy scope reports `PASS=27 FAIL=2 ADVISORY=1`; both failures are the
pre-existing excluded `.codex/config.toml` and social-cover image. The distinct
advisory review found and corrected materializer-call over-reporting and
traceback exposure before the final suite. `git diff --check` passes. No
installation, external replay, model/network call by AgentGov, commit, push,
publication, release, deployment, or NYC change occurred.

## Human-selected Codex native proposal review

The human product owner selected the next connection: Codex production
materialization and native proposal review. Current official Codex documentation,
the installed `codex-cli 0.147.0-alpha.1.2` feature inventory, and a freshly
generated local app-server schema confirm support for MCP form elicitation.
ADR-0015 therefore selects capability-negotiated `elicitation/create` inside
the existing foreground STDIO MCP Adapter instead of a custom App Server client.

The bounded task preserves the original five read-only tools and adds no model,
credential, network service, custom UI, or second host. The current Codex Agent
will materialize only normalized low-risk proposal fields. The Adapter will own
repository/proposal identity, privacy, authority, exact preview, and decision
binding. Only native `action=accept` plus the exact `admit` value may create the
revalidated task. Installation, the existing local Codex configuration, an
external replay, NYC, Git operations, publication, release, and deployment are
not authorized by this implementation task.

The development implementation is now Adapter `1.3.0`. A sixth
`agentgov_task_proposal_review` tool is visible only after the client declares
MCP form elicitation. Codex supplies strict normalized low-risk task meaning;
the Adapter binds the local Git root, creates proposal identity and invariant
privacy/authority fields, builds the existing exact admission plan, and emits
one bounded `elicitation/create` form. Only the response bound to that request
with `action=accept` and `decision=admit` revalidates and exclusively creates
the task. Change, reject, decline, cancel, EOF, malformed input/response,
missing capability, stale plan, target race, and filesystem failure paths are
zero-write and return bounded diagnostics without rejected content or host
paths. Ordinary tool permission grants no admission or downstream authority.

Focused validation passed 26 MCP tests, 16 task-proposal tests with one Windows
symlink skip, 8 reference proposal-Adapter tests, 13 task-contract tests, and
17 user-documentation tests. After the advisory correction, the final complete
Python 3.12 suite passed all 746 tests in 233.792 seconds with two platform-
limited skips. The current task
check reports `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and working-copy scope reports
`PASS=33 FAIL=2 ADVISORY=1`, with only the two explicitly excluded pre-existing
`.codex/config.toml` and social-cover image failing scope. `git diff --check`
passes. A distinct advisory pass found and corrected potential host-path
disclosure from filesystem exceptions. No installation, local config mutation,
external replay, AgentGov model/network call, commit, push, publication,
release, deployment, or NYC change occurred.

## Adapter 1.3.0 architecture and requirements drift correction

A read-only architecture/requirements audit found one blocking MCP protocol
drift, one compatibility drift, one vendor-coupled result name, and stale
source-of-truth wording. The form elicitation `requestedSchema` incorrectly
used a root `additionalProperties` member outside the restricted MCP form-schema
root shape. Elicitation results also rejected every unknown top-level member
even though MCP permits protocol extensions. The result contract named Codex
despite the Core/Adapter boundary being host-neutral, and several planning and
public documentation surfaces still described all MCP tools as five and
read-only or left already-implemented Codex materialization open.

Development Adapter `1.3.0` now emits only the standard restricted form-schema
root fields, accepts and discards top-level elicitation-result extensions while
keeping the standard action and accepted content exact, and returns the
vendor-neutral `agentgov.task-proposal-review-result` contract. A regression
fixture proves that a valid client extension neither blocks exact admission nor
appears in the result. ADR-0015, requirements, automation contracts, plans,
session guidance, README, English public pages, and the localized quickstart now
separate the installed/replayed five-tool `1.2.4` evidence from the
capability-gated sixth proposal tool in uninstalled `1.3.0`.

Focused validation passed 26 MCP tests, 16 task-proposal tests with one Windows
symlink skip, 8 reference proposal-Adapter tests, 13 task-contract tests, 11
status tests, 4 public-document freshness tests, and 17 user-documentation
tests. After a distinct advisory review added the protocol restriction and
extension behavior to ADR-0015, the final Python 3.12 suite passed all 746 tests
in 250.186 seconds with two platform-limited skips. The current task check
reports `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and working-copy scope reports
`PASS=37 FAIL=2 ADVISORY=1`, with only the two explicitly excluded pre-existing
`.codex/config.toml` and social-cover image failing scope. `git diff --check`
passes. No installation, config mutation, external replay, AgentGov
model/network call, commit, push, publication, release, deployment, or NYC
change occurred.

## Approved Adapter 1.3.0 installation-only preflight

The product owner explicitly approved building and hashing the exact current
working source, replacing only the existing pipx AgentGov runtime, and adding
only `agentgov_task_proposal_review` to the existing project Codex
`enabled_tools` list while preserving its absolute launcher and every other
setting. This authorization includes local installed-runtime preflight only.
It does not authorize starting Codex, sending repository context to the OpenAI
service, or performing an external replay; those remain a separate decision.

Read-only preflight established the exact transition. The current installed
server reports Adapter `1.2.4`, five tools, and no proposal-review tool. The
current source reports Adapter `1.3.0`, six tools with form elicitation, and a
non-read-only `agentgov_task_proposal_review`. The existing project config uses
the known working absolute `agentgov.exe` launcher and differs from the new
template only in that launcher and the missing sixth allow-listed tool. The
installation task therefore preserves the absolute launcher and changes only
that list entry.

The exact working source was copied to a system temporary directory and built
offline with the existing Python 3.12 build toolchain. The resulting
`agent_governance_starter-0.3.0rc1-py3-none-any.whl` was 422,624 bytes with
SHA-256
`91BB3BBC6C64F3D73E9AF3F47370ADF88ACE249DD42066A94472A16926DD4198`.
Three processes were verified as one existing `agentgov.exe adapter
governance-mcp --host-profile codex` launcher/child chain before termination.
The leaf and Python child were stopped; the launcher exited with its child. The
wheel was then installed offline with `--force-reinstall --no-deps` through the
pre-existing pipx environment's Python. No new environment or dependency was
created or downloaded.

Installed discovery now reports Adapter `1.3.0`, protocol `2025-11-25`, six
tools with form capability, and a non-read-only/non-open-world proposal tool.
Without form capability it retains five tools and rejects a direct proposal
call with `task_proposal_elicitation_unsupported`. The project Codex config
retains its existing absolute launcher, approval mode, and timeouts; the only
semantic change is appending `agentgov_task_proposal_review` to
`enabled_tools`.

Installed-runtime interaction probes used fresh temporary Git repositories.
They confirmed the restricted `properties,required,type` form-schema root,
ignored and non-echoed client response extensions, zero-write request-changes
and malformed-response paths, exact temporary-repository admission through the
vendor-neutral result contract, and target-race rejection without overwrite.
All temporary repositories, copied source, and wheel caches were removed after
verification. The pipx environment directly passed all 26 MCP tests. Source
focused checks also passed 26 MCP, 13 current-task, 11 status, 17 user-document,
and 4 public-document tests. Task governance reports
`PASS=6 WARN=0 FAIL=0 ADVISORY=1`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; scope reports
`PASS=39 FAIL=1 ADVISORY=1`, with only the pre-existing excluded social-cover
image outside the task. `git diff --check` passes. No Codex process, external
service call, external replay, repository-context transmission, AgentGov model
or network call, commit, push, publication, release, deployment, or NYC change
occurred.

After the independent installation review found no remaining process, cache,
configuration, authority, or evidence-boundary drift, the final complete
Python 3.12 suite passed all 746 tests in 257.618 seconds with two
platform-limited skips.

## Approved Adapter 1.3.0 event-level proposal-review replay

The product owner explicitly approved one fresh transmission of this repository
context to the OpenAI Codex service using installed Adapter `1.3.0` and the
current chat as the human selection channel. The bounded experiment uses a new
ephemeral Codex App Server thread with MCP form elicitation and may exercise one
native `agentgov_task_proposal_review` call. It does not authorize a second
replay or implementation of the proposed task.

The event bridge may retain only the normalized admission plan, exact offered
decision values, the human choice bound to that request, the proposal-review
result, and terminal status. It must discard Agent messages, reasoning, command
output, raw requests, source content, credentials, absolute paths, and unbounded
protocol payloads. Only exact human admission may exclusively create the task
shown in the form; every other decision or failure remains zero-write. Commit,
push, publication, release, deployment, consumer mutation, NYC work, source or
configuration changes, and installed-runtime replacement remain prohibited.

The approved process started the exact local Codex App Server binary but
produced no normalized initialization, thread, form, tool-call, or terminal
event for approximately eight minutes. Its CPU usage remained unchanged; a
read-only network snapshot showed no connection, and no new AgentGov process or
bridge state appeared. The exact process was terminated and confirmed absent.
No decision file, task proposal, admitted task, source change, or raw event file
was created.

A separate local-only probe then reproduced the host boundary without starting
a thread or sending a task. Standard App Server initialization completed within
eight seconds when stderr was copied directly to a null stream. This isolates
the attempt's defect to the Windows PowerShell 5.1 bridge: its asynchronous
stderr event callback stalled before the parent could observe initialization.
The probe did not send repository context, invoke a model, or call AgentGov.
Because the failed process cannot prove that it never crossed the external
boundary, the one-replay authorization is conservatively treated as consumed.
The replay result is `INVALID_HOST_BRIDGE`; live Adapter `1.3.0` proposal-review
behavior remains unmeasured and a corrected retry requires new human approval.

The product owner then explicitly granted a new, separate authorization to send
repository context to the OpenAI Codex service and run one corrected fresh
event-level replay. The authorized host uses the installed Adapter `1.3.0`, a
direct null-stream stderr drain, and the current chat as the human selection
channel. All earlier privacy, no-implementation, single-replay, no-Git, and
fail-closed boundaries remain unchanged.

The corrected host passed a local App Server initialization probe. Its first
local `thread/start` probe then returned JSON-RPC `-32600`: this runtime accepts
the CLI-style `read-only` sandbox value rather than the generated schema/manual
example's `readOnly` value. No turn or model call occurred in that probe. After
the bridge adopted the verified local value and added explicit id 1/2/3 error
terminal states, a second local no-turn probe passed.

The authorized real replay then initialized and created an ephemeral read-only
thread. It submitted the ordinary bounded request and recorded
`status=starting_turn`, proving that the external turn boundary was reached.
App Server closed its output before returning the turn response. The normalized
result was `status=bridge_error`, `stage=starting_turn`, no response error code,
no elicitation, and zero proposal-tool calls. No AgentGov process remained, no
new task appeared, and no raw output was retained. The result is classified
`INVALID_APP_SERVER_EOF`, not a pass or failure of Adapter `1.3.0`. Because the
real request was submitted, the renewed authorization is consumed and no
automatic retry is permitted.

Read-only host-authentication follow-up then returned `Not logged in` from the
same standalone Codex binary. `CODEX_ACCESS_TOKEN`, `CODEX_API_KEY`, and
`OPENAI_API_KEY` were all absent from the child environment; only presence was
tested and no credential value was read. Windows crash-event access was
unavailable. Missing standalone-host authentication strongly accounts for the
EOF at `turn/start`, but the discarded stderr means it is recorded as bounded
causal evidence rather than a recovered verbatim error. Authentication repair
would mutate external/local credential state and requires separate approval.

## End-of-day closeout and next-session plan

The 2026-08-07 development session completed the source and local installed
runtime path from native alignment through human selection, post-selection
Resolve, current-Agent self-review, current-Agent task-proposal materialization,
and capability-gated native proposal review. Development Adapter `1.3.0` is
installed from the exact current source and its privacy, atomicity, schema,
admission, and race protections passed focused installed-runtime preflight.
Earlier fresh live evidence also closed the current-host Alignment Start and
post-selection self-review slices.

The remaining live gap is narrower and explicit. Neither approved `1.3.0`
proposal-review attempt reached an MCP form or proposal tool call. The first
was invalidated by the Windows bridge; the corrected attempt reached
`turn/start` and ended at App Server EOF. The same standalone Codex binary then
reported `Not logged in`. No proposal was admitted, no repository task or
source was created by either replay, and no raw prompt, credential, reasoning,
or unbounded event stream was retained.

The next session should not add another product feature first. It should:

1. separately review and authorize standalone Codex authentication repair;
2. verify login plus initialization/thread/MCP capability locally without
   sending a task;
3. separately authorize one new event-level proposal-review replay;
4. if the native form succeeds, let the human review the exact proposed task,
   without treating admission as implementation authority;
5. review the evidence with the product owner and only then select the next
   requirement. Another host, the independent non-NYC rehearsal, NYC adoption,
   stable promotion, release, and deployment remain future gates.

For repository closeout, the product owner explicitly authorized a commit and
direct push to `main`. Project-local source, tests, contracts, ADRs, plans, and
this dated record are in scope. The host-local `.codex/config.toml` and the
user-owned `docs/assets/linkedin-github-repository-cover.png` remain excluded.
