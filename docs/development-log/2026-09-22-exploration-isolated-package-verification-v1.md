# Isolated exploration package verification - 2026-09-22

## Goal, admission, and stop boundary

Verify the repaired exploration precondition through an isolated installed
package. Alignment journey `mcpj-778455498bbf470aa9cd12b3f2dd796b` records the
human's isolated-runtime selection. The human later permitted necessary
build-tool downloads after the offline tooling preflight found no suitable
local tools. Native proposal `prp-1ced8e2674f64ad884bd007b00541873` admitted
exact task `p0-exploration-isolated-package-verification-v1`. Human `REPLACE`
separately started it, recorded by event
`evt-c7a3c438d7f34af7bd822ec1722f01d5`.

The task owns STATUS, this log, its task record, and the experiment evidence.
Source, tests, scripts, the old distribution manifest, previous records, the
current installed Adapter, and client configuration remain preserved inputs.
Temporary downloads are limited to necessary public build-tool wheels. No
model session, current-client restart, Git operation, publication, release,
deployment, or broader repair is authorized. Stop on an unexpected source,
installation, protocol, privacy, or preservation failure.

## Execution evidence

The old 187-path manifest failed against the current 188-path source. The
missing path is `src/agentgov/scope_observation.py`; identities also differ.
The admitted task permits a separately derived temporary input snapshot and
preserves this historical-manifest gap rather than repairing it.

A verified short temporary root held the downloaded binary build tools,
byte-checked staging, offline wheel, and separate consumer environment.
Python 3.11.9 with setuptools 84.0.0 and wheel 0.48.0 built the local
0.3.0rc1-identified wheel. All 78 package modules and 107 data payloads
matched source in both the wheel and installed consumer.

The temporary installed Adapter reported 1.7.0. One local STDIO process
completed eight bounded requests and fourteen checks, including actionable
invalid-start rejection, same-process correction, inherited-update rejection,
atomic retry, valid exploration, and denied authority. It exited cleanly
without stderr. These synthetic choices are not actual human decisions.
The current installation, configuration, and source hashes remained unchanged.

Detailed versions, hashes, counts, timing, and limits are recorded in
`docs/experiments/exploration-isolated-package-verification-2026-09-22.md`.
Cleanup and task-declared closeout validation are pending at this checkpoint.

## Source-of-truth review

This changes experiment evidence and current status only. The MCP guide and
existing repair record already state that installed-client verification is
separate. README, strategic plan, ADRs/contracts, guides, release metadata,
public HTML/localized pages, source and tests require no behavior or identity
change for this isolated observation. The old manifest remains a known gap.

## Product review context

The source repair now has evidence that it survives packaging and temporary
installation, connecting the repaired native alignment precondition to a
consumer protocol boundary. The short-term benefit is a tested package path
without altering the working client. Long-term reliability, time savings and
adoption benefits remain unknown. A proposed next review is whether to audit
the full current-installation difference and activate the package for a real
client; that could use this evidence but needs a separate decision. No next
feature, installation, manifest repair, or external action is selected here.

## Cleanup checkpoint

A receipt call initially used positional arguments for a keyword-only API.
It failed before cleanup or any file mutation. The corrected call verified
durable evidence before removing the exact task-owned temporary root through
the repository helper. The root no longer exists; original installation,
configuration, and source hashes matched before cleanup. The earlier pending
cleanup state is superseded.

## Validated closeout and advisory review

All five exact task-declared commands passed in evidence
`evd-bb4230fc2796407aa91c9d4de9969710`: focused MCP/documentation and existing
manifest/root-helper tests, task governance, repository governance, task JSON,
and diff whitespace checks. Validation observed no mutation, and completion
reconciled as `verified` while preserving all excluded baseline changes.
The native completion-record tool is not available in this session; the
repository `govern finish` command recorded the evidence without changing the
human task decision. Product source was unchanged in this task, so the prior
source repair's full-suite result was not represented as a new full-suite run.

After validation, native current-Agent self-review
`srv-2bade364c19822d27e18c2a0db02f2af` completed against the allowed task and
experiment evidence. The separate pass found the temporary download, build,
installation, protocol, and cleanup consistent with the admitted isolation
boundary. It retained the failed historical manifest as an unresolved gap
and proposed reviewing that gap before a real-client update. Real-client
activation, repeatability, business benefits, and independent supply-chain
assurance remain unknown. This is advisory self-review, not independent
review or human acceptance; no further task is selected.

This entry supersedes the earlier pending closeout state. The final
documentation annotation is revalidated once; the latest evidence identity
is reported in the handoff without another tracked edit.
