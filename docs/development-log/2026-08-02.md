# Agent Governance Starter Kit Development Log — 2026-08-02

## Outcome

Release-candidate evidence collection no longer requires a maintainer to run
and reconcile a manual list of AgentGov and NYC commands. The new
`agentgov review release` command creates one local, portable review bundle
while preserving the final approve, request-changes, or reject decision for a
human.

## Work completed

- Added strict candidate wheel, manifest filename, metadata-version, and
  SHA-256 matching before any evidence collection.
- Added isolated installation of the exact candidate wheel without consumer
  project dependencies.
- Automated the complete source tests, installed version and manifest checks,
  consumer repository check, Markdown status rendering, and upgrade-policy
  plan.
- Added atomic create-new-only bundle output. Existing paths and symbolic-link
  inputs are never overwritten or followed.
- Added portable `review.json`, human-readable `REVIEW.md`, raw command
  transcripts, the reviewed manifest, and the exact candidate wheel.
- Added a strict release-review schema with an all-false Git, publication,
  release, and deployment authority boundary.
- Added consumer-local stable upgrade review with portable JSON, Markdown UI,
  an exact workflow patch, current project status, four deterministic gates,
  and a pending project-owner decision.
- Added explicit release-candidate blocking and an authority contract that
  distinguishes evidence output from governed-file or Git modification.
- Extended only the 0.2+ managed consumer workflow to download the latest
  stable manifest once, generate the consumer upgrade review, append its UI to
  the GitHub job summary, and upload the complete evidence directory.
- Preserved the published stable 0.1 workflow byte-for-byte and retained
  read-only GitHub permissions with no PR writer.

## Deliberate non-changes

- Artifact building remains a separate release stage.
- No approval decision is inferred or recorded automatically.
- No AgentGov or NYC commit, tag, push, publication, release, or deployment was
  performed.
- NYC remains pinned to published stable AgentGov 0.1.0.

## Validation

- Complete suite: 370 tests passed; one Windows symbolic-link test was skipped
  because the current user lacks link-creation privilege.
- The isolated Python 3.11 wheel build succeeded as
  `agent_governance_starter-0.2.0rc1-py3-none-any.whl` with SHA-256
  `504a80878ee3f13b0ab7162b194ab1ec1aa612b5a27f528ddebe93c40bf18bdd`.
- The exact wheel and immutable manifest passed artifact-integrity, installed
  version, and manifest-contract gates.
- The candidate validated NYC with `PASS=17 WARN=1 FAIL=0 ADVISORY=4` and
  rendered its portable Markdown status.
- NYC release-candidate upgrade planning was correctly blocked from producing
  an upgrade proposal.
- The atomic review bundle passed all seven automated gates, reported
  `ready_for_human_review`, and retained human decision `pending`.
- Consumer-local NYC upgrade review reported `blocked` for the RC channel,
  passed the current NYC governance gate, and left the managed workflow and Git
  state untouched.
- Stable 0.1 workflow rendering matched NYC byte-for-byte at SHA-256
  `9fa83b71b498b058afb2f5bdf777b23ed27933995faedc546faf4321f3974be8`;
  the 0.2 preview retained `contents: read` and contained exactly one automatic
  consumer upgrade review command.

## Release-candidate publication automation

- Added an RC-only GitHub Actions workflow triggered by version-matched
  `vX.Y.ZrcN` tags.
- The workflow validates the tag, package version, and matching release notes;
  reruns the complete tests; builds the wheel; generates and validates a
  `release-candidate` manifest; and creates a GitHub Pre-release.
- `--verify-tag` preserves the separately approved tag boundary, while
  `--prerelease --latest=false` keeps RC artifacts out of the stable latest
  update path.
- Kept the consumer-specific `agentgov review release` bundle outside the
  generic publication workflow. NYC evidence remains a pre-tag human review
  input rather than being replaced by a self-referential CI result.
- Added workflow contract tests and release-channel documentation. The updated
  complete suite passed 372 tests with one platform-limited skip; repository
  governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`.
- No tag, GitHub Release, NYC change, commit, push, or deployment was performed.

## Published RC and stable preparation

- Committed RC publication automation as `20c23ed` and pushed it to `main`.
- GitHub CI run `30706241353` passed all six Ubuntu/Windows and Python
  3.11/3.12/3.13 jobs.
- Created and pushed annotated tag `v0.2.0rc1`; RC workflow run `30706305308`
  passed and published the wheel plus immutable manifest as a Pre-release.
- Independently downloaded both assets. The wheel and manifest agreed on
  SHA-256 `65fc0a64bdb709f3512bd55577d8c055587d4efb93b561377dba3607c0fa0e14`,
  while GitHub's latest stable API continued to return `v0.1.0`.
- Prepared stable source version `0.2.0` and separated bundled installed
  compatibility metadata from strict public manifest artifact validation,
  avoiding an impossible wheel self-hash while preserving the public gate.
- The updated complete source suite passed 373 tests with one platform-limited
  skip.
- Built local stable wheel `agent_governance_starter-0.2.0-py3-none-any.whl`
  with SHA-256
  `08014632b461de1254084eb910c37af60064ebea0d8c6f14af8964c6e9709774`
  and generated a matching strict stable manifest.
- Added weekday scheduled stable-update discovery to the 0.2 managed consumer
  workflow while preserving byte-for-byte recognition of the 0.1 workflow.
- Fixed installed-wheel test and checkout interoperability by recognizing an
  exact content copy of bundled compatibility metadata while retaining strict
  artifact validation for different external stable manifests.
- The AgentGov/NYC release review passed all seven gates. The consumer-local
  NYC review passed all four gates and prepared exactly one workflow change
  from 0.1.0 to 0.2.0 without modifying NYC.
- Stable source changes, the `v0.2.0` tag, stable GitHub Release, and NYC patch
  application remain separately human-controlled and were not performed.

## Stable publication and NYC pilot correction

- Published stable `v0.2.0` after six-environment CI run `30707125192` passed;
  stable release run `30707171113` built and published the verified assets.
- Confirmed GitHub latest points to `v0.2.0`; the public wheel SHA-256 is
  `86803f5a4e97e375d259f3ca13de9c9f94a8b6266dd901990e001cd47f415c8c`.
- Public-manifest NYC review passed four gates and the managed workflow was
  committed as `9ee4656` without including any unrelated NYC changes.
- NYC run `30707309752` verified the public wheel digest, then pip rejected its
  temporary name `agentgov.whl` as an invalid wheel filename.
- Prepared patch 0.2.1 to preserve the canonical wheel filename and added
  installed workflow and stable-manifest regression coverage.
- The 0.2.1 source suite passed 374 tests with one platform-limited skip. The
  local wheel and strict manifest match SHA-256
  `970e36e13ae6cf9df54cf744fb26e940e9a4181b3df210942ee1d8bc048e446e`;
  release review passed seven gates and NYC upgrade review passed four gates.

## Bounded automatic Draft PR development

- Added `agentgov create upgrade-pr`, backed by a minimal GitHub REST client.
- Revalidates the local plan and remote default-branch workflow hash.
- Writes only `.github/workflows/agentgov.yml` on
  `agentgov/update-<version>` and creates only a Draft PR.
- Reuses an exact open PR and recovers an exact branch left by an interrupted
  run; unrelated changes block.
- Accepts only `schedule` and `workflow_dispatch` authorization sources.
- Added a 0.3+ consumer proposal job with job-scoped `contents: write` and
  `pull-requests: write`; ordinary governance remains read-only.
- Exposes `upgrade_automation: proposal_enabled` through `agentgov status`.
- The complete source suite passed 385 tests with one platform-limited skip;
  repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`.
- Verified that NYC local and GitHub `main` share the exact managed 0.2.1
  workflow blob `8c7ac5060e5851e7df7e0f5557c00fed242070c4`.

The published 0.2.1 consumer is read-only. Moving NYC to the 0.3 workflow is a
one-time human-reviewed migration. Compatible releases after that transition
can create Draft PRs without a person copying release details. Merge, release,
deployment, and production execution remain human-controlled. No commit, push,
publication, release, NYC write, or deployment was performed for this slice.

## Continuous benefit monitor development

- Added strict snapshot identity with repository, ref, commit, run, timestamp,
  finding counts, tool version, and report SHA-256.
- Added a create-new-only JSON/Markdown/HTML monitor bundle with a 20-point
  observed trend and explicit denominators.
- Added default-branch baseline restoration from an exact prior AgentGov run,
  rather than trusting an arbitrary same-name artifact.
- Added a 90-day `agentgov-main-baseline` artifact that PR runs can read but
  cannot replace as the trusted baseline.
- Added upgrade automation observation for Draft PR timing and mechanical
  bridge actions requested by the automated path.
- Preserved explicit limits: workflow time is not labor saved, zero observed
  bridge actions is not an avoided-actions estimate, and transitions do not
  prove causality, incidents prevented, coverage, or ROI.
- Added an NYC pilot guide for the future 0.3 consumer migration. No NYC file,
  Git branch, PR, release, deployment, or production action was performed.

## Persona-aware delivery hardening

- Split the 0.3 UI into a PR-author delta/action surface and a trusted
  default-branch owner surface for trend, benefit, and upgrade administration.
- Mapped deterministic FAIL to a blocking annotation/gate and WARN/ADVISORY to
  visible non-blocking annotations without granting pull-request write access.
- Added a read-only default-branch regression job that turns the workflow red
  for GitHub's normal Actions failure notification; no issue or comment write
  permission is granted.
- Moved the Draft PR writer into a separate schedule/dispatch-only managed
  workflow so pull-request execution cannot alter or enter a write-permission
  job.
- Required exact current-version and target-version dry-run reports before an
  automated upgrade branch or Draft PR can be written, and embedded summaries
  plus report digests in the PR body.
- Added action-oriented `missing`, `unchanged`, `improved`, and `needs_review`
  UI states while retaining precise internal diagnostic states.
- Added portable evidence redaction for user/runner paths, credential
  assignments, bearer values, and GitHub token shapes.

## Product review and handoff

- Confirmed that the current product is strongest as a GitHub PR/Actions
  governance backstop and that development-time guidance is the next product
  priority.
- Recorded unresolved decisions for local task context, changed-file scope,
  agent integration, notification authority, the 0.3 bootstrap, and defensible
  benefit evidence.
- Added an ordered remaining-development plan that separates implemented source
  behavior from published 0.2.1 behavior and NYC adoption.
- Kept the core claim boundary explicit: upgrading a governance workflow does
  not by itself improve NYC business code, and `unchanged` is not a benefit.
- Final handoff validation passed 399 source tests with one platform-limited
  skip; repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`.

## Two-workflow contract closure

- Closed the mismatch between the 0.3 two-workflow generator and the earlier
  single-change upgrade contracts.
- The read-only planner now represents explicit `update` and `create` actions,
  recognizes only the named `consumer-ci-v2` bootstrap migration, and blocks
  missing, customized, mismatched, or undeclared workflow layouts.
- Consumer-local review now renders both exact workflow changes and a
  `/dev/null` creation patch without applying either change.
- Compatible 0.3+ Draft PR automation now revalidates and updates both fixed
  managed workflow paths, rejects create/delete migrations, and safely resumes
  a partial bounded write while blocking unrelated branch content.
- Plan, review, and write JSON contracts moved to version 1.1 to expose the
  two-path change set without pretending the previous one-path contract was
  unchanged.
- Updated the upgrade boundary documentation and invariants. No version bump,
  release manifest, RC, NYC write, commit, push, publication, or deployment
  was performed.

Validation: 405 source tests passed with one platform-limited skip. The focused
planner, review, and writer suite passed 33 tests.

## AI Radar governance consistency revalidation

- Revalidated the portable product direction against 14 clean scoped
  governance files from the read-only AI Radar repository at commit
  `3a9323cb2a9ef575da42d29fb17d330ef872afd3`.
- Confirmed that AI Radar governs coding agents before and during work through
  requirement/sprint admission, selective architecture context, bounded
  development loops, fresh verification, action-loop correction, and scoped
  invariant reconciliation. PR analysis is not its core governance mechanism.
- Expanded the extraction map with file-level classifications and explicit
  exclusions. No AI Radar business rule, runtime code, data, prompt, AWS path,
  individual-specific policy, or mechanical gate was copied.
- Added ADR-0009 to make requirement, architecture, and code governance during
  development the AgentGov product core while retaining PR/CI as independent
  replay and evidence infrastructure.
- Reordered the active plan so the task contract, architecture-context
  selection, changed-file boundary, fresh evidence, and completion
  reconciliation precede further PR-centered 0.3 release packaging.
- Updated the governance model, invariants, README, status, case study, and open
  decisions to distinguish the decided product boundary from interfaces that
  remain open.

No AI Radar file, AgentGov runtime behavior, release metadata, Git branch,
commit, push, publication, migration, deployment, or production system was
modified by this documentation decision slice.

## Architecture-drift case recorded

- Recorded `AG-DRIFT-001` as a self-governance case with a commit-backed
  timeline from the original AI Radar extraction through capability contracts,
  adoption work, consumer CI, and upgrade-PR delivery.
- Classified the historical sequence and changed surfaces as deterministic
  evidence, while keeping the conclusion that supporting work displaced the
  product core as an advisory human judgment.
- Identified the missing product invariant and the substitution chain from
  governing coding agents to maintaining governance delivery.
- Made the case the first P0 task-contract acceptance scenario. The future
  contract must preserve requirement and architecture references, changed-path
  facts, fresh evidence, and unresolved advisory decisions before completion.
- Linked the case from the README, governance model, and active development
  plan. No runtime behavior or AI Radar file was changed.

## P0 minimal development task contract

- Added strict contract version `1.0` for a human-declared development task:
  requirement provenance, parent objective and role, goal, non-goals, exact
  path scope, architecture references, acceptance signals, risk, approval,
  stop conditions, and `draft`/`admitted`/`paused` decision state.
- Added zero-dependency structural validation plus safe, readable,
  repository-local reference checks. Absolute paths, traversal, backslashes,
  globs, symlinks, broken references, and inconsistent admitted/approval states
  are deterministic failures.
- Added read-only `agentgov check task <task.json> --repository .`. The command
  does not create or edit the task, inspect Git changes, authorize
  implementation, or grant any remote authority.
- Preserved missing requirement provenance as WARN, omitted architecture
  context as ADVISORY, and objective alignment as an accountable-human
  ADVISORY even when the declared role is `core`.
- Added a structurally valid draft template, passing/incomplete/failing
  fixtures, and the admitted
  `governance/tasks/p0-minimal-task-contract.json` self-governance contract.
  The live task links `AG-DRIFT-001`, ADR-0009, and the corrected P0 plan.
- Documented the experimental interface and kept it outside initialization,
  repository-wide checking, stable 0.2.1 claims, changed-file comparison,
  completion reconciliation, and CI replay.

Validation:

- the live task check returned `PASS=6 WARN=0 FAIL=0 ADVISORY=1`;
- the final complete suite passed 418 source tests with one platform-limited
  skip;
- repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`;
- `git diff --check` passed;
- no AI Radar file, Git branch, commit, push, release, deployment, hook, or
  external system was modified.

## Consolidated product and architecture review plan

- Consolidated the original AI Radar extraction goal, implemented stable and
  development-source assets, the `AG-DRIFT-001` correction, and the new
  Monitor/Dashboard requirement into one Chinese review draft under
  `docs/proposals/`.
- Defined the product center as `Govern -> Observe -> Monitor`, with PR/CI as
  independent replay and upgrade automation frozen as supporting work.
- Included `AGENTS.md`, tasks, ADRs, Invariants, Agent Skills, capabilities,
  controls, dependencies, and evaluation as first-class registry and routing
  inputs with conditional triggers.
- Proposed a privacy-bounded governance event layer and a local static HTML
  Dashboard that can also be uploaded as a GitHub Actions artifact.
- Added phased delivery, end-to-end acceptance scenarios, explicit AI Radar
  exclusions, risks, review questions, and a ready-to-use Claude review prompt.
- This planning slice changed no runtime behavior, Git state, release,
  deployment, AI Radar file, or external system.
- Final validation passed 419 source tests with one platform-limited skip;
  repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`, and
  `git diff --check` passed.

## Claude review resolution

- Accepted the four execution concerns while correcting two implementation
  details: AgentGov already uses exact path/path-prefix scope and deliberately
  rejects glob semantics; fresh evidence must bind the dirty working-tree
  change-set digest rather than rely on `HEAD` equality or file mtime.
- Restricted deterministic scope drift to the admitted structured path
  contract. Natural-language scope remains advisory.
- Restricted `architecture.candidate` in v1 to explicit task references or
  changed-path overlap with artifact-owned applies-to metadata; the trigger is
  advisory and performs no full-text semantic inference.
- Added explicit Dashboard observation scopes. CI-only data cannot claim
  complete development history or compare first discovery across pre-code,
  development, and PR stages.
- Initially defined fresh evidence through command identity, timestamps, task
  digest, comparison base, snapshot `HEAD`, canonical
  staged/unstaged/untracked/renamed change-set digest, and
  before/after/finish equality; the second review below fixes its exclusion and
  validation-artifact semantics.
- Removed editable `registry.json` from the design. Trigger and applies-to
  declarations live only in their owning artifact; Registry, Context, and
  Event outputs are derived and retain provenance.
- Moved compact task profile and real Coding Agent context-consumption testing
  into Phase 1.
- Accepted `govern start/check/finish`, three actor classes plus optional
  label, explicit self-reported stagnation, patch-only core governance updates,
  and 0.3 as the development-governance release line.
- Reduced the Dashboard MVP to Overview, Timeline, and Task Detail while
  retaining minimal observed outcome facts and mandatory basic redaction for
  any export.
- This review-resolution slice changed no runtime behavior, AI Radar file, Git
  state, release, deployment, or external system.

## Claude second-review resolution

- Accepted the second review's Phase 1 approval and converted its Phase 2/3
  conditions into explicit implementation gates rather than leaving them as
  prose in the product plan.
- Added a development trigger and routing specification. Exact prefixes now
  match only on path-segment boundaries, exclusions override inclusions,
  renames check both endpoints, natural-language scope cannot fail, and
  architecture candidates remain advisory.
- Added a fresh-validation-evidence specification that separates the task
  comparison base from snapshot `HEAD`, hashes canonical Git layers, discovers
  non-ignored untracked files with `--exclude-standard`, and excludes only
  untracked local `.agentgov/` tool state. Tracked changes, including
  `.gitignore` and tracked `.agentgov/` paths, remain visible.
- Defined validation-generated tracked or non-ignored artifacts as stale
  evidence with actionable recovery guidance. AgentGov does not modify
  `.gitignore` automatically.
- Clarified that both `validate -> finish -> commit` and
  `commit -> validate -> finish` are supported. The invariant is no governed
  task, `HEAD`, index, worktree, rename, or non-ignored untracked mutation
  between validation and finish.
- Removed persistent `last validation` from the derived in-memory Registry.
  Historical validation can only be derived from the future Event Store.
- Declared one active task per working copy for v1 and prohibited silently
  replacing the local pointer. Parallel Git worktrees may carry separate local
  pointers; shared-working-copy parallelism remains an open design question.
- Revised the consolidated proposal to Revision 3 and split the trigger and
  evidence contracts into focused specs so the product document remains the
  roadmap and decision record.
- This second-review resolution changed no runtime behavior, AI Radar file,
  Git state, release, deployment, or external system.

## Phase 1 derived governance context slice

- Added admitted task `p0-context-selection` for the first reviewed Phase 1
  vertical slice. It bounds Registry/context work and excludes changed-file
  enforcement, events, fresh evidence, Dashboard, hooks, Git transitions, and
  AI Radar mutation.
- Migrated the unpublished development-task preview to contract 1.1. One
  Schema now supports a low-risk `compact` profile and a full `standard`
  profile, with explicit validation commands and accountable owner. Existing
  repository tasks and fixtures were migrated together; no stable 0.2.1
  compatibility claim was changed.
- Added structured Skill routing metadata owned by each `SKILL.md`:
  `triggers`, `non_triggers`, and `applies_to`. The zero-dependency validator
  rejects ambiguous arrays and portable-identifier violations; no separate
  Registry mapping file was added.
- Implemented read-only `agentgov context task` with terminal, JSON, and
  Markdown output. It always selects root `AGENTS.md` and the admitted task,
  preserves explicit requirement/objective/architecture references, routes
  matching Skills, and links capability/control/dependency/evaluation
  governance through artifact-declared path relationships.
- Added strict derived `agentgov.development-context` output with task and
  source digests, selection modes, deterministic/advisory classification,
  reasons, Registry inventory summary, known limits, and all mutation and
  transition authority set to false.
- Added a self-contained independent fixture repository covering compact task,
  `AI_CONTEXT.md`, ADR, Invariants, two Skills, one capability, control, and
  dependency. Tests cover stable serialization, irrelevant-context omission,
  draft/paused refusal, advisory architecture routing, path-linked capability
  selection, and no file mutation.
- The first same-agent Coding Agent consumption run selected 13 governance
  artifacts. Unconditional content embedding produced 85,660 characters, so
  the pilot corrected the default to a 7,552-character reference context with
  explicit provenance. `--include-content` remains opt-in. This observation is
  not presented as general effectiveness evidence.
- Full validation passed 430 source tests with one platform-limited skip.
  Repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`; the
  admitted Phase 1 task remained `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; and
  `git diff --check` passed. No commit, push, release, deployment, AI Radar
  write, or external-system mutation was authorized.

## Phase 2 working-tree scope governance slice

- Recorded the accepted productization constraint: the eventual GitHub product
  must provide guided installation, updates, onboarding, and daily governance
  without requiring users to hand-author JSON or understand the derived
  Registry. That packaging work remains deferred until the core policy
  semantics are proven.
- Added admitted task `p0-changed-file-scope` to bound the Phase 2 slice. It
  excludes Event Store, fresh-evidence verification, Dashboard, hooks,
  automatic fixes, Git writes, and stable-release claims.
- Added one shared path-policy implementation for task validation, context
  routing, and changed-file enforcement. Prefixes compare complete path
  segments, exclusions override inclusions, and invalid absolute, traversal,
  glob, or empty-segment paths are rejected.
- Added a read-only Git change collector covering staged, unstaged, deleted,
  renamed, and non-ignored untracked paths. Rename scope evaluates both old and
  new endpoints; ignored files remain outside the observed change set.
- Added strict `agentgov.development-scope-report` output and
  `agentgov check scope TASK`. Deterministic path facts can produce `FAIL`;
  explicitly referenced architecture changes produce `ADVISORY`, never an
  objective failure. All mutation, commit, merge, and deployment authority is
  false.
- Documented that this slice observes the current index and working tree only.
  Committed-since-base comparison, exceptions, action-loop guidance, Event
  Store, fresh evidence, and Monitor/Dashboard remain later work.
- Fixture tests use isolated real Git repositories and cover staged, unstaged,
  deleted, renamed, ignored, untracked, clean-tree, segment-boundary,
  exclusion-precedence, strict-schema, stable-rendering, CLI-exit, and
  non-repository behavior.
- Full validation passed 443 source tests with one platform-limited skip.
  Repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`; the
  admitted Phase 2 task remained `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; and
  `git diff --check` passed. No commit, push, release, deployment, hook,
  AI Radar write, or external-system mutation was authorized.

## Phase 3 fresh evidence and completion reconciliation slice

- Added admitted high-risk task `p0-fresh-evidence`. Its explicit approval
  bounds local execution of task-declared validation commands while excluding
  Dashboard, export, hooks, automatic remediation, Git transitions, release,
  deployment, and final product packaging.
- Implemented canonical `agentgov.git-change-set.v1` snapshots across
  committed-since-base, staged, unstaged, rename, and non-ignored untracked
  layers. Each layer stores repository-relative change identity and hashes,
  never source content.
- Implemented the reviewed canonical exclusions: ignored untracked paths and
  untracked `.agentgov/` local state are absent; tracked `.agentgov/` changes
  and tracked `.gitignore` changes remain visible.
- Implemented S0/S1 validation evidence and S2 completion reconciliation.
  Evidence binds the task digest, comparison base, snapshot HEAD, change-set
  digest, declared command identities, timestamps, exit codes, and output
  digests. Command text and raw stdout/stderr are not persisted.
- Added `agentgov govern check` and `agentgov govern finish`. Check appends a
  local scope event. Finish can run every declared validation command from an
  explicit base and immediately reconcile, or reuse a selected/latest evidence
  record. Only an unchanged, in-scope snapshot with passing internally
  consistent evidence becomes `verified`; all other cases remain
  `needs_evidence`.
- Added one-file, create-only local events for `scope.checked`,
  `validation.completed`, and `completion.reconciled`. Events record
  `human/coding_agent/ci`, optional vendor-neutral label, outcomes, reason
  codes, small observed counts, and denied authority. Absolute paths and
  credential-like assignments are rejected.
- Documented the execution boundary: admitted project validation commands run
  locally and are not sandboxed by AgentGov. Strict structural and internal
  integrity checks detect corrupt records, but v1 does not claim
  cryptographic attestation against a hostile local actor.
- Isolated real-Git fixtures cover both valid workflow orders; deterministic
  ordering; local-state and ignored-output exclusions; tracked local state;
  validation-generated artifacts; failed commands; edits, stage changes,
  commits, renames, task changes, and `.gitignore` changes after validation;
  committed out-of-scope changes; privacy; internal integrity; CLI JSON; and
  append-only events.
- Full validation passed 461 source tests with one platform-limited skip. The
  live working tree intentionally was not presented as `verified`: it contains
  preserved, uncommitted work from earlier admitted slices outside the Phase 3
  task scope. No scope was widened to hide that fact. Repository governance
  remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`; the admitted Phase 3 task
  remained `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; and `git diff --check` passed.
  No commit, push, release, deployment, hook, AI Radar write, or external
  system was modified.

## Phase 4 local development Monitor slice

- Added admitted task `p0-development-monitor` for the reviewed static
  Dashboard MVP. It excludes central telemetry, hosting, GitHub Pages,
  automatic workflow changes, approval controls, governance writes, event
  export, cross-machine merging, hooks, Git transitions, and release behavior.
- Added strict loading for one-file governance events. Event filename and ID,
  UTC timestamp, actor, task digest, type, outcome, reason codes, metrics,
  evidence reference, sensitive-text boundary, and denied authority are
  validated. Invalid records and conflicting duplicate IDs fail closed;
  identical duplicates are deterministically removed and counted.
- Added explicit trigger reason codes to scope, validation, and completion
  events so the Timeline can answer why governance ran without inferring from
  a failure alone.
- Implemented `agentgov.development-monitor` as a derived read model with
  observation metadata, direct Overview counts, ordered Activity Timeline,
  per-task Detail, and separate `observed`, `inferred`, and `unknown` claims.
  No score, percentage, approval, causal benefit, ROI, requirement correctness,
  or architecture correctness is claimed.
- Implemented self-contained HTML, JSON, and Markdown rendering plus
  `agentgov monitor development`. The default generated file is
  `.agentgov/dashboard.html`; it contains no external request or script and no
  approval or governance mutation controls.
- The generated-file writer refreshes only an untracked AgentGov-owned file
  inside the repository. It rejects tracked targets, symbolic links, paths
  outside the repository, and existing files without the format ownership
  marker. Atomic replacement uses a same-directory temporary file.
- `local_session` is the default supported scope. `ci_only` requires every
  input event to declare actor class `ci`. Both disclose partial history,
  missing sources, and unavailable cross-stage discovery comparison.
  `exported_development` and `combined` remain reserved but fail until the
  separate redacted export contract exists.
- Tests cover Overview/Timeline/Task Detail, ordering, empty history, scope
  honesty, CI actor enforcement, deferred export modes, HTML escaping,
  external-request and authority absence, strict schema, stable rendering,
  duplicate/conflict/corrupt event behavior, safe refresh boundaries, and CLI
  output.
- The Sites Dashboard guidance influenced information hierarchy, responsive
  layout, accessibility labels, and self-contained presentation. Hosting and
  browser preview were intentionally not invoked because the approved product
  architecture calls for a local static artifact in this phase.
- Full validation passed 471 source tests with one platform-limited skip.
  Repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`; the
  admitted Phase 4 task remained `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; and
  `git diff --check` passed. No commit, push, release, deployment, hosting,
  workflow change, AI Radar write, or external-system mutation was authorized.

## Phase 5 guided development session slice

- Added admitted task `p0-guided-govern-start` for the smallest productization
  slice. It excludes hooks, daemons, IDE mutation, automatic Git transitions,
  PR creation, deployment, publication, and release behavior.
- Added `agentgov govern start` with deterministic single-candidate discovery,
  explicit existing-task selection, and low-risk compact task scaffolding.
  Compact creation requires machine-checkable include paths, includes its own
  task file in scope, and detects only conventional Python unittest, npm test,
  Cargo, or Go validation entry points; otherwise `--validate` is required.
- Start renders the task digest, exact comparison-base commit, selected
  governance paths, exact local targets, full compact task content when
  applicable, and denied authority. Writes require exact `START` from a real
  terminal; replacement requires `--replace-active` plus exact `REPLACE`.
  Dry-run and non-interactive execution write nothing. JSON preview is pure and
  dry-run-only.
- Added strict `.agentgov/current-task.json` schema and loading. It stores only
  task path/identity, exact base, start timestamp, and confirming human label.
  One active task is supported per working copy; governance declarations remain
  artifact-owned. Changed task content or admission state fails closed until a
  replacement start is reviewed.
- `govern check` now defaults to the active task. `govern finish` defaults to
  the active task and recorded base, runs fresh validation, and preserves the
  explicit-task/evidence low-level interfaces.
- Added `task.started` events and selected governance references in governance
  event 1.1. The strict loader remains compatible with event 1.0 and treats its
  absent governance reference list as empty. Monitor 1.1 shows task starts and
  selected paths while keeping actual coding-agent consumption unknown.
- Fixed an integration defect found by the new session fixture: untracked
  `.agentgov/` events and the pointer were initially reported as out-of-scope
  changes by the Phase 2 checker. Scope inventory now uses the same canonical
  local-tool-state exclusion as fresh evidence; tracked `.agentgov/` changes
  remain visible and fail when outside scope.
- Added isolated real-Git tests for preview targets, exact confirmation,
  non-interactive cancellation, pure JSON dry-run, compact creation, event and
  Monitor projection, task drift, multi-task ambiguity, explicit replacement,
  active-session check/finish defaults, local-state exclusion, and event 1.0
  compatibility.
- Full validation passed 484 source tests with one platform-limited skip.
  Repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`; the
  admitted Phase 5 task remained `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; and
  `git diff --check` passed. No commit, push, release, deployment, workflow
  change, hook, hosting, AI Radar write, or external-system mutation was
  authorized.

## Phase 6 installed development-governance pilot

- Added admitted task `p0-installed-development-pilot` and ran the first exact
  wheel development-governance loop in disposable Git repositories with no AI
  Radar dependency. The candidate wheel was 247865 bytes with SHA-256
  `9ead26aaebe84317723018bf4f880100e513a3873b08bfb33917ec7beb2a884b`.
- Installed the wheel with `--no-deps` in a fresh Python 3.11.9 virtual
  environment. Both `agentgov` and `agentgov.development_session` loaded from
  that environment's `site-packages`, not the source checkout.
- Recorded a build-environment boundary: offline no-build-isolation failed with
  setuptools 65.5.0 because it did not satisfy the declared `setuptools>=69`
  requirement. Normal PEP 517 isolation satisfied the declared build contract;
  package metadata was not weakened.
- The first Router attempt rejected an abbreviated pilot Skill for missing the
  use condition and required sections. The disposable fixture was corrected
  before task start. Invalid repository governance was not silently omitted.
- The installed Router selected eight relevant artifacts: `AGENTS.md`,
  `AI_CONTEXT.md`, one triggered Skill, one ADR, invariants, parent objective,
  requirement, and current task. The Coding Agent read all eight, changed only
  `src/greeting.py`, kept the function pure, added no speculative interface,
  and performed no downstream Git action.
- The first governed finish remained `needs_evidence` because passing unittest
  execution created non-ignored Python caches. AgentGov identified the two
  repository-relative artifacts; the Coding Agent did not edit `.gitignore` or
  widen scope. That repository was preserved as failure evidence.
- A second fresh pre-task baseline included standard Python ignore rules. Its
  scope check returned `PASS=1 FAIL=0 ADVISORY=1`; declared validation passed;
  completion returned `verified` with fresh and in-scope facts plus advisory
  architecture review.
- The local Monitor contained exactly four events for one task: one start, one
  scope check, one passing validation, and one verified completion. It declared
  `local_session`, `partial` history, and cross-stage discovery unavailable.
- The automation harness rendered the installed CLI start preview but did not
  provide a real TTY. It used the installed package's same plan, exact-confirm,
  and apply functions with a simulated `START` after explicit user authority.
  This is not an uncoached interactive-start observation.
- Recorded two pre-release actions: promote the development package from the
  stable-looking 0.2.1 identity before distribution, and add actionable
  validation-artifact readiness guidance without automatically editing ignore
  policy. Redacted event export remains the next product slice.
- This pilot is internal Coding Agent evidence only. It does not establish
  uncoached human adoption, general effectiveness, causal benefit, ROI,
  requirement correctness, architecture correctness, validation sufficiency,
  or cross-platform behavior.
- Full source validation passed 485 tests with one platform-limited skip.
  Repository governance remained `PASS=16 WARN=2 FAIL=0 ADVISORY=4`; the
  admitted pilot task remained `PASS=6 WARN=0 FAIL=0 ADVISORY=1`; and
  `git diff --check` passed. The AgentGov repository received no commit, push,
  tag, release, workflow change, deployment, or external publication.
