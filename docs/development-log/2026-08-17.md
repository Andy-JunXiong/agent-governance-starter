# Development log — 2026-08-17

## Replay correlation consumption and Harness evidence bridge

### Goal and selection

The product owner asked to continue from the published 2026-08-16 closeout.
Read-only status inspection found no active task and identified the current
product-review choice between an installed disposable reservation rehearsal
and defining reservation consumption plus Harness evidence mapping first.

Native alignment presented the bounded alternatives. The product owner chose
the contract-first direction. The resolved center is to define the smallest
versioned reservation-consumption and Harness-evidence mapping contract before
any installed or real-consumer rehearsal. Native task-proposal review then
admitted `p0-replay-correlation-consumption-bridge-v1` for the exact additive
contract, validator, fixtures, tests, and synchronized documentation scope.

### Non-goals and stop conditions

- Do not install an Adapter or development source in a consumer.
- Do not create, consume, invalidate, or delete a real reservation marker.
- Do not launch a model session or replay.
- Do not change replay preflight, reservation CLI, Harness Contract v1 schema,
  First Deviation behavior, or existing Harness fixtures.
- Do not touch the user-owned `.codex` directory.
- Stop before Git operations, publication, release, or deployment.

### Implementation

Development source now adds `agentgov.replay-correlation-bridge` version `1.0`
as an independent strict JSON contract and dependency-free pure validator. One
bridge binds an `rpf-` correlation identifier to an existing reservation id,
repository-relative marker path, marker digest, and the unchanged Harness v1
field `host.repository_correlation`.

The lifecycle is explicit:

- `reserved` requires the exact reservation-marker evidence and no Harness run;
- `consumed` requires the exact reservation marker plus one valid Harness run
  whose run identity and correlation field match the bridge;
- `invalidated` requires a normalized reason and cannot name Harness evidence;
- `unavailable` requires a normalized reason and cannot name Harness evidence.

All states retain strict privacy, claim-limit, and all-denied authority
boundaries. The validator accepts already-loaded mappings and performs no
filesystem, network, Git, model, consumer, reservation, or Harness write.
Fixtures cover all four lifecycle states. The existing reservation and Harness
implementations and schemas are unchanged.

### Validation before advisory review

The initial focused bridge suite passed all 12 tests. A separate strict-input
review then found that an unhashable non-string `state` could raise instead of
returning a bounded validation error. The validator now rejects that input
cleanly, and the focused suite passes all 13 tests.

The unchanged Harness suite passes all 25 tests, the unchanged reservation
suite passes all 15 tests, user-documentation passes all 29 tests, and public-
documentation freshness passes all 11 tests. The complete suite was run again
after the correction under supported Python 3.11.9 and passed all 881 tests
with 2 platform-limited skips.

Task governance reports `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The first scope
reconciliation accepted all 14 then-current task paths and rejected only the
explicitly excluded, user-owned `.codex/config.toml`; no exception was
inferred. Python compilation and `git diff --check` pass. Final JSON, Schema,
secret-safety, post-documentation scope, and native current-Agent advisory
review checks remain pending before task pause.

### Final validation and advisory closeout

Post-documentation focused validation passed all 13 bridge, 25 Harness, 15
reservation, 29 user-documentation, and 11 public-documentation tests. Task
and repository governance remained at `PASS=3 WARN=1 FAIL=0 ADVISORY=3` and
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Final pre-pause scope reconciliation
accepted all 15 task paths and rejected only the explicitly excluded,
user-owned `.codex/config.toml`; no exception was inferred.

The bridge Schema, task, and four fixtures parse without duplicate JSON keys,
all four fixtures pass the pure document validator, the bounded credential-
pattern scan found no match, and `git diff --check` passed. The optional third-
party `jsonschema` package is not installed in the zero-dependency environment,
so external meta-Schema validation was not run; runtime/Schema identity checks
and the strict fixture validator remain covered by the passing test suite.

Native current-Agent advisory review
`srv-a418a942620ba8a1e88722debf583840` found no correction-required
requirement, architecture, security, or scope drift. It retained bounded
unknowns for a real-consumer bridge record, a future atomic pre-run consume
transition, caller evidence authenticity, network-filesystem behavior, and
third-party meta-Schema validation. Task
`p0-replay-correlation-consumption-bridge-v1` is paused. No installation,
consumer marker mutation, replay, Git operation, publication, release,
deployment, or follow-on work is authorized.

Post-pause closeout validation passed all 13 bridge, 29 user-documentation, 11
public-documentation, and 13 task-contract tests. The paused task check reports
the expected `PASS=2 WARN=2 FAIL=0 ADVISORY=3`; repository governance remains
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, final JSON parsing and `git diff --check`
pass, and the only unrelated working-tree path remains the untouched `.codex`
configuration.

## Disposable installed reservation rehearsal

### Goal and authority boundary

After bridge closeout, the product owner asked to continue with the recommended
one-shot disposable installed reservation rehearsal. Read-only inspection found
that the existing global `agentgov 0.3.0rc1` did not expose `reserve`, while the
committed starter `HEAD` did. Native proposal review admitted
`p0-disposable-installed-reservation-rehearsal-v1` for one offline local
installation into a new temporary Python 3.11 environment, one no-remote
disposable consumer, and at most one visible interactive reservation apply.

The task excluded source, Schema, Harness, workflow, release, real-consumer,
existing-installation, network, replay, Git-remote, publication, release, and
deployment changes. Any installation failure required a stopped result without
retry.

### Installation-precondition stop

The temporary source came from `git archive HEAD` at
`514f5023d7883aeee648ddda63b3e944d4116eac`, excluding the untracked `.codex`
configuration and all other uncommitted work. A new Python 3.11 virtual
environment was created. Its single offline local installation attempt used no
index, dependencies, or build isolation and exited `1` during metadata
preparation.

The environment's `setuptools 65.5.0` is below the declared
`setuptools>=69` build requirement and rejected the repository's current SPDX
license string under its older metadata schema. No disposable `agentgov` entry
point was installed. The consumer directory remained empty, no visible terminal
started, the reservation CLI was invoked zero times, no marker was created, and
no bridge validation ran. The result is
`BLOCKED_INSTALLATION_PRECONDITION`, not reservation failure evidence.

Pre- and post-attempt hashes matched for the existing exposed launcher and pipx
package module. The existing installation therefore remained byte-unchanged.
The temporary artifacts remain in the operating-system temporary area because
cleanup was not authorized; their absolute path and raw installation transcript
are omitted from repository evidence. The exact sanitized record is
`docs/experiments/disposable-installed-reservation-2026-08-17.md`.

### Interim closeout state (superseded)

No retry or alternative installation path is authorized. Documentation tests,
the complete supported-Python suite, task and repository governance, scope,
JSON and secret-safety checks, diff validation, a bounded current-Agent
advisory review, and the final pause transition remain pending.

### Validation and advisory closeout

All 15 reservation, 13 bridge, 30 user-documentation, and 13 task-contract
tests passed. The complete supported-Python 3.11.9 suite passed all 882 tests
with 2 platform-limited skips. Before pause, task governance reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`.

Scope reconciliation accepted all five current-task paths. It rejected 13
preserved paths: 12 paths owned by the preceding uncommitted bridge task plus
the user-owned `.codex/config.toml`. Three accepted paths are intentionally
shared documentation or test surfaces that retain already-reviewed bridge
content alongside this task's additions. No exception or ownership transfer
was inferred. The task JSON parsed, the bounded credential-pattern scan found
no match, and `git diff --check` passed.

Because this fully specified low-risk rehearsal did not start a new alignment
journey, the current Agent performed a distinct bounded advisory review without
claiming native self-review completion. Requirement, authority, privacy,
installation, evidence, and scope facts showed no correction-required drift.
The review confirmed that `BLOCKED_INSTALLATION_PRECONDITION` is not a
reservation failure, no retry occurred, existing installation hashes matched,
and repository evidence contains neither the temporary absolute path nor the
raw installation transcript.

Task `p0-disposable-installed-reservation-rehearsal-v1` is paused. The failed
one-shot attempt cannot authorize a build-tool upgrade, fallback installer,
retry, cleanup, consumer initialization, interactive confirmation, marker,
bridge validation, replay, Git operation, publication, release, deployment, or
follow-on work.

## Reproducible local wheel build and installed-help validation

### Goal and authority boundary

The product owner approved the previously recommended packaging prerequisite
check. Native proposal review admitted
`p0-reproducible-local-wheel-build-validation-v1` for a committed-source wheel
build, wheel inventory, isolated offline installation, installed reservation-
help check, and sanitized evidence. This task did not authorize a reservation
apply, `RESERVE` input, marker, consumer, replay, global-installation change,
Git operation, publication, release, deployment, or cleanup.

### Build and installed-help result

Fresh archives of exact committed revision
`514f5023d7883aeee648ddda63b3e944d4116eac` had matching SHA-256
`54E3D66DD054ACF26DE8C2D9153848925233E10D50672B6164F7900ECF6DBC7A`
and excluded `.codex` plus the uncommitted correlation bridge. With explicit
permission, fixed `setuptools 80.9.0` was downloaded once into a disposable
tools directory and installed offline only into isolated Python 3.11.9 build
environments.

The first build passed metadata preparation but failed during `bdist_wheel`
because its deep Windows temporary root made the shared-template target path
too long. It emitted no wheel. A fresh short-root build from the same archive
and build-tool wheel succeeded without index access, dependency resolution, or
build isolation.

Exactly one unpublished wheel was emitted:
`agent_governance_starter-0.3.0rc1-py3-none-any.whl`, 475529 bytes, SHA-256
`78D6A216FE7344A2E3DA01086A7A92B9F3310BE61DAD2E690B0705003221FDE6`.
Its 173-member canonical sorted inventory has SHA-256
`2BB649EB6EF281260C083E567CC8F92DDE0F50BB6975C700DE17C075115DF5AC`
and partitions into 67 package modules, 100 shared governance assets, and 6
distribution metadata files.

A separate fresh environment installed that wheel with `--no-index --no-deps`.
The installed command reported `agentgov 0.3.0rc1`, and
`agentgov reserve replay-correlation --help` exited `0`. No apply invocation or
`RESERVE` input occurred, no marker or `.agentgov` state directory was created,
and the measured global launcher and pipx package hashes remained unchanged.
The sanitized detail is recorded in
`docs/experiments/reproducible-local-wheel-build-2026-08-17.md`.

### Closeout validation pending

Focused and complete tests, task and repository governance, scope
reconciliation, JSON and secret-safety checks, diff validation, a distinct
bounded current-Agent advisory review, the current-status update, and the final
task pause remain pending. This successful packaging check does not revive the
earlier paused one-shot reservation rehearsal.

### Validation and advisory closeout

All 31 user-documentation tests passed. The first complete-suite invocation
was terminated by its 120-second command limit before returning a final test
result; it was not counted as a test failure. The exact complete suite was
rerun with a sufficient limit under supported Python 3.11.9 and passed all 883
tests with 2 platform-limited skips.

Before pause, task governance reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope reconciliation accepted all five
current-task paths and rejected 15 preserved paths: 14 paths owned by the two
preceding uncommitted tasks plus the user-owned `.codex/config.toml`. Shared
accepted status, log, and test paths retain earlier reviewed content; no
exception or ownership transfer was inferred. The task JSON parsed, bounded
host-path and credential-pattern scans found no match, and `git diff --check`
passed.

Because this fully specified low-risk task did not start a new alignment
journey, the current Agent performed a distinct bounded advisory review without
claiming native self-review completion. The review found that the first
experiment draft recorded a count, partition, and digest for all 173 members
but enumerated only the 67 package modules. The record was corrected to list
all 173 exact member paths, and an exact comparison against the retained wheel
reported 173 document members, 173 wheel members, and zero differences. The
review then found no remaining correction-required requirement, authority,
privacy, implementation, or scope drift.

Task `p0-reproducible-local-wheel-build-validation-v1` is paused. This result
clears the packaging prerequisite only; it does not authorize the separately
reviewed one-shot reservation rehearsal, cleanup, Git operation, publication,
release, deployment, or other follow-on work.

Post-pause task governance reported the expected
`PASS=2 WARN=2 FAIL=0 ADVISORY=3`, and all 31 user-documentation tests still
passed.

## Wheel-backed installed reservation rehearsal

### Goal and authority boundary

The product owner selected the recommended follow-on: use the validated wheel
for one fresh disposable installed reservation rehearsal. Native proposal
review admitted `p0-wheel-backed-installed-reservation-rehearsal-v1`. The
product owner separately authorized exactly one baseline commit in the
no-remote temporary consumer; that authorization did not extend to the starter
repository or any push.

The task permitted one visible interactive apply and required an immediate
stopped result without retry for cancellation, nonzero exit, blocked or unknown
preflight, or uncertain terminal outcome. It excluded replay, consumption,
real-consumer work, network access, existing-installation changes, cleanup,
starter Git operations, publication, release, and deployment.

### Installed preflight and single apply

The retained wheel matched SHA-256
`78D6A216FE7344A2E3DA01086A7A92B9F3310BE61DAD2E690B0705003221FDE6`.
A fresh Python 3.11 environment installed it offline and exposed the expected
reservation help. The synthetic no-remote consumer baseline commit had HEAD
`5bcc3ee68cc6ff697187ceebe442b1c4c10f312d`, a clean README target, strict
`openai.codex-mcp` Adapter `1.5.0` metadata, an empty pre-existing marker
registry, and no related task or correlation collision.

Installed JSON preview returned `READY_TO_RESERVE`; all six repository, target,
task, Adapter, and uniqueness checks passed. The marker did not exist. One
visible terminal then invoked the installed apply exactly once. The product
owner supplied exact `RESERVE`, and the terminal process exited `0`. There was
no retry.

Exactly one reserved marker was created for correlation
`rpf-dec1d2c300000001` and reservation `rrv-8727d1e62d0fde6e`. Its marker
digest is
`sha256:9cbd4cdb06486a78a554ec208c39d00cdba5bddd4b41c178f957c5106989c268`.
The marker validator returned zero errors. Consumer HEAD, remote count, README,
and Adapter content remained unchanged; only the marker registry became
untracked.

One in-memory `reserved` correlation bridge bound to the exact marker passed
the development-source validator with zero errors. Its Harness mapping named
`host.repository_correlation`, while run and evidence remained null. No replay
or consume transition occurred. Existing launcher and pipx package hashes also
remained unchanged. The sanitized record is
`docs/experiments/wheel-backed-installed-reservation-2026-08-17.md`.

### Interim validation state (superseded)

All 15 reservation, 13 bridge, and 32 user-documentation tests pass. Complete
suite, task and repository governance, pre-pause scope reconciliation, JSON,
privacy, diff, bounded advisory review, current-status reconciliation, and task
pause remain pending.

### Validation and advisory closeout

All 15 reservation, 13 bridge, and 32 user-documentation tests passed. The
complete supported-Python 3.11.9 suite passed all 884 tests with 2 platform-
limited skips. Before pause, task governance reported
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`.

Scope reconciliation accepted all five current-task paths and rejected 17
preserved paths: 16 paths owned by the three preceding uncommitted tasks plus
the user-owned `.codex/config.toml`. Shared accepted status, log, and test paths
retain earlier reviewed content; no exception or ownership transfer was
inferred. The task JSON parsed, bounded host-path and credential-pattern scans
found no match, and `git diff --check` passed.

Because this fully specified low-risk task did not start a new alignment
journey, the current Agent performed a distinct bounded advisory review without
claiming native self-review completion. The review found and corrected one
interim status contradiction: the active slice had been labeled `none` before
the task was paused. It found no remaining correction-required requirement,
human-confirmation, Git-authority, privacy, reservation, bridge, implementation,
or scope drift. The reservation remained exactly one create-only marker, and no
evidence was overstated as replay or Harness behavior.

Task `p0-wheel-backed-installed-reservation-rehearsal-v1` is paused. The marker
and all temporary evidence remain retained. This result grants no retry, reuse,
consume transition, replay, cleanup, starter Git operation, publication,
release, deployment, or other follow-on authority.

Post-pause task governance reported the expected
`PASS=2 WARN=2 FAIL=0 ADVISORY=3`; all 32 user-documentation and 13 task-
contract tests still passed.

## Atomic create-only replay claim

### Aligned direction and authority boundary

After the installed reservation rehearsal, the product owner selected the
separate pre-run ownership direction through native alignment journey
`mcpj-b80ac841846844e3802096d7a9095a29`. The resolved center requires one
create-only claim that binds exactly one valid immutable reservation, lets one
local contender win, retains crash-ambiguous ownership, and grants no replay
authority. Native proposal review then admitted
`p0-create-only-replay-claim-v1` for that exact implementation.

The slice excluded reservation, bridge, replay-preflight, and Harness contract
changes; claim expiry, takeover, deletion, or recovery; a claim-to-Harness
consume transition; a real consumer; replay; cleanup; installation; Git;
publication; release; and deployment.

### Implemented contract and CLI

Development source now adds strict `1.0` claim plan, marker, preview, and
result contracts plus dependency-free runtime validation. A plan binds one
reservation marker path and digest to a pre-existing claim registry, normalized
claimant, local Adapter metadata, and the all-denied authority boundary. The
derived claim records one `rcl-` identity, the reservation and `rpf-`
correlation identities, repository-relative paths, the immutable marker
digest, consumer `HEAD`, Adapter identity, claimant, `claimed` status, and the
same denied authority boundary.

`agentgov claim replay-correlation` defaults to read-only terminal or JSON
preview. `READY_TO_CLAIM` requires valid unchanged reservation evidence,
matching consumer `HEAD`, matching strict Adapter metadata, a real pre-existing
claim registry, and no existing claim. Missing or invalid evidence is
`UNKNOWN`; deterministic mismatch or collision is `BLOCKED`. Apply accepts
only terminal format and exact interactive `CLAIM`, repeats the reviewed
checks, and creates the claim with exclusive local file creation. It never
overwrites the reservation or a competing claim. If creation succeeds but a
write becomes uncertain, the path remains present and blocks reuse; there is
no automatic deletion, expiry, recovery, or takeover.

The optional pure binding accepts only the exact matching `reserved`
correlation bridge without modifying it. The existing reservation, bridge,
preflight, and Harness source modules, schemas, and fixtures remain unchanged.
The claim does not authorize, launch, consume, expire, recover, take over, or
prove a replay.

### Validation and native advisory review

The focused claim suite passed all 14 tests with 1 Windows-limited symbolic-
link skip. Unchanged reservation and bridge suites passed all 15 and 13 tests.
User and public documentation suites passed all 32 and 11 tests. The complete
supported-Python 3.11 suite passed all 898 tests with 3 platform-limited skips.
Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`.

Scope reconciliation accepted all 14 current-task paths. It rejected 15
preserved paths owned by earlier admitted tasks or the user-owned excluded
`.codex/config.toml`; no exception or ownership transfer was inferred. The
task, Schema, and fixtures parsed as JSON, the bounded credential-pattern scan
found no match, and `git diff --check` passed.

Because this task followed a human-resolved alignment journey, a distinct
native current-Agent self-review completed as
`srv-673b34910aa08add469a5a586cb1e2a7`. It found and corrected one classification
detail: strict Adapter metadata with invalid field formatting now reports
`UNKNOWN` invalid evidence rather than `BLOCKED` identity mismatch. The review
found no remaining correction-required requirement, architecture, authority,
implementation, security, or scope drift. It retains two explicit unknowns:
network-filesystem exclusive-create behavior and power-loss durability beyond
the file-sync boundary are not validated.

Task `p0-create-only-replay-claim-v1` is paused. No real claim, replay,
reservation or claim mutation, recovery, consume transition, cleanup, Git
operation, publication, release, deployment, or follow-on task is authorized.
Post-pause task governance reported the expected
`PASS=2 WARN=2 FAIL=0 ADVISORY=3`; scope checking now fails closed because a
paused task grants no implementation admission.

## Immutable replay claim recovery evidence

### Aligned direction and authority boundary

The product owner selected immutable supersession evidence through native
alignment journey `mcpj-427c3436246f4766933d12a7c09762ee`. The resolved
center permits read-only abandoned-claim inspection and, after exact human
confirmation, one separately created immutable recovery record bound to the
original reservation and exact raw claim bytes. It preserves the original
claim and grants no replay authority. Native proposal review then admitted
`p0-immutable-replay-claim-recovery-v1` for that exact implementation.

The slice excludes replay or consumption; deletion, mutation, repair, expiry,
or takeover of a claim; a replacement claim; real-consumer execution; cleanup;
installation; Git; publication; release; and deployment. Whether a claim is
actually abandoned remains a human-owned fact rather than a deterministic
classification.

### Implemented contract and CLI

Development source now adds strict `1.0` recovery plan, inspection, marker,
preview, and result contracts. The dependency-free implementation binds one
recovery identity and repository-relative recovery path to one valid immutable
reservation, the exact observed claim path, bounded raw claim byte length and
digest, consumer `HEAD`, Adapter identity, recovery actor and reason, and an
all-denied authority boundary.

Read-only inspection classifies claim evidence as `VALID`, `PARTIAL`,
`MALFORMED`, `MISSING`, `INCONSISTENT`, `UNKNOWN`, or `RECOVERED`. Claim reads
are bounded to one mebibyte plus one detection byte, duplicate JSON keys are
rejected, and rendered reports do not retain raw claim content. Missing,
inconsistent, unsafe, growing, oversized, or otherwise uncertain evidence
fails closed.

`agentgov recover replay-claim` defaults to read-only terminal or JSON
inspection. `READY_TO_RECOVER` requires exact bounded claim bytes, an unchanged
reservation, matching current `HEAD` and Adapter identity, a pre-existing safe
recovery registry, no existing recovery collision, and a declared human reason.
Apply accepts terminal format only, requires exact interactive `RECOVER`,
revalidates all inputs, and creates the marker with exclusive local file
creation. The original reservation and claim are never overwritten or removed.
If a recovery write becomes uncertain after creation, the partial path is
retained and blocks automatic reuse.

Pure ownership evaluation reports `RECOVERED` only when a valid marker binds
the exact reservation and raw claim digest. Any valid bound claim path remains
`ACTIVE`; invalid or uncertain evidence is `UNKNOWN`. Recovery creates no new
owner, replacement claim, consume transition, or replay authorization.

### Validation and native advisory review

The focused recovery suite passed all 17 tests. Coverage includes every
inspection classification, strict plan and marker validation, duplicate-key
rejection, bounded and growing reads, custom claim paths, exact confirmation,
fresh claim and reservation revalidation, collision and exclusive-create race,
partial-write retention, source immutability, ownership binding, CLI guards,
and denied authority. The complete supported-Python 3.11 suite passed all 915
tests with 3 platform-limited skips.

Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
reconciliation accepted all 14 current-task paths and rejected 21 preserved
paths owned by earlier admitted tasks or the user-owned excluded
`.codex/config.toml`; no exception or ownership transfer was inferred. The
task, Schema, plan, and recovered fixture parsed as strict JSON without
duplicate keys, the bounded credential-pattern scan found no match, and
`git diff --check` passed.

Because this task followed a human-resolved alignment journey, a distinct
native current-Agent self-review completed as
`srv-73b279570b18e1aec5775e6d31cfbc59`. It found no remaining
correction-required requirement, architecture, implementation, authority,
security, data, or scope drift. It retains explicit unknowns for authenticated
recovery identity, network-filesystem exclusive-create behavior, power-loss
durability beyond the file-sync boundary, real-consumer recovery, and the
future re-ownership contract.

Task `p0-immutable-replay-claim-recovery-v1` is paused. No real recovery,
replacement claim, re-ownership, replay, consume transition, cleanup, Git
operation, publication, release, deployment, or follow-on task is authorized.

## Interview documentation and public demo synchronization

### Aligned direction and authority boundary

The product owner asked to prepare the project for interviews and make the
public demo match the latest repository truth. Native alignment journey
`mcpj-61d9131f9993495cacbd2449a4ed8152` presented the bounded alternatives.
The product owner selected the comprehensive interview-ready synchronization,
and native proposal review admitted
`p0-interview-docs-demo-sync-v1` for the exact current-documentation, public
HTML/CSS, protecting-test, status, and dated-log scope.

Historical ADRs, releases, experiments, case studies, plans, generated sample
reports, runtime code, release metadata, and the user-owned `.codex` directory
remained outside this task. The task granted no commit, push, publication,
release, deployment, or external-write authority.

### Current documentation and demo source

The README now opens with a compact interview snapshot and direct links to the
live Pages site, bilingual walkthrough, portfolio, illustrative report, and
stable quickstart. The standalone homepage replaces its first long milestone
wall with a concise governed-journey summary while preserving the detailed
history inside a collapsed disclosure. Three literal Jekyll template links
were replaced with locally resolvable standalone HTML links.

The evidence portfolio adds the exact development-source replay-safety chain:
immutable reservation, create-only claim, and separate immutable recovery
evidence. It keeps abandonment and replacement ownership human-owned and
explicitly denies replay authority. English and Chinese quickstarts now expose
the same preview-only reservation, claim, and recovery commands and authority
boundary.

New English and Chinese HTML interview walkthroughs plus one bilingual
Markdown source provide a five-to-ten-minute product story, architecture map,
demo commands, evidence path, likely questions, honest limitations, and the
not-yet-authorized re-ownership candidate. Every current surface distinguishes
stable `0.2.1`, published prerelease `0.3.0rc1`, and newer development source.
The two generated reports were not modified and remain labeled elsewhere as
sanitized illustrative `0.3.0rc1` snapshots.

### Validation and advisory review

All 71 focused documentation, public-site, portfolio, freshness, user-guide,
and interview-documentation tests passed. The new checks verify local relative
links, reject unresolved template expressions, require bilingual cross-links,
keep release commands aligned, and pin the exact generated-report hashes. The
complete supported-Python 3.11 suite passed all 921 tests with 3 platform-
limited skips.

Local desktop browser inspection covered the homepage, the recovery portfolio
section, and both interview walkthroughs. The visible layout, navigation, and
English and Chinese text rendered without an observed regression. The connected
browser exposed no mobile viewport-emulation capability, so mobile-width
visual inspection remains pending and is not represented as completed.

Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
reconciliation accepted 19 current-task paths and rejected 30 preserved paths
owned by earlier admitted tasks or the user-owned excluded
`.codex/config.toml`; no exception or ownership transfer was inferred. The task
JSON parsed, bounded credential and host-path scans found no match, and
`git diff --check` passed.

Native current-Agent advisory review
`srv-5c89045052ce8a1d6b0a583d484f1ca6` found no correction-required
requirement, architecture, implementation, authority, security, data, or scope
drift. It retained the mobile visual gap, later GitHub Pages publication, and
unknown interview-outcome impact as explicit unknowns.

Task `p0-interview-docs-demo-sync-v1` is paused. The local Pages source now
matches the latest documented capability boundary, but the public URL remains
unchanged until a separate human-authorized commit, push, and Pages publication
complete. No historical rewrite, generated-report change, Git operation,
publication, release, deployment, or follow-on product work is authorized by
this closeout.
