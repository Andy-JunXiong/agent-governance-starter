# Development log — 2026-08-25

## Native task-admission summary-first decision card v1

The product owner reported that the native task-admission form led with a long
machine-oriented JSON contract and did not make the actual decision direct.
Alignment journey `mcpj-655db0274fef47b68f16107e5dfd671a` recorded the
summary-card-first direction. Native proposal review then admitted
`p0-native-task-admission-summary-card-v1`, and the product owner separately
instructed the Agent to begin execution.

The first execution action captured the exact local task-start baseline with
digest `sha256:92f340990489d303c716f6c994529d04fe20f8e82dc53929227cc6aac52fa122`.
The source change replaces the JSON-first elicitation message with a short
human-facing summary of immediate effect, task goal, scope-rule counts,
validation count, risks, unknowns, and denied downstream authority. Exact plan
JSON remains below a labeled audit-only technical-details heading. Visible
decision labels now say `Approve only this task`, `Send back for changes`, and
`Do not approve`; the underlying decision values and fail-closed behavior are
unchanged.

The distinct self-review identified that unescaped Markdown or long duplicated
risk lists could undermine the summary. The implementation therefore escapes
human-facing Markdown and shows at most three risks or unknowns plus a remaining
count; the audit JSON retains every exact item.

The MCP host still owns final visual chrome and may not provide a collapsible
technical-detail widget. This slice guarantees content and ordering, not a
host-specific collapse control. It performs no dependency, network, model,
Codex consumer, Git transition, publication, release, or deployment action.

Focused validation passes all 40 MCP tests and all 54 documentation tests. The
complete supported Python 3.11 suite passes all 1084 tests with 4
platform-limited skips in 171.104 seconds. The final task-start comparison is
`PASS=38 PRESERVED=42 FAIL=0 TOTAL=80` across exactly nine post-start paths.
All 22 task records captured at task start remain byte-identical. Task
governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; diff and bounded privacy checks pass.

A distinct native active-Agent self-review completed with result
`srv-a033e2c62c4458dd10074cc92d487f7d`. It found the selected requirement,
implementation, scope, privacy, and authority boundaries consistent after the
Markdown and list-bounding correction. It is advisory current-Agent judgment,
not independent assurance, and cannot establish visual behavior in the current
installed host. Its recommended product question is whether one separately
admitted live proposal check should confirm that the summary is visibly first.

## Live native task-admission summary visibility check v1

The product owner selected one bounded current-host visibility check through
resolved alignment journey `mcpj-0bfc1dfff89246d6bcf663084d5fea51`.
Native proposal review admitted
`p0-live-native-task-admission-summary-visibility-check-v1`; the product owner
then separately reported that the summary appeared before the audit-only
technical JSON, that the three decision choices were clear, and instructed the
Agent to begin the closeout execution.

The first post-take-up baseline command used the wrong capture option and was
rejected by argument parsing before any write. The immediate corrected capture
succeeded with digest
`sha256:ebe7d0999a71c2941b289a6de38b39e02764958fc2ef3a363f650f300b5c1a42`.
The bounded evidence is recorded at
[`docs/validation/live-native-task-admission-summary-visibility-check-2026-08-25.md`](../validation/live-native-task-admission-summary-visibility-check-2026-08-25.md).

The visual result is user-reported current-host evidence, not Codex-run
rendering proof or a cross-host guarantee. The task makes no implementation,
test, installation, Git, publication, release, or deployment change.

The task-start comparison reports `PASS=4 PRESERVED=1 FAIL=0 TOTAL=5`.
Separate hash comparison preserves both prior local scope baselines and the
host configuration. Task governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3`;
repository governance is `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; all 54
user-documentation tests, bounded privacy scanning, and `git diff --check`
pass. Distinct native current-Agent advisory review
`srv-5c73e02fc02ac47e4dae8275e13bb9fb` then found the evidence attribution,
scope, implementation non-change, and privacy boundary consistent. It retains
collapsible details, cross-host rendering, and pre-capture state as explicit
unknowns and grants no downstream authority.

## Protection-event resolution guidance links v1

After accepting the current-host admission presentation result, the product
owner selected deterministic read-only Protection Event guidance through
resolved alignment journey `mcpj-ecf72c37963d4a99980de6236ed26955`.
Native proposal `prp-3639f2b1f6514c74a7adca5303504b3a` admitted exact task
`p0-protection-event-resolution-guidance-links-v1`; the product owner then
separately instructed the Agent to begin execution.

The first post-take-up action captured the exact task-start baseline with digest
`sha256:3d9dd858211404cdcfd67b35e489745731827b1753de9abd118b24e0f162583c`.
Development Monitor contract and schema advance from 1.5 to 1.6. The four
existing protection classes now map deterministically to fixed guidance:
scope boundary reviews task scope and changed paths; validation failure reviews
failed validation evidence; stale evidence requests fresh validation evidence;
and incomplete completion requests missing completion evidence. Each guidance
object has a bounded action identifier, fixed label, `task_detail` target, and
`read_only_navigation` semantics.

JSON retains only those schema-bounded fields. Markdown links internally to
`#task-detail`, and the self-contained HTML links internally to `#tasks`.
Renderers escape guidance labels and fail closed to visible unavailable text
for missing or unsupported targets. The negative-target test initially expected
the artificial unsafe target string to disappear from the entire self-contained
HTML. That expectation was corrected: the embedded audit JSON honestly retains
the supplied test data, while the renderer must—and does—prevent both an
external clickable `href` and an internal link for that invalid target. No
failing behavior or policy assertion was weakened.

All Protection Events continue to report `observed_resolution_unknown`.
Guidance is navigation to visible task context, not proof of remediation,
handling, prevention, or cross-event resolution. The Dashboard gains no
command, decision, policy, exception, Git, publication, release, deployment,
or external-write control.

Focused validation passes all 16 Monitor tests and all 54 documentation tests.
The complete supported Python 3.11 suite passes all 1085 tests with 4
platform-limited skips in 152.363 seconds. The Monitor schema parses and
`git diff --check` passes. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The final task-start comparison is
`PASS=14 PRESERVED=3 FAIL=0 TOTAL=17`: all current changes are inside the
captured include scope and all three pre-existing excluded paths reported by
the comparison remain byte-identical. The first privacy expression was too
broad and matched the `sk-` substring inside ordinary `task-...` identifiers;
adding a token boundary removed those false positives. The corrected bounded
scan passes with zero findings.

Distinct native current-Agent advisory review
`srv-c462bb81898334cbdddb546a94d90e1a` found the selected requirement,
read-model architecture, bounded target and escaping controls, task scope,
implementation evidence, privacy, and denied-authority boundaries consistent.
It is not independent assurance. Whether users find Task Detail sufficient,
host-specific usability, causal benefit, and true cross-event resolution
evidence remain unknown. No network, dependency, consumer, Git transition,
publication, release, or deployment action was used or authorized.

## Live protection-guidance usability check v1

Resolved alignment journey `mcpj-a722d873f52c46f1b8d38e5e13867f8d`
records the product owner's choice to validate one real Protection Event
guidance experience before expanding Dashboard scope. Native proposal
`prp-4b6286564aaf49d1bd98e2c7c81ea6b9` admitted exact validation-only task
`p0-live-protection-guidance-usability-check-v1`; the product owner separately
instructed execution.

The first capture command incorrectly reused the check-phase `--baseline`
option and argument parsing rejected it before any write. The immediate retry
changed only that argument to capture-phase `--output` and created baseline
`sha256:f2f707ec4575a573a972b7cdc43cc411bd1225bc7d5ba2a32ec4802adc0bd5cc`.

A task-owned temporary repository generated one self-contained Monitor 1.6
page with a deterministic scope-boundary Protection Event whose resolution
remained unknown. A hidden Python server bound only to loopback and returned
HTTP 200. Current-browser DOM evidence showed exactly one `Review task scope
and changed paths` link to `#tasks`. The first click set that fragment, resolved
to one Task Detail target inside the Agent-observed viewport, and the page had
zero external links, buttons, or forms.

The product owner reported that the label was clear but Task Detail was not
seen after the first handoff. A separate visual recheck found the current
viewport in the expanded embedded machine-readable Monitor, with the Task
Detail heading above the viewport while the address still retained `#tasks`.
After the same guidance was reactivated and Task Detail was placed at the
viewport top, the product owner asked what the long machine-readable block was
and then explicitly agreed that the current detail was insufficient.

The normalized result is `USER_REPORTED_INSUFFICIENT`: successful internal
anchor behavior did not produce a sufficient human journey. In this bounded
experience the destination was not prominent enough, technical audit JSON
could dominate attention when expanded, and Task Detail exposed high-level
status and counters without the exact failed boundary, affected paths, or
smallest concrete next human action. These are current-browser and human-report
observations, not production, cross-browser, causal-benefit, accessibility, or
cross-event resolution proof.

The browser test tab was closed, the loopback process stopped, and the
task-owned temporary directory deleted and is not recoverable. No screenshot,
raw event payload, source content, absolute temporary path, browser identity,
credential, or host identity is retained in durable evidence. No Monitor
source, schema, test, package, installation, consumer, Git, publication,
release, deployment, or external state changed. The privacy-bounded evidence
is recorded at
[`docs/validation/live-protection-guidance-usability-check-2026-08-25.md`](../validation/live-protection-guidance-usability-check-2026-08-25.md).

All 70 focused Monitor and user-documentation tests pass. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The final task-start comparison is
`PASS=6 PRESERVED=13 FAIL=0 TOTAL=19`. Task JSON, bounded privacy scanning with
zero findings, and `git diff --check` pass.

Distinct native current-Agent self-review
`srv-e9e53298a6beb41ffd3df19cc5734f86` found the selected requirement,
human-versus-Agent evidence attribution, exact scope, privacy boundary,
implementation non-change, and denied authority consistent. It retains
production, cross-browser, accessibility, future-version, causal-benefit, and
repair-design uncertainty and is not independent assurance. Its recommended
product question is whether one separately admitted repair should make Task
Detail prominent and expose the exact failed boundary, affected paths, and
smallest next human action.

## Task Detail actionability repair v1

Resolved alignment journey `mcpj-7b01ba8a8dbe4472aabdbdcc600b7b73`
records the product owner's choice to improve the existing Task Detail rather
than duplicate the content inline or create another view. Native proposal
`prp-986db1c3bc0d4342a952918a909c6f79` admitted exact task
`p0-task-detail-actionability-repair-v1`; the product owner separately
instructed execution. The first execution action captured task-start baseline
`sha256:af863e065c290567367927283efdb7f827edaf5e1fc10d407b737b73d907dd4d`.

The presentation-only repair keeps Monitor contract 1.6, its strict JSON
schema, and the governance-event contract unchanged. HTML and Markdown
guidance now use deterministic task-specific anchors. A task with a visible
Protection Event is prominent and open by default, while unaffected tasks stay
compact. The latest task-specific protection class, outcome, reason codes,
existing recorded counters, deterministic next human review action, and
unknown-resolution statement appear together. Because current validated events
record counts rather than changed paths, affected paths are explicitly
unavailable and no path is inferred. Embedded machine JSON stays accessible
but collapsed and labeled optional technical audit data.

One task-owned temporary repository generated a self-contained Monitor page
with a scope-boundary failure. Current-browser evidence found exactly one
guidance link to the matching task-specific fragment. After activation, the
matching protected card was visible, prominent, and open in the current
viewport; its reason, counts, next action, affected-path limitation, and
unknown-resolution statement were visible together. The optional technical
JSON remained closed, with zero external links, buttons, or forms. The browser
tab was closed, the hidden loopback process stopped, and the task-owned
temporary directory removed and is not recoverable. No separate product-owner
usability judgment had been reported during the Agent-run check. After the
repaired page was separately opened at the matching Task Detail card, the
product owner reported that the result was now clear. This is user-reported
validation, not an Agent-derived usability conclusion.

Focused validation passes all 17 Monitor tests and all 54 user-documentation
tests. The complete supported Python 3.11 suite passes all 1086 tests with 4
platform-limited skips in 157.454 seconds. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON and `git diff --check` pass. The
final task-start comparison is `PASS=19 PRESERVED=8 FAIL=0 TOTAL=27`, and a
bounded secret-pattern scan reports zero findings.

Distinct native current-Agent self-review
`srv-abe763bab1ba293cfbcb862389110784` found the selected requirement,
presentation-only architecture, implementation binding, scope preservation,
fail-closed navigation, missing-path data honesty, privacy, and denied-authority
boundaries consistent. It is a separate advisory pass, not independent
assurance.

This current-host fixture proves only the rendered landing behavior observed
in that run, and the separate positive report proves only clarity for that
bounded human experience. Production and cross-browser behavior, accessibility,
causal benefit, human handling, remediation, changed-path event data, and true
cross-event resolution remain unknown. No commit, push, publication, release,
deployment, installation, or external write was used or authorized.

## Benefit View single-observation cards v1

Resolved alignment journey `mcpj-47a1ad6ad3374112bedb8213d2093849`
records the product owner's choice to build the smallest Benefit slice from one
current observation rather than introduce a cross-window comparator. Native
proposal `prp-514afa79f3f4417ea6df24d50e605393` admitted exact task
`p0-benefit-view-single-observation-cards-v1`; the product owner separately
instructed execution. The first execution action captured task-start baseline
`sha256:a68a93b7289c8b898891dff240918cf1b0fada6521020dd5c79fdcaef1602dc1`.

Development Monitor contract and strict schema advance from 1.6 to 1.7 with a
required `benefit` projection. One deterministic model feeds JSON, Markdown,
and HTML in fixed order: current-scope `observed_fact`; unavailable
`reproduced_comparison`; advisory `supported_inference`; unavailable
`human_feedback`; and `unknown`. Observed facts reuse only validated Overview
counts. Comparison states that no baseline, denominator, applicability rules,
or comparable window is available. Review-prioritization inference does not
claim prevention or causality. Human feedback remains unavailable because the
event contract records none, and the product owner's earlier Task Detail
comment remains documentation evidence rather than runtime data.

The existing governance-event schema and event collection are unchanged. The
separate two-snapshot Benefit Monitor is not imported, replaced, or relabeled.
The view publishes no score, percentage, prevented-outcome, time-saved,
completeness, ROI, approval, handling, or resolution claim. It adds no command,
form, mutation, decision, policy, exception, Git, publication, release,
deployment, network, or external-write control.

A task-owned temporary repository generated one self-contained Monitor page
with one scope failure and one verified completion. Current-browser evidence
found each of the five Benefit card headings exactly once. Comparison and
attributed feedback were visibly unavailable, the supported inference was
advisory, optional technical audit JSON remained closed, and the page contained
zero forms, buttons, or external links. The browser tab was closed, the hidden
loopback process stopped, and the temporary directory removed and is not
recoverable. No separate product-owner usability report was made for this
Benefit view.

Focused validation passes all 18 Monitor tests and all 54 user-documentation
tests. The complete supported Python 3.11 suite passes all 1087 tests with 4
platform-limited skips in 157.379 seconds. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The task and Monitor schema JSON documents
parse, `git diff --check` passes, and a bounded secret-pattern scan of the exact
allowed paths reports zero findings. The final task-start comparison is
`PASS=25 PRESERVED=9 FAIL=0 TOTAL=34`: all current deltas are within the
captured include scope and all excluded pre-existing paths remain byte-identical
at their Git layers.

Distinct native current-Agent self-review
`srv-99505f77f20d82e5306a443b41f088b9` found the selected requirement,
single-projection architecture, renderer and schema binding, admitted scope,
security, privacy, authority, and missing-evidence boundaries consistent. It is
a separate advisory pass rather than independent assurance. The native task
completion-record tool was not exposed in this session, so no completion record
is claimed or fabricated.

Product-owner usability, production and cross-browser behavior, accessibility,
future consumer compatibility, causal benefit, time savings, governance
completeness, ROI, and history outside the observation remain unknown. The next
product review is whether Learning evidence or denominator-aware cross-window
comparison should become a separately admitted slice. No commit, push,
publication, release, deployment, installation, or external write was used or
authorized.

## Learning View current-observation candidates v1

Resolved alignment journey `mcpj-f16e8b37b1b14a08bf332a2117669184`
records the product owner's choice to build the smallest current-observation
Learning slice rather than cross-window trends or human-confirmed learning.
Native proposal `prp-44c7ed5a00074c72bb23b9c3e25f44f6` admitted exact task
`p0-learning-view-current-observation-candidates-v1`; the product owner
separately instructed execution. The first execution action captured task-start
baseline
`sha256:da16f9b55e57afa11fe6961dc7013437392b4245d716eb00ec11e0498ff9608a`.

Development Monitor contract and strict schema advance from 1.7 to 1.8 with a
required `learning` projection. One deterministic model feeds JSON, Markdown,
and HTML in fixed order after Benefit and before technical audit data. The
first card reports zero-inclusive direct counts for the four existing
Protection Event classes. The second marks a class as an advisory repeated-
signal candidate only when at least two unique validated events of that class
occur in the displayed observation; it reports occurrence count, distinct-task
count, and a cross-task Boolean without exposing task identities. Same-task
repetition qualifies, distinct tasks are not required, and generalization is
explicitly disallowed.

The remaining cards preserve the evidence boundary. False-positive
disposition, missed-constraint confirmation, override outcome, consumer-local
configuration need, and general improvement decisions are unavailable because
the current event contract contains no attributed human disposition or
resolution data. Causal improvement, applicability outside the current scope
and window, transferability, future recurrence, time savings, governance
completeness, and ROI remain unknown. No event schema, event store, collection,
command, form, mutation, policy, decision, exception, Git, publication,
release, deployment, network, or external-write capability changed.

A task-owned temporary repository generated one self-contained Monitor page
with two same-task scope-boundary failures and two cross-task validation
failures. Current-browser evidence found each of the four Learning card
headings exactly once. The first candidate showed two occurrences, one
distinct task, and `cross task: false`; the second showed two occurrences, two
distinct tasks, and `cross task: true`. Advisory and unknown labels remained
visible, optional technical JSON stayed closed, and the page contained zero
forms, buttons, or external links. The browser tab was closed, the hidden
loopback process stopped, and the task-owned temporary directory removed and
is not recoverable. No separate product-owner usability judgment was reported
for this Learning view.

Focused validation passes all 19 Monitor tests and all 54 user-documentation
tests. The complete supported Python 3.11 suite passes all 1088 tests with 4
platform-limited skips in 171.509 seconds. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task and Monitor schema JSON documents
parse and `git diff --check` passes. The final task-start comparison is
`PASS=23 PRESERVED=8 FAIL=0 TOTAL=31`: all current deltas are within the
captured include scope and all excluded pre-existing paths remain byte-identical
at their Git layers. A bounded secret-pattern scan of the exact allowed paths
reports zero findings.

Distinct native current-Agent self-review
`srv-66f11d822de3860993f8de8c57342463` found the evidence-layer separation,
fixed display rule, deterministic projection architecture, renderer and schema
binding, admitted scope, data minimization, security, privacy, and denied-
authority boundaries consistent. It is a separate advisory pass rather than
independent assurance. The native task completion-record tool is not exposed
in this session, so no completion record is claimed or fabricated.

Product-owner usability, production and cross-browser behavior, accessibility,
future consumer compatibility, recurrence-rule usefulness, common root cause,
false-positive status, systemic weakness, causal improvement, transferability,
future recurrence, time savings, governance completeness, ROI, and history
outside the observation remain unknown. The next product review is whether
attributed human-confirmed Learning, multi-observation trends, or denominator-
aware cross-window Benefit comparison should become a separately admitted
slice. No commit, push, publication, release, deployment, installation, or
external write was used or authorized.

## Human-confirmed Learning review record v1

Resolved alignment journey `mcpj-fba76dfee529499b924e34c65c1289f7`
records the product owner's choice to add the smallest attributed human-
confirmed Learning layer rather than start multi-observation trends. Native
proposal `prp-be7618e460454c709dd562c334f05063` admitted exact task
`p0-human-confirmed-learning-review-record-v1`; the product owner separately
instructed execution. The first execution action captured task-start baseline
`sha256:a5e472f0cc004c67fdadd25106cab878fc7aabcbb61b1f570fc15711d26cd2b6`.

ADR-0017 and strict schema `agentgov.learning-review` 1.0 define a separate,
immutable, create-only local review record. The record binds one fixed
Protection Event signal class to at least two sorted exact source event IDs and
a deterministic candidate digest. It records canonical workflow attribution
`human_product_owner`, one of six fixed dispositions, bounded reason codes,
`human_judgment` semantics, unknown resolution, and all authority false. It is
not a lifecycle event; existing event storage, governance-event export, and
development-event export formats remain unchanged.

`agentgov review learning` previews the exact candidate, disposition,
destination, claim limits, and denied authority without writing. `--apply`
requires an interactive terminal and exact `RECORD`. After confirmation the
command reloads current validated events and rechecks class eligibility, the
two-event rule, sorted event IDs, and candidate digest before exclusive
creation. Cancellation, non-interactive input, unsafe state, malformed data,
duplicates, and a stale candidate all fail without a review write.

Development Monitor and its strict schema advance from 1.8 to 1.9. The same
model feeds JSON, Markdown, and HTML. Local generation loads only strict review
records and projects a judgment only when its candidate digest and exact event
set still match the current repeated-signal candidate. The human card reports
fixed disposition counts and bounded attribution without exposing task or
source-event identities. Stale records are counted but not presented as
current judgments. Exported, CI-only, and combined scopes state that human
review input is unavailable because the unchanged exports do not carry it.
Judgment remains separate from handling, resolution, shared cause, correctness,
recurrence, improvement, prevention, benefit, time savings, completeness, and
ROI.

A temporary loopback-only fixture rendered two cross-task scope-boundary
failures and one exact current `false_positive` review. Current-browser evidence
found the human card `recorded`, actor role `human product owner`, and
`resolution=unknown`; the source line reported records read 1, matched 1, and
stale 0. The Learning region exposed neither task nor event identities,
technical audit JSON stayed collapsed, and the page exposed zero forms,
buttons, or external links. The controlled tab, local server, temporary script,
and temporary directory were removed. No separate product-owner usability
judgment was reported for this feature.

Focused validation passes all 91 task-related tests with one platform-limited
skip. The complete supported Python 3.11 suite passes all 1101 tests with 5
platform-limited skips in 162.749 seconds. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The task, Learning-review schema, and
Monitor schema parse as JSON, and `git diff --check` passes. The exact allowed-
path whole-file privacy scan identifies only three disclosed synthetic token
and password shapes in the existing rejection fixture; the baseline-bounded
task-delta scan reports zero findings. The final task-start scope comparison is
`PASS=32 PRESERVED=10 FAIL=0 TOTAL=42`, with every current delta inside the
captured include scope and every excluded pre-existing path preserved.

Distinct native current-Agent self-review
`srv-c54d016d344292892c206bbc57de333d` found the selected requirement,
separate sidecar architecture, exact scope, single-model implementation,
interactive confirmation and safe-path controls, data minimization, privacy,
unknown-resolution, and denied-authority boundaries consistent. It is a
separate advisory pass rather than independent assurance. The native task
completion-record tool is not exposed in this session, so no completion record
is claimed or fabricated.

Product-owner usability, production and cross-browser behavior, accessibility,
packaging, consumer compatibility, personal authentication, conflict,
amendment, revocation, supersession, real-world vocabulary sufficiency,
trends, causal benefit, and ROI remain unknown. The next product review is to
try the command against one real repeated candidate before choosing trends or
denominator-aware cross-window Benefit. No commit, push, publication, release,
deployment, installation, network request, or external write was used or
authorized.

## Independent automatic user-journey rehearsal v1

Resolved alignment journey `mcpj-faf96c23807f4bc0bc830544c1bb0dd4`
records the product owner's choice to run a clean disposable synthetic journey.
Native proposal `prp-de669814cffd463286b413e50387545f` admitted exact task
`p0-independent-automatic-journey-rehearsal-v1`; the product owner separately
instructed execution. The first execution action captured task-start baseline
`sha256:e4adc21f159f708e18b175e06c2e97d2499172908ba49cd31e0ba3714c75c6ea`.

One operating-system temporary root received committed distribution inputs by
binary-safe Git archive and seven exact current distribution overlays. Every
overlay's source/stage byte digest matched. A retained local build backend
matched its expected digest and was installed offline into one fresh Python
3.11 build environment.

The first and only wheel-build attempt failed while copying a packaged template
to a measured 264-character target path. The error is consistent with the
legacy Windows path-length boundary, but does not prove sole causality. The
required first-deviation rule produced normalized outcome
`STOPPED_AT_WHEEL_BUILD_PATH_LENGTH_BEFORE_FIXTURE_AND_MODEL`; there was no
repair, shorter-root retry, artifact substitution, or source change.
The sanitized durable record is
[`docs/experiments/independent-automatic-journey-rehearsal-v1-2026-08-25.md`](../experiments/independent-automatic-journey-rehearsal-v1-2026-08-25.md).

No wheel, installed runtime, fixture repository, fixture Git baseline, external
Codex session, model request or turn, native consumer form, or consumer
AgentGov call was produced. Consequently the intended proposal, human
admission, task take-up, edit, validation, completion, self-review, Monitor
card, and handoff journey remains untested. This is not independent automatic-
experience success and is not a fresh uncoached human pilot.

The single task-owned temporary root was resolved inside the operating-system
temporary boundary and removed. No task-owned asynchronous process was
started. Persisted evidence contains no absolute temporary path, process
identifier, raw command output, source, patch, prompt, response, credential,
token, or session identifier.

The first focused documentation run found one traceability gap: this log did not
name the experiment record's stable path. Adding that link did not change the
rehearsal outcome or retry the build. The corrected focused run passes all 55
tests. The complete supported Python 3.11 suite passes all 1102 tests with 5
platform-limited skips in 157.801 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON parsing and
`git diff --check` pass. The final task-start comparison is
`PASS=15 PRESERVED=20 FAIL=0 TOTAL=35`, and the bounded persisted-evidence
privacy scan reports zero findings.

Distinct native current-Agent self-review
`srv-f7ba5d19b6d2810efe76f09515f8af79` found the first-deviation requirement,
artifact-preparation separation, exact task scope, evidence implementation,
cleanup, privacy, unknowns, and denied-authority boundaries consistent. It is a
separate advisory pass, not independent assurance. The native task completion-
record tool is not exposed in this session, so no completion record is claimed
or fabricated.

The next product review is whether to admit a separate short-Windows-build-root
hardening and rehearsal slice. This evidence grants no source, Git,
publication, release, deployment, credential, or external-write authority;
none was used.

## Windows short build-root contract v1

Resolved alignment journey `mcpj-85e00f9b9e04452fae958be7ecd5d788`
records the product owner's choice to repair and validate the build boundary
before separately considering rehearsal v2. Native proposal
`prp-9e8bbf31c5a94d8aa0709e0a93602951` admitted exact task
`p0-windows-short-build-root-contract-v1`; the product owner separately
instructed execution. The first execution action captured task-start baseline
`sha256:af8055546857e4f557adb01f4ca181f8886385156ab6717f82ffe20a4ff0abe0`.

The new repository-internal `scripts.short_build_root` contract always uses the
resolved operating-system temporary directory. It allocates an exclusive direct
child with fixed `agv-` plus eight-lowercase-hex naming, applies 72-character
root and 240-character projected-path limits, and rejects absolute, driven,
traversal, backslash, whitespace, control-character, missing, and over-budget
projected paths before writing. Its representation and normalized report omit
host paths. Cleanup requires the exact existing, non-link, direct child and the
same resolved temporary boundary.

All 7 focused tests pass with 1 platform-limited symbolic-link skip. A review
found that the first test seam allowed a caller-selected temporary base; the
implementation was tightened to always query the operating-system boundary,
and tests now mock only that query. The corrected focused suite still passes.

The only retained `setuptools 84.0.0` wheel matched expected SHA-256
`51A52592B3B99E102B609654876BD65F19F999935166D1352678931132B0C670`.
The artifact validator's first invocation was rejected at Python parsing after
PowerShell removed embedded quotes. It created zero roots and made zero build
attempts. Encoding the unchanged driver corrected only the command transport.

The one real validation created one short root, staged 199 committed regular
files, matched all 7 exact-current overlays byte-for-byte, and reduced the
formerly failing projected target from 264 to 207 characters. Its first and
only wheel build produced one 188-member wheel with SHA-256
`sha256:9f10b4a55eb45912e3e5ee3a39a968a2ba8cb8da534173f1c73d248d586f72fd`
and member-inventory SHA-256
`sha256:6ca19e08b0658442eab2616ec25f260265ddfcc8fed734e50a0e47f54fbd05da`.
The offline runtime install and installed-version match preceded a stop at the
combined installed shared-template presence-and-byte-digest gate.

The exact root was then removed, including the wheel and runtime. Consequently
the available sanitized result cannot distinguish a wrong installed-location
assumption from an absent or byte-different template. No rebuild, alternate
path, artifact substitution, download, network call, external Codex session,
model turn, consumer form, Git operation, publication, release, deployment,
credential change, or external write occurred.

The durable record is
[`docs/build-validation/windows-short-build-root-contract-v1-2026-08-25.md`](../build-validation/windows-short-build-root-contract-v1-2026-08-25.md).
The outcome is
`STOPPED_AFTER_WHEEL_BUILD_AT_INSTALLED_SHARED_TEMPLATE_CHECK`: the v1 wheel-
build blocker is cleared, but the complete repair task is not accepted and
rehearsal v2 remains unstarted and unauthorized.

All 7 focused short-root tests pass with 1 platform-limited symbolic-link skip.
The complete supported Python 3.11 product suite passes all 1102 tests with 5
platform-limited skips in 160.340 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON parsing and
`git diff --check` pass. The final task-start comparison is
`PASS=17 PRESERVED=22 FAIL=0 TOTAL=39`, and the bounded allowed-path privacy
scan reports zero findings.

Distinct native current-Agent self-review
`srv-ff4abd2557c7c69c7d286775516c201d` found the partial-requirement
attribution, internal-tool architecture, scope, implementation and corrected
test seam, cleanup security, privacy, unknowns, and denied-authority boundaries
consistent. It explicitly retained the installed shared-template signal as
unsatisfied. This is a separate advisory pass, not independent assurance. No
completion record is claimed because full task acceptance was not satisfied
and the native completion-record tool is not exposed in this session.

## Installed shared-data path contract diagnostic v1

The product owner selected the no-build diagnostic through resolved alignment
journey `mcpj-e694d12fc43b4fae8c2ec12dfcb5820b`. Native proposal
`prp-74dea4e1c20346fe87a9eeb83cfef63c` admitted exact task
`p0-installed-shared-data-path-contract-diagnostic-v1`; the product owner then
separately instructed execution.

The read-only diagnostic compared the repository `data-files` declaration,
the wheel `.data/data/...` key shape, the already installed pip 24.0 wheel
destination implementation, pip scheme calculations for two synthetic Windows
prefixes, and Python 3.11's `nt_venv` data scheme. Both pip calculations and
`sysconfig` placed `data` at the exact runtime prefix. Pip maps a wheel
`.data/data/<subpath>` member to `scheme.data/<subpath>`. Together these
contracts support the prior `<runtime>/share/...` location assumption.

The diagnostic does not retroactively prove the removed runtime's contents.
Installed-file presence and source/installed byte identity remain unknown
because the previous combined gate did not retain those sub-results and the
runtime was removed. The sanitized durable record is
[`docs/build-validation/installed-shared-data-path-contract-diagnostic-v1-2026-08-25.md`](../build-validation/installed-shared-data-path-contract-diagnostic-v1-2026-08-25.md).

The admitted validation list contained an unavailable `python` launcher and
nonexistent `agentgov task validate` and `agentgov repo validate` CLI forms.
Those declared invocations failed before validation and wrote nothing. The
task record was not changed. Semantic equivalents using `py -3.11`,
`agentgov check task`, and `agentgov check repository` passed: all 7 focused
short-root tests passed with 1 platform-limited skip; all 1102 product tests
passed with 5 platform-limited skips in 168.423 seconds; task governance was
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance was
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; task JSON and `git diff --check` passed.
The prior evidence and both helper files remained byte-identical.

The task-start scope baseline was not captured before initial read-only
diagnosis and document drafting. The three document changes were exactly
reverted, baseline
`sha256:761b45d804b4aadf13d40e58e2eecd6c275373d9965c758546d80e908edc0795`
was captured, and the documents were restored. Consistent with its own
contract, this baseline begins at capture and does not claim to prove earlier
state. The final post-capture comparison is
`PASS=19 PRESERVED=22 FAIL=0 TOTAL=41`.

Distinct native current-Agent advisory review
`srv-eac6541cb05f7f95fa5851dd19c12e9e` found the requirement decomposition,
documentation-only architecture, post-capture scope, toolchain inference,
privacy boundary, retained unknowns, and denied authority consistent. It
preserved the validation-command defect and delayed-capture limit rather than
treating them as exceptions. This is a separate self-review pass, not
independent assurance. The native task-completion-record tool is unavailable
in this session, so no completion record is claimed.

No AgentGov wheel build or installation, virtual-environment creation, product
source or packaging change, model call, consumer rehearsal, network access,
credential use, Git mutation, publication, release, deployment, or external
write occurred. Native completion evidence is not claimed while the admitted
command defect and historical pre-capture boundary remain open.

## Retained installed shared-template inspection v1

The product owner selected the retained-artifact inspection through resolved
alignment journey `mcpj-3993a26f8dc44cafbb99607d55fc37d0`. Native proposal
`prp-4e715fe7fcac47549a79c3eef3e1f51e` admitted exact task
`p0-retained-installed-shared-template-inspection-v1`; the product owner then
separately instructed execution. The first execution action captured baseline
`sha256:83ef50d825da41ff7f0b31e968667c119ef79200e87cacfb62fcbe266e9197b5`.

Preflight found exactly one retained `setuptools 84.0.0` wheel and its digest
matched the prior expected identity. One validated short root used a
207-character longest projected target under its 240-character limit. All
seven expected current distribution overlays matched source/stage byte
digests. One backend install, one wheel build, one runtime creation, and one
offline runtime install were attempted without a retry, repair, alternate
artifact, network request, or second root.

The resulting wheel had 188 members and the same canonical inventory digest
as the prior successful short-root wheel. Exactly one intended shared-template
member existed under `.data/data/share/...`; that member matched the source.
The fresh runtime's actual `scheme.data` equaled its root, the scheme-derived
installed file existed as a regular non-link file, and source/installed bytes
matched SHA-256
`e596a428f37f914c6652d650c36df7a47261d6d75118858b6a6ebf99abd91fd6`.

The run is nevertheless not accepted as fully satisfying the task. The driver
used `git archive HEAD` without the exact distribution pathspec and staged all
747 committed regular files. Read-only classification reproduced the prior
protocol's 199 exact committed distribution inputs. This is a real
source-staging breadth deviation even though setuptools emitted the identical
188-member inventory. The run stopped without repair or a second build, using
outcome
`STOPPED_AFTER_VERIFIED_SHARED_TEMPLATE_GATES_AT_SOURCE_STAGE_BREADTH_REVIEW`.

The sanitized durable record was written while the foreground driver retained
the wheel and runtime. The exact cleanup continuation then removed the one
verified root, including the wheel, build environment, runtime, staged source,
caches, and temporary files. The record is
[`docs/build-validation/retained-installed-shared-template-inspection-v1-2026-08-25.md`](../build-validation/retained-installed-shared-template-inspection-v1-2026-08-25.md).

No product source, packaging declaration, consumer fixture, model call,
network or credential access, Git mutation, commit, push, publication, release,
deployment, or external write occurred. Rehearsal v2 remains unstarted.

All 7 focused short-root tests pass with 1 platform-limited skip. The complete
supported Python 3.11 suite passes all 1102 tests with 5 platform-limited skips
in 161.963 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON and `git diff --check` pass. The
final task-start comparison is `PASS=21 PRESERVED=22 FAIL=0 TOTAL=43`, and
post-cleanup inspection finds zero current short roots.

Distinct native current-Agent advisory review
`srv-77008f76376b004de694dcdd7be14d9b` found the positive installed-template
evidence, single-attempt boundary, evidence-before-cleanup sequence, exact-root
cleanup, repository scope, privacy, and denied authority consistent. It
retained the 747-versus-199 staging breadth as an unsatisfied task constraint
and did not recommend automatic rehearsal-v2 continuation. This is a separate
self-review pass, not independent assurance. No completion record is claimed
because the admitted source-staging constraint was not satisfied.

## Exact distribution pathspec replay v1

Resolved alignment journey `mcpj-d41c9a8d895b4c18ad80ddded912ff50`
records the product owner's choice to close the 747-versus-199 staging gap.
Native proposal `prp-ca02c6a4b12d4de0b5981c169b778672` admitted exact task
`p0-exact-distribution-pathspec-replay-v1`; the product owner separately
instructed execution. The first execution write captured task-start baseline
`sha256:eae7535ba08225739df4ff62e7c14f4392c2d6d84badf4736f3e7370e071c7fb`.

Read-only preflight found one digest-matched `setuptools 84.0.0` wheel, zero
existing short roots, an unchanged three-file short-root helper, exactly seven
current distribution overlays whose prior Git-layer identities all matched,
and the protocol's exact 199 committed inputs. Three nested governance schema
files selected by the prior `governance/*.schema.json` Git/PowerShell path
semantics explain why a strict single-directory interpretation first counted
196. That rejected preflight created no root and made no build or install
attempt.

The one real run created one short root and archived the explicit 199-file
manifest rather than broad `HEAD`. It extracted 199 committed regular files,
applied the exact seven overlays with seven source/stage byte matches, and held
201 staged regular files after the two new overlay files. The input-manifest
SHA-256 is `f81a12e42e8fa1739b6af6c708e8b254374fc8e04dcb03af19251ae7e08331c5`;
the overlay-manifest SHA-256 is
`861b869c153f11e35b619085a63e43b59984df0f319a8bf988e69ae4f5695122`.

One offline backend install, one wheel build, one runtime creation, and one
offline runtime install succeeded without retry or substitution. The wheel
contains 188 members and reproduces canonical inventory SHA-256
`6ca19e08b0658442eab2616ec25f260265ddfcc8fed734e50a0e47f54fbd05da`.
Exactly one intended shared-template member exists. The source, wheel member,
and installed file share SHA-256
`e596a428f37f914c6652d650c36df7a47261d6d75118858b6a6ebf99abd91fd6`;
the runtime's actual data scheme equals its root and the scheme-derived file
is present. Outcome is `PASSED_EXACT_DISTRIBUTION_PATHSPEC_REPLAY`.

The sanitized record was written while the foreground driver retained the
verified root and all artifacts at
[`docs/build-validation/exact-distribution-pathspec-replay-v1-2026-08-25.md`](../build-validation/exact-distribution-pathspec-replay-v1-2026-08-25.md).
The exact continuation then removed 1 of 1 task roots and all contained
artifacts. Formal validation remains in progress. No product source,
packaging declaration, test, helper behavior, consumer, network, credential,
model, Git, publication, release, deployment, external write, or rehearsal-v2
action was used or authorized.

Post-cleanup inspection found zero current short roots. All 7 focused
short-root tests passed with 1 platform-limited skip. The complete supported
Python 3.11 suite passed all 1102 tests with 5 platform-limited skips in
173.347 seconds. Task governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3`;
repository governance is `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; task JSON and
`git diff --check` pass. The task-start comparison is
`PASS=10 PRESERVED=35 FAIL=0 TOTAL=45`, and the bounded task-delta privacy
scan reports zero findings.

The first full-suite invocation mistakenly used an interactive terminal and
blocked at the expected confirmation-reader test. It was interrupted without
repository mutation; the same declared command then ran noninteractively to
the complete passing result above. This validation transport correction did
not repeat the wheel build or runtime installation.

Distinct native current-Agent advisory review
`srv-4ca63255d2c567f06599266e2c23db5e` found the exact requirement result,
scope preservation, one-build/one-install implementation, evidence-before-
cleanup order, exact cleanup, privacy, and denied authority consistent. It
retained cross-version and released-artifact behavior, repeatability,
product-owner acceptance, usability, adoption, causal benefit, ROI, and
rehearsal-v2 behavior as unknown. This is a separate self-review pass, not
independent assurance. The native `agentgov_task_completion_record` tool is
not exposed in this session, so no native completion record is claimed or
fabricated. The exact replay task result and its declared validation are
otherwise complete.

## Independent automatic-governance journey rehearsal v2

Resolved alignment journey `mcpj-9400f2a376c14d18962cd67fc1ea8c04`
records the product owner's selection of one bounded independent automatic
journey. Native proposal `prp-dbf35246e84f4b40af0911160ac300a4`
admitted task `p0-independent-automatic-journey-rehearsal-v2`; the product
owner then separately instructed execution. The first execution write captured
task-start baseline
`sha256:401b7cd6aa6ebeb59817ca306cbb0f0fe2441c0479cc950dc0a2998666de90cc`.

Preflight reached the first stop condition before temporary-root allocation.
The preceding exact replay evidence retains the expected 199-input count and
manifest digest but does not retain the 199 repository-relative paths or a
deterministic rule that recreates them. The current packaging declaration and
committed source/data inputs reconstruct 188 paths with manifest SHA-256
`3041c6be461ca4b564f92aabdfa7a73c9b9436b30d50e90b9aa5888c4eb26be9`,
which does not match the preceding digest. The missing 11 path identities
cannot be recovered from a cryptographic digest.

The exact seven current overlays remain reproducible and match the preceding
overlay digest. One retained build backend and all three short-root helper
files match expected identities, and no existing short root was present.
Those positive observations do not clear the committed-source gate. Guessing
the missing paths would violate the exact-pathspec and single-build contract,
so the rehearsal stopped as
`STOPPED_AT_NON_REPLAYABLE_EXACT_PATHSPEC_PREFLIGHT`.

The durable record is
[`docs/experiments/independent-automatic-journey-rehearsal-v2-2026-08-25.md`](../experiments/independent-automatic-journey-rehearsal-v2-2026-08-25.md).
No task root, build, runtime, fixture, external Agent session, model turn,
consumer form, or consumer tool call was created. No cleanup target existed.
Configuration and trust state were measured only by identity/existence;
credentials were not read. No source, package, test, helper, consumer, Git,
publication, release, deployment, or external-write authority was used.

Formal validation and the distinct advisory review follow this durable
evidence. The admitted baseline-check command omits required CLI arguments;
the defect will be reported alongside the correct repository-standard
equivalent rather than hidden by modifying the admitted task.

The short-root suite passed all 7 tests with 1 platform-limited skip, the
focused documentation suite passed all 55 tests, and the complete supported
Python 3.11 suite passed all 1102 tests with 5 platform-limited skips in
163.618 seconds. Task governance returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. JSON, `git diff --check`, the bounded
privacy scan, and the zero-short-root gate passed. Final scope is
`PASS=13 PRESERVED=34 FAIL=0 TOTAL=47`.

All exact admitted validation commands were invoked. The baseline command and
both option-first governance commands failed during argument parsing because
their recorded CLI forms are invalid. The correct baseline, task-governance,
and repository-governance equivalents passed. The admitted task record was not
edited, so the command defects and the unsatisfied all-commands-success signal
remain visible alongside the otherwise passing closeout checks.

Distinct native current-Agent advisory review
`srv-b672422d039e26b8dd8b604abf970621` found the first-deviation attribution,
path-level replayability gap, zero-attempt boundary, scope preservation,
privacy and denied authority, command defects, and remaining unknowns
consistent. It did not treat the selected end-to-end outcome or all-command
acceptance signal as complete. This is not independent assurance. The native
completion-record tool is not exposed, so no completion record is claimed or
fabricated.

## Replayable distribution-input manifest v1

Resolved alignment journey `mcpj-434a8abbdf734be18bd19e0087fca15b`
records the product owner's selection of the canonical-manifest direction.
Native proposal `prp-7be1fa3646ac459a924b6a20ae8a6db9` admitted task
`p0-replayable-distribution-input-manifest-v1`; the product owner then
separately instructed execution. The first implementation write captured
task-start baseline
`sha256:ea8ce40944cdc31860614c3d2e03fb0e0e6f409f62e42b4d284f00a2b0e400f7`.

The new strict schema, static repository manifest, and dependency-free
read-only checker define the currently supported distribution-input subset.
Independent derivation and the persisted declaration both contain 186 paths.
The path-list identity is
`sha256:cfea3a3632bd75d1f4db51168d257c465987d3fe620fda2449abcd86f42fcd03`;
the path-plus-content identity is
`sha256:a986da7096e257b90e984084373f923ad460ff4c95f172a08fe4cf2d1fccacaf`.
The current Git classification is 178 committed, 5 tracked delta, 3 untracked
overlay, and 0 deletion.

The contract explicitly supersedes, rather than claims to recover, the prior
omitted 199-path list. Eleven isolated checker fixtures pass with one
platform-limited symbolic-link skip, and the 70-test package, initializer, and
documentation run passes. The complete supported Python 3.11 suite passes all
1102 tests with 5 platform-limited skips in 167.313 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Final captured scope is
`PASS=23 PRESERVED=33 FAIL=0 TOTAL=56`; both JSON checks and
`git diff --check` pass.

Distinct native current-Agent advisory review
`srv-7256959ad87fdc6eff08d2835b596493` found the selected requirement,
supported subset, exact identities, scope preservation, privacy, and denied
authority consistent. It retained artifact parity, future journey behavior,
adoption, reliability improvement, time savings, causal benefit, and ROI as
unknown. This is a separate self-review pass, not independent assurance. The
native task completion-record tool is unavailable, so no completion record is
claimed or fabricated.

No package declaration, runtime source, consumer, configuration, credential,
artifact build, model, Git write, publication, release, deployment, or
external write occurred. A future rehearsal is not authorized by this record.

## Manifest-driven artifact parity proof v1

Resolved alignment journey `mcpj-307fd958853441f29d111205b412e4ec`
records the product owner's selection of the bounded artifact-parity proof.
Native proposal `prp-e8a01255bf104341a8e76ca3b613d934` admitted task
`p0-manifest-driven-artifact-parity-proof-v1`; the product owner then
separately instructed execution. The first implementation write captured
task-start baseline
`sha256:b097edf3232e23ea7664c4f37461319333622ad8987e06fb86ff8086d5b4be38`.

Read-only preflight found zero short roots, one retained setuptools 84.0.0
wheel with one expected digest match, one compatible retained backend runtime,
and three of three expected short-root helper identities. No dependency was
installed. The current manifest checker passed at 186 paths.

One verified short root received exactly those 186 current regular files. All
186 source-stage byte comparisons, the exact path set, and both canonical
digests matched; links and extra inputs were zero. Exactly one offline,
no-isolation build returned zero and emitted one wheel. The wheel has 189
regular members and inventory SHA-256
`15919824a78e07f1d69e2585a76845af009283164ab7b6235a5fe075c5a8a9c2`.
All 76 package and 107 data payloads were present with 183 byte matches, zero
missing, extra, or mismatched managed members, six separately classified
generated metadata members, and zero unexpected unmanaged members. Wheel
SHA-256 is
`bba4350f3bff9313eb16273d8cac425a6542e16d9a86183d557cf6ce79aac023`.

After emitting that sanitized result, the foreground driver encountered closed
standard input while awaiting the evidence-before-cleanup continuation. This
is the first deviation; the task outcome is
`STOPPED_AT_EVIDENCE_HANDSHAKE_TRANSPORT_AFTER_ARTIFACT_PARITY`, not a full
pass. No second build, repair, substitution, or cleanup attempt occurred in
that driver.

The durable evidence record was written while exactly one short root and its
identity-matched wheel remained. Bounded cleanup then independently required
that unique direct temporary child and wheel identity, removed 1 of 1 roots
through the existing helper, and found zero remaining roots. The sanitized
record is
[`docs/build-validation/manifest-driven-artifact-parity-proof-v1-2026-08-25.md`](../build-validation/manifest-driven-artifact-parity-proof-v1-2026-08-25.md).

Formal validation and the distinct advisory review follow. No network,
dependency install, external Agent, model, consumer form, source, package
declaration, helper, checker, test, configuration, credential, Git mutation,
publication, release, deployment, or full journey was used or authorized.

All eight exact admitted validation commands were then invoked and passed. The
manifest checker verified 186 paths with both canonical digests unchanged; the
18 focused manifest/short-root tests passed with 2 platform-limited skips; and
the complete supported Python 3.11 suite passed all 1102 tests with 5
platform-limited skips in 168.724 seconds. Task governance returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Task JSON, `git diff --check`, the
zero-short-root gate, and final scope
`PASS=17 PRESERVED=40 FAIL=0 TOTAL=57` passed.

Distinct native current-Agent advisory review
`srv-b6844e103871c733be7ddc28a3e35ec6` kept the artifact result and the
evidence-handshake deviation separate, confirmed the single-build boundary,
scope and privacy preservation, cleanup, denied authority, and unknowns, and
did not claim overall acceptance. It is a separate self-review pass, not
independent assurance. The native completion-record tool is not exposed, so no
completion record is claimed or fabricated. No commit, push, publication,
release, deployment, retry, or new task is authorized by this closeout.

## Evidence-gated short-root cleanup v1

After the product owner explicitly selected the evidence-handshake repair,
native proposal `prp-2186fb4fb7254ea7acbbc6186c0c4f30` admitted task
`p0-evidence-gated-short-root-cleanup-v1`; the product owner then separately
instructed execution. The task digest is
`sha256:07aaf5101066309560f094ba1310f5b1b7fca904861e0ef59e32e27e0fb212e0`.

The first capture command mistakenly used check's `--baseline` option instead
of capture's `--output` option. It failed during argument parsing with zero
writes. Read-only inspection confirmed no target existed; the corrected
create-only invocation then captured baseline
`sha256:5ef376aa1a268c566f0f4ec21dba55972a93896fad03f32920605636ee767731`
before implementation. The boundary does not claim earlier state.

The repository-internal short-root helper now creates a privacy-bounded receipt
only for a normalized repository-relative regular non-link evidence file whose
bytes match an exact canonical SHA-256 identity. The evidence-gated removal
operation revalidates that identity before delegating once to the existing
exact-root remover. It has no prompt, stdin read, or TTY branch. Invalid,
missing, escaping, linked, non-file, changed, or byte-mismatched evidence stops
before cleanup and leaves the root present.

All 12 focused tests pass with 2 Windows privilege-limited symbolic-link skips.
They cover TTY-reported, redirected, and closed input, patched-input non-use,
one-call cleanup, evidence failures, root preservation, and privacy-bounded
reports. Formal complete-suite, governance, scope, JSON, diff, exact changed-
path, and distinct advisory review results follow.

No wheel build, retry, dependency installation, network, public CLI or
installed-package change, consumer, external Agent, model, credential, Git
mutation, publication, release, deployment, or external write occurred. The
historical artifact-parity outcome remains stopped and a new artifact replay is
not authorized by this task.

Formal validation completed. All 12 short-root tests pass with 2 platform-
limited symbolic-link skips, all 55 focused documentation tests pass, and the
complete supported Python 3.11 suite passes all 1102 tests with 5 platform-
limited skips in 163.241 seconds. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; task JSON and `git diff --check` pass.
Final task-start scope is `PASS=35 PRESERVED=28 FAIL=0 TOTAL=63`, with exactly
the seven approved implementation and closeout paths changed after capture.

A distinct bounded current-Agent advisory pass found the no-stdin design,
evidence-before-removal order, path/link/type checks, canonical digest and
revalidation behavior, root preservation, privacy, exact changed paths, and
denied authority consistent. It retains future driver integration and
concurrent adversarial filesystem replacement as unknown. This fully specified
task had no resolved alignment journey, so no native self-review result is
claimed. The native completion-record tool is not exposed, and no completion
record is fabricated. No new artifact replay or downstream action is
authorized.
