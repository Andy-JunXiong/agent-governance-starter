# Development log - 2026-08-28

## Interview delivery and main-branch closeout

### Closeout admission and boundary

The product owner requested an end-of-day documentation update, one commit, and
one ordinary non-force push to `origin/main`. Native proposal
`prp-238d4a95d4e443e1961059a9c5e89ef9` admitted task
`p0-2026-08-28-session-closeout-v1`; its create-only task-start baseline is
`sha256:baceb2d4e014e5f67e98a9c91e618f03041c7ab50fbb8ce050a5edc581e05150`.
The task includes the accumulated artifact replay implementation and evidence,
bilingual interview demo, whole-project story, presenter guide, tests, task
records, selected baselines, and related documentation. Local `.codex`
configuration and explicitly unrelated historical scope baselines remain
outside the intended commit.

The Git request authorizes one reviewed source-history closeout only. It does
not authorize a pull request, force push, history rewrite, package release,
deployment, hosting, or external publication workflow.

### Full-suite finding and human direction

The first full-suite run exposed an obsolete landing-page assertion after the
de-AI copy changed the primary call to action from “Open the guided interview
demo” to “Open the refund demo.” The assertion was updated without weakening
the exact href check; the resulting 79 focused walkthrough and interview tests
pass.

Artifact script validation then stopped at the filesystem manifest boundary.
The README is one of the exact 186 distribution inputs, and its interview-link
update changed the derived content digest from
`sha256:a986da7096e257b90e984084373f923ad460ff4c95f172a08fe4cf2d1fccacaf`
to `sha256:b00be073afa541c3130cc620c84bd2bba2afa7b48ec4e70a4ff74998bc7fb8d9`.
The path count and path digest were unchanged. No commit or push had occurred.

Alignment journey `mcpj-6cba12f8f7184a8d9228cd01febddb6e` presented two
stable choices. The product owner selected the recommended separate identity
refresh rather than leaving the README change local. Native proposal
`prp-787d1c97b3464fcd82a43e8860e1496e` admitted task
`p0-readme-manifest-identity-refresh-replay-v1`; its baseline is
`sha256:cc2f0e79235e46a3263476253ccc94374903aa39b063a8cdce66bf157d64dfd2`.

### Manifest refresh and sole replay

The distribution manifest retains exactly 186 sorted paths and path identity
`sha256:cfea3a3632bd75d1f4db51168d257c465987d3fe620fda2449abcd86f42fcd03`.
Only its derived content identity and the controller's matching expected
constant changed. All 78 artifact-chain tests then passed with five Windows
privilege-limited symbolic-link skips, and the read-only manifest check passed.

Fresh preflight confirmed the retained Python 3.11.9 / pip 24.0 / setuptools
84.0.0 environment, backend-wheel SHA-256
`51a52592b3b99e102b609654876bd65f19f999935166d1352678931132b0c670`,
an absent new evidence target, and zero short roots. The fixed controller was
invoked exactly once by module name through one strict JSON stdin request. It
returned `PASS` with controller/dry/actual attempts `1/1/1`; no retry or
alternate invocation occurred.

The replay copied 186/186 inputs with byte equality and built one wheel with
SHA-256
`sha256:c898ce93bb8b8a820bd68d909393096fcdc2f9a4ecb04509a4dd44acb75bac4a`.
The wheel retained 189 regular members, 183 managed byte matches, zero managed
mismatches, six generated metadata members, and zero unexpected unmanaged
members. New durable evidence at
`docs/build-validation/readme-manifest-identity-refresh-replay-v1-2026-08-28.md`
has SHA-256
`d6c96190bc867a697f22a4cc7ea759c7d1ae7c25fac8a657178d60e5b51bac7f`.
Receipt-gated cleanup completed and the final short-root count is zero.

The earlier replay evidence remains byte-identical at SHA-256
`ffc06da3060b459ff39c8602365126a860e8228dd6320c8c88f69cbc8c6b8715`.
The new local PASS is a maintenance replay for the refreshed README-inclusive
identity; it does not erase or rewrite the earlier historical run and does not
prove cross-platform behavior, repeated reliability, adoption, interview
benefit, time saving, production behavior, or return on investment.

### Final validation, review, and Git result

The full repository suite passes 1,111 tests with five Windows privilege-
limited symbolic-link skips. The full scripts suite passes 111 tests with six
such skips; the exact artifact chain separately passes 78 tests with five such
skips. All 88 focused public-entry, walkthrough, demo, interview-documentation,
product-site, and user-documentation tests pass.

Final task governance for `p0-2026-08-28-session-closeout-v2` reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The distribution manifest, both task JSON
records, prior and new evidence identities, whitespace, and `git diff --check`
pass. Final v2 captured-scope validation reports
`PASS=186 PRESERVED=1 FAIL=0 TOTAL=187`.

Native current-Agent self-review
`srv-fe956072419f5578994e91e4a2c1fbb8` completed as a distinct advisory pass.
It found the selected split requirement, unchanged 186-path architecture,
refreshed content identity, one-attempt replay, v2 closeout scope, updated
primary-entry tests, privacy boundary, and denied downstream authorities
consistent. It retains cross-platform behavior, repeated reliability,
independent assurance, browser pacing, adoption, production behavior, and
interview outcome as unknown.

The reviewed staged snapshot contains 106 delivery files. Secret-like token and
private-key scanning and added-line absolute-host-path scanning found no match;
local `.codex/config.toml` and 18 excluded historical baselines remain unstaged.
The first staged whitespace check found four trailing spaces used for Markdown
hard breaks in two historical build-validation records; those spaces were
removed before the final staged check. No test or product boundary was changed
by that cleanup.

`git fetch origin` succeeded after the sandbox required its existing approval
to write `.git/FETCH_HEAD`. Local `HEAD` and `origin/main` were both
`734b427` before commit, with divergence `0/0`; no merge, rebase, or history
rewrite was required. A single commit cannot contain its own final identifier
or a post-push observation, so Git history owns the commit identity and the
completion handoff reports the verified post-push `HEAD == origin/main` result.
This Git closeout is not a release, deployment, hosting action, package
publication, pull request, or product-adoption claim.
