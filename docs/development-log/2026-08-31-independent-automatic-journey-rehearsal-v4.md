# Independent automatic-governance journey rehearsal v4 - 2026-08-31

## Goal and stop condition

Run one fresh, uncoached automatic-governance journey from the verified
187-path current source. Use one attempt, retain only normalized evidence, and
stop at the first reproducibility, scope, privacy, host, protocol, or
state-preservation deviation without retry or repair.

## Human direction, admission, and take-up

Resolved alignment journey `mcpj-ebd8235ac4794007a8367abd0317f7e5`
recorded the product owner's selection of the fresh v4 rehearsal. Native
proposal `prp-b979ba4353ad4352a21240f858500c62` admitted exact task
`p0-independent-automatic-journey-rehearsal-v4`. The product owner separately
supplied exact `REPLACE`, and current source captured baseline
`sha256:19339bdc5f9ebb2ea367c9554465557a2bb9a983dec065425d3eaa7ec2e0c197`
before execution.

## Actions and first deviation

Read-only task-start comparison passed with one unchanged included task record,
seven byte-identical preserved exclusions, and zero failures. The repaired
distribution manifest then passed with 187 paths, zero differences, zero
tracked deltas, and zero untracked overlays.

The next mandatory environment gate observed Python 3.12.10 and pip 25.0.1,
but no installed `setuptools` module. The local pip cache contained zero
`setuptools 84.0.0` wheel candidates and zero expected-digest matches. The
rehearsal therefore stopped as `STOPPED_AT_OFFLINE_BUILD_TOOLING_PREFLIGHT`.

No dependency was installed or downloaded. No alternate backend or runtime was
selected. There was no controller invocation, build, wheel, runtime install,
synthetic repository, fixture commit, external Codex session, request, native
form, tool call, retry, repair, substitution, temporary-root creation, or
cleanup. Codex CLI 0.146.0 was available but not started.

Durable normalized evidence is in
`docs/experiments/independent-automatic-journey-rehearsal-v4-2026-08-31.md`.

## Validation and remaining gap

Closeout validation completed on 2026-09-01. The focused documentation suite
passes all 64 tests. The complete suite passes all 1,135 tests with six
platform-conditioned skips. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The task-start comparison preserves all
seven exclusions and admits all three v4 documentation deltas. Task JSON and
`git diff --check` pass. These results validate only the stopped snapshot; the
end-to-end automatic journey remains unestablished, and no build or consumer
result is claimed.

Distinct native current-Agent self-review
`srv-386c5e927b4aa664563ccf3e609aa90f` found the first-deviation attribution,
architecture boundary, admitted scope, zero downstream attempts, and privacy
limits consistent. It is not independent assurance or human acceptance and
keeps installed behavior, automatic tool selection, and user benefit unknown.

No source, historical v3 evidence, user file, configuration, credential,
existing consumer, Git history, publication, release, deployment, or external
state was changed. A later review may consider one separately admitted offline
Python 3.12 build-tooling bootstrap; this record grants no such authority.
