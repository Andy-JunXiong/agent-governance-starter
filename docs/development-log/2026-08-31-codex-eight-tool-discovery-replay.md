# Real Codex eight-tool discovery replay - 2026-08-31

## Goal and admitted boundary

The human resolved alignment journey
`mcpj-3710f6d3115944eebfe027239f57d301` to discovery-only validation. Native
proposal `prp-400e337a2caf45d7b43002e414d9a4f5` admitted task
`p0-codex-eight-tool-discovery-replay-v1`, and the human separately replaced
the prior active-task pointer after reviewing the start preview.

The task permits one real Codex eight-tool discovery replay, bounded evidence,
current status, its documentation contract test, and this dated log. It
excludes permanent Codex configuration, MCP implementation, stateful completion
or drift invocation, Git, publication, deployment, and release.

## Replay observations

Codex CLI 0.146.0 accepted a one-off configuration override for a required
current-source AgentGov STDIO server and the canonical eight-tool allow-list.
An installed-launcher attempt closed during MCP initialize-response generation,
so it is not positive evidence and no repair was attempted.

A negative-control turn with all tools prohibited returned no names, exposing
the role of deferred discovery. A second repository-context turn allowing only
tool search returned all eight fully qualified AgentGov names. The stronger
repeat ran outside the repository working directory and returned the identical
set, excluding project rule text as the source of the names. Neither event
stream contained an AgentGov tool-call event.

The project configuration digest remained
`4cca2d57edeaddfe52d3e6c4dd4d774192bbdbcab4e84e07df73e14a861c0348`
before and after replay. No permanent configuration or runtime source changed.

## Validation and closeout boundary

The first admitted task preserved one unsupported validation entry,
`agentgov govern task`. The supported read-only equivalent,
`python -m agentgov check task`, passed, but it could not silently substitute
for the exact admitted command. V1 therefore remains unchanged and was not
presented as verified completion.

Native proposal `prp-589489eb5b724b6099881fcfafe5291a` admitted corrected
closeout task `p0-codex-eight-tool-discovery-replay-v2`. The human separately
replaced the active pointer. V2 retains the same evidence scope and uses only
supported validation commands; it does not repeat the network replay, repair
the installed launcher, or invoke completion and drift-review tools.

Final v2 closeout validation passes all 58 focused documentation tests and all
1,114 repository tests with five Windows privilege-limited symbolic-link
skips. V2 task governance reports `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository
governance reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and whitespace validation
passes. The raw scope check keeps pre-existing excluded changes visible as
failures and grants no exception or ownership transfer.

Native current-Agent advisory self-review
`srv-93b23c7f2c410b16a15660136a2153dc` examined requirement conformance,
implementation evidence, privacy, scope, and installed-launcher attribution.
It requested no correction. Stateful invocation, the installed-launcher root
cause, cross-version reliability, and independent privacy assurance remain
unknown.

Discovery is bounded as successful; completion and drift invocation remain a
separate unproven requirement. No commit, push, publication, release, or
deployment is claimed or authorized.
