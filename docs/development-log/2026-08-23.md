# 2026-08-23 development evidence

## Deterministic ambient-process attribution gate v1

After the installed-schema validation stopped at its absolute-zero process
gate, the product owner first selected repair of the native self-review binding.
The refreshed installed Adapter then completed the same native self-review
`start -> complete` journey successfully. Through the subsequent alignment
journey `mcpj-8fcb15b266d042469bbb91da48eb265c`, the owner selected a reusable
deterministic process-attribution component over an experiment-only procedure.
Native proposal review admitted exact task
`p0-deterministic-process-attribution-gate-v1` through proposal
`prp-c88ee5a4bd34445c8583ba3014c5d5e9`, and the owner separately instructed
the Agent to execute it.

The new dependency-free `process_attribution_gate` module accepts only
in-memory normalized per-process facts and preflight and postflight counts. It
classifies a complete pre-existing observation as `ambient`, a new observation
belonging to the exact task root or descendant tree as `task_owned`, and
incomplete, contradictory, or unattributed evidence as `indeterminate`. The
gate permits a complete nonzero ambient baseline. It returns `ready` only when
all per-instance facts reconcile with both snapshots and no task-owned process
remains, returns `blocked` for a known remaining task-owned process, and remains
`indeterminate` for incomplete identity, unknown lineage, an unaccounted new
process, malformed input, or either unreconciled snapshot.

Per-instance facts preserve the distinction between an ambient process that
exits and a same-class task process that remains, even when aggregate counts do
not change. Results retain only normalized process classes, counts, statuses,
and stable reason codes. The module accepts no raw command line, process
identifier, username, absolute path, or unrelated process data. It performs no
process enumeration or intervention, subprocess, filesystem, network, Codex,
App Server, schema-generation, MCP, model, user-configuration, or public CLI
operation. Actual transient host observation and task-root tracking remain a
separately authorized driver responsibility.

All 19 focused deterministic tests pass. They cover zero and nonzero
ambient baselines, task-owned exit and residual states, same-class replacement,
duplicate normalized classes, incomplete identity, unknown and contradictory
lineage, new unattributed processes, exact snapshot reconciliation, malformed
observations and counts, bounded safe class labels, stable finding order,
privacy non-retention, and zero external or filesystem calls. Final
documentation testing passes all 52 tests. The supported Python 3.11 full suite
passes all 1043 tests with 3 platform-limited skips in 165.089 seconds. Task
governance reports `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance
reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passes.

Scope reconciliation passes all 7 current-task-owned paths and reports 19
pre-existing untouched findings. Five are exact task exclusions; fourteen
earlier task, experiment, source, or test paths are outside every current
include prefix because the proposal did not list them as exclusions. This is
retained as a non-green scope-accounting gap without an exception, ownership
transfer, or current-task mutation. The current callable governance inventory
does not expose `agentgov_task_completion_record`, so no completion record is
fabricated.

Native current-Agent self-review
`srv-b7983be09b86b0da35352e9ffdd9cd07` completed as a distinct separate pass.
It found no correction-required implementation, privacy, or requirement drift.
It confirmed the fail-closed classifications, per-instance same-class
replacement protection, exact normalized input boundary, no-operation design,
and explicit statement that installed-schema validation has not been retried.
It retained the fourteen-path proposal-exclusion omission as a scope-accounting
gap and actual host lineage availability as unknown. The review is advisory,
not independent, and grants no acceptance, exception, observation, retry, Git,
publication, release, deployment, or other downstream authority.

The stopped installed-schema validation has not been retried. No host process
was enumerated, started, stopped, or modified; no user configuration was read
or changed; and no Codex invocation, App Server launch, schema generation,
model turn, Git operation, publication, release, or deployment occurred.

## Reusable App Server schema diagnostic v1

The product owner selected an internal reusable Adapter module over a public
CLI or test-only harness through alignment journey
`mcpj-31ed88f36ce7456899f03e8646ea43bb`. Native proposal review then admitted
exact task `p0-reusable-app-server-schema-diagnostic-v1` through proposal
`prp-cd6f5cfdd067433a90296687c4426ce0`, and the owner separately instructed
the Agent to execute it.

The new dependency-free `app_server_schema_diagnostic` module accepts only an
in-memory schema and normalized request mapping. It recursively discovers a
direct or nested `thread/start` method definition, resolves bounded local JSON
references, and checks required fields, primitive types, string enums, and
explicit additional-property constraints. It returns only `compatible`,
`incompatible`, or `indeterminate` plus stable reason codes and normalized
field names. Missing decisive constraints, unresolved or cyclic references,
and ambiguous parameter definitions remain `indeterminate` rather than being
guessed compatible.

The result retains no request values or raw schema fragments. It is not a
public CLI, and the module has no command, subprocess, filesystem, network, App
Server, MCP, thread, model, or user-configuration behavior. Static
compatibility still cannot prove runtime thread creation or required-MCP
readiness. The previous v3 parser stop
and installed-schema compatibility therefore remain historical and unknown;
this task creates the reusable diagnostic foundation only.

All 20 focused deterministic tests cover direct and nested discovery, named
parameter fallback, local method, field, and parameter references, required
fields, primitive types, string enums, explicit and missing additional-property
constraints, malformed inputs, unresolved and cyclic references, ambiguous
contracts, duplicate-code ordering, privacy-bounded output, and zero external
or filesystem calls. All 20 focused tests and all 50 focused user-documentation
tests pass. The supported
Python 3.11 full suite passes all 1022 tests with 3 platform-limited skips in
154.504 seconds. Task governance reports `PASS=3 WARN=1 FAIL=0 ADVISORY=3`,
repository governance reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`, the task JSON
parses, and `git diff --check` passes.

Scope reconciliation admits all 8 current-task-owned paths and retains 13
failures for explicitly excluded, pre-existing, and untouched state: user-owned
`.codex/config.toml`, six earlier task records, and their six experiment
records. No exception, ownership transfer, or current-task mutation of those
paths is inferred. The current callable governance inventory does not expose
`agentgov_task_completion_record`, so no Starter completion record is
fabricated.

Native current-Agent self-review
`srv-d9411632e1e1004c6dd746243c65be5c` completed as a distinct separate pass.
It found one deterministic ordering collision when the same reference-error
code could occur once without a field and once with a field. The implementation
now uses an explicit stable `(code, field)` key, and a dedicated regression test
passes before the final full-suite result above. The review found the final
implementation, privacy boundary, scope account, internal non-CLI direction,
and downstream-authority wording consistent. Installed-schema constructs
outside the tested subset, live-host behavior, runtime compatibility, cross-
version utility, and user value remain explicit unknowns. The review is
advisory, not independent, and grants no acceptance, Git, publication, release,
deployment, or follow-on authority.

## Product review carried forward

The product owner reviewed the corrected release-review consumer summary and
reported that it was clear and concise. This closes the preceding task's first
human clarity check for that owner and artifact only. It is not a broad
usability result.

## Taxi historical cross-domain record closeout

The product owner selected completion of the Taxi cross-domain pilot evidence
as the next P1 direction through alignment journey
`mcpj-a43221ce00b14114aed3b11f79c941c6`. Read-only evidence review then found
that the historical adoption commit and current contract state were available,
but exact timed start, timed stop, and elapsed duration were unavailable. The
historical closeout therefore supports no ten-minute or unassisted-pass claim.

A second alignment journey `mcpj-7781c3a0cddf49828a8cbe00a8173ac9`
presented the remaining maintainer choices. The owner selected the conservative
closeout: accept the empty dependency graph only for the current single
declared capability, defer tracked artifact configuration for lack of a
demonstrated retained-artifact and review need, preserve the artifact warning,
and keep the Taxi repository read-only.

Native proposal review admitted exact task
`p1-taxi-cross-domain-pilot-record-closeout-v1` through proposal
`prp-44c07c2cc45a4139a5bed829b91ba789`. The product owner separately instructed
the Agent to execute it.

The new repository-owned
[Taxi cross-domain adoption record](../experiments/taxi-cross-domain-adoption-pilot.md)
binds the historical adoption to Taxi commit
`8145376ed31f58f6261591a3db74ac6c2387cd76`. It records the missing timing
facts rather than substituting the commit timestamp, preserves only friction
already documented on 2026-07-24, and distinguishes that historical observation
from a 2026-08-23 read-only `PASS=17 WARN=1 FAIL=0 ADVISORY=4` check. The record
stores no machine-specific source path or Taxi code, data, credentials,
workflow content, business policy, or runtime data.

The prepared semantic-relation gap-analysis template remains excluded and
unfilled. No semantic schema, checker, report field, AgentGov behavior, Taxi
file, project workflow, Git operation, publication, release, or deployment was
changed.

## Validation

The first focused documentation run found one closeout-log wording mismatch:
the log said the exact timing was not recorded but did not use the shared
`unavailable` and `ten-minute` boundary terms protected across the other truth
surfaces. The log was corrected and no test was weakened. The rerun passed all
43 user-documentation tests.

The supported Python 3.11 full suite passed all 995 tests with 3
platform-limited skips in 157.700 seconds. Repository governance reported
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`. All 7 task-owned paths passed scope
reconciliation. The report retained one failure for the pre-existing,
explicitly excluded, and untouched `.codex/config.toml`; no exception or
ownership transfer is inferred.

Task JSON parsing, bounded added-content privacy scanning, and
`git diff --check` passed. The Taxi reference repository retained its
pre-existing untracked nested starter directory and had no tracked diff from
this task. No Taxi project test or workflow was run.

The current callable governance inventory does not expose
`agentgov_task_completion_record`, so no native completion record is
fabricated.

Native current-Agent self-review
`srv-fa84e6d63dc7caa77141ec9799a47292` completed as a distinct post-validation
pass. It found the admitted requirement, historical/current evidence
separation, task-owned scope, contract test, privacy boundary, read-only Taxi
inspection, and unchanged authority consistent. It retained unavailable timing
and intervention detail, usefulness to another maintainer, cross-repository
generalization, and future artifact value as explicit unknowns. The review is
advisory and grants no acceptance, Git, publication, release, deployment, or
follow-on authority.

## Disposable automatic-journey rehearsal

The product owner selected an independent non-NYC automatic rehearsal through
resolved alignment journey `mcpj-2c53eb797bb04cf9b3665c635a50d0dd`.
Native proposal review admitted exact task
`p0-disposable-automatic-journey-rehearsal-v1` through proposal
`prp-e2fda91144164dd0b569328f668193c2`, and the owner separately instructed the
Agent to begin execution.

A fresh operating-system temporary directory was created and the current
development-source initializer wrote a synthetic governance scaffold. The
initializer reported 47 unresolved adaptation placeholders. Read-only evidence
identified development AgentGov `0.3.0rc1`, Codex CLI `0.146.0`, and Starter
HEAD `02d4a954fa8c784a207cf68b36a30e302587cc7c`. Official Codex configuration
guidance was checked for project trust and stdio MCP command, working-directory,
environment, required-server, timeout, and tool allow-list fields.

The deterministic readiness review then found an exact admitted-contract
conflict. The task requires native scope and completion evidence in a fresh
repository, while also requiring that repository to remain uncommitted and
forbidding every commit. AgentGov scope and canonical completion snapshots
require a valid commit-identified `HEAD` and comparison base; repository tests
create a baseline commit before exercising those contracts. Creating a
baseline would exceed authority, while omitting one would make completion
ineligible.

The task therefore stopped as
`BLOCKED_BEFORE_GIT_BASELINE_AND_MODEL_SESSION`. The Agent did not initialize a
usable Git baseline, write project MCP configuration, start an external Codex
session, relay a consumer form, repair the task, reinterpret the no-commit
boundary, or retry. The synthetic scaffold is retained without cleanup, and
its generated name and absolute path are not recorded.

Observed interaction counts are one native admission decision, one separate
take-up, two manual preparation writes, one deterministic interruption, zero
external sessions, zero model turns, zero consumer forms, zero model-session
retries, and zero restarts. The fail-closed stop was visible in the current
human surface. Consumer Monitor protection events, resolution links,
end-to-end interaction burden, causal benefit, ROI, coverage, general
effectiveness, and uncoached-human usability remain unknown.

The durable evidence is recorded in
[the disposable rehearsal record](../experiments/disposable-automatic-journey-rehearsal-2026-08-23.md).
No existing consumer, product source, user-owned `.codex` state, dependency,
retained runtime, remote, external Git state, publication, release, deployment,
CI, or production system changed. A later attempt requires a new product
choice and separately admitted task that either permits one local baseline
commit or identifies an already committed disposable fixture.

All 44 focused user-documentation tests passed. The supported Python 3.11 full
suite passed all 996 tests with 3 platform-limited skips in 153.967 seconds.
Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope reconciliation
admitted all 6 task-owned paths and retained one failure for the pre-existing,
explicitly excluded, untouched user-owned `.codex/config.toml`; no exception
or ownership transfer is inferred. Task JSON parsing, bounded retained-evidence
privacy scanning, and `git diff --check` passed. A metadata-only preservation
check found the temporary directory retained with 31 files and no `.git`
directory; it did not inspect file contents.

Native current-Agent self-review
`srv-a419b87ea09ed7659242d53cf3992ba3` completed as a distinct separate pass.
It found the failure classification, Git-baseline dependency, admitted scope,
documentation consistency, validation, privacy, temporary-resource
preservation, and authority boundaries consistent. It retained the intended
meaning of “uncommitted,” possible unborn-repository support, native consumer
behavior, and the retained temporary directory's later lifecycle as unknown.
The review grants no correction, retry, Git, publication, release, deployment,
or external authority.

The current callable governance inventory does not expose
`agentgov_task_completion_record`, so no Starter completion record is
fabricated.

## Disposable automatic-journey rehearsal v2

The product owner selected one local synthetic baseline commit through resolved
alignment journey `mcpj-1cca8f6aaeba41f39c58c016d2e18623`. Native proposal
review admitted exact task
`p0-disposable-automatic-journey-rehearsal-v2` through proposal
`prp-5865e9f05bcb4b6397878c251d833176`, and the owner separately instructed
the Agent to begin execution.

A fresh operating-system temporary repository received only synthetic Harbor
Notes content and the development-source governance scaffold. One local
baseline commit was created as explicitly admitted. Deterministic preflight
confirmed one clean commit, zero remotes, Codex CLI `0.146.0`, development
AgentGov `0.3.0rc1`, Adapter `1.6.0`, a parseable project MCP configuration,
and all eight intended native tools.

Exactly one external Codex session was launched with one ordinary bounded
change request. A terminal compatibility confirmation preceded a project-trust
prompt. The running session disabled project-local configuration, started one
read-only model turn without AgentGov, and persisted a user-level trust entry.
The Agent interrupted the turn and exited the same session. No native consumer
form, AgentGov call, consumer task, implementation write, consumer validation,
completion record, or consumer self-review occurred. No retry, restart,
configuration repair, trust-entry cleanup, or second session followed.

The result is `STOPPED_AFTER_UNTRUSTED_PROJECT_FALLBACK`. The retained
[v2 rehearsal record](../experiments/disposable-automatic-journey-rehearsal-v2-2026-08-23.md)
contains only normalized evidence. It stores no raw request, response,
transcript, source, validation output, credential, private data, temporary
absolute path, external session identity, or model-private reasoning.

Post-session checks found the temporary repository clean at its single
baseline commit with zero remotes and no consumer task. The user-level trust
entry remains an unresolved retained-runtime mutation because the admitted
task grants no cleanup or repair authority. No existing consumer repository,
Starter product source, dependency, remote Git state, publication, release,
deployment, CI, or production system changed.

All 45 focused user-documentation tests passed. The supported Python 3.11 full
suite passed all 997 tests with 3 platform-limited skips in 147.440 seconds.
Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The task JSON,
bounded retained-evidence privacy scan, and `git diff --check` passed.

Scope reconciliation admitted all 6 v2 task-owned paths and retained three
failures for explicitly excluded pre-existing state: user-owned
`.codex/config.toml` and the v1 task and experiment record. No exception,
ownership transfer, or v2 mutation of those paths is inferred.

The current callable Starter governance inventory does not expose
`agentgov_task_completion_record`, so no Starter completion record is
fabricated.

Native current-Agent self-review
`srv-ee553812dfda1bd52e6c657b0ef400b6` completed as a distinct separate pass.
It found the admitted baseline correction, trust-boundary stop, one-session
limit, zero consumer writes, v2 scope, retained-evidence privacy, validation,
disclosed user-state mutation, and denied cleanup authority consistent. It
retained the best future trust design, same-host readiness proof, later
temporary-resource lifecycle, and effect of the trust entry as explicit
unknowns. The review is advisory and grants no cleanup, replay, Git,
publication, release, deployment, or external authority.

## Disposable automatic-journey rehearsal v3

After separate cleanup removed the v2 synthetic trust entry, the product owner
accepted a trust-independent App Server direction. Native proposal review
admitted exact task `p0-disposable-automatic-journey-rehearsal-v3` through
proposal `prp-8a42b6c98f4242bf89fe0b4c8d911958`, and the owner separately instructed
the Agent to begin execution.

Read-only preflight found the retained synthetic Harbor Notes repository clean
at one local commit with zero remotes, zero matching user-level trust entries,
Codex CLI `0.146.0`, and AgentGov `0.3.0rc1`. The complete user configuration
byte digest was captured without retaining its value or content.

Exactly one Codex App Server process received the AgentGov MCP definition only
through one-run CLI configuration overrides. The client declared structured
MCP form elicitation support and completed initialization. Its only
`thread/start` request asked for an ephemeral thread but returned an RPC error
before a thread response, status inventory, or model turn. The attempt stopped
as `STOPPED_BEFORE_EPHEMERAL_THREAD_AND_MODEL_TURN`, with first-deviation code
`app_server_thread_start_rpc_error`. No repair, diagnostic restart, direct MCP
probe, second process, second thread request, or model request followed.

Post-state checks found the user configuration byte digest unchanged, the
synthetic trust match still absent, the synthetic repository clean at one
commit with zero remotes, and no matching App Server or AgentGov governance-MCP
process. Consumer threads, model turns, forms, tool calls, tasks, writes,
validation, completion records, and self-reviews were all zero.

The normalized retained evidence is recorded in
[the v3 rehearsal record](../experiments/disposable-automatic-journey-rehearsal-v3-2026-08-23.md).
It contains no raw request, response, transcript, source, validation output,
credential, private path, external session identity, configuration digest, or
model-private reasoning. The retained synthetic repository remains outside any
cleanup authority. No Starter commit, push, pull request, publication, release,
deployment, CI, or production action occurred.

All 46 focused user-documentation tests passed. The supported Python 3.11 full
suite passed all 998 tests with 3 platform-limited skips in 152.646 seconds.
Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; task JSON parsing,
corrected bounded privacy scans, and `git diff --check` passed.

Scope reconciliation admitted all 6 v3-owned paths and retained five failures
for explicitly excluded pre-existing state: user-owned `.codex/config.toml`,
the v1 task and experiment, and the v2 task and experiment. No exception,
ownership transfer, or v3 mutation of those paths is inferred. The current
callable governance inventory does not expose
`agentgov_task_completion_record`, so no Starter completion record is
fabricated.

A distinct bounded current-Agent advisory review found no correction required
within the admitted v3 evidence scope. It confirmed the task admission,
single-process and single-request limits, first-deviation stop, zero-model
boundary, unchanged user configuration, clean synthetic post-state, retained
privacy, validation, and denied downstream authority. It keeps the RPC cause,
required-MCP initialization, tool discovery, form behavior, consumer journey,
and causal benefit unknown. Because v3 had no resolved alignment journey, this
is neither native self-review completion nor an independent review. It grants
no diagnostic, repair, replay, Git, publication, release, deployment, or
external authority.

## App Server thread-start no-model diagnostic v1

Native proposal review admitted exact task
`p0-app-server-thread-start-no-model-diagnostic-v1` through proposal
`prp-fba270b82f2f4bdd94f922eecacf4170`, and the product owner separately
instructed the Agent to start.

Preflight found one semantically matching retained Harbor Notes repository,
clean at one local commit with zero remotes. Its governance scaffold and
project configuration were present, its normalized user-level trust match was
absent, and no matching App Server or AgentGov governance-MCP process was
running. The complete user configuration byte digest was captured without
retaining its value or content.

The read-only installed-protocol inspection launched two help commands in one
parallel batch. PowerShell resolved both `codex` commands to a script wrapper
blocked by local execution policy. After observing that first deviation, the
Agent stopped without executable substitution, protocol comparison, App Server
launch, thread request, MCP startup, model request, configuration repair, or
retry. The normalized result is
`STOPPED_BEFORE_PROTOCOL_COMPARISON_AND_APP_SERVER_REQUEST`, with
first-deviation code `powershell_codex_script_wrapper_blocked`.

Post-state checks found the user configuration byte digest unchanged, the
synthetic trust match still absent, the synthetic repository clean at one
commit with zero remotes, and no matching process. The normalized retained
evidence is recorded in
[the diagnostic record](../experiments/app-server-thread-start-no-model-diagnostic-v1-2026-08-23.md).
It retains no raw command output, protocol payload, source content, validation
output, credential, private path, external identity, configuration digest, or
model-private reasoning. The v3 RPC cause remains unknown.

All 47 focused user-documentation tests passed. The supported Python 3.11 full
suite passed all 999 tests with 3 platform-limited skips in 149.716 seconds.
Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; the task JSON, bounded
primary-record privacy scan, and `git diff --check` passed.

Scope reconciliation admitted all 6 diagnostic-owned paths and retained seven
failures for explicitly excluded pre-existing state: user-owned
`.codex/config.toml`, all three v1-v3 task paths, and all three v1-v3 experiment
records. No exception, ownership transfer, or diagnostic mutation of those
paths is inferred. The current callable governance inventory does not expose
`agentgov_task_completion_record`, so no Starter completion record is
fabricated.

A distinct bounded current-Agent advisory review found no evidence correction
required. It confirmed the admitted stop, zero-App-Server and zero-model
claims, post-state, scope, privacy, and denied downstream authority. It also
observed that executable resolution was not verified before the two-command
batch, preventing the intended protocol comparison; whether a platform-native
entry point would succeed remains unknown. Because this fully specified task
had no resolved alignment journey, this is neither native self-review
completion nor an independent review. It grants no diagnostic, substitution,
repair, replay, Git, publication, release, deployment, or external authority.

No commit, push, pull request, publication, release, deployment, CI,
production, user-configuration, or synthetic-repository change occurred.

## App Server parser-preflight static comparison v3

Native proposal review admitted exact task
`p0-app-server-parser-preflight-static-comparison-v3` through proposal
`prp-ca4a8f760484435fbe5b1cfd0122c258`, and the product owner separately
instructed the Agent to start.

Fresh preflight found the retained Harbor Notes repository clean at one local
commit with zero remotes and zero normalized trust matches. The user
configuration byte digest was captured without retaining its value, no matching
process or task-owned temporary directory existed, and command discovery found
exactly one platform-native Windows `codex.cmd` while leaving the PowerShell
script wrapper unselected.

The exact planned parser was invoked once against an in-memory synthetic
schema. Its process exited nonzero without returning the required normalized
self-test result. The task stopped as
`STOPPED_AT_PARSER_PREFLIGHT_BEFORE_SCHEMA_GENERATION`, with first-deviation
code `parser_self_test_process_failed_without_normalized_result`. No parser
change, rerun, replacement, Codex invocation, schema generation, temporary
directory, App Server daemon, initialize request, thread request, MCP startup,
model request, or retry followed. The cause remains unknown because no raw
error output is retained.

Post-state checks found the user configuration digest unchanged, the trust
match still absent, the synthetic repository clean at one commit with zero
remotes, and no matching temporary directory, parser process, App Server, or
governance-MCP process. The normalized retained evidence is recorded in
[the parser-v3 record](../experiments/app-server-parser-preflight-static-comparison-v3-2026-08-23.md).
It retains no raw parser error, source payload, schema, executable path,
temporary path, protocol payload, credential, private identity, configuration
digest, or model-private reasoning. The parser failure cause, v3 request
classification, and RPC cause remain unknown.

All 49 focused user-documentation tests passed. The supported Python 3.11 full
suite passed all 1001 tests with 3 platform-limited skips in 157.029 seconds.
Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; the task JSON, bounded
primary-record privacy scan, and `git diff --check` passed.

Scope reconciliation admitted all 6 parser-v3-owned paths and retained eleven
failures for explicitly excluded pre-existing state: user-owned
`.codex/config.toml`, the five earlier v1-v3, diagnostic-v1, and static-v2 task
paths, and their five experiment records. No exception, ownership transfer, or
parser-v3 mutation of those paths is inferred. The current callable governance
inventory does not expose `agentgov_task_completion_record`, so no Starter
completion record is fabricated.

A distinct bounded current-Agent advisory review found no evidence correction
required. It confirmed the parser gate, first-deviation stop, zero-Codex and
zero-model claims, post-state, scope, privacy, and denied downstream authority
without claiming parser diagnosis, schema generation, or compatibility. It
identified three successive one-off diagnostic plumbing stops as a product
stagnation risk and retained a reusable, unit-tested Adapter diagnostic as
product-review input only. Because this fully specified task had no resolved
alignment journey, this is neither native self-review completion nor an
independent review. It grants no source change, diagnostic, parser work, schema
generation, App Server request, Git, publication, release, deployment, or
external authority.

No commit, push, pull request, publication, release, deployment, CI,
production, user-configuration, or synthetic-repository change occurred.

## App Server static protocol inspection v2

Native proposal review admitted exact task
`p0-app-server-static-protocol-inspection-v2` through proposal
`prp-348e4540ef8d48d3b0c357930cd305b4`, and the product owner separately
instructed the Agent to start.

Fresh preflight found the retained Harbor Notes repository clean at one local
commit with zero remotes and zero normalized trust matches. The user
configuration byte digest was captured without retaining its value, and no
matching App Server or AgentGov governance-MCP process was running. Read-only
command discovery returned exactly one platform-native Windows `codex.cmd` and
separately identified the unselected PowerShell script wrapper.

One bounded help query through the native entry succeeded. One experimental
App Server schema generation then completed in one verified task-owned
operating-system temporary directory. Before analysis, the comparison
controller attempted to use an unavailable encoding helper. The task stopped
as `STOPPED_AFTER_SCHEMA_GENERATION_BEFORE_PROTOCOL_COMPARISON`, with
first-deviation code `schema_analysis_controller_base64_encoder_unavailable`.
No helper replacement, schema reread, second generation, App Server daemon,
initialize request, thread request, MCP startup, model request, or retry
followed.

The already registered cleanup path removed the exact task-owned temporary
directory. Post-state checks found no matching temporary directory or process,
the user configuration digest unchanged, the trust match still absent, and the
synthetic repository clean at one commit with zero remotes. The normalized
retained evidence is recorded in
[the static v2 record](../experiments/app-server-static-protocol-inspection-v2-2026-08-23.md).
It retains no raw schema, help output, executable path, temporary path,
protocol payload, credential, private identity, configuration digest, or
model-private reasoning. The v3 request classification and RPC cause remain
unknown.

All 48 focused user-documentation tests passed. The supported Python 3.11 full
suite passed all 1000 tests with 3 platform-limited skips in 158.675 seconds.
Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; the task JSON, bounded
primary-record privacy scan, and `git diff --check` passed.

Scope reconciliation admitted all 6 static-v2-owned paths and retained nine
failures for explicitly excluded pre-existing state: user-owned
`.codex/config.toml`, the four earlier v1-v3 and diagnostic-v1 task paths, and
their four experiment records. No exception, ownership transfer, or static-v2
mutation of those paths is inferred. The current callable governance inventory
does not expose `agentgov_task_completion_record`, so no Starter completion
record is fabricated.

A distinct bounded current-Agent advisory review found no evidence correction
required. It confirmed native-entry selection, the one-generation limit,
first-deviation stop, cleanup, zero-daemon and zero-model claims, post-state,
scope, privacy, and denied downstream authority without claiming schema
analysis or compatibility. It identified the comparison controller's missing
encoding preflight as a current-Agent sequencing defect that left compatibility
`indeterminate`. Because this fully specified task had no resolved alignment
journey, this is neither native self-review completion nor an independent
review. It grants no inspection, parser replacement, schema generation, App
Server request, Git, publication, release, deployment, or external authority.

No commit, push, pull request, publication, release, deployment, CI,
production, user-configuration, or synthetic-repository change occurred.

## Installed App Server schema static validation v1

Through alignment journey `mcpj-3fa8601e66034ea2bb6a3d9e7ba91261`, the
product owner selected a controlled installed-schema validation of the reusable
diagnostic. Native proposal review admitted exact task
`p0-installed-app-server-schema-static-validation-v1` through proposal
`prp-22567e1a1fdf4289916ce396d513dc09`, and the owner separately instructed the
Agent to execute it.

Read-only preflight found the retained synthetic repository clean at one local
commit with zero remotes, zero matching trust entries, one platform-native
Codex entry, an unselected script wrapper, and no task-owned temporary
directory. The complete user configuration byte digest was captured without
retaining its value. All 20 focused reusable diagnostic tests passed.

The next gate found six existing relevant processes. Because the admitted task
required zero relevant processes and a stop at the first deviation, execution
stopped as `STOPPED_AT_PROCESS_PREFLIGHT_BEFORE_SCHEMA_GENERATION`, with code
`relevant_processes_present_at_preflight`. The minimal driver self-test,
temporary-directory creation, Codex invocation, schema generation, JSON
discovery, diagnostic analysis, App Server launch, thread request, MCP startup,
model turn, substitution, and retry counts were all zero. Installed-schema
compatibility remains `indeterminate`.

Post-state checks found the configuration digest unchanged, trust matches still
zero, the synthetic repository still clean at one commit with zero remotes,
and no task-owned temporary directory. No relevant process was started,
stopped, or modified. The normalized retained evidence is recorded in
[the installed-schema static validation record](../experiments/installed-app-server-schema-static-validation-v1-2026-08-23.md).
It contains no raw process command line, executable or temporary path,
configuration digest, schema, generated filename, request value, credential,
private identity, transcript, or model-private reasoning.

The stop demonstrates fail-closed enforcement but does not classify the
installed schema or prove runtime thread and required-MCP readiness. Whether a
future gate should compare an unchanged ambient-process baseline instead of
requiring absolute zero is product-review input only; it grants no gate change,
process intervention, replay, Git, release, deployment, or external authority.

No commit, push, pull request, publication, release, deployment, CI,
production, user-configuration, or synthetic-repository change occurred.

All 51 focused user-documentation tests passed. The supported Python 3.11 full
suite passed all 1023 tests with 3 platform-limited skips in 154.932 seconds.
Task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository
governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; task JSON parsing and
`git diff --check` passed. Scope inspection passed all 6 current-task-owned
paths and reported 17 explicitly excluded pre-existing paths; no exception,
ownership transfer, or current-task mutation is inferred.

Native current-Agent self-review start succeeded, but the completion call
rejected its returned request identity under the completion tool's own binding
rule. No native review result is claimed or fabricated, and no manual
substitute is presented as native completion. The bounded pre-submission pass
identified the absolute-zero ambient-process gate as the material next product
question. The governance-tool failure grants no exception, replay, Git,
publication, release, deployment, or external authority.

## Native proposal worktree scope binding v1

Through alignment journey `mcpj-b778b31304c34f12bb5385b3a429dccd`, the
product owner selected mandatory pre-admission classification instead of an
advisory warning. Native proposal review admitted exact task
`p0-native-proposal-worktree-scope-binding-v1` through proposal
`prp-2dbb1eddee574b0490d49d579d0c0369`, and the owner separately instructed the
Agent to execute it.

Development Adapter `1.7.0` now obtains a reusable read-only Git change
inventory before native proposal elicitation. The inventory contains only
normalized repository-relative layer, status, current path, and optional
rename/copy source-path metadata. It covers staged, unstaged, non-ignored
untracked, deleted, renamed, and copied records, including both endpoints of a
rename or copy, without reading file contents. Every existing path must match
the proposed includes or excludes; incomplete classification returns
`task_proposal_scope_incomplete` before the form and writes nothing.

The exact inventory is retained with the prepared plan. After an exact human
admit response but before `apply_task_admission_plan`, the Adapter reads the
inventory again. Any difference or inspection failure returns the bounded
`task_proposal_plan_stale` error and creates no task. The native input remains
owner-free; human owner binding, exact decision mapping, target-race
protection, non-admission behavior, generic proposal materialization, terminal
admission, and the later scope checker remain unchanged. Explicit exclusions
classify existing state but grant no ownership, mutation, exception, Git, or
downstream authority.

A distinct native current-Agent self-review completed as
`srv-6235896e6ae446a68454b7d3da64ce3e`. It found one correction-required privacy
edge: a Git filename using path syntax rejected by scope policy could have
escaped as a filename-bearing ordinary exception. The Adapter now converts
that classification failure into the same privacy-safe repository-unavailable
diagnostic, and the focused MCP suite passes after correction. The review
retained cross-host path encoding, live friction, and the small interval between
final inventory comparison and exclusive task creation as explicit unknowns;
no transactional Git lock or independent assurance is claimed.

All 51 focused change-scope and MCP tests and all 53 focused user-documentation
tests pass. The supported Python 3.11 full suite passes all 1050 tests with 3
platform-limited skips in 157.018 seconds. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; `git diff --check` passes. Scope
reconciliation passes all 15 current-task-owned paths. Its 22 other findings
are exactly the pre-existing paths explicitly excluded by this task; no changed
path remains outside both include and exclude scope, and no exception or
ownership transfer is inferred.

Adapter `1.7.0` was not installed, consumer-replayed, published, released, or
deployed. No existing task decision was rewritten; no worktree cleanup,
installed-schema retry, process intervention, App Server launch, schema
generation, user-configuration change, commit, push, pull request, CI,
publication, release, deployment, or external action occurred.
