# Development log - 2026-08-19

## Product-owner M1 review

The session began from the current status stop condition rather than selecting
a roadmap item independently. Native alignment presented three stable
directions, and the human selected the recommended real M1 product review. A
visible PowerShell window ran the documented development-source demo so the
operator, rather than the automation channel, supplied the real `START` and
scope-resolution decisions.

The product owner selected option `2`, `narrow_changes`, and supplied a terminal
screenshot. The visible result recorded the human choice, clearly labelled
scripted remediation, real corrected scope `PASS`, pre-approved validation
`PASSED`, fresh completion reconciliation `VERIFIED`, final state
`REVIEW_READY`, and the explicit final-human-authority boundary. This is
user-reported validation; it is not represented as a Codex-run interactive
terminal observation. The product owner then selected the aligned direction to
propose M2.

## M2 governed refund walkthrough

Native task proposal review admitted
`p0-governed-refund-walkthrough-v1` with ownership limited to one standalone
English HTML page, its semantic test, the two README discovery surfaces, this
log, current status, and its task record. The frozen Landing, existing CSS and
JavaScript, AgentGov Kernel, schemas, `.github`, user-owned `.codex`, hosting,
release, deployment, and Chinese localization were excluded.

The new page reuses `guide.css` and `guide.js`. Its compact story shows the
human-admitted calculation path, simulated calculation and approval-policy
edits, real AgentGov `PASS` and `FAIL` scope facts, `BLOCKED` completion, the
human narrow-changes choice, clearly labelled scripted remediation, corrected
scope, `PASSED` validation, `VERIFIED` evidence, `REVIEW_READY`, and the final
human authority boundary. The wording states that AgentGov refused completion
validation but did not stop or roll back an external coding agent, did not
perform the remediation, and recorded no final acceptance.

Four new semantic tests protect the story order, exact provenance and authority
claims, future-0.3 development-source maturity, a 190-to-300-word main reading
range, local asset references, no inline script or style, README links, and the
absence of a Landing link. The M1 README now connects the executable fixture to
the separate M2 explanation, and the top-level README exposes the local source
candidate while stating that it remains unpublished.

## Validation and review

The focused M2, M1, user-documentation, and public-freshness run passed all 56
tests. The first complete-suite run reached the 120-second command timeout and
returned no final test result; it is recorded as an incomplete attempt, not a
failure. The unchanged rerun completed in 176 seconds and passed all 935 tests
with 3 platform-limited skips. After the closeout documents and task pause were
written, the expanded related run passed all 75 tests, including task-contract
and development-context coverage, and the final complete-suite rerun again
passed all 935 tests with 3 skips in 175 seconds.

Before task pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`,
and repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The
initial scope check accepted all five then-changed task-owned paths and rejected
only preserved user-owned `.codex/config.toml`. That rejection granted no
exception and caused no ownership expansion.

Native aligned current-Agent self-review completed as a distinct advisory pass.
It found the selected story, local implementation, frozen-Landing scope, source
maturity, provenance labels, and authority boundaries represented and
test-protected. It did not claim independent review. It preserved actual
reading time, unbriefed-reader comprehension, browser/device presentation,
localization value, adoption, hiring, and conversion as unknown.

After pause, the task check reported `PASS=2 WARN=2 FAIL=0 ADVISORY=3`; its
additional warning correctly states that implementation is no longer admitted.
Repository governance remained `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. The final
scope command refused to inspect a paused task, as designed. `git diff --check`
passed, and the final working-tree inventory contained only the seven admitted
M2 paths plus preserved user-owned `.codex`.

The task is paused with the M2 candidate local and unpublished. No commit, push,
pull request, package publication, release, deployment, hosting change, Landing
change, or localization occurred. The next product review is one M3
unbriefed-reader test: after one uninterrupted read, ask what AgentGov blocked,
who changed the code, and who still controls acceptance. That review grants no
follow-on implementation or Git authority.

## M2 actor and CTA wording correction

An analytical cold read answered the three planned comprehension questions but
identified two copy risks. Section 4's heading could make a hurried reader
attribute code restoration to the human, and both page CTAs used internal M1
terminology without defining it. The same analysis estimated a human-equivalent
reading range rather than reporting actual human timing. It is advisory input,
not genuine M3 reader evidence.

Native proposal review separately admitted
`p0-governed-refund-walkthrough-copy-correction-v1` for only the walkthrough,
its semantic test, current status, this existing dated log, and the correction
task record. The prior M2 README changes and task record, user-owned `.codex`,
Landing, site assets, Kernel, schemas, localization, Git, and publication
remained outside the correction scope.

Section 4 now reads `A human declines the scope expansion`. The navigation CTA
now reads `Run the source demo`, and the final CTA reads `Run the executable
demo`. The explanatory sentence still says that recording the choice does not
edit code or expand scope and that AgentGov did not perform the scripted
remediation. `PASS`, `PASSED`, `VERIFIED`, `BLOCKED`, and `REVIEW_READY` remain
unchanged because the executable runner emits those distinct stage results.

One new assertion group raises the M2 semantic total to five tests and rejects
the superseded CTA labels and all undefined M1 or M2 page vocabulary. All 60
directly related walkthrough, task-contract, development-context, and
user-documentation tests pass. The complete repository suite passes all 936
tests with 3 platform-limited skips in 162 seconds.

Before pause, correction-task governance reported `PASS=3 WARN=1 FAIL=0
ADVISORY=3`, and repository governance reported `PASS=26 WARN=2 FAIL=0
ADVISORY=4`. Scope accepted all five correction-owned paths and rejected the
preserved prior-task README changes, prior M2 task record, and user-owned
`.codex/config.toml`. No exception or ownership transfer was inferred.

The fully specified correction did not start alignment. A distinct bounded
current-Agent advisory review found the actor attribution, CTA vocabulary,
exact provenance terms, frozen Landing, and authority boundaries preserved; it
does not claim native self-review or independence. The correction task is
paused. Genuine M3 human timing and comprehension, browser/device presentation,
localization, Landing linkage, Git, and publication remain separate decisions.

## M3 actor-attribution correction

The product owner reported a 70-second unbriefed-reader attempt against the
corrected M2 page. The reader correctly identified completion validation as the
blocked action and the human as final acceptance owner, but incorrectly said
that AgentGov modified and restored the code. Codex did not observe the reader,
verify the unbriefed condition independently, or measure the time. The result is
therefore user-reported evidence: two correct answers and one material
actor-attribution misunderstanding, not a complete M3 comprehension pass.

Native proposal review admitted
`p0-governed-refund-walkthrough-actor-attribution-v1` for only the walkthrough,
its semantic test, current status, this dated log, and the new task record. The
prior README and M2 task changes, user-owned `.codex`, Landing, site assets,
Kernel, schemas, localization, Git, and publication remained outside scope.

Section 2 now states that the demo script, not AgentGov, simulates the coding
agent changing both files and that AgentGov changes neither. Section 4 uses the
existing three-card layout to separate `Human decision`, `Demo script action`,
and `AgentGov role`. The human selects the exact `Decline expansion and narrow
the changes` option without editing code or widening scope; the demo script
performs clearly labelled restoration of only the approval-policy file; and
AgentGov checks corrected scope and fresh evidence without modifying or
restoring code.

The first focused run failed one existing provenance test because the initial
three-card rewrite had removed the exact human-choice label. The page restored
that label rather than weakening the test. The unchanged rerun then passed all
61 directly related walkthrough, task-contract, development-context, and
user-documentation tests. The main narrative remained inside the protected
190-to-300-word range. The complete repository suite passed all 937 tests with
3 platform-limited skips in 171 seconds.

Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
accepted all five task-owned paths and rejected the preserved prior-task README
and task-record changes plus user-owned `.codex/config.toml`. No exception or
ownership transfer was inferred.

This fully specified correction did not start alignment. A separate bounded
current-Agent advisory review found the three actors, original human-choice
label, exact stage-status vocabulary, frozen Landing, and authority boundaries
preserved; it does not claim native self-review or independence. The task is
paused. A different genuinely unbriefed reader must repeat the same three
questions before the project claims comprehension or decides on localization,
Landing linkage, Git, or publication.

## M3 restoration-actor clarity correction

The product owner later reported a one-minute attempt in which the reader
correctly identified completion validation, the simulated coding agent as the
modifier, and the human as final acceptance owner, but said that the simulated
agent also restored the code. Codex did not observe the attempt, measure the
time, or independently verify that the reader was unbriefed. It is recorded as
another two-of-three result and as the input for this correction, not evidence
that the corrected wording works.

Native proposal review separately admitted
`p0-governed-refund-walkthrough-restoration-clarity-v1` for only the
walkthrough, its semantic test, current status, this existing dated log, and
the new task record. The task began on 2026-08-19 and final validation completed
after the session crossed into 2026-08-20; the admitted stable log path was
preserved rather than expanding scope. Earlier README and task-record changes,
user-owned `.codex`, Landing, site assets, Kernel, schemas, localization, Git,
hosting, and publication remained outside scope.

Section 2 now states that the simulated coding agent changes the files but does
not perform the restoration shown later. Section 4 now makes the contrast
direct: the demo script—not the simulated coding agent, AgentGov, or the
human—performs clearly labelled scripted remediation and restores only the
out-of-scope approval-policy file. The existing human decision, AgentGov
checking role, status vocabulary, final-acceptance boundary, static structure,
and local-only delivery remain unchanged.

A new regression test protects the distinct modifier and restorer. All 7 page
tests pass. An initial related-suite command omitted the repository module path
and produced two import errors; no test was changed. Re-running with the
documented module path passed all 62 walkthrough, task-contract,
development-context, and user-documentation tests. The full repository suite
then passed all 938 tests with 3 platform-limited skips in 165 seconds. After
the closeout files were written, the unchanged final full-suite rerun passed
the same 938 tests with 3 skips in 163 seconds.

Before pause, task governance reported `PASS=3 WARN=1 FAIL=0 ADVISORY=3`, and
repository governance reported `PASS=26 WARN=2 FAIL=0 ADVISORY=4`. Scope
accepted all five task-owned paths and rejected six preserved prior-task or
user-owned paths. No exception or ownership transfer was inferred.

This fully specified correction did not start alignment. A separate bounded
current-Agent advisory review found the modifier/restorer contrast, human
decision boundary, exact status vocabulary, frozen Landing, and local-only
boundary preserved; it does not claim native self-review or independence. The
task is paused. A different genuinely unbriefed reader must test the revised
wording before the project claims complete M3 comprehension or chooses any
localization, Landing, Git, hosting, or publication action.
