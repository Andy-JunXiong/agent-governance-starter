# Development log - 2026-08-13

## Goal

Record the completed AIRBNB runtime, Completion Verified, and Bounded Handoff
journey as sanitized portable evidence without importing consumer content or
expanding any Git, release, deployment, or production authority.

## Bounds

- Evidence reconciliation and documentation only in AgentGov.
- Do not modify AIRBNB or copy its source, binary fixture, raw lifecycle files,
  absolute paths, private data, credentials, or raw conversation.
- Do not change AgentGov runtime, schemas, templates, release identity,
  workflows, public release artifacts, or external systems.
- Do not commit, push, open a pull request, run CI, release, deploy, or change
  production pricing authority.

## Observed consumer journey

- A prior AIRBNB replay had already established native proposal review, exact
  human task admission, a bounded implementation, static validation, and a
  distinct advisory review, but its runtime was unavailable.
- The product owner selected AIRBNB again to close only the missing runtime,
  Completion Verified, and Bounded Handoff transitions.
- Narrow proposal-only tasks admitted the governance-rule synchronization,
  deterministic synthetic-fixture regeneration, serving-test lifespan update,
  and final cumulative reconciliation before their respective writes.
- An isolated Python 3.11.9 environment ran the documented research-only
  scenario with an illustrative base result of about 266 AUD at MEDIUM
  evidence. The synthetic artifact retained `research_only` deployment
  authority.
- All 5 delivery tests, all 13 serving tests, and the complete 79-test suite
  passed with no failure or skip. The runtime feature declaration and fitted
  preprocessor each contained the same 23 features.
- Consumer repository governance reported 19 PASS, 2 WARN, 0 FAIL, and
  4 ADVISORY findings. The warnings concerned unresolved scaffold placeholders
  and an unconfigured capability-artifact directory; they were not represented
  as deterministic failures.
- AgentGov recorded an admitted cumulative task start, a passing 45-path scope
  reconciliation, six successful declared validation commands, fresh evidence
  with an unchanged working-copy digest, and
  `completion.reconciled: verified`.
- The human separately confirmed `session.handed_off: handed_off`. A later
  read-only preview returned `already_handed_off`, preserving one stable
  terminal event and ending only automatic routing responsibility.
- No excluded core model, evidence, release-gate, serving implementation,
  workflow, Docker, or legacy file had a diff.

## Advisory review

The work consisted of fully specified proposal-only tasks and had no resolved
alignment journey. Following the current consumer instruction contract, the
same Coding Agent performed and disclosed a separate bounded advisory review;
it did not fabricate a journey handle or claim native self-review completion.
The review reported no observed requirement, scope, implementation, security,
data, or authority drift. This is self-review, not independent evidence.

## Evidence limits

This journey demonstrates supported-runtime execution, fresh validation,
Completion Verified, and a distinct human-confirmed Bounded Handoff for one
consumer working copy. It does not demonstrate the automatic primary
experience, uncoached adoption, product effectiveness, causal benefit,
independent review, another consumer's result, consumer commit or push, CI
enforcement, required checks, release, deployment, or production pricing
authority. The consumer remains one commit behind its remote baseline and its
working changes remain uncommitted and unpushed.

The development evidence stores command identities, exit codes, output
digests, task and change-set bindings, and authority denials rather than raw
command output. The supported-runtime result was independently reproduced in
the same isolated Python 3.11.9 environment during AgentGov evidence review.

## Product connection

This builds on the generated-consumer task-directory repair and the 2026-08-11
AIRBNB native-admission replay. It closes that record's explicit runtime,
Completion Verified, and Bounded Handoff gaps without promoting a new Kernel
concept.

The next capability is not yet decided. Possible later work includes a separate
AIRBNB Git decision, another consumer reproduction, or continued automatic
primary-experience product work; none is authorized by this evidence record.

## AgentGov validation target

- Focused user-documentation and public-freshness tests.
- Validation of the exact admitted AgentGov evidence-recording task.
- Repository governance check and `git diff --check`.
- Final scope review preserving unrelated untracked local files.

## NYC consumer journey

The product owner separately selected a sanitized record of NYC as the second
independent consumer journey. This record uses only normalized facts from the
reviewed consumer evidence; it does not copy NYC source, raw lifecycle events,
runtime products, absolute paths, private data, or credentials.

- Python 3.11.9 ran the repository's existing reviewed synthetic scenario.
- The scenario validated its schema, created a temporary Bronze partition and
  zone lookup, built Silver, passed the demand quality gate, and produced
  non-empty Gold lineage. Generated products stayed in the test temporary
  directory.
- The focused runtime scenario passed 1 test and the complete 68-test NYC suite
  passed.
- Repository governance reported 17 PASS, 1 WARN, 0 FAIL, and 4 ADVISORY
  findings. The warning was the unconfigured capability-artifact directory.
  All 4 agent-skill contracts passed.
- An exact human-confirmed compact task started the local journey. One initial
  scope check reported two task-pre-existing untracked showcase images; those
  user-owned files remained untouched and outside task scope. An exact
  temporary Git exclude list allowed the subsequent reconciliation to compare
  only the two admitted paths, which passed.
- Four declared validation commands exited zero. Fresh evidence passed snapshot
  and scope reconciliation, producing `completion.reconciled: verified`.
- The human separately confirmed `session.handed_off: handed_off`; a repeat
  preview returned `already_handed_off` and appended no second event.

NYC's formal CI remains on AgentGov 0.2.1. The consumer lifecycle used local
development source for AgentGov 0.3.0rc1 and is not a formal upgrade, release,
or adoption of that release candidate. No TLC download, production-data access,
model training, candidate promotion, forecast publication, scheduling,
deployment, commit, or push occurred. The consumer changes remain uncommitted
and unpushed.

This builds on NYC's reviewed synthetic Bronze, Silver, demand-quality, and Gold
pipeline and on AgentGov's minimum lifecycle distinction. It supplies a second
bounded portability example after AIRBNB. It does not prove uncoached automatic
adoption, inventory completeness, control effectiveness, production forecast
quality, product effectiveness, causal benefit, independent review, publication,
release, deployment, or external authority.

The next product capability is not yet decided. Formal NYC version migration,
another consumer reproduction, and additional automatic-experience work each
remain separate possible requirements requiring a new human decision and task
admission.
