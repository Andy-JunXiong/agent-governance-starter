# Development log - 2026-08-27

## Post-repair no-retry artifact replay v1

### Direction, admission, and baseline

The product owner selected one post-repair real replay through resolved
alignment journey `mcpj-b6e7aa5a13f545f1ae0924e1e2189739`. Native proposal
`prp-0d633d1e3f55466b8381d3dc15295915` admitted the exact no-code-change
execution task. Create-only task-start baseline
`sha256:9d2be71eff91920182d567117a49a339f46e592946fd35f18805b50cb1ce2703`
was captured before execution.

Fresh read-only preflight passed the 186-path manifest and both canonical
identities, the exact retained backend-wheel identity, absent evidence target,
zero short roots, task governance, captured scope, and whitespace. A
hand-written diagnostic probe initially omitted the product's
`PYTHONNOUSERSITE` isolation and failed before controller execution. The fixed
bounded probe then identified exactly one retained Python 3.11.9 / pip 24.0 /
setuptools 84.0.0 candidate with a callable build backend and vendored-wheel
support. The diagnostic was not a controller, formal readiness, driver,
action, or build attempt.

### Sole controller execution

The fixed controller was invoked exactly once by module name with one strict
JSON stdin request. Its bounded result was `PASS` with controller attempts 1,
Harness dry attempts 1, and Harness actual attempts 1. The validated internal
chain performed one readiness probe, one driver/action, one offline build, one
exclusive durable evidence write, one receipt revalidation, and one
evidence-gated cleanup. No retry or alternate invocation occurred.

All 186 manifest inputs were copied with byte equality. The build emitted one
wheel with SHA-256
`sha256:64f48914b60ae9a7638275f1a9f3b3727bc15b7226c76b37babe08475bae685a`,
189 regular members, 183 managed byte matches, zero managed mismatches, six
generated metadata members, and zero unexpected unmanaged members. Durable
evidence was written at the admitted target with identity
`sha256:ffc06da3060b459ff39c8602365126a860e8228dd6320c8c88f69cbc8c6b8715`.
The final short-root count is zero.

The durable evidence template retains its earlier static heading/date and
intentionally stops its own claims before gated cleanup. It was not modified
after receipt validation. The controller `PASS` result and final zero-root
observation are the separate post-cleanup evidence.

No repair, alternate backend, manual cleanup, network, dependency installation,
Git mutation, commit, push, publication, release, deployment, external Agent,
or model action occurred.

### Closeout validation

Immediate post-run captured scope was
`PASS=22 PRESERVED=20 FAIL=0 TOTAL=42`. The task-declared combined artifact
chain then passed 78 tests with five Windows privilege-limited symbolic-link
skips, and all 55 user-documentation tests passed. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and final captured scope is
`PASS=24 PRESERVED=20 FAIL=0 TOTAL=44`. Manifest, task JSON, zero short roots,
whitespace, and `git diff --check` pass. No replay attempt remains or is
permitted.

Distinct native current-Agent self-review
`srv-19f1a033c247e0c082e72a3054bb8192` found the admitted requirement,
architecture chain, one-attempt execution, artifact identities, cleanup,
scope, privacy, and denied-authority boundaries consistent. It retained the
static evidence-heading/date limitation, unknown cross-platform and repeated
reliability, and lack of adversarial sandbox proof. The review is advisory
rather than independent assurance. The native task completion-record tool was
not exposed, so no completion record is claimed.

## Artifact replay interview walkthrough v1

### Direction, admission, and baseline

The product owner selected a focused three-to-five-minute walkthrough through
resolved alignment journey `mcpj-ea817cbb30274487959e92fe5a68a103`.
Native proposal `prp-0a32f822898d461f9c2d5492aab116d2` admitted the exact
documentation-only task. Create-only task-start baseline
`sha256:5698cf9894883b96d6de81e5fb6d4f0404759ecaab709cf45e336d57c1d42f12`
was captured before documentation edits.

### Documentation slice

`docs/artifact-replay-interview-walkthrough.md` now packages the completed
artifact chain as a concise spoken story: problem, architecture, two
manifest-boundary failures and repairs, the sole post-repair result,
evidence-gated cleanup, tradeoffs, honest limitations, likely questions, and
repository-relative evidence links. README review navigation links the new
walkthrough.

The walkthrough records the observed 1/1/1 controller, dry, and actual attempt
counts; 186 copied and byte-matched inputs; built-wheel identity
`sha256:64f48914b60ae9a7638275f1a9f3b3727bc15b7226c76b37babe08475bae685a`;
durable-evidence identity
`ffc06da3060b459ff39c8602365126a860e8228dd6320c8c88f69cbc8c6b8715`;
and final zero-short-root observation. It explicitly leaves cross-platform,
recovery, future-toolchain, repeated-reliability, adoption, time-saving,
interview, causal-benefit, and return-on-investment outcomes unknown.

The receipt-validated evidence file was not edited or regenerated. No replay,
build, implementation, contract, dependency installation, network, Git,
publication, release, deployment, external-Agent, or model action occurred.

### Closeout validation

All 55 user-documentation tests pass. Task governance is
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and captured scope is
`PASS=21 PRESERVED=27 FAIL=0 TOTAL=48`. The immutable evidence retains SHA-256
`ffc06da3060b459ff39c8602365126a860e8228dd6320c8c88f69cbc8c6b8715`.
Task JSON, whitespace, and `git diff --check` pass.

Distinct native current-Agent self-review
`srv-af358ab0c280c088103127d900784095` found the chosen requirement,
architecture split, recorded identities, scope, privacy, and denied-authority
boundaries consistent. It retained actual speaking time, interviewer
preference, adversarial resistance, portability, repeated reliability, and
interview benefit as unknown. The review is advisory rather than independent
assurance. The native task completion-record tool was not exposed, so no
completion record is claimed.

## Bilingual artifact replay HTML walkthrough v1

### Admission and baseline

The product owner requested both English and Simplified Chinese browser
presentations. Native proposal `prp-054822a697bc4a059b880e5a6783ea37`
admitted the exact local-only page task. Create-only task-start baseline
`sha256:503e8b37a70950a0c6ee710bf01d6b62e88456cdaca8ffd6cfdf0e42d1962f96`
was captured before page edits.

### Implementation

`docs/artifact-replay-interview.html` and
`docs/artifact-replay-interview.zh-CN.html` now provide equivalent
three-to-five-minute presentation routes. Both include a strong outcome hero,
timed story cards, controller-to-driver architecture flow, two manifest-boundary
repairs, the exact 1/1/1 attempt result, artifact metrics and identities,
evidence-gated cleanup, honest limitations, expandable likely questions, and
repository-relative evidence links.

The pages reuse `guide.css` without changing it and add only
`artifact-replay-interview.css` for the focused flow, timeline, metrics, and
responsive layout. They use local assets, restrictive CSP, semantic HTML,
visible focus behavior, reciprocal language switching, and no inline script or
style. README and the Markdown walkthrough link both pages.

The Sites building guidance influenced the reuse of the established site
system, responsive first viewport, local assets, and no unnecessary imagery or
client state. Repository authority takes precedence over its hosting handoff:
this task performed no hosting or publication.

### Closeout validation

All 56 user-documentation tests pass, including focused reciprocal language,
responsive, CSP, UTF-8, critical evidence-fact, and navigation coverage. Task
governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and captured scope is
`PASS=28 PRESERVED=27 FAIL=0 TOTAL=55`. The receipt-validated evidence retains
SHA-256 `ffc06da3060b459ff39c8602365126a860e8228dd6320c8c88f69cbc8c6b8715`.
Task JSON, whitespace, and `git diff --check` pass. Browser visual QA was not
requested and remains unknown.

A distinct bounded current-Agent review found the two languages aligned on
sections, attempt counts, artifact identities, cleanup semantics, evidence
links, non-claims, local-asset policy, and denied hosting authority. It retains
actual browser rendering, spoken duration, and interview benefit as unknown.
This fully specified task had no new resolved alignment journey, so no native
self-review result is claimed. The native task completion-record tool was not
exposed.

## Bilingual artifact replay evidence-detail UI v1

### Feedback, admission, and baseline

The product owner supplied a screenshot and identified four concrete problems:
the verified-result green was too bright, the palette felt inconsistent, the
cards were too abstract, and the page did not distinguish real evidence from
presentation abstraction. Native proposal
`prp-0c02d43eb2ac41d59bccf8453b71c80b` admitted the exact bilingual local UI
refinement. Create-only baseline
`sha256:a961bed739e8e9e33441457fd6e76656b61b34b51b8d294ff5fc57745feb9f64`
was captured before edits.

### Implementation and provenance model

Both HTML pages now use a restrained paper, ink, muted-purple, and soft-sage
palette. The fluorescent result surface is removed. A new evidence model labels
real replay observations, editorially derived explanation, and unknown outcomes.
It states that the recorded attempt counts, artifact identities, failure
sequence, and cleanup observations are repository evidence; presentation order,
timing, architecture grouping, and wording are explanation; portability,
repetition, time saving, adoption, and interview impact remain unknown.

Every architecture node and result card is keyboard-focusable and exposes a
local CSS detail panel on both hover and focus. The panels define each number,
connect `183 managed + 6 generated = 189 regular`, explain the zero mismatch,
unmanaged, retry, and root observations, and retain the pre-cleanup versus
post-cleanup evidence distinction. No JavaScript, inline style, or external
asset was added.

### Browser QA and closeout validation

Local browser QA confirms the muted visual hierarchy, the Chinese evidence
taxonomy, a visible `Wheel members 189` panel through keyboard focus and mouse
hover, and the reciprocal English route. All 56 user-documentation tests pass.
Task governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; and captured scope is
`PASS=29 PRESERVED=29 FAIL=0 TOTAL=58`. The immutable evidence retains SHA-256
`ffc06da3060b459ff39c8602365126a860e8228dd6320c8c88f69cbc8c6b8715`.
Task JSON, whitespace, and `git diff --check` pass.

A distinct bounded current-Agent review found the palette, bilingual evidence
taxonomy, concrete relationships, hover and focus accessibility, critical
identities, scope, and denied hosting authority consistent. It retains touch
usability in a real presentation, other-browser rendering, preferred density,
and interview benefit as unknown. This fully specified task had no new resolved
alignment journey, so no native self-review result is claimed. The native task
completion-record tool was not exposed.

## Bilingual replay story v1

### Feedback, admission, and baseline

The product owner supplied Claude and GPT reviews that converged on the same
problem: the focused replay pages were technically sound but read like a
system specification because they led with nouns, components, and results
instead of a person, a question, a conflict, and a decision. Native proposal
`prp-cc64124b25a24bb58cfadf0c6e46a2d2` admitted the exact bilingual,
local-only rewrite. Create-only baseline
`sha256:0baca9d61fff04473b1d41dc9d839352a073e4e13ce0087dde98aa9e1c68ea1a`
was captured before edits.

### Story rewrite

Both artifact replay pages now open with the recorded verification question:
whether cleanup actually waits for durable evidence and a revalidated receipt.
They then narrate the real sequence in first person: the first replay stopped
before build because a PATH-free worker tried to launch bare Git; the restricted
environment was preserved; validated manifest facts moved inward; inspection
found the same Git coupling inside the driver; a filesystem-only revalidation
repair removed it; and one separately admitted replay completed the 1/1/1 gate.

The system diagram, eight metric cards, hashes, and evidence taxonomy remain as
supporting proof. The result copy restores the observed `186 / 186` byte-match
fact and retains `183 managed + 6 generated = 189 regular`, zero retry, and
zero final roots. Focused tests enforce chronological ordering in both
languages, the absence of named invented origin or belief claims, critical
facts, CSP, UTF-8, interactions, and the muted palette.

This rewrite does not claim why the human originally created AgentGov, what
they initially believed, that they ran a scoring experiment, or that the page
improves interview outcomes. Those facts are not established by the replay
evidence. No replay, build, network, Git mutation, hosting, publication,
release, deployment, or external-Agent action occurred.

### Browser QA, validation, and advisory review

Local browser QA confirms the Chinese question-stop-decision-second-dependency-
result chronology appears before the supporting evidence, the `Wheel members
189` disclosure exposes the exact `183 + 6 = 189` relationship, and the English
route presents the corresponding story. All 56 user-documentation tests pass.
Task governance is `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance is
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; captured scope is
`PASS=30 PRESERVED=29 FAIL=0 TOTAL=59`. The immutable evidence retains SHA-256
`ffc06da3060b459ff39c8602365126a860e8228dd6320c8c88f69cbc8c6b8715`.
Task JSON, whitespace, and `git diff --check` pass.

A distinct bounded current-Agent review found the bilingual chronology,
first-person evidence mapping, absent invented claims, architecture and metric
support, critical identities, interaction behavior, scope, and denied hosting
authority consistent. Personal project origin, preferred spoken phrasing,
touch behavior, other-browser rendering, repeated reliability, portability,
and interview benefit remain unknown. This fully specified task had no new
resolved alignment journey, so no native self-review result is claimed. The
native task completion-record tool was not exposed.

## End-of-day artifact replay Git handoff

The product owner explicitly requested related-documentation closeout followed
by one commit and one non-force push to GitHub `main`. Native proposal
`prp-1ace7f8b48c947d48e68605367d116e7` admitted task
`p0-session-closeout-push-main-v1`. Create-only baseline
`sha256:23cfbef42d2758931e1b5b37cba1b21c23961c5eca29c2271160c23907c248f2`
was captured before this handoff entry.

The intended commit contains the accumulated artifact replay caller,
readiness, Harness, driver, and controller source; their tests; the bounded
build-validation records; the admitted task records; the Markdown and
bilingual HTML interview material; and the README, STATUS, automation-contract,
dated-log, and user-documentation test updates that keep those surfaces
consistent. Local `.codex` configuration and unrelated historical `.agentgov`
scope baselines remain outside the intended stage.

Before committing, the closeout requires complete intended-diff review, full
repository and artifact-script unit tests, task and repository governance,
captured-scope comparison, immutable replay-evidence hashing, JSON and
whitespace checks, a bounded secret-safety review, and staged-diff inspection.
The Git procedure then requires fetching `origin`, stopping on divergence,
creating one non-release commit on `main`, pushing `HEAD:main` without force,
and verifying local and remote identity. Git history owns the resulting commit
identifier and transport result; this entry does not pre-claim them.

This closeout grants no release, deployment, hosting, package publication, PR,
force-push, history rewrite, external-Agent, or model authority.

## Whole-project bilingual interview public story v1 — Slice 1

### Direction, admission, and baseline

Resolved alignment journey `mcpj-c209071de8b24b9a805be1f8a0e0112d` records
the product owner's choice to create two complementary interview surfaces:
first a standalone bilingual public project story, then a separately governed
Interview Guide revision. Native proposal
`prp-bc5d4f62f7cd45689d9978bffa07a1a4` admitted only the first slice. The
create-only task-start baseline is
`sha256:b71453a661329227c13fef61063a80c4e3fd851e5a3e35e8b616e9bab91143de`.

The product owner confirmed four narrative facts before implementation: they
are AgentGov's primary and sole product and engineering owner; reviewing each
feature's short-term value, long-term value, and next-stage relationship is a
real long-standing habit; they verified accumulated drift before rapidly
returning development to the core MVP; and the supplied Airbnb and artifact
replay attribution list governs claims about personal, joint, and
Agent-proposed decisions.

### Implementation and evidence boundary

`docs/project-interview.html` and `docs/project-interview.zh-CN.html` now provide
natural English and Simplified Chinese versions of the same five-act story.
The default route is deliberately compact, while one architecture flow and
three substantial proof points support a longer screen-share discussion. The
pages distinguish stable `0.2.1` capability from `0.3.0rc1` and development
source, and state the central limitation directly: task-level verification
cannot decide whether the right product is being built.

The Taxi example remains product-learning context, Airbnb remains one observed
human-and-Agent workflow, and artifact replay remains one bounded Windows
engineering case. The artifact account explicitly says that the Agent proposed
most low-level repairs and the human reviewed and approved them. The pages make
no broad adoption, security, portability, repeated-reliability, automated
product-drift detection, or interview-success claim. `docs/index.html` receives
one discoverability link; the existing Interview Guide is unchanged in this
slice.

The Sites building skill influenced the responsive, local-asset, accessible
static-page implementation and reuse of the repository's visual vocabulary.
Repository authority controls the handoff: no hosting or publication occurred.

### Validation state

All nine focused interview and product-site tests pass. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; final captured-scope validation reports
`PASS=29 PRESERVED=33 FAIL=0 TOTAL=62`; task JSON and `git diff --check` pass.

Native current-Agent self-review
`srv-a427b5ee4c794753496a57b378914a51` completed as a distinct advisory pass.
It found the Slice 1 sequencing, one-flow architecture, attribution boundary,
captured scope, static-page constraints, and public-data boundary consistent.
It retains rehearsal comprehension, browser and touch behavior, and external
publication suitability as unknown. The native task completion-record tool is
not exposed in this session.

Actual spoken duration, screen-share pacing, cross-browser rendering, touch
behavior, and interview outcome remain unknown. Slice 2 has separate scope and
requires its own native admission before the Interview Guide changes.

## Whole-project Interview Guide integration v1 — Slice 2

### Admission and start boundary

Native proposal `prp-89b0b53c0da348be8ddd4bef31dfcebe` separately admitted
the Interview Guide integration selected by alignment journey
`mcpj-c209071de8b24b9a805be1f8a0e0112d`. The create-only Slice 2 baseline is
`sha256:298f83876bc4818a29a9c7f5697574f43c91f34a3917d53b313fd8f7206fc2b3`.
It preserves Slice 1, the accumulated artifact work, and every unrelated dirty
path rather than treating them as new Slice 2 changes.

### Interview operating model

`docs/interview-guide.md`, `docs/interview-guide.html`, and
`docs/interview-guide.zh-CN.html` now own delivery mechanics rather than
duplicating the public story. Each route starts with a complete two-minute
answer and four explicit safe cut points. A six-step five-to-seven-minute
screen share moves deliberately from the whole-project story to the stable
governed-refund example, one lifecycle explanation, the optional artifact
replay engineering deep dive, evidence-on-demand, and an honest close.

The guides provide separate software and AI engineering, platform and
infrastructure, and product and technical product branches. Likely questions
cover the distinction from a linter, deterministic versus advisory semantics,
why product drift remains a human judgment, the strongest engineering proof,
artifact-repair authorship, and the next-requirement boundary. The Airbnb and
artifact attribution follows the product owner's confirmed list: human-
originated, joint, and Agent-proposed but human-approved decisions stay
distinct.

The established Guide visual system and client behavior were reused; no CSS,
new JavaScript state, image, external asset, hosting, or publication was added.
The Sites building skill influenced the responsive static structure and
evidence-on-demand navigation, while repository authority kept the work local.

### Validation state

All ten focused interview and product-site tests pass. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; final captured-scope validation reports
`PASS=30 PRESERVED=37 FAIL=0 TOTAL=67`; task JSON and `git diff --check` pass.
One initial
`git diff --check` failure identified a Markdown hard-break trailing-space
sequence; it was removed before closeout.

A distinct evidence-bounded current-Agent pass found the Guide's operational
responsibility, bilingual route parity, role branches, attribution wording,
release boundary, local-asset policy, navigation, scope, and claim limits
consistent. Actual timing, comprehension, browser behavior, and interview
benefit remain unknown. A second native self-review could not start because the
tool reported that self-review had already started for this alignment journey
during Slice 1. No native Slice 2 self-review result is claimed, and the native
task completion-record tool is not exposed. Implementation is complete, but
that native closeout limitation remains unresolved.

Actual spoken timing, interviewer comprehension, branch effectiveness,
cross-browser rendering, touch behavior, external publication suitability, and
interview outcome remain unknown. This slice authorizes no Git, hosting,
publication, release, deployment, or automatic next requirement.

## Bilingual interviewer-facing refund demo v1

### Product correction, admission, and baseline

The product owner's screenshots established three concrete failures in the
existing interview surfaces: three proof links produced `404` responses, the
eight-node architecture flow could escape its card, and the whole-project
story was supporting narrative rather than the screen-share demo an interviewer
should watch. The product owner approved a guided-demo correction and admitted
native proposal `prp-9c47df2a589f459d995bcd1781e394b4` as task
`p0-bilingual-interviewer-facing-refund-demo-v1`. The task-start baseline is
`sha256:cbdbf4d341815656a5b2983e9656ab1ffdb55dde12d8ebde9647c279c452f0ea`.

### Interview surface and evidence boundary

`docs/interview-demo.html` and `docs/interview-demo.zh-CN.html` now own what the
interviewer sees. Their local CSS and JavaScript present exactly six states:
the human admits a one-file refund change; the simulated coding Agent also
changes an unadmitted policy file; AgentGov reports one in-scope pass and one
out-of-scope failure; continuation is blocked; the human declines scope
expansion; and the demo script restores only the unadmitted file before fresh
evidence reaches `REVIEW_READY`. Back, Next, and Restart are visible and
keyboard-focusable.

The actor and authority labels are deliberate. The page does not claim live
Python execution, an AgentGov source-code change, automatic prevention or
rollback, human acceptance, merge, release, deployment, or downstream
authority. It links to the executable refund walkthrough for code-level proof,
the whole-project story for background, and artifact replay for an optional
engineering deep dive.

The landing page and README now make the guided demo primary. The Interview
Guide is explicitly presenter preparation, while the project story is optional
background. The broken Taxi and Airbnb proof links now target existing Markdown
records. The architecture flow uses four, two, and one responsive columns so
all eight nodes remain contained. The Sites building skill influenced the
responsive, accessible, local-asset implementation; repository authority kept
the result local, with no hosting or publication.

### Validation and advisory review

All 23 focused demo, walkthrough, interview-documentation, and product-site
tests pass. All changed proof URLs returned HTTP 200 from a local documentation
server. Browser QA exercised all six English states plus Back and Restart, the
Chinese Next/Back/Restart route, desktop and 375-pixel narrow layouts, and the
project-story architecture at approximately 845 and 375 pixels. No document-
level horizontal overflow was observed, every architecture node remained
within the flow container, and the repaired Taxi URL reached its exact source.

Task governance reports `PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance
reports `PASS=26 WARN=2 FAIL=0 ADVISORY=4`; final captured-scope validation is
`PASS=52 PRESERVED=32 FAIL=0 TOTAL=84`; task JSON and `git diff --check` pass. A
distinct evidence-bounded current-Agent review found the requirement, actor
model, authority boundary, bilingual parity, local-asset policy, links,
responsive behavior, navigation hierarchy, and admitted scope consistent.
This fully specified correction did not start a new alignment journey, so no
native self-review result is claimed. The native task completion-record tool
is not exposed.

Actual spoken pacing, interviewer comprehension, behavior in the eventual
interview environment, external publication suitability, and interview outcome
remain unknown. This task authorizes no Git, hosting, publication, release,
deployment, or automatic follow-on work.

## Interview narrative voice de-AI pass v1

### Direction, admission, and baseline

The product owner reviewed the combined critique that the interview pages were
still speaking in governance-document language even after their factual
structure improved. They confirmed the factual source behind the project story:
recording short-term value, long-term value, and the relationship to the next
stage was a real long-standing habit; they were the primary product and
engineering owner; and once accumulated drift was confirmed they quickly
returned development to the core MVP. They selected the de-AI narrative pass
and admitted native proposal `prp-5e080e7c50964b85958daf0358d8d1bb` as task
`p0-interview-narrative-voice-de-ai-pass-v1`.

The create-only task-start baseline is
`sha256:d095ca25ed108f28a59948f13633677b0cda1df49c07229e6f7662e7d41d7c86`.
It preserves every pre-existing excluded change while allowing only the named
interview surfaces, focused tests, task record, status, and this dated closeout
record to evolve.

### Layered interview result

The interviewer-facing demo keeps exactly six interactive states and the
existing Back, Next, and Restart behavior. It now leads with a concrete person,
an extra policy change, the resulting stop, a human choice, a repair, and a
`REVIEW_READY` result. Actor provenance appears once, and one short disclosure
keeps the guided replay distinct from live execution.

The whole-project story is now a four-part causal account rather than a
five-act evidence dashboard. It explains why the product exists, how repeated
benefit notes exposed drift, why development returned to the smallest MVP, and
how the refund demo proves the core behavior. The previous three-proof grid is
reduced to one primary proof, with artifact replay available only as an optional
engineering follow-up.

The presenter guide retains the material that is useful before an interview but
should not be narrated by the public pages: timings, cut points, role branches,
authorship answers, technical terminology, release caveats, optional evidence,
and claim limits. Its 30-second and two-minute scripts were rewritten for
spoken delivery. The artifact replay keeps the two real failures, the decision
to leave Git outside the PATH-free worker, the deeper dependency repair, the
single no-retry replay, and evidence-before-cleanup ordering. Repeated visible
evidence taxonomy and defensive non-claims were removed; one concise
authorship statement and one local-Windows limitation remain.

The landing page now routes to the demo in ordinary engineering language. The
Sites building skill influenced the responsive static structure, local-asset
discipline, and separation between audience layers. Repository governance
controls the handoff, so no hosting or publication occurred.

### Validation and advisory review

All 72 focused demo, interview-documentation, product-site, and
user-documentation tests pass. Task governance reports
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`; repository governance reports
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`; captured-scope validation reports
`PASS=59 PRESERVED=32 FAIL=0 TOTAL=91`; task JSON and `git diff --check` pass.

A distinct current-Agent advisory review found the six-state behavior, factual
human causal sequence, bilingual claim strength, one-proof hierarchy,
engineering evidence chain, local static architecture, and captured scope
consistent. It found no invented dramatic turning point, no new external data,
and no changed security boundary. This fully specified task did not start an
alignment journey, so no native self-review result is claimed. The native task
completion-record tool is not exposed in this session.

Actual spoken duration and naturalness, interviewer comprehension, behavior in
the eventual interview environment, external publication suitability, and
interview outcome remain unknown. The implementation is complete within its
admitted local scope. The next product review is an aloud rehearsal of the
30-second answer, two-minute story, and six-step demo; it grants no automatic
follow-on task, Git, hosting, publication, release, deployment, or external
authority.
