# AIRBNB isolated-runtime preparation - 2026-08-20

## Claim boundary

This record covers one fresh temporary Python 3.11 environment for the
retained clean AIRBNB clone and one no-model MCP discovery exchange. It clears
the previously observed executable-binding prerequisite only. It is not a live
Codex replay, AIRBNB-local task admission, consumer validation, product-
effectiveness result, or authority to change consumer files, repair a global
installation, commit, push, publish, release, deploy, or clean up anything.

No raw prompt, transcript, model output, consumer source content, credential,
private data, or absolute host path is retained here.

## Preserved inputs

Before runtime creation, the retained AIRBNB clone was detached at exact commit
`d70615527d9acdde3893ce645d1923606173acf6`, clean, and without remotes. Its
committed Codex configuration retained the measured byte identity from the
clean-clone preflight.

The source AIRBNB worktree remained on its original committed HEAD with exactly
one modified README and one untracked prior task. Its branch, HEAD, two-path
status, measured README and task blob identities, and Codex-configuration byte
identity were unchanged before and after this preparation. No cleanup, reset,
stash, overwrite, commit, or metadata mutation was performed.

The retained wheel was still the only matching
`agent_governance_starter-0.3.0rc1-py3-none-any.whl` candidate and retained
SHA-256
`78D6A216FE7344A2E3DA01086A7A92B9F3310BE61DAD2E690B0705003221FDE6`.

## Offline isolated installation

Exactly one new virtual environment was created under the operating-system
temporary area with Python 3.11.9. The exact retained wheel was installed into
that environment with both `--no-index` and `--no-deps`; no package index or
dependency resolution was used. The isolated interpreter reports distribution
version `0.3.0rc1`, and its console entry point exists.

No global Python, pipx environment, launcher, user configuration, persistent
PATH, existing environment, AIRBNB file, or Starter product source was changed.
The temporary environment is retained because cleanup is not authorized, and
its absolute path is deliberately omitted.

## Configured-command launch proof

From the retained clean AIRBNB clone, one shell process prepended only the new
environment's scripts directory to its own PATH and invoked the unchanged
configured command name `agentgov`. Command resolution found both the isolated
and pre-existing launchers, with the isolated launcher first. The invocation
reported `agentgov 0.3.0rc1`.

The same command then completed a JSON-RPC discovery, MCP initialize, and
`tools/list` exchange with no `tools/call`, model request, or external host
turn. It reported:

- AgentGov protocol `2026-07-28` among the supported versions;
- negotiated MCP protocol `2025-11-25`;
- Adapter version `1.5.0`;
- exactly seven expected form-capable governance tools; and
- no Agent-supplied `owner` input on `agentgov_task_proposal_review`.

After the exchange, the clean clone remained at the same commit, clean, without
remotes, and with the same Codex-configuration byte identity. The source
AIRBNB worktree and pre-existing global launcher also retained their measured
identities.

## Readiness classification

The prior runtime-binding blocker is **CLEARED FOR LIVE-REPLAY BINDING**: the
unchanged AIRBNB command can resolve and start the exact isolated release
candidate when that process-local PATH is supplied. This does not prove that a
later fresh Codex session will inherit the binding, nor does it authorize or
constitute the live replay. That session behavior remains an explicit unknown
for the next product review.

No external model turn, network request, AIRBNB-local task admission, consumer
source or configuration change, consumer validation, global repair, Git
operation, publication, release, deployment, or cleanup occurred.

## Validation and review

All 13 task-contract tests and all 36 user-documentation tests passed. The
complete repository suite passed 938 tests with 3 platform-limited skips in
170 seconds. Before pause, task governance reported `PASS=3 WARN=1 FAIL=0
ADVISORY=3`; repository governance reported `PASS=26 WARN=2 FAIL=0
ADVISORY=4`. Scope accepted all four task-owned Starter paths and rejected only
the preserved user-owned `.codex/config.toml` plus the two excluded prior-
preflight records; no exception or ownership transfer was inferred. The task
JSON parsed, bounded credential-pattern and absolute-host-path scans returned
zero matches, and `git diff --check` passed.

A separate bounded current-Agent advisory review found the exact wheel,
single-environment, offline-installation, configured-command, preservation,
privacy, and all-denied downstream-authority claims consistent with the
observed evidence. It retained one material unknown: whether a later fresh
Codex session will inherit the same process-local binding. The task was fully
specified and started no new alignment journey, so this review does not claim
native self-review completion or independent audit status. No correction-
required drift was found.
