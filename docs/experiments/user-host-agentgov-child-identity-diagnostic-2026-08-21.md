# User-host AgentGov child identity diagnostic - 2026-08-21

## Purpose and authority

The product owner selected child-identity attribution through resolved
alignment journey `mcpj-1fd2da6fc67e4fd28e356ccc5bef7587`. Native proposal
review admitted `p0-user-host-agentgov-child-identity-diagnostic-v1`, and the
product owner separately started it.

The task permitted transient read-only Windows process observation and at most
one separately approved no-turn user-host Codex startup. It did not authorize
a launcher wrapper or edit, persistent tracing, raw session or broad-log
inspection, `turn/start`, model interaction, retry, repair, cleanup, Git
operations, publication, release, deployment, or broader activation.

## Identity and observer preflight

The retained Python 3.11.9 runtime and AgentGov `0.3.0rc1` distribution, the
remote-free AIRBNB clone at
`d70615527d9acdde3893ce645d1923606173acf6`, its two existing binding changes,
the original AIRBNB HEAD and checked binding hashes, the user Codex
configuration hash and byte length, and Codex `0.149.0-alpha.4` matched the
preceding records. Original AIRBNB status retained its existing local
AgentGov-directory permission warning.

The diagnostic parent placed the retained runtime Scripts directory first on
process-local `PATH`; the bare project command resolved to the retained
launcher. `CODEX_HOME` and `PYTHONPATH` overrides were absent, while the
existing `ELECTRON_RUN_AS_NODE` environment flag was present. Complete
environment values were not retained.

One existing AgentGov process and five existing Codex processes were recorded
and treated as unrelated. Persistent or event-based Windows process tracing
was unavailable and was not enabled. One hidden PowerShell child that exited
normally after 800 milliseconds proved that transient CIM polling could
observe an exact child by parent relationship and path presence. It started no
Codex, MCP, network, or repository action.

## Single observed user-host startup

The product owner separately approved exactly one user-host Codex App Server
startup. An in-memory observer polled only descendants of that exact new Codex
process. It normalized parent class, process basename, executable
classification and digest, and whether the AgentGov arguments matched the
project declaration. It did not retain process identifiers, usernames,
absolute paths, raw command lines, complete environment values, or unrelated
process data.

The connection initialized against the real user Codex home. The diagnostic
sent `initialized` and one `thread/start` using `approvalPolicy=never` and
`sandbox=workspace-write`; it never sent `turn/start`.

The observer captured this descendant chain:

- the diagnostic Codex process started a console host;
- the diagnostic Codex process started the retained `agentgov.exe`, whose
  SHA-256 was
  `FA2F2CDB39B2199CE4E37D3298123CA412EEF550AB7A35BC22BF1E7E99A3ED0E`;
- its arguments matched `adapter governance-mcp --host-profile codex`;
- that retained launcher started the retained `python.exe`, whose SHA-256 was
  `21BB438C0D4A6F1F164B9A646F6EE000340185E5871180AEC06DB8D3F07C0082`.

The pre-existing older global AgentGov process was not a descendant and was
not selected by the diagnostic launch.

The required MCP handshake still closed while producing the initialize
response, and `thread/start` returned `-32603`. No `thread/started`, turn,
item, prompt, model payload, Agent response, or tool-call event occurred. The
two stderr lines were classified only as the known PowerShell shell-snapshot
warning; their raw content was not retained. The exact diagnostic process was
terminated after the first result, and no retry or second launch occurred.

## Classification and remaining unknown

The deterministic result is
`RETAINED_AGENTGOV_CHILD_CONFIRMED_USER_HOST_FAILURE_PERSISTS`:

- parent-process retained launcher resolution: pass;
- project configuration and expected arguments: pass;
- actual MCP launcher identity and digest: retained AgentGov, pass;
- actual launcher child identity and digest: retained Python, pass;
- user-host App Server connection initialize: pass;
- required MCP initialize handshake: fail with reproduced `-32603`;
- thread creation: fail before `thread/started`;
- model turn or governed consumer journey: not run.

The evidence rules out accidental selection of the older global AgentGov
launcher. The remaining reproducible boundary is now the real user Codex-home
or host-environment context while running the intended retained executable.
Windows process APIs did not expose the child's complete environment without
the prohibited wrapper, debugger, persistent trace, or raw-log path. The
actual inherited environment and the causal user-home state therefore remain
unknown. This task made no repair.

## State, privacy, and preservation

The user Codex configuration hash and byte length, retained clone HEAD,
zero-remotes state, two changed binding paths, and three checked clone hashes
remained unchanged. The exact diagnostic Codex, AgentGov, and Python processes
exited. All pre-existing processes remained and were not touched.

A metadata-only five-minute window over the user Codex home found 18 recently
modified files and 7 recently created files across batch, JSON, lock, log,
SQLite, and SQLite sidecar extensions. Multiple pre-existing Codex processes
were active throughout, so those observations cannot be attributed to this
diagnostic. No path, filename, content, session identity, or installation
identity was inspected or retained, and no cleanup was attempted.

This record contains no raw protocol response, prompt, transcript, source
content, credential, private data, user or temporary absolute path, process or
thread identity, raw command line, complete environment value, external-model
payload, or unbounded host log. No launcher change, repair, model turn,
governed replay, dependency download, cleanup, commit, push, publication,
release, deployment, or broader activation occurred.

## Starter validation

The exact Python 3.11.9 repository suite passed all 946 tests with 3 platform-
limited skips in 261.213 seconds. Task governance returned
`PASS=3 WARN=1 FAIL=0 ADVISORY=3`, repository governance returned
`PASS=26 WARN=2 FAIL=0 ADVISORY=4`, and `git diff --check` passed. Scope
reconciliation admitted the 4 exact task-owned paths and retained 14 failures
for explicitly excluded prior-task or user-owned paths; no exception or
ownership transfer was inferred. The task JSON parsed, and bounded host-
absolute-path and recognized credential-marker scans returned zero matches.

## Completion and advisory review

The current Agent's callable governance inventory did not expose
`agentgov_task_completion_record`, so no Starter completion record was
fabricated. Native current-Agent self-review
`srv-d0e389eb2f38b3cbe6e47e346f2db0e6` completed as a distinct advisory pass
with requirement, architecture, scope, implementation, and security
observations. It found the retained-child conclusion, observer fallback,
admitted-path ownership, validation, privacy, attribution limits, and stop
boundaries consistent.

The review retained the complete child environment, causal user-home state,
initialize-response code path, shared-file attribution, and possible
unobserved background network attempts as unknown. It granted no task, repair,
scope, exception, Git, publication, release, deployment, or external
authority.
