# Example capability implementation evidence — {{PROJECT_NAME}}

This repository-local placeholder gives the example capability an explicit,
reviewable evidence file. Replace it with references to the real deterministic,
model, prompt, or hybrid implementation.

## Intended behavior

Produce the documented example outcome using only the validated input
contract. Do not perform external writes or invent evidence that is not present
in the input.
